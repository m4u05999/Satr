# قبول دفعة 2.16.21 — الكود عند 32aff6a

تاريخ التجربة: 2026-09-08 بتوقيت الرياض. الإصدار الظاهر 2.16.20، والبناء يتضمن الالتزامات الخمسة التالية للوسم v2.16.20. لم يُنشَر إصدار جديد.

## البناء

الأمر: npm run dist:dir -- --config.directories.output=dist/accept-main-32aff6a --publish never

نجح البناء، وأُطلقت الحزمة نفسها مع مجلد userData منفصل و--lang=ar. ملفات اللغات الموجودة: ar.pak (943105 بايت) وen-US.pak (490357 بايت). ثبتت مطابقة ملفات eventtrace/main/preload/preview/codexmcp وsrc/ui/app.js داخل app.asar للمصدر. لم يُبنَ مثبت NSIS في هذه الجولة، فلا قياس جديد لحجمه.

## القبول البشري والقياس الحي

| التجربة | الدليل | النتيجة والحد |
|---|---|---|
| ظهور إجابة عربية | أرسل المالك الطلب المحدد وصوّر الإجابة في الحزمة | الجملة المطلوبة ظاهرة. لا يعيد هذا وحده حادثة OBS-142 |
| توصيل عدادات OBS-142 | قراءة API من هدف القشرة المحزومة عبر CDP محلي 127.0.0.1:9432 | للدور الأول emitted=13 وsent=13 وازدياد استقبال الواجهة 13؛ صفر إسقاط مسجل |
| أداة القرائية الأصلية | سجل جلسة التجربة يحوي استدعاءين browser_readability واستدعاءي browser_set_viewport، ولا browser_evaluate؛ ولقطة المالك تعرض الصفحة والتقرير | العرضان الفعليان 390×800 و839×800؛ 76 عنصراً لكل منهما؛ لا مخالفة مرصودة، وستة اتجاهات غير محسومة |
| دور يستخدم الأدوات | لقطة المالك وقراءة عدادات الدور الثاني | emitted=370 وsent=370 وازدياد استقبال الواجهة 370؛ صفر إسقاط مسجل |

عند نهاية التجربتين: مجموع استقبال الواجهة 384 = حدث تحديث أولي + 13 + 370. قراءة العدادات استقرت أثناء كل لقطة، وdropsOverflow=0.

خرج أداة القرائية نص منسق يحذف الحقول الصفرية؛ قيم direction/font/contrast/overflow=0 وunseen.shadow_roots=0 وunseen.iframes=0 وtruncated=false في جدول المساعد مشتقة من هذا الخرج وعقد formatReadability المقروء، وليست JSON خاماً أعادته الأداة. عدد unseen.direction=6 معلن صراحةً في الخرجين. لا تُعد هذه العناصر الستة مجتازة، ولا يشمل القياس نصوص div/span/button المجردة أو بقية حدود الأداة.

اللقطة الثانية وحدها تقص أعلى الجدول؛ ثُبت العرضان والعدد من سجل الاستدعاءات، وقُرئ الجدول الكامل من DOM. القبول البصري شهادة المالك، والقراءات التشخيصية دليل مكمل.

## الطقم الكامل

المشغّل: D:\sater\run-full-suite.ps1 في PowerShell خارجية، بمتابعة طرفية مستقلة.

- head=32aff6a، branch=main.
- البداية 2026-09-08 00:54:22؛ النهاية 01:03:34.
- النتيجة 100/100، وSUITE_EXIT=0، وعلامة .done تحمل 0.
- المدة المحسوبة 552 ثانية (9 دقائق و12 ثانية).
- السجل المحفوظ: full-suite-external.log بترميز UTF-16LE. نثره العربي مشوه؛ حقول ASCII والخاتمة مرجع النتيجة.
- لم تُعد الاختبارات بعد النجاح، ولم تتغير ملفات المصدر.

## حدود التشخيص والمتبقي

OBS-143 اجتازت استدعاء الأداة الأصلية في النسخة المبنية. OBS-142 تبقى مفتوحة: نجحت المحاولتان، ولم يُعَد إنتاج فقد الإجابة القديم.

لا يصح تعميم مساواة مجاميع العدادات: sent يشمل أحداثاً مولدة في main خارج emit؛ lang_slip يصل مباشرة دون زيادة sent؛ عداد الواجهة تراكمي بلا رمز دور؛ وتوجد مرشحات لا تسجل سبب الإسقاط. العدادات الحالية تثبت التوصيل وبعض الإسقاطات الصريحة ولا تحسم وحدها كل موضع فقد. لا تُسجل هذه الجولة إصلاحاً جديداً لذلك السبب.

طلب winget #429004 للرأس a80bede75514caf9773987fc9141e15dd5d61555 اجتاز الفحوص 01–10 وCLA في آخر تحقق، وبقي OPEN دون دمج. رابط المتابعة: https://github.com/microsoft/winget-pkgs/pull/429004

هذا القبول يخص الكود عند 32aff6a؛ يُحفظ ضمن تجهيز 2.16.21. بناء المثبّت النهائي وفحص التثبيت والترقية أدلة منفصلة، ولا يُنسب نجاحها إلى هذا الطقم.

بصمة SHA-256 لسجل الطقم المحلي: AB940813B4BCD8F520F4EFF7A7C2796C05DD75A7A725DD1B795C0AC2799BD13C
