# خطة تنفيذ سطر 2.0 — مراحل بمعايير قبول

> قاعدة ذهبية: مرحلة واحدة في كل جلسة عمل. بعد كل مرحلة شغّل التطبيق وتحقق من
> كل معيار قبول قبل الانتقال للتالية.

---

## المرحلة 0 — تشغيل الأساس ✅ (جاهزة في هذه الحزمة)

الهيكل الحالي: Electron + محادثة تعمل عبر `claude -p stream-json` + قائمة `/` أساسية + اختيار مجلد أصلي.

**معايير القبول (تحقق منها أولاً قبل أي تطوير):**
- [ ] `npm install` ثم `npm start` يفتح النافذة بدون أخطاء في الـ console
- [ ] إرسال «مرحبا» يعيد رداً عربياً سليم العرض
- [ ] رسالة ثانية تُكمل نفس المحادثة — النموذج يتذكر سياق الرسالة السابقة.
  ملاحظة: معيار القبول هو **استمرارية السياق** لا ثبات المعرّف — بعض إصدارات
  `claude -p --resume` تعيد `session_id` جديداً في كل تشغيل (تفريع جلسة) مع حفظ
  السياق، فتغيّر المعرّف في شريط الحالة ليس خطأ (الواجهة تلتقط المعرّف الجديد
  من حدث `result` وتستأنف به)
- [ ] كتابة `/` تظهر القائمة، والأسهم + Enter تعمل
- [ ] زر 📁 يفتح نافذة اختيار مجلد ويندوز الأصلية
- [ ] زر الإيقاف يوقف طلباً جارياً
- [ ] `npm run dist` ينتج مثبّت `dist/Satr Setup 2.0.0.exe` يعمل بعد التثبيت

---

## المرحلة 1 — متصفح الجلسات

قراءة جلسات Claude Code المحفوظة محلياً وعرضها واستئنافها.

- المصدر: `~/.claude/projects/` — كل مشروع مجلد باسم مسار مرمّز، بداخله ملفات `.jsonl`
  (كل ملف جلسة؛ افحص بنيتها الفعلية على الجهاز أولاً قبل كتابة المحلل)
- أمر `/جلسات` يفتح لوحة جانبية: عنوان مشتق من أول رسالة مستخدم + التاريخ + المشروع
- النقر على جلسة = تعيين sessionId و cwd ثم استئنافها، مع عرض آخر رسائلها في المحادثة
- IPC جديد: `satr:listSessions` و `satr:readSession` في main.js (قراءة فقط)

**قبول:** أرى جلساتي الحقيقية مرتبة بالأحدث، وأستأنف جلسة قديمة وتكمل من حيث توقفت.

---

## المرحلة 2 — لوحة الأذونات الحية (الترقية إلى Agent SDK)

النقلة الأهم: استبدال `claude -p` بـ **Claude Agent SDK** في العملية الرئيسية.

- ثبّت حزمة الـ SDK الرسمية (تحقق من اسمها الحالي في التوثيق)
- عند طلب إذن لأداة: مربع حوار عربي في الواجهة (الأداة + التفاصيل + موافقة / رفض / موافقة دائمة)
- بث الرد النصي تدريجياً (token streaming) بدل انتظار اكتمال الرسالة
- زر مقاطعة حقيقي أثناء عمل النموذج
- أبقِ مسار `-p` القديم كاحتياط خلف راية إعداد (fallback) حتى تثبت صلابة المسار الجديد

**قبول:** أطلب مهمة تتطلب تنفيذ أمر نظام في وضع الصلاحيات «افتراضي»، يظهر لي مربع
موافقة عربي، أوافق فيكمل، وأرفض في طلب آخر فيتوقف عن تلك الأداة ويكمل بدونها.

> **🔒 قرار نهائي — سلوك الأذونات (لا يُغيَّر):** المحرك يضبط
> `settingSources: ['user','project','local']`، فيحترم قوائم السماح
> (`allow`) في `settings.json`/`settings.local.json` تماماً كـ Claude Code التفاعلي.
> الأدوات المُصرّح بها مسبقاً تُنفَّذ دون مربع الحوار العربي — هذا **سلوك مقصود ومطابق
> لـ Claude Code، لا خلل**. مربع الحوار يظهر فقط للأدوات غير المُصرّح بها. تم تثبيت هذا
> القرار؛ لا تُعدّله ولا تجعل المربع يعترض كل أداة.

---

## المرحلة 3 — عارض التعديلات (Diff) العربي ✅ (مكتملة ومقبولة)

- عند أداة Edit/Write: عرض قبل/بعد بألوان (أخضر إضافة، أحمر حذف)، الكود LTR داخل واجهة RTL
- قابل للطي، مع عدّاد أسطر مضافة/محذوفة
- زر «تراجع» يعيد الملف لما قبل التعديل (احتفظ بنسخة المحتوى السابق في الذاكرة)

**قبول:** أطلب تعديل ملف، أرى diff ملوّناً واضحاً، وزر التراجع يعيد الملف فعلياً.

التنفيذ: خطّافا SDK `PreToolUse`/`PostToolUse` يلتقطان قبل/بعد (انظر `electron/agent.js`)،
حساب الفرق في `electron/diff.js` (نقي بلا اعتماديات)، حدث `file_edit` + IPC `satr:undoEdit`.
انظر «عارض الفرق العربي» في CLAUDE.md للعقد الكامل.

---

## المرحلة 4 — منصّة @ للملفات + لصق الصور ✅ (مكتملة ومقبولة)

- كتابة `@` تفتح متصفح ملفات المشروع (بحث تدريجي)، الاختيار يدرج المسار في البرومبت
- لصق صورة من الحافظة (Ctrl+V) يحفظها مؤقتاً ويرفقها بالطلب (الـ SDK يدعم الصور)

**قبول:** `@src/ind` يظهر index.html وأختاره، وألصق لقطة شاشة ويصف النموذج محتواها.

التنفيذ: `electron/files.js` (سرد قراءة-فقط) + IPC `satr:listFiles` + حقل `images` في
`satr:send` (محرك sdk فقط). انظر «منصّة @» في CLAUDE.md للعقد الكامل.

---

## المرحلة 5 — محوّل Gemini CLI (إثبات التعددية)

- طبقة `adapters/`: واجهة موحدة `{start, onEvent, stop}` — حوّل منطق claude الحالي إلى `adapters/claude.js`
- أضف `adapters/gemini.js` (لـ Gemini CLI وضع غير تفاعلي مشابه — افحص توثيقه الحالي)
- قائمة «المحرك» في الواجهة: Claude Code / Gemini CLI

**قبول:** أبدّل المحرك من القائمة وأحادث Gemini بنفس الواجهة العربية.

---

## المرحلة 5.5 — تكافؤ الأوامر الأساسية ✅ (مكتملة) ثم **تجميد الأوامر**

دفعة أخيرة مغلقة من ثلاثة أوامر يعتمدها مستخدم Claude Code اليومي، قبل الإطلاق:

- **`/موصلات` (`/mcp`)**: لوحة حالة خوادم MCP (متصل/قيد الاتصال/يحتاج مصادقة/فشل/معطّل)
  مع إعادة اتصال وتفعيل/تعطيل. عبر دوال تحكّم SDK في تشغيل عابر (`withControlQuery`).
  حدّ معروف مقبول: لا مصادقة OAuth داخل التطبيق (يصادَق الموصّل من Claude Code ثم «تحديث»).
- **`/سياق` (`/context`)**: امتلاء نافذة السياق (نسبة + رموز + توزيع الفئات) عبر `getContextUsage()`.
- **`/ضغط` (`/compact`)**: ضغط/تلخيص المحادثة عبر إرسال `/compact` كدور SDK؛ بطاقة قبل/بعد،
  والجلسة تكمل. تحقّق حيّ: `pre_tokens 25528 → post_tokens 1879`، نفس الجلسة، والسياق محفوظ.

التنفيذ: IPC `satr:mcpStatus`/`satr:mcpAction`/`satr:contextUsage` + حدث `compact_boundary`.
انظر «أوامر التكافؤ مع Claude Code» في CLAUDE.md للعقد الكامل.

> **🔒 تجميد الأوامر:** هذه آخر دفعة أوامر قبل الإطلاق. **لا تُضَف أوامر تفاعلية جديدة**
> بعدها (لا hooks ولا subagents ولا أوامر مخصّصة — ميزات متقدمة خارج النطاق). فكرة لوحة
> `/تكلفة` في المرحلة 6 مؤجَّلة احتراماً للتجميد (و`/سياق` يغطّي جزءاً من الحاجة). الانتقال
> التالي للمرحلة 6 (تلميع المنتج) مباشرةً.

**قبول:** `/موصلات` يعرض موصّلاتي بحالاتها وأعيد اتصال واحد؛ `/سياق` يُظهر نسبة الامتلاء
وتوزيع الرموز؛ `/ضغط` يضغط محادثة طويلة وتكمل بالملخّص.

---

## المرحلة 6 — تلميع المنتج النهائي

- [x] أيقونة تطبيق (build/icon.ico) وعلامة «سطر» في شريط المهام — `scripts/make-icon.js` +
  `setAppUserModelId` + `build.win.icon`
- [x] **معالج أول تشغيل (مانع الإطلاق)**: بوابة عربية تحجب المحادثة حتى يتوفّر Claude Code،
  IPC `satr:preflight` (node/npm/claude) + `resolveClaudeBin(force)`، خطوات إرشاد + «أعد الفحص».
  انظر «بوابة أول التشغيل» في CLAUDE.md.
- [ ] تحديث تلقائي (electron-updater) من GitHub Releases — **مؤجَّل للمرحلة 7** (يحتاج مستودعاً
  عاماً وإصدارات لا توجد بعد)
- ~~لوحة `/تكلفة`~~ — مُلغاة (تجميد الأوامر، انظر المرحلة 5.5)

> **⛔ متطلّب حرج — مانع إطلاق (لا غنى عنه):** «سطر» صار يعتمد على Claude Code المثبّت
> عالمياً — محرك SDK يستدعي الثنائي العام عبر `pathToClaudeCodeExecutable` (انظر
> `resolveClaudeBin` في `electron/agent.js`)، ومحرك CLI الاحتياطي يحتاجه أيضاً. لذا
> معالج أول التشغيل **يجب** أن:
> 1. يكتشف غياب Claude Code (نفس منطق `resolveClaudeBin` + فحص الإصدار).
> 2. يرشد المستخدم خطوة بخطوة بالعربية للتثبيت: تثبيت Node ثم
>    `npm install -g @anthropic-ai/claude-code` ثم تسجيل الدخول `claude` مرة واحدة.
> 3. يمنع الوصول للمحادثة قبل توفّره (مع زر «أعد الفحص»).
> هذا **مانع إطلاق وليس تحسيناً اختيارياً** — بدونه يفشل التطبيق صامتاً عند أول رسالة.

**قبول:** مستخدم جديد بلا Claude Code يُرشَد بالعربية لتثبيته خطوة بخطوة، وبعد الفحص
يصل لأول محادثة ناجحة دون فتح طرفية يدوياً.

---

## المرحلة 7 — إطلاق المصدر المفتوح ✅ (مكتملة ومقبولة)

«سطر» مشروع مجتمعي مفتوح المصدر برخصة MIT، مُطلَق على GitHub: https://github.com/m4u05999/Satr

- [x] **الرخصة:** ملف `LICENSE` برخصة MIT (© 2026 محمد عبدالرحيم).
- [x] **README عربي:** شرح المشكلة والحل، خطوات التثبيت، ولقطات شاشة «قبل/بعد»
  (`assets/before.png` و`after.png` و`satr.png`) تُظهر فرق عرض العربية (RTL + التشكيل).
- [x] **مستودع GitHub:** إعداد المستودع، `.gitignore` عام، وقوالب القضايا العربية
  (`.github/ISSUE_TEMPLATE/`: تقرير علّة + اقتراح ميزة). الإصدار `v2.2.0` يرفع المثبّت يدوياً.
- [x] **الرعاية:** `.github/FUNDING.yml` (`ko_fi: m4u05999`) — زرّ رعاية متواضع بلا إلحاح.
- [x] **بيان الاستقلالية:** قسم صريح في README أن «سطر» أداة مجتمعية مستقلة، ليست منتَج
  Anthropic ولا مدعومة منها؛ Claude علامة لـ Anthropic.
- [x] **بيان الخصوصية:** قسم صريح في README — لا تتبّع، لا جمع بيانات، المحادثات تمر عبر
  Claude Code المثبّت لدى المستخدم فقط.
- [x] **CONTRIBUTING.md** عربي: التشغيل، القواعد المعمارية المثبّتة، فلسفة «مرحلة واحدة».

**قبول:** يصل زائر جديد لصفحة GitHub، يفهم ما المشروع من README العربي ولقطاته، يثبّته
بحرّية، ويرى بوضوح أنه مشروع مستقل عن Anthropic بلا أي تتبّع. **تم تحقيقه.**

---

## المرحلة 8 — الطرفية العربية المدمجة (BiDi Terminal) — قيد التنفيذ

> كانت «فكرة مؤجلة» (الخندق التنافسي بعيد المدى) — رُقّيت لمرحلة رسمية. **التصميم الكامل
> والقرارات المثبّتة في `docs/PHASE8-DESIGN.md` — اقرأه أولاً وجوباً قبل أي مرحلة فرعية.**
> المعمارية المعتمدة: نسخة xterm.js حقيقية واحدة (محرك حالة) + عارضان — BiDi HTML
> افتراضياً للأوامر السطرية، وعارض xterm.js الشبكي لتطبيقات الشاشة البديلة (vim/htop).
> الفتح بزرّ 🖥️ في الشريط العلوي لا بأمر `/` جديد (احتراماً لتجميد الأوامر).

> **🔒 قرار أسلوب القبول (مثبّت):** التجربة أثناء التطوير عبر `npm start` فقط (نافذة
> التطبيق مباشرة — لا تثبيت). **لا يُبنى ولا يُختبر مثبّت NSIS في قبول المراحل 8.2–8.4**؛
> اختبار المثبّت بند واحد نهائي قبل الإصدار. (سلسلة التحزيم متحقَّقة آلياً في 8.1:
> `npm run dist` ينجح، node-pty يعمل من `app.asar.unpacked` بمحرك Satr.exe المحزوم نفسه.)

### 8.1 — الأساس: node-pty + طرفية xterm.js قياسية ✅ (منجزة — بانتظار اختبار القبول)

- [x] node-pty (استثناء اعتماديات واعٍ — موثَّق في CLAUDE.md القاعدة 5) + توثيق البناء
- [x] xterm.js مُضمّن (vendored) في `src/vendor/` عبر `scripts/vendor-xterm.js`
- [x] `electron/term.js`: pty واحد + IPC (`satr:termStart/termInput/termResize/termKill` +
  قناة أحداث `satr:term`) + قتل عند الإغلاق — العقد الكامل في CLAUDE.md
- [x] لوحة طرفية في الواجهة بعارض xterm.js القياسي (بلا عربية بعد — مقصود)
- [x] التحقق من التحزيم: `npm run dist` والوحدة الأصلية تعمل من `app.asar.unpacked`

**قبول 8.1** (عبر `npm start` — قرار أسلوب القبول أعلاه): أفتح الطرفية وأنفّذ `dir`
و`git status` بألوانهما؛ برنامج تفاعلي يعمل؛ Ctrl+C يقطع؛ إغلاق «سطر» يقتل الصدفة.
(بند المثبّت مؤجَّل للفحص النهائي؛ تحقَّق آلياً: `PACKAGED_PTY echo=true`.)

### 8.1.5 — تصليب الواجهة (دفعة مغلقة قبل 8.2) ✅

بطلب مراجعة المالك بعد تجربة 8.1: دفعة إصلاحات عملية **مغلقة** (تصليب لا إعادة تصميم)
تجهّز الهيكل لما ستبنيه 8.2/8.3 داخل لوحة الطرفية:

- [x] طيّ النموذج/المحرك/الصلاحيات خلف زرّ ⚙ منبثق (نفس المعرّفات — صفر تغيير منطق)
- [x] مقبض سحب لارتفاع لوحة الطرفية + حفظ/استرجاع من localStorage
- [x] حالة نشطة لزرّي 🖥️ و⚙ + نقطة حالة الصدفة (خضراء/حمراء) في رأس اللوحة
- [x] تعريب شريط الحالة («لا جلسة»/«جلسة: …»/«الكلفة: …») بسلوك RTL سليم
- [x] إصلاح انكسار تلميحات الحالة الفارغة (قائمة تلميحات منسّقة بدل فقرة تنكسر)
- [x] أشرطة تمرير داكنة موحّدة

**قبول:** الشريط العلوي: مجلد + جلسة جديدة + 🖥️ + ⚙ فقط؛ اختيارات ⚙ تعمل كالسابق؛
سحب حافة الطرفية يغيّر ارتفاعها ويبقى بعد إعادة التشغيل؛ لا نص إنجليزي في شريط الحالة.

### 8.2 — عرض BiDi للمخرجات (قلب المرحلة) ✅ (منجزة — بانتظار اختبار القبول)

إسقاط HTML **نافذي** للقراءة فقط من Buffer نسخة xterm.js (سطر = عنصر `dir=auto`، ألوان
spans بلوحة 256 + truecolor) بتحديث تفريقي (بصمة لكل سطر) مُجمَّع بـ rAF، والتصاق بالذيل
ينفك بالتمرير. انظر «ملاحظات تنفيذ 8.2» في وثيقة التصميم (الإسقاط النافذي عوّض خطة
الإلحاق — أصحّ عند تشبّع scrollback وأوفى لمبدأ «محرك واحد»). زرّ «العرض: عربي/شبكي»
للتبديل اليدوي حتى يأتي التلقائي في 8.4. تحقق آلي: Buffer API (نص عربي + ألوان palette)
سليم على xterm 6 المضمّن.

**قبول 8.2** (عبر `npm start`): خرج عربي مشكّل RTL صحيح (`echo "مرحبا بالعالم"` /
`git status` بأسماء عربية / سكربت يطبع مختلطاً)؛ وفيضان build لا يجمّد الواجهة
(استجابة < 100م.ث + ذاكرة مستقرة).

### 8.3 — الإدخال والتحديد ✅ (منجزة — بانتظار اختبار القبول)

سطر إدخال HTML عابر أسفل اللوحة (الصدى: صدى الصدفة هو المعروض الوحيد — قرار §8.2 من
وثيقة التصميم)، وعقد المفاتيح المنفَّذ حرفياً من §8.3: Enter (وفارغ ⇐ `\r`)، Ctrl+C/Z/D،
▲▼ تاريخ بحقل فارغ، Tab محجوب بتصميم، لصق متعدد الأسطر خام. النسخ من عرض BiDi تحديد
متصفح أصلي (نص DOM منطقي فالنسخ منطقي صحيح). في الوضع الشبكي يختفي الصف (إدخال خام
عبر xterm). لا IPC جديد.

**قبول 8.3** (عبر `npm start` — عقد §8.3 كاملاً): أمر باسم ملف عربي يُكتب بعربية مثالية
في الحقل ويُنفَّذ بلا ازدواج عرض؛ محثّ y/n يُجاب؛ Ctrl+C يقطع ويفرّغ؛ ▲ وحقل فارغ
يستدعي التاريخ وEnter ينفّذه؛ نسخ سطر عربي من العرض يلصق سليماً خارجياً؛ Tab لا يعمل
بتصميم موثّق (تلميح ظاهر بجوار الحقل).

### 8.4 — التبديل التلقائي والتلميع ✅ (منجزة — بانتظار اختبار القبول)

ثلاث طبقات تبديل: (1) `buffer.onBufferChange` — شاشة بديلة ⇐ شبكي، والعودة تسترجع
اختيار المستخدم (تحقق آلي: `["alternate","normal"]` على xterm 6 المضمّن)؛ (2) أوامر
معروفة تعكس العربية بنفسها (claude — بموافقة المالك بعد لقطة القبول) تُكشف من سطر
الإدخال وتبدّل للشبكي مع شريط تنبيه وزرّ عودة؛ (3) الزرّ اليدوي يضبط التفضيل.

**قبول 8.4** (عبر `npm start`): فتح تطبيق شاشة بديلة (مثل `python -c "import curses; …"`
أو vim إن وُجد) يبدّل للشبكي تلقائياً والخروج يعيد العربي؛ كتابة `claude` في سطر الإدخال
تبدّل للشبكي مع التنبيه وزرّ «العرض العربي» يعيد؛ جلسة مختلطة كاملة بلا كسر.

---

## خارطة ما بعد المرحلة 8 (اعتُمدت بعد اكتمال الطرفية — 2026-07-08)

> نفس القاعدة الذهبية: مرحلة واحدة في الجلسة، ومعايير قبول قبل الانتقال.
> المرحلة 8 خرجت للاستخدام الفعلي وأثبتت أن لقطات المالك أثناء العمل الحقيقي
> هي أفضل مصدر إصلاحات (7 إصلاحات جوهرية في يوم واحد) — لذا الاستقرار أولاً.

### المرحلة 9 — الإطلاق العلني لـ v2.3 (جلسة قصيرة واحدة)

- [ ] رفع الإصدار إلى 2.3.1 (يضم إصلاحات ما بعد المثبّت الأول: تنظيم الإجراءات،
  عرض الإذن، منع AskUserQuestion، وقاية/تعافي الجلسات) وبناء المثبّت النهائي
- [ ] README: قسم «الطرفية العربية المدمجة» بلقطتي قبل/بعد (طرفية عادية تعكس
  العربية مقابل عرض «سطر» المشكّل) — الحجة التسويقية الأقوى
- [ ] إصدار GitHub Release v2.3.1 بالمثبّت وملاحظات إصدار عربية (عبر gh)

**قبول:** زائر جديد يفهم ميزة الطرفية من README ويحمّل المثبّت من Releases ويعمل.

### المرحلة 10 — فترة استقرار (أسبوع استخدام حقيقي — لا تطوير)

المالك يستخدم «سطر» يومياً في مشاريعه ويجمع الملاحظات؛ تُعالج على دفعات صغيرة
(كل دفعة جلسة) بدل مقاطعة الاستخدام. **لا ميزات جديدة خلالها.**

### المرحلة 11 — التحديث التلقائي (electron-updater)

كانت مؤجلة من المرحلة 6 لغياب مستودع عام وReleases — **الشرطان تحققا**. تُنهي
عناء إعادة التثبيت اليدوي نهائياً. تتطلب توقيعاً أو تعايشاً مع تحذير SmartScreen —
يُبحث قبل التنفيذ.

### المرحلة 12 — أسئلة الاختيار العربية (استرداد AskUserQuestion)

بدل منعها (إصلاح 23692df): واجهة أزرار اختيار عربية حقيقية في المحادثة — تفوّق
تجربة على Claude Code الطرفي نفسه. **تتطلب بحثاً أولاً**: هل يتيح SDK إعادة
نتيجة الاختيار (canUseTool يعطي سماح/رفض فقط اليوم)؟ إن لم يتح، تبقى ممنوعة.

### المرحلة 13 — تحسينات الطرفية (دفعة مغلقة من المؤجلات)

- «وضع بث الحروف» اختياري: كل ضغطة تُرسل خاماً ⇐ يفتح إكمال Tab وCtrl+R
- زرّ 👁 لإخفاء الكتابة (كلمات المرور) — مؤجل موثّق من 8.3
- تبويبات/تقسيم طرفيات متعددة (يكسر قيد «طرفية واحدة» بقرار واعٍ)

### المرحلة 14 — تكافؤ الدفعة الثانية (معتمدة بعد جرد متحقق — ثم **تجميد جديد**)

> جرد حيّ (يوليو 2026): توثيق Claude Code الرسمي + أنواع SDK المثبَّت + اختباران حيّان
> (وكيل فرعي حقيقي بثّ أحداثاً متداخلة، وsupportedCommands أعاد 27 أمراً).
> دفعة مغلقة بأربعة بنود بترتيب المالك؛ بعدها تجميد أوامر جديد.

- **14.1 — مزامنة قائمة الأوامر** ✅ (منجزة — بانتظار اختبار القبول):
  `agent.listCommands` (تشغيل عابر ⇐ `supportedCommands()`) + IPC `satr:listCommands` +
  دمج ديناميكي في قائمة «/» (لون مميز، إدراج في المحرّر ثم إرسال كدور — نمط /ضغط) +
  معالجة حدث `system/commands_changed` لتحديث منتصف الجلسة (يُستبدل الكاش كاملاً).
  المستبعد: clear/compact/context (لها نسخ عربية أصلية).
  **قبول:** كتابة `/` تُظهر الأوامر الأصلية فوراً وأوامر CLI تلحق (بلونها)؛ اختيار
  `/verify` أو `/تفقيط` يُدرج الأمر ويُرسل كدور ويعمل؛ القائمة تتصفح بالأسهم مع تمرير.
- **14.2 — الوكلاء الفرعيون**: تعريفات `.claude/agents` (نمط skills.js) + رقاقات متداخلة
  تحت رقاقة الوكيل عبر `parent_tool_use_id` + `forwardSubagentText` + تسميات عربية.
- **14.3 — نقاط الحفظ /rewind**: **تحقق حيّ أولاً** لـ `rewindFiles(uuid,{dryRun})` —
  أي غموض في الاسترجاع ⇐ توقف وتقرير بدل البناء.
- **14.4 — راكبان** (جلسة واحدة): `/effort` في ⚙ + `/add-dir` (مجلدات إضافية).

### أفكار مؤجلة (بلا موعد)

- محوّل Gemini CLI (المرحلة 5 الأصلية — إثبات التعددية) ثم Codex
- إدخال صوتي عربي (Web Speech API)
- نقاط حفظ git تلقائية قبل المهام الكبيرة
- نسخة macOS و Linux
- عربية داخل تطبيقات الشاشة الكاملة (BiDi للشبكة البديلة)
