# العدد ٠٠٨ — الأربعاء (ملخّص البريد — الملف الكامل لم يُنقل بعد)

> `SATR RADAR · EDITION 008 · 2026-09-09 · WED` · ٧ بنود (١ مباشر · ٣ يستحق OBS · ٣ للعلم) · الجبهات اليومية F1/F2/F5/F8 · **الرادار يقترح ولا ينفّذ**

**حدّ هذا الملف معلن**: هذا **ملخّص البريد** لا متن العدد. بريد «رادار سطر — العدد ٠٠٨ — 2026-09-09» (‏thread `1a084829234bfb76`) حمل مرفقاً واحداً هو `README.md`، بينما `008.md` و`state.json` سُلّما ملفَّين قابلين للتنزيل **داخل جلسة Claude (‏Cowork)** ولم يُنقلا بعد. المالك يستبدل هذا الملف بالأصل حين يُنقل — ولا يُختلق متنٌ هنا.

## ما صحّحته في الحالة

`HEAD = f230c98` والوسم `v2.16.21` — **صفر التزام جديد** منذ العدد ٠٠٧، والقفل بلا تغيّر. وتحرّكت خمس حزم في السجلّات: ‏`agent-sdk 0.3.263 → 0.3.266` · `claude-code 2.1.263 → 2.1.266` · `electron 44.2.0 → 44.3.0` · `gemini-cli 0.58 → 0.59` · ووسم `v26` لـ`electron-builder 26.16.1`. وتصحيحان: ‏`0.3.264` غير موجودة أصلاً، و`sdk.d.ts` متطابق بين `0.3.265` و`0.3.266`.

## مباشر — يلزم قراراً أو كوداً (‏F2 · يهدد سطر)

‏SDK ‏`0.3.265` قلبت افتراض `systemPromptSnapshot` من «مطفأ حين تمرّر `append`» إلى «**مُشغَّل دائماً**»: النصّ المُلحَق يُسجَّل في أول طلب ويُعاد حرفياً في كل طلب لاحق واستئناف، وأي `append` مختلف **يُتجاهل** حتى الضغط أو جلسة جديدة. وسطر يُلحق فيه اسم النموذج نفسه (‏`envbrief.js:136` عبر `runtimeenv.environmentLine`) والذاكرة وكتالوج المهارات، ويمرّر `resume` و`model` معاً — فتبديل النموذج في جلسة قائمة يترك النموذج يقرأ سطر بيئة قديماً، **صامتاً بلا خطأ**.

سطر على `0.3.261` فهو **غير متأثّر اليوم**: هذه **بوابة على الترقية** — إمّا `systemPromptSnapshot: false` صراحةً، وإمّا نقل الأجزاء المتغيّرة إلى ذيل الدور كما يفعل `context.js` للمحوّلات. ولا بند عن هذا في أيّ changelog؛ قِيس بمقارنة `sdk.d.ts`.

## يستحق OBS

1. ‏`sdkMcpServerManifests` الجديد يُلغي مصافحة MCP قبل أول دور، وسطر يسجّل أربعة خوادم داخل العملية.
2. ‏`2.1.265` حصّن كلود كود من مرشّحات `filter.*.clean` يعلنها مستودع متداخل — وهي الفئة التي أعلن `gitsafe.js` خروجها بحجّة صارت أضعف.
3. ‏`cd` صار يبقى بين الأدوار في جلسات SDK — **دلالة مجلد العمل تغيّرت**.

## للعلم

- إصلاحان آخران لاستقرار البادئة في `2.1.265` (‏التصديق الثالث لمحور E).
- ‏`npm audit` على القفل الحيّ: ‏٢٣ تنبيهاً و١ حرجة، وسلسلة `electron-builder` كلها `dev:true` فالانكشاف **جهاز البناء لا المستخدم**.
- البوابات ساكنة.
