# العدد ٠٠٣ — خط أساس الجبهة الحادية عشرة

> `2026-09-04 · F11 tech scouting · 6 axes · 24 evaluated · 11 open / 8 watch / 5 grave` · المصدر: صفحة «رادار سطر» على claude.ai · **الرادار يقترح ولا ينفّذ**

الجبهة الهجومية تعمل بالعكس: من **رغبة معلنة** لكل محور إلى ما يشبعها. كل مرشّح مرّ بالأسئلة الخمسة (المحور، ما يتغيّر للمستخدم العربي، الرخصة، الصيانة، كلفة الدمج) وخرج بنتيجة واحدة من ثلاث. الرخص وتواريخ النشر من npm/crates.io/PyPI أو صفحة المستودع مباشرةً — GitHub API كان محجوباً فاعتُمدت السجلّات. هذا خط الأساس فتجاوز سقف الخمسة؛ الأعداد الأسبوعية القادمة تلتزم به.

الأرقام: افتح OBS / راقب / مقبرة. **ما أنجزه المالك منذ العدد ٠٠١ ولم يعد مرشّحاً:** حزمة MSIX للمتجر، manifests winget وPR ‎#429004، حقائق Certum (‏OBS-096)، هجرة DeepSeek إلى v4، فصل كتالوج NIM عن Groq — فسقطت من القائمة أدناه رغم أن البحث أعادها.

| الجبهة | البنود |
|---|---|
| **A** المعاينة | ٣ / ٣ / ١ |
| **B** الطرفية | ٤ / ٣ / ٠ |
| **C** التوزيع | ٢ / ٢ / ٢ |
| **D** مهارات الوكيل | ٤ / ٢ / ٠ |
| **E** تخفيض الرموز | ٤ / ٣ / ٠ |
| **F** وكيل سطر الخاص | ٣ / ٤ / ٣ |

## افتح OBS — رغبة مُشبَعة بمرشّح ناضج ورخصة سليمة

### سطر يُبطل التخزين المؤقت للبرومبت بنفسه كل دور — كتلة الميزانية وخريطة المستودع داخل system prompt تغيّران البادئة

*F11 · E تخفيض الرموز · تقنية لا مكتبة* — `measured`

قراءة الكود: `buildBlindContext` يختم system prompt بكتلة `<satr_context_budget … starting_input_tokens≈N>` تتغيّر كل دور، و`repo_map` يُرتَّب حسب مصطلحات البرومبت — فالبادئة التي يعتمد عليها التخزين المؤقت تُبطَل عند بداية كل دور على كل المزوّدين. الأرقام من المزوّدين أنفسهم: DeepSeek إصابة الكاش $0.014 مقابل $0.14 (‏−90٪) وTTFT من ١٣ث إلى ٠٫٥ث على 128K، بمطابقة بادئة من الرمز صفر بوحدة ٦٤ رمزاً؛ Groq خصم ٥٠٪ على المخزَّن لعائلة `gpt-oss` فقط بحدّ أدنى ١٢٨–١٠٢٤ رمزاً وTTL ساعتان؛ Qwen implicit cache = ٢٠٪ من سعر الإدخال بحدّ أدنى ١٠٢٤ رمزاً؛ Ollama/llama.cpp يعيد استعمال KV تلقائياً — قياس nanobot: نقل تعريفات الأدوات إلى كتلة system رفع إعادة الاستعمال من ٤٤٪ إلى ٩٩٫٨٪ وخفض الرموز المعاد تقييمها من ٤٧٥٢ إلى ١٥. و`usage.js` يقرأ `cached_tokens` و`prompt_cache_hit_tokens` أصلاً — البنية جاهزة للقياس.

- **الأثر**: الدور الثاني فصاعداً أرخص وأسرع على DeepSeek وGroq وQwen، وعلى Ollama تختفي فترات الصمت بعد الضغط.
- **الكلفة**: ١–٢ يوم، بلا تبعية: نقل كتلة الميزانية وخريطة المستودع إلى رسالة ذيلية، وإظهار نسبة إصابة الكاش في الواجهة.
- **النتيجة**: افتح OBS — أعلى عائد/كلفة في المسح كله، والعيب محدّد بالسطر.

المصادر: [deepseek · context caching](https://api-docs.deepseek.com/news/news0802/) · [groq · prompt-caching](https://console.groq.com/docs/prompt-caching) · [model studio · context-cache](https://www.alibabacloud.com/help/en/model-studio/context-cache) · [nanobot · ollama prompt cache](https://nanobot.wiki/docs/0.3.0/guides/configure-ollama-prompt-cache)

### مسح نتائج الأدوات القديمة قبل التلخيص — طبقة بلا نموذج تسبق الضغط

*F11 · E تخفيض الرموز · تقنية لا مكتبة* — `−48% / −50%`

Anthropic cookbook (وكيل بحث ≈329K رمز): ذروة السياق 335,279 → 173,137 بالمسح وحده (‏−48٪) بلا كلفة استدلال، و→ 169,164 بالتلخيص (‏−50٪) بملخّص ≈2,783 رمزاً حفظ الحقائق العليا وأسقط التفاصيل الدقيقة. OpenCode الإنتاجي: `keep.tokens` 15,000 و`buffer` 20,000، ومخرجات الأدوات تُقصّ إلى ٢٠٠٠ حرف في الذيل، وآخر 40,000 رمز محمية. وتحذير مقيس من hermes-agent: إعادة بناء system prompt عند الضغط تُبطل KV cache فتظهر «توقفات لدقائق» محلياً. في سطر `MAX_SUMMARY_CHARS = 12000` ولا طبقة مسح بلا نموذج قبله.

- **الأثر**: جلسات أطول قبل الضغط، وبلا صمت على Ollama بعده — والمرساة الذيلية تبقى في الذيل لا في البادئة.
- **الكلفة**: ٢–٣ أيام، بلا تبعية.
- **النتيجة**: افتح OBS — يكمّل البند السابق.

المصادر: [claude cookbook · context engineering](https://platform.claude.com/cookbook/tool-use-context-engineering-context-engineering-tools) · [opencode · compaction](https://opencode.ai/v2/docs/compaction/) · [hermes-agent#4319](https://github.com/NousResearch/hermes-agent/issues/4319)

### ضريبة الرموز العربية مقيسة — وعقد اللغة يمسّ النثر فقط لا الكود ولا نتائج الأدوات

*F11 · E تخفيض الرموز · قياس* — `1.35–1.7× · 3.3×`

الجدول المجمّع: cl100k ‏3.30× (١٥٢ مقابل ٤٦ رمزاً لسؤال واحد)؛ o200k (‏gpt-oss/GPT-4o) ‏1.35× إلى ‏1.70×؛ Qwen2.5 ‏1.70×؛ Mistral-7B ‏3.86×؛ خصوبة الرمز/كلمة على العربية: Jais 1.67 · gpt-4o 2.12 · Llama-3 2.37 · Qwen1.5 2.44 · Phi-3 ‏5.46؛ وورقة ALLaM: Llama-2 ≈٦–٧ رمز/كلمة مقابل ALLaM ≈١٫٥–٢. **لا قياس عربياً منشوراً لـClaude أو DeepSeek** — وسطر يستطيع إنتاجه من `usage.js` بنفسه. المهم: في قياس Anthropic شكّلت نتائج الأدوات ٩٦٪ من النافذة، فالضريبة تقع على نثر المخرجات لا على أغلب السياق.

- **الأثر**: يحوّل «العقد يكلّف» من انطباع إلى رقم لكل نموذج، ويحدّد أين يُصرف جهد التخفيض (النثر) وأين لا (الأدوات).
- **الكلفة**: يوم واحد: مقياس في `usage.js`/`langmetric.js` — رموز المخرجات ÷ الأحرف للمقاطع العربية مقابل الإنجليزية من عدّادات المزوّد.
- **النتيجة**: افتح OBS (قياسي).

المصادر: [llm-language-token-tax](https://github.com/vfalbor/llm-language-token-tax) · [arxiv 2608.09046](https://arxiv.org/html/2608.09046) · [arabic-tokenizers-leaderboard](https://huggingface.co/spaces/MohamedRashad/arabic-tokenizers-leaderboard) · [ALLaM paper](https://arxiv.org/pdf/2407.15390)

### تحصين `edit_file`: تطبيق متعدد الكتل غير مرتَّب، وارتداد بتطبيع المسافات، وبوابة «اقرأ قبل التعديل»

*F11 · E · F · تقنية لا مكتبة* — `20%→61% · +10%`

سطر يستعمل search/replace أصلاً؛ الناقص ما أثبتته المشاريع الأخرى: Aider — تنسيق udiff رفع النجاح من ٢٠٪ إلى ٦١٪ على GPT-4 Turbo، وإزالة الترقيع المرن ضاعفت أخطاء التعديل ٩ مرات؛ Cline — خوارزمية order-invariant multi-diff رفعت نجاح التعديل بأكثر من ١٠٪؛ Crush — ارتداد بتطبيع المسافات وإلزام القراءة قبل التعديل. وCline ينشر حزمة تقييم `evals/diff-edits` يمكن محاكاتها لقياس `edit_file` على النماذج المجانية قبل اعتمادها. تحذير: الأرقام نجاح تعديل لا رموز — لم أجد مصدراً أولياً يقيس رموز search/replace مقابل الملف الكامل.

- **الأثر**: النماذج المجانية الأضعف تنجح في التعديل بدل الفشل ثم إعادة كتابة الملف كاملاً (ما يضاعف رموز الإخراج).
- **الكلفة**: ١–٢ يوم للتحصين + ٢ يوم لتقييم صغير على نمط Cline، بلا تبعية.
- **النتيجة**: افتح OBS.

المصادر: [aider · unified diffs](https://aider.chat/docs/unified-diffs.html) · [cline · diff edits +10%](https://cline.bot/blog/improving-diff-edits-by-10) · [crush · edit.go](https://github.com/charmbracelet/crush/blob/main/internal/agent/tools/edit.go)

### chrome-devtools-mcp — التحكّم القياسي للوكيل بمعاينة سطر عبر CDP بدل الأفعال المخصّصة

*F11 · A المعاينة · Apache-2.0* — `1.8.0 · 2026-08-25 · 49.3k★`

MCP رسمي من فريق Chrome DevTools: ٥٦+ أداة (snapshot/screenshot/input/network/console/performance) فوق CDP، بلا تبعيات (يحزم puppeteer داخله)، إيقاع أسبوعي. سطر يملك Chromium أصلاً: `remote-debugging-port` على 127.0.0.1 بمنفذ عشوائي يجعل ضيف الـwebview هدف CDP. تحذير مثبت: v0.20.1 كسر Electron (‏`Target.getDevToolsTarget`، issue #1197 مغلق بلا PR ظاهر) — Electron ليس هدفاً مدعوماً رسمياً؛ يوجد fork يعالجه (`holepunchto/electron-devtools-mcp`). البديل الأثقل Playwright MCP (0.0.80، يجرّ playwright كاملاً) يبقى خطة B.

- **الأثر**: الوكيل يفحص صفحة خادم التطوير بالأدوات القياسية التي يعرفها، ويحصل على a11y snapshot نصّي رخيص بدل لقطة شاشة.
- **الكلفة**: ٣–٥ أيام؛ لا تبعية في package.json (يُشغَّل خارجياً كـCodex CLI)؛ لحلقة سطر الخاصة عميل MCP stdio صغير ~٣٠٠ سطر.
- **النتيجة**: افتح OBS — يُختبر أولاً مع الـwebview الحقيقي لأن Electron قد يكسر بعض الأدوات.

المصادر: [chrome-devtools-mcp](https://github.com/ChromeDevTools/chrome-devtools-mcp) · [issue #1197 (Electron)](https://github.com/ChromeDevTools/chrome-devtools-mcp/issues/1197) · [electron-devtools-mcp fork](https://github.com/holepunchto/electron-devtools-mcp)

### @mozilla/readability + turndown محقونتان في الـwebview — قارئ ويب رخيص بالرموز بلا متصفح إضافي

*F11 · A المعاينة · Apache-2.0 · MIT* — `readability 0.6.0 · turndown 7.2.4`

مكتبة Reader View من Firefox (١٥٥KB، صفر تبعيات) تُحقن بـ`webview.executeJavaScript` فتعيد نصّاً نظيفاً ثم Markdown عبر turndown. بديل أحدث `defuddle` (MIT، 0.19.3، 2026-08-22) لكنه ٢٫٧MB. وأثقل منه وأغنى: `agent-browser` من Vercel (Apache-2.0، 0.36.0 بتاريخ 2026-09-01، 41k★) — CLI بـRust يعطي snapshot لشجرة a11y مضغوطة بمراجع `@e1` ويتصل بمتصفح قائم عبر `--cdp`؛ ثنائي ويندوز أصلي ٩٣MB يُنزَّل ولا يُحزم.

- **الأثر**: «اقرأ توثيق المكتبة» يكلّف مئات الرموز بدل عشرات الآلاف، والنص العربي يبقى بترتيبه المنطقي لأنه من DOM لا من لقطة — وهذا يخفّف الإطار الإنجليزي الذي يغذّي الانزلاق (OBS-001).
- **الكلفة**: ١–٢ يوم لـReadability (ملف vendored واحد + NOTICE)؛ ٢–٣ أيام لـagent-browser كأداة خارجية اختيارية.
- **النتيجة**: افتح OBS لـReadability الآن؛ agent-browser معه بشرط تثبيت الـAPI (0.3x يتغيّر أسبوعياً).

المصادر: [mozilla/readability](https://github.com/mozilla/readability) · [turndown](https://github.com/mixmark-io/turndown) · [vercel-labs/agent-browser](https://github.com/vercel-labs/agent-browser)

### خريطة logical↔visual لكل سطر: bidi-js لتحديد المستويات، ونموذج CodeMirror 6 للمؤشّر والتحديد البصريين

*F11 · B الطرفية · MIT · 12KB* — `bidi-js 1.0.3 (2023-07) · CM6 6.43.11 (2026-09-03)`

`bidi-js` تنفيذ UAX#9 خالص بـJS (‏`getEmbeddingLevels`/`getReorderSegments`/المرايا/الأقواس N0)، ‏12KB مصغّراً، يطبّق UBA 13 ويعلن اجتياز اختبارات المطابقة — ركوده منذ 2023 مقبول لأن الخوارزمية لا تتغيّر. و`@codemirror/view/src/bidi.ts` أوضح نموذج DOM مكتمل ومصان لـ`moveVisually` والتحديد المرسوم مستطيلات منفصلة لكل run (MIT، إصدار أسبوعي). ومواصفة terminal-wg BiDi (‏BDSM ‏`CSI 8 h/l`، SCP، `DECSET 2500/2501`) مع تنفيذ VTE المرجعي تحوّل خندق سطر من «خدعة عرض» إلى تنفيذ مواصفة يستطيع TUI الوكيل أن يخاطبها — وتجيب عن سؤال التحديد المنطقي/البصري نصّاً.

- **الأثر**: الأسهم تتحرك بصرياً وBackspace منطقياً، والنسخ بالترتيب الصحيح في الأسطر المختلطة — الجودة التي تفرّق سطر عن نسخة ar-terminal (‏OBS-098).
- **الكلفة**: ٣–٥ أيام لـbidi-js (‏vendored، بلا تبعية) + ٥–٨ أيام لنقل نموذج CM6 + ٣–٥ أيام لمعالجات CSI عبر `parser.registerCsiHandler`.
- **النتيجة**: افتح OBS (ثلاثة بنود مترابطة؛ الأول أساس للاثنين).

المصادر: [lojjic/bidi-js](https://github.com/lojjic/bidi-js) · [codemirror/view · bidi.ts](https://raw.githubusercontent.com/codemirror/view/main/src/bidi.ts) · [terminal-wg · BiDi escape sequences](https://terminal-wg.pages.freedesktop.org/bidi/recommendation/escape-sequences.html)

### harfbuzzjs — امتلاك التشكيل داخل العارض المزدوج بدل الاعتماد على تشكيل Chromium الضمني

*F11 · B الطرفية · MIT · 427KB wasm* — `harfbuzzjs 1.6.1 · 2026-08-31`

HarfBuzz الرسمي مُصرَّفاً إلى WASM (‏`shape()` و`getGlyphInfosAndPositions()` و`glyphToPath()`)، صفر تبعيات. ما يعطيه ولا يعطيه Chromium: **cluster map** من الرسم إلى الحرف المنطقي وعرض كل رسم بدقة، فتتطابق خلايا xterm مع الحروف المشكّلة ويقع المؤشّر على الحرف الصحيح حتى مع اللقاءات. لا يفعل BiDi (بتصميم HarfBuzz) — يُقرن بـbidi-js. البدائل سقطت: rustybuzz-wasm ميت منذ 2021، harfrust بلا WASM رسمي و«لا Arabic fallback shaper»، opentype.js محرّك ميزات خاص لا يضاهي HarfBuzz، fontkit ٥٫٦MB بتسع تبعيات.

- **الأثر**: مؤشّر وتحديد على الحرف الصحيح مع التشكيل واللقاءات — بشرط أن تكون خرائط الخلايا هي المشكلة فعلاً لا الخط.
- **الكلفة**: ٥–١٠ أيام لنموذج أولي؛ **يضيف تبعية تشغيل** (~510KB) — وهذا يحتاج التبرير الذي تشترطه القاعدة ٥.
- **النتيجة**: افتح OBS كتجربة محدودة: إن كفى Chromium مع `unicode-bidi` وخط مناسب فيُدفن؛ وإلا فهذا الحل الوحيد الناضج بـJS.

المصادر: [harfbuzz/harfbuzzjs](https://github.com/harfbuzz/harfbuzzjs) · [harfrust (no Arabic fallback)](https://github.com/harfbuzz/harfrust)

### قبل أي بديل: قِس تفاضلية electron-updater التي تملكها، واقطع ٤١MB من الحزمة بـ`electronLanguages`

*F11 · C التوزيع · ما تملكه أصلاً* — `measured on 33.4.11`

من كود app-builder-lib 26.15.3 (‏`differentialUpdateInfoBuilder.js`): عند `differentialPackage` الافتراضي يُجبر الأرشيف على `dictSize=1MB` و`solid=false` — أي أن `compression: "maximum"` بلا أثر على NSIS؛ قياس مطوّري electron-builder: تغيير صغير على ٧١٫٥MB ⇒ تنزيل ١٫٢٤MB. القيد (‏`AppUpdater.js:668`): التفاضلي يحتاج المثبّت السابق في الكاش، فأول تحديث بعد تثبيت يدوي كامل، والتفاضلي من الثاني. وقياسي المباشر على Electron 33.4.11 win32-x64: مجلد `locales/` ٥٥ ملفاً بـ42.2MB؛ الإبقاء على `ar.pak` و`en-US.pak` = 1.4MB ⇒ **توفير 40.8MB على القرص و≈7.3MB في المثبّت** عبر `electronLanguages: ["ar","en-US"]` (متاح على ويندوز منذ app-builder-lib 24.2). ما لا يُحذف: swiftshader وd3dcompiler (ارتداد الأجهزة بلا GPU).

- **الأثر**: مثبّت أصغر بـ٧MB وتثبيت أخفّ بـ٤١MB بلا أثر على العربية أو الإنجليزية؛ وتحديثات ~1–2MB من الثاني فصاعداً إن ثبت أن blockmap يعمل.
- **الكلفة**: ٠٫٥ يوم لكلٍّ منهما، بلا تبعية.
- **النتيجة**: افتح OBS — وVelopack (MIT، آخر التزام اليوم، delta من أول تحديث، توقيع اختياري) يبقى «راقب» ويُفتح فقط إن أثبت القياس فشل التفاضلية الحالية.

المصادر: [app-builder-lib](https://www.npmjs.com/package/app-builder-lib) · [electron-builder#6265](https://github.com/electron-userland/electron-builder/issues/6265) · [velopack · deltas](https://docs.velopack.io/packaging/deltas)

### مواصفة Agent Skills صارت معياراً بـ٤٤ عميلاً — مهارات سطر السبع قابلة للنقل، والمدقّق أربعة تعبيرات تُنقل إلى `load_skill`

*F11 · D مهارات الوكيل · Apache-2.0 / CC-BY-4.0* — `spec updated 2026-08-09 · 44 clients`

الحقول: `name` (≤64، `[a-z0-9-]`، يطابق المجلد)، `description` (≤1024)، واختيارياً `license` و`compatibility` و`metadata` و`allowed-tools` (تجريبي)؛ progressive disclosure (‏~100 رمز للبيانات الوصفية → جسم <5000 → موارد عند الطلب). آخر تغييرات: 2026-08-04 هيكل المجلدات و`metadata`، 2026-05-16 الأرقام في الاسم، 2026-03-30 `allowed-tools`. العملاء: Claude Code وCodex وGemini CLI وOpenCode وCursor وCopilot وGoose وKiro… ومنها تطبيقات سطح مكتب مثل Mux وEmdash — يمكن لسطر التقدّم للقائمة. والمدقّق المرجعي `skills-ref` بايثون «للعرض فقط»؛ الأفضل نقل قواعده إلى JS.

- **الأثر**: مهارات `satr-*` تعمل في أي وكيل بلا تعديل، وسطر يرفض المهارة المكسورة قبل تحميلها، ويظهر في قائمة العملاء الرسمية.
- **الكلفة**: يوم واحد: validator داخلي + إضافة `license: FSL-1.1-MIT` و`metadata.lang: ar` للمهارات السبع، بلا تبعية.
- **النتيجة**: افتح OBS.

المصادر: [agentskills.io/specification](https://agentskills.io/specification) · [agentskills.io/clients](https://agentskills.io/clients) · [agentskills/agentskills](https://github.com/agentskills/agentskills)

### استيراد المهارات وتقييمها: `npx skills` يثبّت في `.agents/skills/` — مسار سطر نفسه — و`skill-creator` يعطي evals.json للمهارات السبع

*F11 · D مهارات الوكيل · MIT · Apache-2.0* — `skills 1.5.23 · 2026-08-18 · 30.4k★`

`vercel-labs/skills`: تثبيت/بحث/تحديث من أي مستودع إلى ٧٦ وكيلاً، ودليل skills.sh بـ security audit لكل مهارة وAPI (‏`/skills/search`، `/skills/audit`؛ الوصول غير المصادَق لم يُختبر). زر «أضف مهارة» في سطر = `npx skills add owner/repo -a universal`. وصيغة `.claude-plugin/marketplace.json` فهرس مفتوح يمكن لسطر قراءته مباشرة — «متجر عربي» برابط واحد. و`skill-creator` في anthropics/skills (Apache-2.0) يحمل `run_eval.py` و`benchmark.json` (with_skill مقابل without_skill: pass_rate/tokens/time) موثّقاً في agentskills.io. المحتوى العربي الموجود: `hurmoz` ٦٣ مهارة بأجسام عربية (٢٠ سعودية: زاتكا، نفاذ، سداد…) MIT لكنها `platforms: [linux, macos]` وراكدة منذ 2026-05-30؛ و`ArabAgentSkills` ٢٢ مهارة MENA بالإنجليزية، alpha، نشطة 2026-08-10.

- **الأثر**: كل مهارة `satr-*` تُقاس رقمياً قبل الإصدار (أمثلة عربية في evals.json)، والمستخدم يثبّت مهارة من المجتمع بنقرة — والمساهمة بـ`platforms: [windows]` في hurmoz أرخص من إعادة كتابة ٦٣ مهارة.
- **الكلفة**: ١–٢ يوم لـnpx skills (spawn، بلا تبعية) + ١–٢ يوم لـevals.json + يوم لاختبار خمس مهارات hurmoz على ويندوز.
- **النتيجة**: افتح OBS (ثلاثة بنود)؛ hurmoz «افتح OBS محدود» للمساهمة لا الاستهلاك؛ ArabAgentSkills وopenai/skills «راقب».

المصادر: [vercel-labs/skills](https://github.com/vercel-labs/skills) · [skills.sh · api](https://www.skills.sh/docs/api) · [agentskills.io · evaluating skills](https://agentskills.io/skill-creation/evaluating-skills.md) · [hurmoz](https://github.com/Moshe-ship/hurmoz) · [ArabAgentSkills](https://github.com/ArabAgentSkills/Skills)

### ثلاث حلقات مفتوحة تُقرأ لا تُعتمد: pi بسبع أدوات و١٠١ ألف نجمة، وOpenCode لمعاملات الضغط، وCline لتقييم التعديل

*F11 · F وكيل سطر · MIT · MIT · Apache-2.0* — `pi 0.85.0 (2026-09-04) · opencode 1.18.27 · cline 3.0.61`

`pi-coding-agent` (TypeScript، MIT): سبع أدوات فقط (read/bash/edit/write/grep/find/ls)، تعديل search/replace، ضغط تلقائي عند الفيض، ومزوّدوه Groq وDeepSeek وNIM وllama.cpp — أقرب بنية لحلقة سطر ذات العشرين أداة، وبأكثر من ١٠١ ألف نجمة على npm اليوم. OpenCode (MIT، ١٩٦ ألف نجمة، Kilo Code بُني فوقه) مرجع إنتاجي لتقليم نتائج الأدوات. Cline (Apache-2.0) يحمل حزمة تقييم `evals/diff-edits`. أما Roo Code فأُرشف في 2026-05-15، وAmp لم يعد مفتوحاً. وحكم المحور: **لا تبعية جديدة** — أنماط تُنقل إلى `looprunner.js`.

- **الأثر**: حلقة سطر تكتسب ما أثبتته الحلقات الأكبر (الضغط، إعادة المحاولة عند الفيض، التقليم) وتحتفظ بما يميّزها: عقد اللغة، البخل بالرموز، ويندوز.
- **الكلفة**: ١–٢ يوم دراسة لكل منها، صفر تبعيات.
- **النتيجة**: افتح OBS (تعلّم لا اعتماد).

المصادر: [pi-coding-agent](https://github.com/earendil-works/pi/blob/main/packages/coding-agent/README.md) · [opencode · compaction](https://opencode.ai/v2/docs/compaction/) · [cline](https://github.com/cline/cline)

## راقب حتى ينضج — الرغبة صحيحة والمرشّح ليس بعد

### Kitesurf مستضاف فقط ووعدت Cloudflare بفتحه — وجواب «هل يشكّل Blitz العربية؟»: نعم على مستوى النص، لا على مستوى اتجاه الحاوية

*F11 · A المعاينة* — `Kitesurf 2026-08-06 · Blitz 0.3.0-beta.2 2026-08-24`

Kitesurf مبنيّ من Blitz + Stylo + Parley + Boa ويدعم CDP؛ المدونة: «we're going to open source Kitesurf once we're ready». الأدلة على التشكيل: Parley يشكّل عبر HarfRust ويطبّق UAX#9 كاملاً في `bidi.rs` (‏X1–X10، W1–W7، N0–N2، I1–I2) فوق ICU4X، وأصلح حركة المؤشّر في RTL (‏#298). **القيود المثبتة**: `blitz-dom` لا يمرّر `dir`/`direction` إلى بناء الفقرة، فاتجاه الفقرة يُكتشف من أول حرف قوي ولن يُحترم `dir="rtl"` على حاوية تبدأ بكلمة لاتينية؛ وHarfRust بلا Arabic fallback shaper للخطوط الناقصة GSUB. الاختبار الأول عند فتح المصدر: صفحة `dir="rtl"` تبدأ بكلمة إنجليزية.

المصادر: [blog.cloudflare.com/kitesurf](https://blog.cloudflare.com/kitesurf/) · [parley · bidi.rs](https://docs.rs/crate/parley/latest/source/src/bidi.rs) · [blitz-dom · construct.rs](https://docs.rs/crate/blitz-dom/0.3.0-beta.2/source/src/layout/construct.rs)

### تعريفات الأدوات عند الطلب: سطر تحت العتبة الآن — ويصير «مباشر» إن اعتُمد Groq المجاني مساراً رئيسياً

*F11 · E تخفيض الرموز* — `Satr ≈2.5–2.9k tokens/turn in tool schemas`

نمط Tool Search عند Anthropic: خفض >٨٥٪ لتعريفات الأدوات في إعداد نموذجي ≈55k رمز، والدقة ارتفعت لا انخفضت (Opus 4: ٤٩٪→٧٤٪)، والتوصية عند ≥١٠ أدوات أو >10k رمز. قياسي على سطر (chars/4، تقديري): ٢٠ أداة = 10,038 حرف JSON ≈ 2.5–2.9k رمز لكل طلب؛ النواة الستّ (read/edit/write/list/search/run) ≈ 0.73k فقط؛ أكبرها `propose_memory` و`update_task_ledger`. على Groq (‏8K TPM) هذا ~٢٠٪ من ميزانية الدقيقة. تحذير: تغيير مصفوفة tools أثناء الجلسة يُبطل البادئة — الاختيار لكل جلسة/وضع لا لكل دور. وتعليق `adapters/index.js` «Groq يردّ 400 على tools» لم يعد صحيحاً بحسب وثائق Groq.

المصادر: [anthropic · advanced tool use](https://www.anthropic.com/engineering/advanced-tool-use) · [groq · tool use](https://console.groq.com/docs/tool-use)

### العزل على ويندوز: مرشّحان حقيقيان في alpha، وثلاثة طرق مغلقة

*F11 · F وكيل سطر* — `sandbox-runtime 0.0.75 (2026-09-01) · mxc-sdk 0.8.0 (2026-08-22)`

`@anthropic-ai/sandbox-runtime` (Apache-2.0) صار له دعم ويندوز: حساب محلي `srt-sandbox` + مرشّحات WFP للشبكة + ACLs، بخطوة تثبيت واحدة مرتفعة الصلاحية (UAC مرة) ثم التشغيل بلا admin — قيوده: أدوات المستخدم per-user غير مرئية للحساب المعزول، ولا overrides لكل أمر. وMicrosoft MXC (MIT، `@microsoft/mxc-sdk`) بـbackends AppContainer/Isolation Session/Windows Sandbox/microVM يتطلّب Win11 24H2 وREADME يصف سياساته «متساهلة أكثر من اللازم». المغلقة: Windows Sandbox CLI لا يعيد مخرجات العملية، وAppContainer الخام C++، وmini-swe-agent (MIT، بروتوكول bash نصّي بلا tool calling) يستحق العودة فقط إن قِيس فشل tool calling على النماذج المجانية.

المصادر: [sandbox-runtime](https://github.com/anthropic-experimental/sandbox-runtime) · [microsoft/mxc](https://github.com/microsoft/mxc) · [mini-swe-agent](https://github.com/SWE-agent/mini-swe-agent)

### أفضل مزيج مجاني اليوم: DeepSeek V4 Flash/Pro عبر NIM للقوة، وgpt-oss-120b على Groq للسرعة — بتحفّظ على أرقام Vals

*F11 · F وكيل سطر* — `Vals AI 2026-09-01 · Aider leaderboard`

الترتيب النسبي (SWE-bench Verified عند Vals AI، 2026-09-01): DeepSeek V4 Pro ‏96.4٪ · Kimi K3 ‏93.4٪ · Qwen 3.8-27B ‏90٪ · DeepSeek V4 Flash ~89٪ · Nemotron 3 Ultra ‏81٪ · Nemotron 3.5 Lightning ‏71٪ · gpt-oss-120b ‏46٪ (‏Aider polyglot 41.8٪). الأرقام مرتفعة جداً مقارنة بالأجيال السابقة — تُستعمل ترتيباً لا قيمة مطلقة. وحساب البادئة الثابتة في سطر (‏envbrief ≈2.7k حرف + أدوات ≈2.5k رمز + repo map ≤1.6k ≈ 5k رمز/طلب) يعني على Groq المجاني **طلباً واحداً في الدقيقة** عملياً — وهو ما يجعل بندي الكاش والمسح أعلاه شرطاً لا تحسيناً.

المصادر: [vals.ai · swebench](https://vals.ai/benchmarks/swebench) · [aider · leaderboards](https://aider.chat/docs/leaderboards/) · [groq · rate limits](https://console.groq.com/docs/rate-limits)

### بقية المراقَبات: WezTerm وGhostty للتصميم، ICU4X حين يتجزّأ، Electrobun حين يعرض Bun.Terminal، والتخمين والتوجيه حين يُقاسا على وكيل

*F11 · B · C · E* — `watch list`

**الطرفية**: WezTerm نموذج «علامة bidi لكل سطر + hint للفقرة» (‏`wezterm-bidi` MIT، مطابق ١٠٠٪ لاختبارات Unicode) لكن سلوك التحديد غير موثّق؛ Ghostty PR #11079 (itijah) آخر نشاط 2026-08-10 وجزء التحديد لم يُكتب؛ ICU4X على npm (‏Unicode-3.0) يحمل BiDi وتجزئة لكن الـwasm ‏20.5MB — يستحق العودة حين تُنشر حزم مجزّأة؛ `@mapbox/mapbox-gl-rtl-text` (BSD-2، 2026-03) حيلة تشكيل مقفول على الخلية بـ98KB لكنها تفقد اللقاءات. **التوزيع**: Velopack (MIT، delta من أول تحديث، توقيع اختياري، Komac 2.16 يدعم مثبّتاته) مشروط بقياس التفاضلية الحالية؛ Electrobun 2.0.1 (MIT، Windows 11 «رسمي») لا يشغّل node-pty، والسؤال الوحيد: هل يعرض `Bun.Terminal` على ويندوز؟ **الرموز**: فك التشفير التخميني في llama-server ‏2.4–2.9× سرعة على Qwen2.5-Coder (سرعة لا رموز، ودعم Ollama غير مؤكّد)؛ توجيه المهام الثانوية لنموذج صغير (RouteLLM ‏−85٪ كلفة على دردشة لا وكيل)؛ OpenRouter كنقطة OpenAI-compatible إضافية بصفر كود لكن حدود المجاني ٥٠ طلب/يوم.

المصادر: [wezterm-bidi](https://docs.rs/wezterm-bidi/latest/wezterm_bidi/) · [ghostty#11079](https://github.com/ghostty-org/ghostty/pull/11079) · [npm · icu (ICU4X)](https://www.npmjs.com/package/icu) · [velopack](https://github.com/velopack/velopack) · [electrobun](https://github.com/blackboardsh/electrobun) · [llama.cpp · speculative](https://github.com/ggml-org/llama.cpp/discussions/10466)

## المقبرة — مرفوض بسبب مكتوب، ولا يُقترح ثانيةً

### خمسة قبور بأسبابها

*F11 · graveyard* — `5 entries · 2026-09-04`

- **Lightpanda**: رخصة AGPL-3.0 (غلاف npm فقط Apache)، لا ثنائي ويندوز إلا WSL2، ولا يرسم ولا يشكّل نصّاً أصلاً — لا يشبع أي رغبة.
- **Tauri 2**: مع sidecar لـNode: أسابيع من إعادة الكتابة، الـsidecar ‏~50MB يلغي مكسب الحجم، node-pty يُشحن خارج الحزمة، ولا يحلّ SmartScreen ولا التوقيع — الكلفة لا تشتري رغبة.
- **V8 snapshots**: وelectron-delta: الأول أسرع إقلاعاً ٣٦٪ لكن الحجم **زاد** 227→238MB (آخر التزام 2024-11)؛ الثاني بلا رخصة (‏UNLICENSED)، آخر نشر 2022، ويشترط دالة توقيع.
- **mem0 / Letta**: كتبعية: أرقامهما (‏+26٪ على LOCOMO، −91٪ p95) مقارنةً بـ«إرسال المحادثة كاملة» وهو ما لا يفعله سطر؛ نهج الملفات مُثبت (‏−50٪ قراءات، −48٪ ذروة سياق في cookbook Anthropic) و`memory.js` + `propose_memory` يفعلانه. كذلك LiteLLM/Portkey: خوادم Python/منفصلة لا تُدمج في Node.
- **محرّكات تشكيل ساقطة**: rustybuzz-wasm (ميت 2021)، fontkit (‏5.6MB بتسع تبعيات، 2024)، opentype.js (محرّك ميزات لا يضاهي HarfBuzz)، unicode-bidirectional (‏2017 مع lodash)، Servo/Stylo (‏MPL؛ Stylo لا يشكّل نصّاً)، WebDriver BiDi (‏Electron لا يعرضه وCDP متاح)، Windows Sandbox CLI (بلا I/O للعملية)، Roo Code (مؤرشف 2026-05-15).

---

**ما تحقّقتُ منه من المصدر الأول:** الرخص وتواريخ النشر لكل مرشّح من npm/crates.io/PyPI أو ملف LICENSE الخام (‏GitHub API محجوب فاستُعيض بالسجلّات و14 استنساخاً ضحلاً) · مواصفة agentskills.io وسجل git لتغييراتها · كود app-builder-lib للتفاضلية وقياس Electron 33.4.11 محلياً · وثائق الكاش عند DeepSeek وGroq وQwen وOpenAI · `bidi.rs` في Parley و`construct.rs` في blitz-dom · ملفات سطر: `tools.js` و`context.js` و`looprunner.js` و`usage.js` و`adapters/`. **ما لم يُتحقّق منه:** NIM المستضاف لا يوثّق prompt caching · هل تُحتسب cached tokens ضمن TPM عند Groq · دعم Ollama للتخمين · هل يعرض Electrobun ‏`Bun.Terminal` على ويندوز · skills.sh API بلا مصادقة · تشغيل أي مهارة من hurmoz على ويندوز · أرقام Vals الأولية (‏96.4٪) قد تعكس harness مختلفاً · تقدير رموز أدوات سطر بـchars/4 لا بعدّاد مزوّد · لا مصدر أولي لرموز search/replace مقابل الملف الكامل · لا قياس عربياً منشوراً لـClaude أو DeepSeek.
