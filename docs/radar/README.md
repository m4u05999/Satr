# رادار سطر — الأعداد

نسخة Markdown من صفحة «رادار سطر» (claude.ai) كي يقرأها Claude Code والوكلاء من داخل المستودع. **الرادار يقترح ولا ينفّذ**: ما يُقبل من بنوده يُسجَّل في `docs/OBSERVATIONS.md` بقرار المالك، وهذا المجلد سجلّ الاقتراحات لا القرارات.

| العدد | التاريخ | الملف | الملخّص |
|---|---|---|---|
| 010 | 2026-09-12 | [`010.md`](010.md) | 2026-09-12 · window 2d · 7 items · daily fronts F1/F2/F5/F8 |
| 009 | 2026-09-10 | [`009.md`](009.md) | 2026-09-10 · window 1d · 7 items · daily fronts F1/F2/F5/F8 — نسخة البريد |
| 008 | 2026-09-09 | [`008.md`](008.md) | 2026-09-09 · window 1d · 7 items · daily fronts F1/F2/F5/F8 — ملخّص البريد |
| 007 | 2026-09-08 | [`007.md`](007.md) | 2026-09-08 · window 1d · 6 items · daily fronts F1/F2/F5/F8 — ملخّص البريد |
| 006 | 2026-09-07 | [`006.md`](006.md) | 2026-09-07 · window 1d · 6 items · daily fronts F1/F2/F5/F8 — نسخة البريد |
| 005 | 2026-09-06 | [`005.md`](005.md) | 2026-09-06 · window 1–3d · 15 items + 5 F11 · all fronts |
| 004 | 2026-09-05 | [`004.md`](004.md) | 2026-09-05 · window 1d · 7 items · daily fronts F1/F2/F5/F8 |
| 003 | 2026-09-04 | [`003.md`](003.md) | 2026-09-04 · F11 tech scouting · 6 axes · 24 evaluated · 11 open / 8 watch / 5 grave |
| 002 | 2026-09-04 | [`002.md`](002.md) | 2026-09-04 · window 1d · 7 items · daily fronts F1/F2/F5/F8 |
| 001 | 2026-09-03 | [`001.md`](001.md) | 2026-09-03 · window 60–90d · 29 items |

> **مصدر كل عدد معلن**: ‏٠٠١–٠٠٥ و٠١٠ منقولة من صفحة «رادار سطر» كاملةً. أما ٠٠٦ و٠٠٩ فـ**نسخة نصّ البريد** (قريبة من الأصل، وترويسة كل ملف تصرّح بذلك)، و٠٠٧ و٠٠٨ **ملخّصا البريد** لأن الملفين الكاملين داخل مرفق (`radar-007.zip`) أو داخل جلسة Cowork ولم يُنقلا — المالك يستبدلهما بالأصل حين يُنقل، ولا يُختلق متنٌ فيهما. وهذه النسخة من `state.json` **مدموجة**: ما كتبه المالك بيده يغلب — و`baseline.satr = 2.16.22` و`baseline.engines_on_owner_machine` منقولان من نسخة المستودع كما هما (آخر تحديث ٢٠٢٦-٠٩-٠٦) — وما رصده الرادار أُضيف.

## كيف تُقرأ

- الجبهات الدفاعية F1–F10 تسأل «هل يمسّ سطر؟» وتُدرَّج: **مباشر** (يلزم قراراً أو كوداً) · **يستحق OBS** (يُسجَّل ولا يُنفَّذ) · **للعلم**. ومنذ العدد ٠٠٦ يحمل كل بند وسم عدسة: يخدم سطر / يهدد سطر / يطوّر سطر.
- الجبهة الهجومية F11 (الأحد) تبدأ من رغبة معلنة لكل محور، وكل مرشّح يمرّ بخمسة أسئلة (المحور، الأثر على المستخدم العربي، الرخصة، الصيانة، كلفة الدمج) ويخرج بنتيجة: **افتح OBS** · **راقب حتى ينضج** · **مقبرة**.
- `state.json` هو الحالة الآلية: `baseline` (المثبّت في سطر — يقرؤه الرادار من `package.json`) و`baseline.engines_on_owner_machine` (إصدارات المحرّكات على جهاز المالك — **يكتبها المالك** بـ`npm run radar:baseline -- --write`) و`latest_seen` (لقطة السجلّات وقت العدد) و`open_threads` و`tech_watch` (الرغبات، المراقبة، **المقبرة**، `npm_blocklist`). قبل اقتراح مكتبة أو تقنية راجع المقبرة كي لا يُعاد ما رُفض بسبب مكتوب — و`npm run test:radar-graveyard` يعضّ.

## الحالة الحالية (من state.json · محدَّثة في العدد ٠١٠)

**المستودع:** `HEAD = 403becf` (‏٢٠٢٦-٠٩-١٢) والوسم `v2.16.21` — دفعة سطح المكتب على main **بلا إصدار**: ١٥ التزاماً و٣٤٠ ملفاً و`+50,459 / −4,734` سطراً، و`package-lock.json` **بلا تغيّر بايتاً واحداً** و`dependencies` أربع كما كانت. المعين `native/satr-uia` ثنائيّ NativeAOT يُشحن عبر `build.extraResources` لا حزمة npm.

**بوابة مفتوحة على الترقية:** ‏`agent-sdk` مثبَّت `0.3.261` والأحدث `0.3.269`؛ أي ترقية تتجاوز `0.3.263` يلزمها `systemPromptSnapshot: false` (أو نقل الأجزاء المتغيّرة إلى ذيل الدور). والدفعة نفسها تحمل الآن: `sdkMcpServerManifests` · `maxEffortLevel` (وسقف المؤسسة فوقه) · `defaultToNo`/`suppressAlwaysAllowRule` · `verification_required` · `pending_permission_requests` المضمونة · `resume_reason` · `reloadPlugins({holdOnCacheImpact})` · `context_usage.kind` · **و`get_hooks_listing` و`list_permission_rules`**.

**تصحيح أمني مقيس (العدد ٠١٠):** التنبيهات التشغيلية (`dev:false`) **سبع لا واحدة** — `js-yaml` (عبر `electron-updater`) و`ip-address` و`fast-uri` عالية، و`hono` و`@hono/node-server` و`qs` و`body-parser`. و`npm update --package-lock-only js-yaml qs body-parser ip-address fast-uri` يعطي ٢٣ ⇒ ١٨ والتشغيلي ٧ ⇒ ٢ **بلا مساس بـ`package.json` وبلا عبور بوابة snapshot** — بينما `npm update` بلا أسماء يعبرها صامتاً.

**مفتوح للـOBS (F11):**
- E: prefix stability for prompt caching (budget block + repo_map out of system prompt) — UPSTREAM CONFIRMED THREE TIMES NOW: 2.1.261 (re-sent tool/skill announcements), and 2.1.265 twice (foreground-spawned subagent resume changing its tool list and system prompt prefix; agent teammates and resumed subagents moving SubagentStart hook context and preloaded skills out of the prefix). Satr ships seven satr-* skills, 39 tools in one in-process server, and uses subagents - the same recipe
- E: tool-result clearing before summarization (keep/buffer params à la OpenCode)
- E: Arabic token-tax metric in usage.js/langmetric.js
- E: bashOutputMaxChars / taskOutputMaxChars on the Claude Code path (2.1.265 added a 1GB cap on tool results saved to disk plus a truncation notice - that protects the disk, not the context window, so it does not replace this item)
- E: /skill-doctor measurement for the seven satr-* skills (pairs with OBS-114)
- E: mdream vs turndown A/B measured through usage.js (20 pages, 10 Arabic) + U+200F/U+202B preservation test — replace or reject with a number
- A: chrome-devtools-mcp over Electron CDP
- A: @mozilla/readability + turndown injected in preview; agent-browser optional
- B: bidi-js vendored + CodeMirror 6 bidi model + terminal-wg CSI handlers
- B: harfbuzzjs limited experiment (adds ~510KB dep — justify or bury)
- C: measure the electron-updater blockmap differential (electronLanguages half DONE — landed as OBS-104 in 171c1e2, 38.9MB saved)
- C: @microsoft/winappcli MSIX channel — GATE LIFTED (ConPTY measured inside the MSIX container, and the Store package is certified and live). Re-evaluate on the next Sunday sweep: with the Store channel already shipping, what does winappcli still buy?
- D: agentskills.io validator in load_skill + license/metadata fields on satr-* skills
- D: npx skills import + marketplace.json reader + skill-creator evals.json; hurmoz contribution
- F: study pi / OpenCode compaction / Cline diff-edit eval — patterns only, no deps
- ENGINE UPGRADE BATCH (new 2026-09-09, ed. 008): any move past agent-sdk 0.3.263 must carry systemPromptSnapshot:false (or move the varying append into the turn tail), and should carry sdkMcpServerManifests for the four in-process servers in the same batch; and any move past claude-code 2.1.264 changes cwd semantics between turns
- ENGINE UPGRADE BATCH (updated 2026-09-10, ed. 009): the systemPromptSnapshot gate now has a first-party escape hatch - 2.1.267 documents `--system-prompt-snapshot off` and describes recording as current behaviour, so the 'is it gated?' probe is no longer needed. Add to the same batch: read maxEffortLevel (new in 2.1.267, client-side clamp, lowest-wins across settings files) before building the effort picker from supportedEffortLevels, or label the clamped levels
- ENGINE UPGRADE BATCH (updated 2026-09-12, ed. 010): four more named contracts now ride the same batch. From 0.3.268 - pending_permission_requests / pending_user_dialogs are ALWAYS present on a success initialize from CLI 2.1.268+, so absence means an older CLI, not 'nothing pending' (and an inherited prompt can stay answerable without appearing); resume_reason distinguishes an auto re-run of an interrupted turn, whose user_message_uuid names the INTERRUPTED turn's own last prompt; reloadPlugins({holdOnCacheImpact}) refuses to apply a reload that would change the tool list while the prompt cache depends on it; context_usage rows carry kind:'used'|'free'|'buffer'|'deferred' with an explicit 'classify on this, never on the English name' ('deferred' = out-of-window tool schemas, the axis-E tools-on-demand pattern visible in the contract). From 0.3.269 - get_hooks_listing and list_permission_rules (see the thread), and Settings.bashEditDiffEnabled. Note the overlap with what the owner landed 2026-09-11: a07bbb4 conversation continuity, OBS-153, OBS-147/148.

**قيد المراقبة:** headroom-ai · NVIDIA/SkillEvaluator · text-shaper · Kitesurf · Blitz/Parley · Playwright MCP · agent-browser (Vercel) · Velopack · Electrobun / Bun.Terminal · deferred tool schemas (tool search) · speculative decoding (llama-server) · small-model routing · mini-swe-agent bash-only fallback · @anthropic-ai/sandbox-runtime (Windows alpha) · Microsoft MXC · WezTerm bidi model / Ghostty #11079 · coder/ghostty-web · ICU4X npm · @mapbox/mapbox-gl-rtl-text · @devsamhan/arabic-bidi / bidi-shaper / kashida · hurmoz / ArabAgentSkills / openai skills · OpenRouter endpoint · humain-m3 open weights · Apollo Watcher / AIR Security / NVIDIA SkillSpector

**المقبرة:** Lightpanda (AGPL-3.0; no Windows binary; no rendering/shaping) · Tauri 2 + Node sidecar (weeks of rewrite; sidecar ~50MB; node-pty outside bundle; solves neither SmartScreen nor signing) · V8 snapshots (electron-link/mksnapshot) (size increased 227->238MB; last commit 2024-11) · electron-delta (UNLICENSED; last publish 2022-08; requires signing function) · mem0 / Letta as dependency (numbers vs 'send whole conversation'; file memory proven; memory.js exists) · LiteLLM / Portkey (Python/server-side; not embeddable in Node) · rustybuzz-wasm (dead since 2021) · fontkit / opentype.js (for shaping) (5.6MB+9 deps (2024) / feature engine not HarfBuzz-grade) · unicode-bidirectional (2017, immutable+lodash deps) · Servo / Stylo (for Satr) (MPL; Stylo is CSS only; Servo bidi limited) · WebDriver BiDi (Electron doesn't expose it; CDP available directly) · Windows Sandbox CLI (wsb exec) (no process I/O) · raw AppContainer (RunInSandbox) (C++ tooling; no gain over MXC) · Roo Code (archived 2026-05-15) · Skilldex (third skill registry after two already evaluated, and stale: skilldex@0.1.1 last published 2026-02-21, solo author)

**أنجزه/فتحه المالك منذ ٠٠١:** MSIX package for Microsoft Store · winget manifests + PR microsoft/winget-pkgs#429004 · Certum facts verified (OBS-096) · DeepSeek v4-flash/v4-pro migration · NIM/Groq catalog split + allam-2-7b · OBS-097 RTL issues channel · OBS-098 ar-terminal · edit_file hardening landed (OBS-108) · SDK upgraded 0.3.176 -> 0.3.261; user_message_uuids batching read at agent.js:2223 · OBS-114 satr-guide bundle cap with written justification; 4KiB cap on SKILL.md · OBS-117/118/121/122 Windows gate work (PR #42) · OBS-133/134/135 sessions-panel grouping fix — buildProjectCwdMap, orphan-joins-project guard (PR #71, 2026-09-06) · OBS-136 GitSpawn hardening — electron/gitsafe.js, gitArgs on all five git callers (968fcd3, PR #77, 2026-09-06) · OBS-137 five SDK contracts re-probed on 0.3.261 / CC 2.1.261 + main.js comments corrected (5aa0e8d, PR #82) · OBS-140/141 CLAUDE_SDK_CAN_USE_TOOL_SHADOWED — shadowing measured file-by-file, hookguard reports and PreToolUse enforces (c8893dc / 7da5570 / ce2836c / 5ed7608 / 7fb62f5) · OBS-139 test:termjobs retry entry with written justification (4ad2a7e) + CI Windows step retry (fe999ba) · OBS-142 a turn rendering empty while its text is in the session log (634b6e4, PR #93) · releases 2.16.18 / 2.16.19 / 2.16.20 · OBS-143 landing-page readability — one real low-contrast finding fixed and three false direction findings corrected in the preview probe itself (b1154cc, PR #98) · OBS-142 turn event counter — electron/eventtrace.js, four measurement points, 24 guard checks (29a817b, PR #97). A measuring tool, not a cure: the root cause is still open · OBS-104 electronLanguages ar/en-US — 38.9MB saved, re-measured before applying (171c1e2, PR #95) · Microsoft Store channel first — MSIX certified and live (Store ID 9N7F5RKQJ9WF), README and landing page lead with the Store, winget msstore command verified live (171c1e2, PR #95) · winget manifests for 2.16.20 + OBS-096 diagnosis correction (32aff6a, PR #99) · release 2.16.21 with acceptance evidence and a Windows install/upgrade validation script (f230c98, PR #100) · OBS-153 conversation continuity + connections + mobile control batch (a07bbb4, PR #107, 2026-09-11) · agent reply readability - heading ladder and Markdown renderer (7668147, PR #105) · CLAUDE.md diet: architecture moved verbatim into docs/internals, 170-line core (37c8c08, PR #102) + MODEL-ROUTING policy (f441cef, PR #103) · Windows desktop surface, steps 1-4 (2026-09-11/12): native/satr-uia NativeAOT helper with five methods and ZERO runtime dependency (0c38f92, PR #109) · surface.js extraction (b7d79bd, PR #111) · desktopguard.js five guards as a pure module (b68f5ca, PR #112) · desktop.js + eight tools + satr-desktop server (d2e9ae8, PR #113) · OBS-154 gate-zero measurement inside the MSIX container (968fef9/695ac1a) · OBS-156 opened by the owner (hookguard fingerprints SessionStart only) - this was the radar's ed.006 thread; it is now the owner's note and must not be re-proposed · .claude/rules/ with eleven rule files + .claude/settings.json + docs/TOKEN-DIET-PLAN.md (the name of axis E, written as a plan)
