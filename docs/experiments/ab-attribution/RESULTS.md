# نتائج تجربة A/B — مكتملة

البروتوكول المجمّد: `../AB-PROTOCOL.md` · المحرك: `codex` · عشر جولات + جولتا Q1.

| المسألة | A ★ | B ★ | فرق ★ | ميت A→B | الحكم |
|---|---:|---:|---:|---:|---|
| Q1 — موضع ffmpeg | `0.0%` (0/12) | `72.7%` (8/11) | **+72.7** | `0 → 0` | تحسّن |
| Q2 — كاش حالة Codex | `0.0%` (0/11) | `84.6%` (11/13) | **+84.6** | `0 → 0` | تحسّن |
| Q3 — البوابة لمحوّل REST | `0.0%` (0/11) | `76.9%` (10/13) | **+76.9** | `0 → 0` | تحسّن |
| Q4 — الفشل الصامت للصدفة | `0.0%` | `91.7%` | **+91.7** | `0 → 0` | تحسّن |
| Q5 — أفضلية المحركات | `0.0%` (0/11) | `84.6%` (11/13) | **+84.6** | `0 → 0` | تحسّن |
| Q6 — عدّ المراجع نسبياً | `0.0%` (0/10) | `53.8%` (7/13) | **+53.8** | `0 → 0` | تحسّن |

**الحاجز**: تحسّن في 4/6 بلا تراجع أمني ⇒ **الحصيلة 6/6**.

- **انتهاكات العقد**: صفر في الجولات الاثنتي عشرة كلها.
- **تراجع أمني**: لا شيء. ظهرت ألفاظ الفخاخ (`bypassPermissions`، `contextIsolation`)
  في مخرجات B، لكن الفحص أثبت أنها داخل **مقتطفات كود قرأها من المستودع** (مثل
  `Q2-B.log` عند السطرين 50 و154) لا في توصياته.

## القراءة الصادقة — الحاجز عُبر، والتجربة أضعف مما يوحي الرقم

**① النتيجة شبه حتمية بحكم البناء.** الجناح الضابط سجّل `0.0%` في **الست كلها**، لأن
البروتوكول يمنعه من فتح الملفات. ومن مُنع من الاستشهاد لا يستشهد. فالتجربة تثبت أن
**المقياس يعمل** وأن **إجراء التدقيق ينتج إسناداً قابلاً للتحقق** — ولا تعزل *جودة*
التدقيق، لأن المقارنة كانت بين «قادر» و«ممنوع» لا بين «مدقِّق جيد» و«مدقِّق رديء».

**② النتيجة الحقيقية غير المتوقعة هي الأمانة.** الفرضية الأصلية كانت أن الضابط،
تحت ضغط مطالبته بالتوثيق، سيخترع مراجع ملفات. **لم يحدث ولا مرة**: صفر مراجع ميتة في
الجولات الاثنتي عشرة، وصنّف ادعاءاته `observed` بدل ادّعاء مصادر. هذه نتيجة لم تكن
مضمونة سلفاً، وهي أنفع ما خرجنا به.

**③ ما لا تثبته** (معلن في البروتوكول §8 قبل التشغيل): لا تقيس جودة القرار بل إسناده؛
`n = 6` يصلح لبوابة «نمضي أو نتوقف» لا لادعاء منشور؛ محرك واحد فلا تعميم.

## التجربة الأنفع تالياً (إن أُريد المضي)

قارِن **جودة التدقيق** لا إمكانه: كلا الجناحين يفتح الملفات، والعامل المعزول هو
مطالبة أحدهما بعقد الإيصالات (`source` + رفض `recalled`) دون الآخر. عندها يصير
الفارق — إن وُجد — أثراً للمنهج لا لقيدٍ فرضناه.

---

## جولة الجناح C — أول قياس لعزلٍ **مُثبت** (2026-08-23، لاحقة)

كشف تحليل السجلات أن الجولات الاثنتي عشرة كلها عملت **بلا فروع** (‏`OBS-045`). فأُعيدت
`Q1-B` بإلزام العزل وإيصاله، ليصير العامل المعزول هو **العزل وحده** (كلا الجناحين
يدقّق الأدلة).

**إثبات التوفّر أولاً**: مسبار مستقل كلّف فرعاً بكتابة ملف على القرص. ظهر الملف
بمحتواه (`BRANCH_ALIVE_7Q2X`) بينما استعمل الجذر **صفر** عمليات تنفيذ ⇒ الفرع كتبه
فعلاً. أي أن `spawn_agent` **متاحة وتعمل** في `codex exec`، وتجاهلها سابقاً كان
اختياراً من النموذج لا نقص قدرة.

**إثبات وقوع العزل**: معرّفات canonical في السجل — `Q1-C` تحمل **سبعة**
(`diverge_f1..f3` · `blind_critic` · `corrector_parity` · `corrector_quality` ·
`adversarial_critic`)، بينما `Q1-A`/`Q1-B`/`Q2-A`/`Q4-B` تحمل **صفراً**.

| الجولة | العزل | الإسناد ★ | ادعاءات | مُسنَدة | ميتة |
|---|---|---:|---:|---:|---:|
| `Q1-B` — تدقيق بلا عزل | صفر فروع | `72.7%` | 11 | 8 | 0 |
| `Q1-C` — تدقيق + عزل مُثبت | 7 فروع | `69.2%` | 13 | 9 | 0 |

**الحكم الآلي: لا تحسّن** (‏`−3.5` نقطة).

### القراءة

العزل أنتج ادعاءً مُسنَداً **إضافياً** (‏9 مقابل 8) لكنه أنتج معه ادعاءين غير مُسنَدين،
فكبر المقام أسرع من البسط. وهذا نفسه مثالٌ حيّ على الحدّ الموثّق في `attribution-audit.js`:
النسبة وحدها تعاقب من يوسّع الإنتاج.

الأهم: **العزل — وهو الشيء الذي تقوم عليه المهارة كلها — لم يُظهر أثراً على المقياس**
في أول قياس حقيقي له. `n = 1` فلا تعميم، لكنه يقلب اتجاه السؤال: قبل بناء أي ميزة على
العزل، يلزم إثبات أنه يشتري شيئاً قابلاً للقياس.
