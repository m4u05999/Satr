# تجربة A/B — أثر تدقيق الأدلة على إسناد الوقائع

أُجريت 2026-08-23 ضمن أسبوع خطة قرار العصف الثاني، بوابةً لقرار «هل تُبنى غرفة العصف
ميزةً؟». المحرك: `codex` · اثنتا عشرة جولة (ست مسائل × جناحين).

- `PROTOCOL.md` — البروتوكول **المجمَّد قبل التشغيل**، ومعه تعديلا الـpilot الموثّقان.
- `RESULTS.md` — الأرقام والحكم، ومعها قراءة صادقة لحدود ما تثبته.
- `claims/` — الادعاءات الخام لكل جولة، دليلاً قابلاً لإعادة القياس.

## الحصيلة

**6/6 تحسّن** (الحاجز كان يطلب 4/6)، بصفر انتهاكات عقد وبلا تراجع أمني.

**لكن** الجناح الضابط سجّل `0.0%` في الست كلها لأن البروتوكول يمنعه من فتح الملفات —
فالمقارنة كانت بين «قادر» و«ممنوع» لا بين تدقيقٍ جيد وآخر رديء. التفصيل في `RESULTS.md`.

## إعادة القياس

```
node scripts/attribution-audit.js --compare \
  docs/experiments/ab-attribution/claims/Q1-A-claims.json \
  docs/experiments/ab-attribution/claims/Q1-B-claims.json
```

المقياس الحاسم `verifiableRate` يفتح كل مرجع `مسار:سطر` ويتحقق من مداه، فالأرقام
تُعاد من المصدر لا تُصدَّق. الجذر الافتراضي هو المستودع؛ لتغييره مرّر `--root`.

⚠️ مراجع الادعاءات تشير إلى الشجرة كما كانت وقت التشغيل. تعديل الملفات لاحقاً قد
يزحزح الأسطر فتظهر مراجع «ميتة» لم تكن كذلك — أعد القياس عند الالتزام `0b6d684`
إن أردت مطابقة الأرقام المنشورة حرفياً.

سجلات التشغيل الخام (‏7.6م.ب) خارج المستودع عمداً، في
`D:\sater\prompts-studio-bs\brainstorm2\ab\`.
