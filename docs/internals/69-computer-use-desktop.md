## استعمال الحاسوب — سطح ويندوز: المعين والمراجع والحرّاس والأدوات الثماني والواجهة (الخطوات ٠–٥)

> المواصفة الملزمة: `docs/COMPUTER-USE-DESKTOP.md` (§٥ الملفات · §٦ التسجيل · §٧ العقد · §٨ بروتوكول المعين ·
> §٩ الحرّاس · §١٠ ترتيب البناء). هذا الملف سجلّ **ما بُني فعلاً** بالأرقام، والقرارات التي مسّت المواصفة
> معلنةً. الخطوتان ٦ (قياس الرموز) و٧ (قياس عربي على ويندوز عربي) لم تبدآ.

ممنوع بالتصميم في كل الطبقات: لا `desktop_evaluate` · لا نقر بالإحداثيات · لا نموذج رؤية · لا تعداد نوافذ بلا
اختيار المستخدم. إن لم يظهر العنصر في الشجرة فالجواب «لا أستطيع».

### الخريطة

| الطبقة | الملف | الدور |
|---|---|---|
| المعين | `native/satr-uia/` (‏`Program.cs` · `Interop.cs` · `README.md`) | UIA فوق COM مولَّد، NativeAOT ملفاً واحداً، أسطر JSON على stdio |
| المرجع | `electron/surface.js` | الجيل والبصمة و`stale_ref` — نسخة `w` مستقلة عن نسخة `s` للمتصفح |
| الحرّاس | `electron/desktopguard.js` | الخمسة (موديول نقي) |
| الإدارة | `electron/desktop.js` | العملية، اللقطات، ترجمة المراجع، الرسائل، السجل، التسليم |
| الأدوات | `electron/agent.js` (‏`desktopTools`) | الثماني في خادم `satr-desktop` |
| القنوات | `electron/main.js` · `electron/preload.js` | علم `desktopControl` + `satr:desktopTargets/Select/Clear/Status` |
| الواجهة | `src/ui/components/desktop-panel.js` · `src/ui/app.js` · `chat.js` | المنتقي وسجلّ الأفعال والعلم |
| الموجز | `electron/envbrief.js` | `DESKTOP_TOOL_NAMES` + سطر سياسة، حين يُسجَّل الخادم وحده |

### الخطوة ٠ — البوابة صفر (‏OBS-154)

المسبار `scripts/uia-probe/` داخل حزمة MSIX المسجَّلة بـ`-PreventBreakaway`: `packageFullName` صحيح، المفكرة 26 عقدة
و25 مسمّاة، الإقلاع 149 م.ث والتعداد 48 واللقطة 29؛ وخارجها 27/25 بإقلاع 183 م.ث. ثم المعين الإنتاجي (ناتج CI) داخل
الحاوية: إقلاع **46 م.ث**، تعداد 75، لقطة 34. الشاهد السالب: بلا `-PreventBreakaway` يعود `packageFullName=null`.

### الخطوة ١ — المعين بخمسة توابع (‏`0c38f92`، PR #109)

NativeAOT ملف واحد **1,738,752 بايت**، إقلاع **75 م.ث** في CI، و`test:uia-helper` ‏62 فحصاً. تحذيرات التقليم وAOT أخطاء
في `satr-uia.csproj`، ولا حزمة NuGet. محلياً `dotnet build` (‏JIT) لأن AOT يحتاج رابط MSVC.

### الخطوة ٢ — `surface.js` (‏`b7d79bd`، PR #111)

استخراج منطق المرجع/الجيل/البصمة/العقد من `preview.js` بلا تغيير سلوك؛ مصنع ببادئة، والنسختان مستقلتان.

### الخطوة ٣ — `desktopguard.js` (‏`b68f5ca`، PR #112)

الحرّاس الأربعة الأولى نقية (جلسة مجمَّدة بهوية ثلاثية، احتواء كلي بالمستطيل، إسقاط حقل السرّ وأبنائه، حجب بالاسم)
و`describeAction` لسطر السجل؛ `test:desktopguard` ‏86 فحصاً.

### الخطوة ٤ — `desktop.js` والأدوات الثماني وخادم `satr-desktop`

**المعين إلى §٨ كاملاً** (الإصدار 0.2.0): `session/select` · `element/state` · `input/key` · `input/scroll` ·
`capture/window` و`className` في `targets/list`. وتغيّر فيه ثلاثة أشياء بنيوية:
- **رقم الهدف ثابت** لكل نافذة (مفتاحه المقبض والعملية) ولا يُعاد استعماله: نافذة أُغلقت ثم فُتحت تأخذ رقماً جديداً
  (مقيس: ‏`w14` ⇒ `w15`)، وسرد جديد لا يعيد ترقيم نافذة قائمة — فلم يعد السرد يمسح اللقطات.
- **الجلسة إلزامية**: كل تابع غير التعداد يرفض `not_allowed` قبل `session/select`، والمستطيل المرسَل يطابق الحيّ
  حرفياً. هذا **الشق الثاني من الفحص المزدوج** للحارس ٢: مستطيل العنصر يُقرأ حياً قبل الفعل.
- **البصمة في المعين**: `role+name+rect` لحظة اللقطة تُقارَن بالعنصر حياً بعد الاحتواء ⇒ `target_changed` (كان/صار).

**`desktop.js`** — عملية واحدة بلا صدفة، مهلة إقلاع 800 م.ث (قبول الخطوة ١؛ `SATR_UIA_BOOT_TIMEOUT_MS` للأبطأ)،
مهلة لكل طلب، والمهلة أو الردّ المشوّه يقتلان العملية؛ **إعادة إقلاع واحدة لكل جلسة** تعيد ربط النافذة بهويتها
(عملية واسم ومستطيل مطابقة، نافذة واحدة فقط) وإلا تنتهي الجلسة؛ الإغلاق مع `clearTarget` وعند `before-quit`.
المسار: `SATR_UIA_EXE` ⇐ `resources/satr-uia/satr-uia.exe` ⇐ `native/satr-uia/out-aot` ⇐ `out`؛ غيابه ⇒ لا خادم
وإشعار عربي واحد. كل فعل: `surface.locatorError` ⇒ `desktopguard.checkAction` ⇒ المعين، ثم سطر
`desktop_activity {text}` من `describeAction` (عبر `emitToWindow` في main.js — الواجهة تعرضه في الخطوة ٥)، ثم
**لقطة تفاضلية إن رخُصت** (اللقطة السابقة ≤ 250 عنصراً و≤ 600 م.ث): مشية جديدة تُربط بمراجع الجيل نفسه بالبصمة،
والجديد يمدّ الجيل (‏`recordAction`) — فمرجع النموذج يبقى صالحاً رغم إعادة ترقيم المعين (مقيس في الاختبار: ‏`NoRect`
انتقل في المعين من `e7` إلى `e8` ووصله النقر بمرجع النموذج نفسه).

**التسجيل (§٦)**: `agent.js` يحسم `desktopRegistration` عند أول دور ويثبّته للجلسة (خريطة بسقف 200)، فتبديل العلم
أثناءها يُتجاهل ولا تتغيّر كتلة الأدوات ولا الموجز. الأدوات الثماني تمرّ على `canUseTool` ومربع الإذن و`PreToolUse`
كأي أداة؛ المربع يعرض النافذة والعنصر والنص من `desktop.permissionDetail`.

### الأرقام (2026-09-11، ويندوز `10.0.19045`، معين JIT محلي)

- `test:uia-helper`: **129 فحصاً** — إقلاع JIT 253 م.ث · لقطة المفكرة 27 عقدة/24 مسمّاة في 32 م.ث · `Ctrl+End` ثم
  `Backspace` حذف آخر محرف (مقروء من المحرّر) · `Ctrl+S` كتب الملف على القرص · التمرير بـScrollPattern ‏0% ⇒ 1.89% ·
  الالتقاط 1362×759 في **15,186 بايت PNG** و50 م.ث · بعد التكبير `setValue` و`scroll` خارج مستطيل الجلسة ⇒
  `not_allowed` بلا كتابة · زرّ غيّر اسمه ⇒ `target_changed` (‏`button "press-me"` ⇒ `button "pressed"`) · قتل النافذة
  ⇒ `closed`.
- `test:desktop` (قطعي بمعين مزيّف): **132/132** — منها 12 ترجمة رمز، وانهيار/إعادة/نهاية، ورفض الردّ المشوّه، وصفر تسريب.
- `test:desktop-live` («افتح المفكرة، اكتب سطراً، احفظ» عبر معالجات الأدوات نفسها): **33 فحصاً** — أول سرد مع
  الإقلاع 207 م.ث · `desktop_snapshot` ‏27 عنصراً في 38 م.ث · `desktop_type` ‏45 م.ث · `desktop_press_key Ctrl+S` ‏39 م.ث ·
  **السطر العربي على القرص بعد 191 م.ث** · `stale_ref` بعد لقطة أحدث (0 م.ث، بلا وصول المعين) · `closed` بعد إغلاق
  المفكرة (2 م.ث) · `stale_ref` بعد إعادة فتحها واختيارها (‏`w15` لا `w14`) · خمسة أسطر سجل لخمسة أفعال ناجحة.
- ناتج AOT لهذه الدفعة من تشغيل `uia-helper.yml` رقم `34644988115` على `4738497`: **1,931,264 بايت** (1.84 م.ب، ملف
  واحد) · `initialize` **69 م.ث** · 135 فحصاً (تشغيل سابق على الرأس نفسه: 114 م.ث/134 — العدد والزمن يتفاوتان بحلقة
  استقرار المستطيل؛ الحدّان 8 م.ب و800 م.ث).

### مراجعة القائد بالقياس والدمج (2026-09-12، ثنائي AOT من التشغيل أعلاه — `SATR_UIA_EXE`)

- `test:uia-helper` **132 فحصاً** (إقلاع بارد 187 م.ث) · `test:desktop` **136/136** · `test:desktop-live` **33 فحصاً**: أول سرد
  118 م.ث · لقطة 31 · كتابة 37 · `Ctrl+S` 33 · على القرص بعد 187 · `stale_ref`/`closed`/`stale_ref` وw15 ⇒ w16.
- عضّة تجاوز الحارس بيد القائد (‏`checkAction` ⇒ `null`): يسقط `test:desktop` ‏5/136 ثم استعادة نظيفة.
- **داخل حزمة MSIX** المسجَّلة بـ`-PreventBreakaway` (سائق جلسي — `OBS-163`): `packageFullName` صحيح · إقلاع 44 م.ث ·
  تعداد 88 · `session/select` 0 · لقطة 27/24 في 30 · اللقطة قبل الاختيار ⇒ `not_allowed` · `capture/window` 9,268 بايت
  PNG في 31 م.ث؛ وخارجها `null` بالأرقام نفسها. **نصّ اللقطة البنيوية 722 بايت مقابل 12,360 محرف base64 للصورة** —
  رقم أولي للخطوة ٦.
- خاتمة `test:full` للمنفّذ من سجله: `head=4738497` · `SUITE_EXIT=0` · 122/122. **دُمج** بكلمة المالك في `main` بـ`d2e9ae8` (#113).

### الخطوة ٥ — منتقي النافذة وسجلّ الأفعال في الواجهة (2026-09-12)

**المكوّن** `src/ui/components/desktop-panel.js` (‏`<satr-desktop-panel>`): Shadow DOM بـ`adoptedStyleSheets` حصراً
(‏`panelSheet` + ورقته — لا `<style>` مضمّن ولا `style=`، القاعدة ٨)، مسجَّل في `surfaceCoordinator` سطحَ `panel`
فتُحجب المعاينة الأصلية وتُغلق بقية اللوحات. ثلاث بطاقات: **التحكم** (مفتاح `desktopControl` وسطر «يُسجَّل مع أول
رسالة في الجلسة»، وتنبيه «يسري مع الجلسة القادمة» حين تكون الجلسة قد ثبّتت قرارها)، و**المنتقي** (قائمة
`desktopTargets` + صندوق المختارة + «ألغِ الاختيار»)، و**سجلّ الأفعال** (سقف 200، الأقدم يسقط، بزمن الواجهة وزر مسح).

- **لا «سبب مكتوب» لكل نافذة محجوبة، بل سطر ثابت**: `listTargets` يُسقط المحجوبة في `selectable()` ولا يعيد سبباً
  ولا `className`، فلا شيء تعرضه الواجهة عنها. السطر: «نوافذ النظام ونافذة سطر والمصغّرة لا تُعرض»، والاختبار
  يثبت أن اللوحة تعرض ما وصلها حرفياً (ثلاثة صفوف لثلاث نوافذ، وصفر ذكر لرقم عملية).
- **إعادة السرد مرة واحدة**: رفض `not_allowed` عند الاختيار يعني غالباً أن المستطيل تغيّر بين السرد والنقر، فتُعاد
  القائمة **مرة واحدة** وتُعاد المحاولة بالهوية نفسها؛ وتكرار الرفض يعرض رسالة `main` كما هي. مقيس: سرد واحد
  ومحاولتان — لا حلقة، والاختيار السابق لا يضيع.
- **العلم** `desktopControl` في `app.js` بنمط `browserControl` حرفياً و**محور مستقل عنه** (لا اقتران):
  `localStorage: satr_desktop_control`، يُرسل في حمولة `send`، ويُطفأ مع «جلسة جديدة» — ومعه **يُسحب اختيار
  النافذة** (`desktopClear`) لأن الاختيار «لجلسة واحدة» (الحارس ١)، وإلا ورثت الجلسة الجديدة نافذة سابقتها.
- **الحارس ١ عند الإرسال**: علم مفعّل لمحرك SDK بلا نافذة مختارة ⇒ **لا يُرسل الدور**، وإشعار بزرّ يفتح اللوحة،
  ونصّ المستخدم يبقى في المحرّر.
- **الحارس ٥ في مكانين**: كل `desktop_activity` يصير سطراً دائماً في اللوحة **وسطراً في بطاقة أداة `desktop_*`
  الجارية في المحادثة** (`chat.js: addDesktopActivity`، نمط `addScreenshot`) فلا تعتمد الرؤية على فتح اللوحة.
- **قناة رابعة `satr:desktopStatus`** (‏`{available}` من `desktop.isAvailable()` بلا إقلاع المعين): زرّ الدرج
  `#desktopToggle` مخفي في الترميز وتكشفه العملية الرئيسية وحدها — فنسخة بلا معين (قبل دفعة الربط `OBS-161`)
  لا تعرض ميزة لا تعمل. النمط نفسه لزرّ 📱.
- **الاتجاه**: أسطر السجل عربية تحوي رموزاً لاتينية (`ضُغط Ctrl+S …`) فتُحسم بـ`textDir` على العنصر لا
  بـ`plaintext`، والقياس بالبكسل لا بالخاصية المحسوبة.

**الأرقام (`test:desktop-panel` — Electron حيّ تحت CSP، مجموعتان):** المكوّن **15 فحصاً** وحده، والقشرة الحقيقية
(‏`src/index.html` عبر خادم الـharness مع mock لقنوات `desktop*`) **10 فحوص**، وصفر `securitypolicyviolation`
وصفر خطأ console. الأدلة المقيسة: **ثمانية أحداث ⇒ ثمانية أسطر في اللوحة وثمانية في المحادثة** · سطر مختلط يرسو
RTL (أول محرف على بعد **27.1px** من اليمين مقابل 252.5px من اليسار) · مربع الإذن يعرض `desktop.permissionDetail`
حرفياً وأول محرف على بعد **10.8px** من اليمين (425.5px من اليسار) وزرّ «موافقة دائمة» مخفي · سقف 200 يُسقط الأقدم ·
نصّ فيه HTML يُعرض نصاً ولا يُنفَّذ `onerror`. ومشهدا `ui:audit` **50** (داكن) و**51** (فاتح): أدنى تباين نصّي
**4.97:1** في الفاتح (التلميح والميتا وزمن السطر) و**7.60:1** في الداكن، والنصوص الرئيسية 11–13.7:1.

**الدمج والقبول** — دُمجت في `main` بالالتزام `02f8110` (‏PR #115، 2026-09-12) بعد قبول **آلي مصوَّر** بدل نقرات المالك
الثماني: سائق Node خارج المستودع (‏`D:\sater\accept-driver\drive-step5.js`) يشغّل نسخة مبنية من `7028beb` بمنفذ تصحيح
Chromium وملف شخصي معزول، ينفّذ الخطوات الثماني عبر DevTools داخل الشجرة الظلية، يرسل طلباً حقيقياً (دور SDK واحد،
~$0.73)، ينقر «سماح» نيابةً عن المالك داخل نسخة الاختبار وحدها ويسجّل كل نقرة بصورتها، ويكتب `report-step5.md`.
الحكم **8/8**: صفر صفوف `Satr`/`electron` في المنتقي، بوابة الإرسال منعت الدور بلا نافذة، «موافقة دائمة» غائب في
`desktop_type`/`press_key` وحاضر في `targets`/`snapshot` (قرار ج)، 7 أسطر `rtl` في اللوحة و7 في المحادثة، الملف
83 ⇐ 122 بايت، والجلسة الجديدة أطفأت العلم وسحبت الاختيار. سقطة الخطوة ٣ الأولى كانت علماً موروثاً في الملف الشخصي
(‏`OBS-178`)، وعضّة بمعيار معكوس أثبتت أن السائق يسجّل الفشل. ما كشفه الدور: النموذج التفّ على غياب نصّ حقل التحرير في
اللقطة بالحافظة وPowerShell (‏`OBS-177`). القائد راجع الصور بيده ودمج بكلمة المالك.

### القرارات التي مسّت المواصفة (معلنة)

1. **مرجعان لا مرجع واحد**: §٧ تكتب `w<targetIndex>:e<elementIndex>`، بينما `surface.js` (الخطوة ٢) يقرأ الرقم بعد
   البادئة **جيلاً**. النموذج يرى `w<جيل اللقطة>:e<n>` (نمط `s<جيل>` في المتصفح)، و`desktop.js` يترجمه إلى مرجع المعين.
   لو عُرض مرجع المعين لصار مرجع لقطة سابقة يُحلّ صامتاً على عنصر آخر يحمل الرقم نفسه. **قُبل بقرار مالك
   2026-09-12 وصُحّح سطر §٧** في المواصفة؛ ازدواج الحرف `w` بين معرّف النافذة ومرجع الجيل يُقاس في الخطوة ٦ (‏`OBS-164`).
2. **الرسائل**: `whyClosed` تسمّي أدوات المتصفح (`open_preview` · `browser_snapshot` · `browser_handoff`)، فالحرفية كانت
   ستوجّه النموذج إلى الأداة الخطأ. لسطح المكتب الصياغة نفسها بأسماء أدواته (`desktop.MESSAGES`)، و`not_allowed`
   مدخل واحد جديد في `whyClosed` يُستهلك منها، و`target_changed` يُستهلك منها كما هو.
3. **رموز المعين ⇒ §٧**: `target_changed` ⇒ `stale_ref` · `focus_failed`/`read_only`/`disabled` ⇒ `not_allowed` ·
   `unsupported_pattern` ⇒ `not_found` · `uia_unavailable` ⇒ `closed` · `internal`/`bad_request` ⇒ `not_found`.
   ورمزان خاصّان بالأداة كنظيريهما في المتصفح: `bad_key` و`bad_input` (وسائط خارج الحدّ).
4. **`element/state` تابع تاسع** ليس في §٨: `desktop_wait_for {ref}` ينتظر به بلا مشي الشجرة فلا يُبطل المراجع؛
   و`desktop_wait_for {text}` يمشي الشجرة ويعيد لقطة جديدة معلنة.
5. **`build.extraResources` من `out-aot` لا `out`**: `out` فيه ناتج JIT محلياً (مشغّل صغير + `satr-uia.dll`)،
   فحزم المشغّل وحده يشحن معيناً لا يعمل. `out-aot` هو مجلد نشر CI نفسه، وغيابه يُتخطّى بتحذير.
6. **نافذة «سطر» نفسها لا تُعرض ولا تُختار** (عملية `process.pid`): وإلا نقر الوكيل «سماح» في مربع إذنه. خارج
   الحرّاس الخمسة لأن `desktopguard` نقي لا يعرف رقم العملية؛ يحرسه `test:desktop`.
7. **المفاتيح**: قائمة بيضاء في المعين وحده (لا نسخة ثانية في JS تتباعد)؛ لا مفتاح ويندوز ولا `F1–F12`، وتركيبات
   مغادرة النافذة `not_allowed`. الإرسال بعد التحقق من المقدّمة وإلا `focus_failed` بلا إرسال.
8. **لا «موافقة دائمة» للأفعال الأربعة** (`desktop_click` · `desktop_type` · `desktop_press_key` · `desktop_scroll` في
   `NEVER_ALWAYS_TOOLS`، مفروضة في `resolvePermission` لا في الواجهة وحدها). وجدتها المراجعة الأمنية للدفعة (متوسطة):
   `alwaysAllowed` تعيش عمر التطبيق لا الجلسة، فمنحها لنقرة في المفكرة كان سيُسقط التأكيد البشري لكل نقرة لاحقة في أي
   نافذة يختارها المستخدم بعدها — نقيض الحارس ١ «لا يُوسَّع بموافقة دائمة». موافقة الدور تبقى، والقراءات الأربع كسائر
   الأدوات («كأي أداة» في §٩ يُقرأ: لا مسار جانبي يُعفي، والتضييق ليس مساراً جانبياً). **قرار مالك 2026-09-12: يبقى
   على الأفعال الأربعة** (القرار ج في مراجعة #113).

9. **`stderr` يُعرض لسطح المكتب وحده**: `agent.js` يبثّ `desktop.unavailableMessage()` على `stderr`، والواجهة
   **لا تعرض `stderr` عموماً** (فيه خرج CLI خام) — فبقي الإشعار ساقطاً صامتاً. الدفعة تعرض هذا النص وحده
   بمطابقة بادئته في `app.js`، والاختبار يثبت وصول الرسالة الحقيقية من `desktop.js` **وأن `stderr` عاماً آخر
   لا يُعرض**. أما عرض بقية رسائل `stderr` الموجّهة للمستخدم فقرار أوسع خارج هذه الدفعة.
10. **نسخة `PROCESS_LABELS` في الواجهة**: المكوّن لا يستطيع استيراد وحدة Electron، فالخريطة العربية منسوخة
    فيه ومكشوفة على الصنف، و`test:desktop-panel` يقارنها بـ`desktopguard.PROCESS_LABELS` (صُدِّرت لهذا) فلا
    تتباعدان بصمت.
11. **سحب الاختيار مع «جلسة جديدة»**: ليس في المواصفة نصّاً، وهو مقتضى الحارس ١ («لجلسة واحدة») — وإلا بقيت
    جلسة سطح المكتب في `main` حيّة عبر محادثات.
12. **نسخة «سطر» أخرى محجوبة بالاسم** (‏`Satr.exe` و`electron.exe` في `BLOCKED_PROCESSES`، مراجعة القائد لـ#115):
    القرار ٦ يحجب نافذة النسخة نفسها بـ`process.pid` وحده، فنسخة ثانية تعمل معها (إنتاج مثبَّت مع نسخة تجربة، أو
    `npm start` مع المثبَّتة) كانت تُعرض وتُختار — فينقر وكيلُ نسخةٍ «سماح» في مربع إذن الأخرى، وهو الخطر نفسه.
    مقيس في `test:desktopguard` (‏`createSession` ترفض عملية `Satr`/`electron` برقم عملية آخر).

### حدود مُصرَّح بها

- **تحرّك النافذة بعد اختيارها** يُخرج عناصرها من المستطيل المأذون: الأفعال `not_allowed` واللقطة تنبّه، وإعادة الاختيار
  بيد المستخدم (لا توسيع آلياً للجلسة) — مسجَّل `OBS-162`. ورقم الهدف في المعين مفتاحه `(hwnd, pid)` فإعادة استعمال
  المقبض داخل العملية نفسها بين سردين حالة حدّية مسجَّلة `OBS-165`.
- **النوافذ المنبثقة** (حوار «حفظ باسم» مثلاً) تظهر في شجرة مالكها لكن مستطيلها قد يتجاوز النافذة ⇒ `not_allowed`.
- **`SendInput` لا يُبلغ عن حجب UIPI** (نافذة بصلاحيات أعلى): الإرسال يُحسب ناجحاً ولا يصل.
- المعين لا يُبنى في بوابة الإصدار `release.yml` بعد: ناتج `uia-helper.yml` artifact، ونسخه إلى `out-aot` قبل
  `npm run dist` خطوة يدوية حتى تُضمّ بقرار مستقل — مسجَّل `OBS-161` (قرار مالك 2026-09-12: دفعة ربط مستقلة قبل أي
  إصدار يكشف الميزة).
- سائق البوابة صفر `scripts/uia-probe/run.js` لا يعرف `session/select` فلا يقيس المعين ≥ 0.2.0 — `OBS-163`.

### الاختبار

`npm run test:desktop-panel` (الواجهة: المكوّن + القشرة الحقيقية تحت CSP، في `SUITE`) ·
`npm run test:desktop` (قطعي، في `SUITE`، يعمل على POSIX) · `npm run test:desktop-live` و`npm run test:uia-helper`
(ويندوز، في `SUITE` و`SKIP_ON_POSIX`) · `test:desktopguard` · `test:surface` · `test:envbrief` (الجرد المستقل).
