### موثوقية الإرسال — مهلات الإقلاع والإيقاف (إصلاح 2026-07-30)

- **العلة المشخّصة**: عملية `codex app-server` حيّة غير مستجيبة كانت تعلّق الدور في
  «يستعد» بلا نهاية (`request()` وعد عارٍ بلا مهلة في تسلسل الإقلاع)، وتعليق
  `turn/interrupt` عند الإيقاف كان يعلّق `stopAll` فيحبس قفل `sendRequestBusy` في
  `main.js` إلى الأبد — كل رسالة لاحقة ترتد «انتظر اكتمال بدء الطلب السابق» حتى
  إعادة تشغيل التطبيق. نفس البنية في مسار SDK حين يعلق `agent.start` قبل الحسم.
- **الإصلاح**: `request(method, params, timeoutMs?)` في `codex.js` صار يقبل مهلة
  اختيارية (الطلبات بلا مهلة كما كانت)؛ طلبات الإقلاع الخمسة
  (initialize/thread-resume/thread-start/compact/turn-start) مقيدة بـ
  `BOOT_REQUEST_TIMEOUT_MS=60000` فتفشل صريحاً في مسار catch القائم
  (spawn_error + result خطأ + cleanup يقتل العملية)، و`turn/interrupt` في `stop()`
  مقيد بـ`INTERRUPT_TIMEOUT_MS=5000` ثم cleanup (نمط forceClose في SDK). وفي
  `main.js`: `stopAll(false)` داخل مسار الإرسال داخل `Promise.race` بسقف
  `STOP_ALL_SEND_TIMEOUT_MS=15000` (المقابض تُسحب مزامنةً وأحداث الدور القديم
  محجوبة بـ`runSeq`)، و`await agent.start` بسقف `SDK_START_TIMEOUT_MS=90000` —
  التجاوز يعيد رسالة عربية ويوقف التشغيل اليتيم عند حسمه المتأخر. تجاوز البيئة
  `SATR_CODEX_BOOT_TIMEOUT_MS`/`SATR_CODEX_INTERRUPT_TIMEOUT_MS` (‏100..600000)
  للاختبار القطعي حصراً.
- **التحقق**: `npm run test:send-liveness` (قطعي، بلا شبكة — fixtures بنمط
  `CODEX_BIN=node`): إقلاع صامت يفشل خلال المهلة بدل الصمت الأبدي، إيقاف على قناة
  ميتة يُحسم خلال المهلة، وحرس نصية على حصون main وحدود تجاوز البيئة؛ مسجل في
  `test:full`. عقود Codex الخمسة (contract/steer/compact/account/mcp-panel) أعيد
  تشغيلها خضراء بعد التعديل، و`eval:agent` ‏12/12.


### انقطاع الشبكة أثناء الدور — التصنيف والمراقب (2026-09-13)

- **العلة المقيسة**: سجل `WLAN-AutoConfig` أثبت سقوط الواي-فاي (نقطة اتصال هاتف عبر محوّل
  TP-Link USB — «disconnected by the driver») من 14:49 إلى 15:47؛ الدور بدأ 15:33 داخل
  الانقطاع، فأعاد Claude Code المحاولة ~3 دقائق **صامتاً** (رسائل `system/api_retry` كانت
  تُمرَّر خاماً وتتجاهلها الواجهة) ثم أخرج `API Error: Can't reach the API server … (ENOTFOUND)`
  وخرج برمز 1، فألصقت الواجهة صندوقاً ثانياً «فشل تشغيل أمر Claude Code — تأكد أنه مثبت
  ومسجّل دخوله». ثلاثة أدوار لاحقة (منها واحد بعد إعادة تشغيل سطر) بقيت «يستعد» بلا ردّ حتى
  أوقفها المالك — فبدا أن إعادة التشغيل هي العلاج بينما العلاج كان عودة الشبكة (15:47).
- **الإصلاح** (بلا اعتماديات): `electron/neterror.js` مصنّف نقيّ بالنصّ (درس OBS-189) يُلحق
  `net {kind, code, message}` بأحداث `result` الخاطئة و`spawn_error` و`api_retry` في **قُمع
  `emit` المشترك في `main.js`** — فيشمل SDK وCodex وKimi ومحوّلات REST بسطر واحد، وبعد
  `acceptEvent` كي يحفظ سجل المحادثة الأصل. `agent.js` يطبّع `api_retry` (أرقام مُنقّاة ونصّ
  مقصوص) ويوسم خروج Query التابع لنتيجة خاطئة بـ`kind:'exit_after_result'` فلا يُرسم له
  صندوق ثانٍ. `electron/connectivity.js` مراقب يبدأ **بعد أول خطأ مصنَّف فقط** ويفحص حلّ
  الأسماء (`dns.lookup` لثلاثة مضيفين، لا `navigator.onLine` لأن محوّلات Hyper-V/VirtualBox
  «Up» دائماً) كل 5 ث حتى 20 دقيقة، ويبثّ `connectivity` عند الانقطاع مرة وعند العودة مع مدتها
  أو عند الاستسلام معلناً. الواجهة: سطر «يعيد الاتصال بالخادم (n/m)» يُحدَّث في مكانه، ورسالة
  عربية تقول السبب الحقيقي («انقطع الاتصال… تعذّر حلّ اسم الخادم… لا حاجة لإعادة تشغيل سطر»)،
  وإشعارات الانقطاع/العودة تدعو إلى «أعد المحاولة».
- **حدّ مُصرَّح به**: لا يعالج انقطاعاً يفوق ميزانية إعادة محاولة CLI؛ يجعل الحالة **مرئية
  وصادقة** ويُبقي المحادثة محفوظة. لم يُقَس حياً بقطع الشبكة فعلاً (الاختبار قطعي بمؤقّتات
  وبحث محقونين + حرس نصية على الوصلات الأربع).
- **التحقق**: `npm run test:neterror` (23 فحصاً، ضمن `test:full`).

### بقاء تنبيه إعادة المحاولة بعد انتهاء الانتظار — 2026-09-22

لقطة المالك تعرض المحاولة 9 من 10 بمهلة 37 ثانية ورمز ازدحام المزوّد. لا تثبت
وحدها أن العملية معلّقة أو أن المحاولة التالية بدأت. ثبت بالكود أن retry-note
كان يبقى بعد وصول النص أو الأداة أو نتيجة الدور وإيقافه، وأن نص overloaded
المشترك مع الخطأ النهائي كان يَعِد بإعادة تلقائية حتى خارج حدث api_retry.

أصبح نص التصنيف يصف ما أبلغ به المزوّد فقط. رسالة الانتظار تصف المهلة المعلنة
ولا تعرضها كعدّ تنازلي، وتزول مع استئناف النص الرئيسي أو الأداة، والنجاح والفشل
وخروج المحرك وإيقاف المستخدم. يعود عنوان سجل العمل إلى النشاط الجاري.
لم نغيّر سياسة إعادة محاولة Claude ولم نفرض مهلة تقطع أداة طويلة أو طلب إذن؛
عدم وصول تحديث لاحق من المحرك يحتاج سجل الجلسة المعنية ويبقى غير محسوم.

الحارس `test:daily-loop-ui` يشغّل الواجهة الفعلية بمحاكاة أحداث المحرك، ويتحقق
من إزالة التنبيه، وعدم تكديسه، وعدم إنهاء الدور بمجرد بلوغ رقم المحاولة الأخير،
وتحرير زر الإرسال عند النتيجة. `test:neterror` يحرس النص النهائي بلا وعد تلقائي.
هذا إثبات لمسار العرض، وليس إعادة إنتاج ازدحام فعلي لدى المزوّد. التعديل في
المصدر، ثم دخل الحزمة `dist/acceptance-retry-build/win-unpacked/Satr.exe`؛
النسخة المبنية المفتوحة سابقاً لا تلتقط التعديل تلقائياً.

دليل العضّة (نسخة مصدر بديلة يقرأها خادم حارس الواجهة؛ ملف الإنتاج لا يتغير):

```text
زرع: node dist/acceptance-budget-20260922/retry-bite.cjs
قبل: original=1 mutant=0 · بعد: original=0 mutant=1
فشل الحارس: daily-loop-ui: AssertionError [ERR_ASSERTION]: بقي انتظار المحاولة بعد النجاح
رمز خروج الحارس: 1
استعادة داخل السكربت: fs.writeFileSync(copy, source)
بعد الاستعادة: original=1 mutant=0 · PRODUCTION_UNCHANGED=true
```

#### حسم الجلسة المصوّرة `314fb986…`

راجعنا سجل Claude وسجل محادثة سطر بعد وصول صورتين إضافيتين. التوقيت أدناه UTC
في 2026-09-22؛ الدليل المنقّح في `dist/acceptance-budget-20260922/session-retry-audit.json`
لا يتضمن نصوص المستخدم أو مدخلات الأدوات أو الاعتمادات.

- الدور `run-357837a3-df71-4d00-85f3-d58b33d22713` بدأ 00:46:48.959 وانتهى
  بحالة `failed` عند 00:56:50.080. سجل Claude عشر رسائل `system/api_error`،
  للمحاولات 1..10، كلها بحالة 529، من 00:52:23.460 إلى 00:56:08.208.
  مهلة التاسعة 36541ms والعاشرة 33505ms تطابقان تقريب الشاشة إلى 37 و34 ثانية.
  وصل الخطأ النهائي `server_error` بحالة 529 عند 00:56:49.932؛ سلسلة الإعادة
  حتى الخطأ النهائي نحو 266 ثانية، وليست كامل مدة الدور البالغة نحو عشر دقائق.
- الدور التالي `run-d682a9ef-7793-4e2a-b7c2-f5c875c36c7a` بدأ 01:12:53.447؛
  سجل المحاولة الأولى بحالة 529 عند 01:13:08.517، ثم انتهى `stopped` عند
  01:13:14.894، ومعه رسالة interruption في Claude عند 01:13:14.922.
  لا يوجد `stopSource` في هذا الدور، لذلك لا ننسب الإيقاف إلى نقرة بشرية بالاستنتاج.

هذه الواقعة تثبت إعادة محاولة فعلية وانتهاء الدور الأول بفشل المزوّد، لا انتظاراً
أبدياً داخل سطر. الصورتان تثبتان بقاء التنبيه القديم بعد الفشل والإيقاف وعودة زر
الإرسال؛ إصلاح العرض يعالج هذا التناقض ولا يعالج ازدحام المزوّد نفسه. لا تعمم
النتيجة على أي تعليق آخر، ولا تثبت نجاح النشر على الموقع الذي كان الوكيل يعدّله.

#### قبول عرض نهاية المحاولة في الحزمة — 2026-09-22

نجح البناء `term_38` وطابقت بايتات 16 ملفاً مختاراً، ومنها مكوّن المحادثة وتصنيف
الخطأ، مع المصدر. في تجربة `live_84ae503710bbf9b8193c08c9` المعزولة، عُرضت
محاكاة معلنة باستدعاء مكوّن المحادثة دون طلب محرك أو نقل اعتماد. نقر المالك
«إنهاء المحاولة التجريبية» وأرسل لقطة: اختفى تنبيه الانتظار، وظهر الخطأ النهائي
التجريبي، وتبدّلت الحالة إلى «تعذّر الإكمال». هذه الفجوة البصرية مجتازة؛ ليست
إثباتاً لتعافي المزوّد أو اختباراً لاتصال Claude حقيقي. تفاصيل المعيار والموارد
في `dist/acceptance-retry-build/ACCEPTANCE.md`.
