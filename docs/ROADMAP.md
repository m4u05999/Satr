# خريطة تطوير «سطر» — الدفعات 1–4 (اعتُمدت 2026-07-09) + الدفعة 5 (اعتُمدت 2026-07-11)

> من تحليل SWOT شامل + مصفوفة فجوة مقابل Cursor (جلسة 2026-07-09، بموافقة المالك).
> **التموضع المعتمد**: «رفيق البرمجة العربي لكل النماذج» — محادثة + طرفية + عارض + وكيل.
> **ليس** استنساخ IDE. لا نطارد Tab-completion ولا محرّراً كاملاً — نطوّق نقطة عمى Cursor
> (العربية) ونمتلك حلقة وكيل متعددة النماذج.
> منهجية التنفيذ الثابتة: مرحلة فرعية واحدة في الدور، قبول المالك بـ npm start بعد كلٍّ،
> تحديث التوثيق مع كل عقد جديد.
>
> **تعديل منهجية التحقق (قبول المالك 2026-07-11 — من م-2 فصاعداً)**: التحقق الحيّ الآلي
> المفصّل (مسبار معزول بعشرات الحالات) لم يعد إلزامياً لكل دفعة — يُستخدم **عند الضرورة
> فقط**: منطق محفوف (تفاعل معقّد، عزل أمني، حالات حدّية، ما يصعب مراجعته بالعين). **يبقى
> إلزاماً دائماً** (رخيص — ثوانٍ، يمنع تسليم مكسور): فحص بنائي (`node --check`) + إقلاع
> حقيقي نظيف (صفر أخطاء عارض/CSP). الاختبار اليدوي التفصيلي يتولّاه المالك عند الحاجة.

---

## خلاصة الفجوة مقابل Cursor — لقطة تاريخية (قبل الدفعتين 1–2، مثبّتة)

- **فجوتنا الجوهرية**: Cursor «يرى ويلمس الكود» (محرّر + وكيل لأي نموذج + فهرسة)؛
  «سطر» يفعلها عبر عيون Claude فقط، ومحوّلات REST (Gemini/DeepSeek/Qwen) **دردشة عمياء**
  (لا ترى ملفات، لا تحرّر، لا أدوات).
- **تفوّقنا**: طرفية BiDi عربية (مكسورة في Cursor)، تجربة عربية كاملة، أذونات حيّة عربية،
  تعدد نماذج رخيص، مجاني ومفتوح.
- ⚫ خارج التموضع (قرار): Tab completion، محرّر كامل، فهرسة embeddings عميقة.

---

## الدفعة 1 — «النموذج يرى المشروع» ✅ (مكتملة)

> الأثر: فوري ومرئي؛ المخاطرة: صغيرة. تحوّل المحوّلات من «دردشة عمياء» إلى «تفهم مشروعك».

- **1.1 ✅ — حقن @الملفات للمحوّلات**: عند إرسال رسالة فيها `@مسار` بمحرك غير SDK، يقرأ
  main.js محتوى الملف (بسقف حجم) ويحقنه في البرومبت المُرسَل للمحوّل بصيغة واضحة
  (اسم الملف + محتواه في كتلة كود). Claude/SDK لا يتأثر (يقرأ بنفسه).
  تنقية: المسار داخل cwd حصراً (لا `..`)، سقف حجم لكل ملف وسقف إجمالي. التنفيذ مثبت في
  `electron/inject.js` ومسار إرسال المحوّلات في `electron/main.js`.
  **قبول**: `@src/index.html اشرح هذا الملف` مع DeepSeek يشرح المحتوى الفعلي.
- **1.2 ✅ — لوحة ملفات/عارض قراءة خفيف**: شجرة ملفات جانبية (تعيد استخدام `files.js`
  القائم) + النقر يفتح **عارض قراءة** (ليس محرّراً) بتظليل بسيط، الكود LTR داخل RTL.
  زر 📄 في الشريط العلوي (ليس أمر `/` — احتراماً لتجميد الأوامر). التنفيذ مثبت في
  `src/ui/components/files-panel.js` و`src/ui/components/file-viewer.js`، ويحرس الحفظ
  الآمن `npm run test:viewer-security`.
  **قبول**: أتصفح شجرة مشروعي وأفتح ملفاً وأقرؤه بوضوح.
- **1.3 ✅ — ذاكرة المحوّلات على القرص**: سجل محادثات REST يُحفظ في `~/.satr/chats/`
  (بدل الخريطة في الذاكرة التي تُمسح بإعادة التشغيل) + استئناف بعد إعادة التشغيل.
  التنفيذ مثبت في `electron/chats.js` ووصل `satr:listChats`/`satr:readChat` في
  `electron/main.js` و`src/ui/components/sessions-panel.js`.
  **قبول**: أحادث DeepSeek، أغلق «سطر»، أفتحه، أكمل المحادثة بذاكرتها.

## الدفعة 2 — «حلقة الوكيل الخاصة» ✅ (مكتملة)

> تحوّل «سطر» من واجهة Claude إلى منصة مستقلة: diff/تراجع/أذونات تعمل مع كل النماذج.

- **2.1 ✅ — بروتوكول أدوات فوق openai-compatible**: حلقة tool-calling (النموذج يطلب
  أداة ⇒ «سطر» ينفّذ ⇒ يعيد النتيجة) بأدوات أولية: read_file / list_files. التنفيذ في
  `electron/tools.js` و`electron/adapters/openai-compatible.js`.
- **2.2 ✅ — أدوات الكتابة بأذونات عربية**: write_file / edit_file تمرّ بمربع الإذن العربي
  القائم + بطاقات diff + تراجع (إعادة استخدام مسار file_edit/undoEdit). التنفيذ في
  `electron/tools.js` و`src/ui/components/perm-dialog.js` و`src/ui/lib/diff.js`.
- **2.3 ✅ — أداة تنفيذ أوامر**: run_command عبر الطرفية المرئية (نمط run_in_terminal
  المثبّت) بموافقة إلزامية. التنفيذ في `electron/tools.js` و`electron/term.js`.
- **2.4 ✅ — تعميم الحلقة**: نفس الحلقة لـ Gemini (function calling بصيغته) — كل نموذج
  جديد يرث الوكيل كاملاً. التنفيذ في `electron/adapters/gemini.js`، ويحرس سجلّ المزوّدين
  `npm run test:adapters`.

## الدفعة 3 — Enterprise MVP

- **3.1 ✅ — مزوّد Ollama محلي**: نُفّذ أولاً ضمن `enterprise/` ثم نُقل إلى Community
  في `electron/adapters/ollama.js`؛ نماذج محلية بلا إنترنت (سيادة بيانات).
- **3.2 ✅ — الترخيص وهوية الإصدار**: تحقق Offline موقّع بـEd25519 + feature-flags، وهوية
  بناء مستقلة وتشخيص fail-safe ومفتاح خاص خارج Git/CI.
- **3.3 ✅ — لوحة استهلاك/تكلفة محلية**: رموز/كلفة لكل مزوّد وجلسة؛ **🧭 التجميع
  المركزي لم يُنفَّذ بعد** ويبقى ضمن قدرات Enterprise المخططة في `docs/EDITION-MATRIX.md`.
- **3.4 ✅ — سجل تدقيق محلي**: من فعل ماذا (أوامر، تعديلات ملفات، أدوات)؛ **🧭 المركزية
  وسياسة الاحتفاظ لم تُنفَّذا بعد** وتبقيان ضمن قدرات Enterprise المخططة.
- **3.5 ✅ — سلسلة بناء خاصة**: checkout خارجي + Artifact خاص + منع النشر/التحديث العام
  + فحص `app.asar` وprovenance للمصدرين والملفات.

## الدفعة 4 — التوسّع (حسب ما يثبت طلبه)

- **4.1 ✅ (2026-07-11) — تحديث نظام التصميم (أولوية المالك)**: Design Tokens حديثة
  (اقتباس قيم من أنظمة مفتوحة مثل Radix Colors — نسخ قيم CSS variables لا تثبيت حزم) +
  خط عربي مفتوح مضمّن (IBM Plex Sans Arabic بترخيص OFL — vendored مثل xterm، لا CDN) +
  تلميع اللوحات والحركات. **القيدان الصارمان**: صفر اعتماديات وقت تشغيل + CSP بلا inline
  (قاعدتا CLAUDE.md 5 والأمان) — الاستلهام من المفتوح نعم، الاعتماديات لا.
  نُفّذت: مقياس Sand الداكن (رمادية دافئة) + Grass/Red دلاليين والهوية الذهبية ثابتة،
  خط مضمّن عربي+لاتيني × 400/500/700 woff2 (~190ك.ب) عبر scripts/vendor-fonts.js،
  حجم الأساس 16px (قرار مالك عند القبول)، حركات rise/pop موحّدة عبر --dur/--ease.
  التفاصيل في قسم «نظام التصميم» بـ CLAUDE.md.
- **4.1-ب ✅ (2026-07-11) — دفعة UX سريعة (من مراجعة UX بموافقة المالك)**: التصاق ذكي
  بالذيل + زر «⬇ الأحدث» (كان البث يسحب القارئ قسراً) · أزرار نسخ للكود والرسائل ·
  حفظ مسودة المحرّر (satr_draft) · بحث فوري في لوحة الجلسات · إشعار نظام عند اكتمال
  الدور والنافذة بالخلفية. واجهة خالصة — بلا IPC جديد وبلا أوامر جديدة.
- **4.2 ✅ (2026-07-11) — محادثات المحوّلات في لوحة الجلسات** (طلب المالك أثناء قبول 1.3):
  اللوحة تدمج ~/.satr/chats مع جلسات كلود بالأحدث أولاً (اسم المزوّد بدل المجلد)،
  IPC قراءة فقط satr:listChats/readChat (تطبيع صيغتَي OpenAI/Gemini + فكّ ترويسة حقن @)،
  والنقر يستأنف: تبديل محرك + sessionId — الدور التالي يكمل من ذاكرة القرص.
- **4.3 ✅ (2026-07-11) — تظليل كود بسيط في عارض القراءة**: مميّز رموز يدوي بلا
  اعتماديات (تعليقات/نصوص/كلمات مفتاحية/أرقام بألوان tokens)، اللغة من الامتداد،
  حالة تعليق كتلي عابرة للأسطر، وتدهور آمن لنص خام (سطر عملاق/امتداد مجهول).
  وأثناء القبول (لقطتا مالك: CLAUDE.md مشوه ثم مشتت): أساس اتجاه موحّد للملف كله
  («تلقائي»: امتداد كود = LTR؛ غيره بإحصاء المحارف ⇐ RTL للعربي الغالب) + زر
  «الاتجاه: تلقائي/RTL/LTR» مؤقت. درس مثبّت: dir=auto لكل سطر يشتت (أرساء متناوب).
- **4.4 ✅ (2026-07-11) — عرض RTL للعربي في بطاقات diff** (ملاحظة المالك من قبول 2.2):
  نفس قاعدة العارض (كود = LTR؛ عربي غالب = مرآة rtl-doc: أرقام وإشارة يميناً +
  نص يرسو يميناً ويلتف) — ورث دروس 4.3 الثلاثة مباشرة.
- **4.5 ✅ (2026-07-11) — تحرير خفيف في عارض القراءة (ترقية 1.2)**: زرّ «✏️ تحرير»
  يبدّل لـ textarea بديل (لا contenteditable — قرار نطاق بموافقة المالك: تراجع/إعادة
  أصليان، بلا تظليل أثناء التحرير) + حفظ عبر IPC جديد `satr:writeFile` بمسار tools.js
  المؤمَّن نفسه (resolveExisting/readBefore/commitWrite — ملف قائم فقط، ≤1م.ب، لا ثنائي)
  + بطاقة diff وتراجع في المحادثة (تعود في الردّ لا حدثاً — حدث خارج دور يسقط على حارس
  الكتلة) + مؤشر «●» وCtrl+S وسؤال عند إغلاق غير محفوظ. لا تحرير للمقصوص (يُتلف البقية).
- **4.6 ✅ (2026-07-11) — فهرسة مشروع بسيطة (بحث دلالي خفيف)**: محرك `electron/search.js`
  بلا فهرس دائم وبلا اعتماديات (embeddings خارج التموضع — قرار معتمد): تطبيع عربي +
  مطابقة جزئية (تصيب camelCase) + ترتيب بالنقاط، ميزانية 2ث وسقوف. طرفان بمحرك واحد:
  أداة `search_code` للمحوّلات العمياء (قراءة بلا إذن — نمط Grep، كل المزوّدين يرثونها)
  + حقل 🔍 في لوحة 📄 (IPC ‏`satr:searchFiles`) والنقر يفتح العارض ويقفز للسطر مميّزاً.
  تحقق حيّ: مسح مشروع «سطر» كاملاً في ~160مث، «save viewer» تصيب saveFromViewer،
  و«لقطه» بالهاء تصيب «لقطة» بالتاء المربوطة.
- **4.7 ✅ (2026-07-11) — لوحة تغييرات git ± («فرق» — تفسير البند بموافقة المالك)**:
  «ماذا تغيّر منذ آخر التزام» في مكان واحد — زرّ ± يفتح لوحة بالملفات المتغيّرة
  (شارات + عدّادات) والنقر يفرد بطاقة diff (بمرآة RTL، بلا تراجع — ليست لقطات «سطر»).
  `electron/gitdiff.js`: git بلا shell + porcelain -z للأسماء العربية، المعدَّل بتحليل
  git diff الموحّد والجديد/المحذوف بـ computeDiff (درسا CRLF وسقف LCS في CLAUDE.md).
  عرض فقط — لا commit/stage/revert (قرار نطاق). تحقق حيّ: مستودع «سطر» + مستودع
  اصطناعي (عربي/محذوف/ثنائي/بلا HEAD) + غير مستودع + دقة العدّادات بعد إصلاحي CRLF وLCS.
- **4.8 ✅ (2026-07-11) — تصدير المحادثة 📤 («مشاركة»)**: زرّ 📤 يصدّر المحادثة الحالية
  Markdown — القرص مصدر الحقيقة للمحرّكين (`electron/exporter.js`: جلسات كلود كاملة عبر
  readFullSession بالمعرّف UUID، ومحادثات المحوّلات عبر chats.read بلا سقف)، والحفظ في
  الواجهة Blob + تنزيل (لا مسار كتابة جديداً). تحقق حيّ: جلسة كلود حقيقية (20 رسالة)
  ومحادثة DeepSeek حقيقية (14) من القرص + notfound.
- **قبول المالك (2026-07-11)**: الدفعات 4.5–4.8 اختُبرت كلها بـ npm start وقُبلت
  بلقطات شاشة (بطاقتا تحرير العارض + نتائج البحث + لوحة ± + حوار التصدير) —
  **الحاجز مرفوع**: يجوز بدء تفكيك Web Components.
- ✅ تفكيك الواجهة إلى Web Components (§5-د/UX-COMPOSER.md) — **اكتمل (2026-07-11)**:
  14 دفعة ت-0…ت-13 كلها في `docs/COMPONENTS-PLAN.md` (آلية الأنماط adoptedStyleSheets
  مثبّتة بتحقق حيّ). **متبقٍ على المالك**: جولة الاختبارات اليدوية المُجمَّعة.
  - **ت-0 ✅ (2026-07-11) — البنية التحتية**: نقل `<style>` إلى `src/styles/base.css`
    والسكربت الضخم إلى `src/ui/app.js` (كلاسيكي خارجي — IIFE بـ 'use strict' أصلاً)،
    update-csp يتسامح مع صفر كتل (توجيهات بلا هاشات، يبقى حارساً)، `ui/lib/sheet.js`.
    تحقق حيّ: إقلاع بلا أي خطأ عارض/CSP (ELECTRON_ENABLE_LOGGING).
  - **ت-1 ✅ (2026-07-11) — `<satr-agents-panel>` أول مكوّن (إثبات النمط)**: Shadow DOM +
    adoptedStyleSheets + ورقة اللوحات المشتركة `ui/lib/panel.css.js` (تضم ما لا يعبر
    حدود Shadow: reset + button/input + reduced-motion). نقل حرفي لمنطق openAgents؛
    حالة الفتح `[open]`. تحقق حيّ: مسبار معزول تحت CSP الفعلية (فتح/صفوف/tokens
    تعبر/زر ✕) + إقلاع التطبيق الحقيقي بصفر أخطاء.
  - **ت-2 ✅ (2026-07-11) — `<satr-skills-panel>`**: أول method عامة
    (`getSkillsPayload(cwd)` — القشرة تسألها عند الإرسال، واحتياط 'all' قبل الترقية)؛
    الحالة وlocalStorage داخل المكوّن. درس: `.skill-hint` مشترك مع فراغ لوحة الموصّلات
    ⇒ بقي في base.css حتى ت-3. تحقق حيّ: مسبار معزول (payload كاملة الدورة) + إقلاع نظيف.
  - **ت-3 ✅ (2026-07-11) — `<satr-mcp-panel>` + `<satr-context-panel>`**: أول أحداث
    مخصّصة («panel-refresh» لإعادة الفتح بحالة طازجة + «notice» لإشعارات الإجراءات)؛
    عقد السياق open(cwd, sessionId, busy)؛ دَين skill-hint حُسم؛ Escape موحّد في القشرة.
    تحقق حيّ: مسبار معزول شامل + إقلاع نظيف.
  - **ت-4 ✅ (2026-07-11) — `<satr-sessions-panel>`**: المكوّن يملك الجلب/الدمج/البحث؛
    الاستئناف حدث `session-resume` والقشرة تنفّذ (حالة عميقة بقيت). تحقق حيّ: مسبار
    شامل + إقلاع نظيف.
  - **ت-5 ✅ (2026-07-11) — `ui/lib/diff.js` + `ui/lib/highlight.js` (مقدَّم من ت-7) +
    `<satr-git-panel>`**: أول وحدات مشتركة بجسر window.SatrUI للقشرة + ورقة diff.css.js
    (ازدواج موثّق مع base.css حتى ت-12). تحقق حيّ: مسبار شامل (بطاقات Shadow وlight +
    مرآة عربية + تراجع) + إقلاع نظيف.
  - **ت-6 ✅ (2026-07-11) — `<satr-files-panel>`**: الشجرة الكسولة + بحث 4.6 داخل
    المكوّن؛ فتح الملفات حدث `file-open {rel, line}` نحو عارض القشرة (ت-7).
    تحقق حيّ: مسبار شامل + إقلاع نظيف.
  - **ت-7 ✅ (2026-07-11) — `<satr-file-viewer>`**: العارض كاملاً (تظليل/اتجاه/تحرير/
    حفظ ببطاقة عبر حدث file-saved + handleEscape للسلسلة). **درس مثبّت**: retargeting
    نقرات Shadow على مستمع المضيف — الحل composedPath()[0] (ينطبق على ت-8).
    تحقق حيّ: مسبار كشف العلّة وأكد الإصلاح + إقلاع نظيف.
  - **ت-8 ✅ (2026-07-11) — `<satr-gate>` + `<satr-perm-dialog>`**: البوابة ذاتية
    الفحص (gate-ready يرفع الحجب)، والأذونات بطابورها وردّها داخل المكوّن
    (request/closeAll + notice). تحقق حيّ: مسبار شامل + إقلاع نظيف.
  - **ت-9 ✅ (2026-07-11) — `<satr-terminal-panel>`**: منطقة الطرفية كاملة (453 سطراً
    معزولة ذاتياً) نُقلت حرفياً لمكوّن light DOM (بلا Shadow — xterm يقيس المستند؛
    غلاف display:contents). عقد adoptModelTerm/setTermOpen. تحقق حيّ: مسبار بـ xterm
    الحقيقي (عربي في BiDi + مفاتيح الإدخال + exit + تبويب النموذج) + إقلاع نظيف.
  - **ت-10 ✅ (2026-07-11) — `<satr-composer>`**: قرار الحالة حُسم — send() قشرة،
    والمكوّن يملك الميكانيكا (مرفقات/قائمتا / و@ مع CLI/مسودة/شريط الخلفية) ويبث
    composer-send؛ أوامر «/» تُحقن setCommands. تحقق حيّ: مسبار 6 مشاهد + إقلاع نظيف.
  - **ت-11 ✅ (2026-07-11) — `<satr-topbar>` + ⚙**: مفاتيح المزوّدين/اختيار المجلد/
    المجلدات الإضافية (getExtraDirs)/منبثق ⚙/قسم EE — light DOM بنمط ت-10.
    تحقق حيّ: مسبار 5 مشاهد + إقلاع نظيف.
  - **ت-12 ✅ (2026-07-11) — `<satr-chat>`**: خيط المحادثة كاملاً (ماركداون/نسخ/التصاق/
    بطاقات رسائل وفرق ووكلاء وضغط/إشعار/عدّاد الكلفة) — light DOM بنمط ت-9؛ مجرى
    satr:event بقي orchestration في القشرة بعقد كتلة newAssistantBlock(label) الحرفي.
    **حُسم ازدواج diff**: نسخة base.css حُذفت وdiffSheet تُعتمد على المستند (تخدم light
    وShadow معاً)، وسقط آخر مستهلك لجسر SatrUI. تحقق حيّ: مسبار 40/40 + إقلاع نظيف.
  - **ت-13 ✅ (2026-07-11) — التنظيف النهائي**: app.js وحدة ES (تعمل أولاً بترتيب
    الوسوم)، جسر window.SatrUI أُزيل كلياً (استيراد مباشر فقط)، update-csp رُوجع
    (بلا هاشات + يبقى حارساً)، الوثائق حُدّثت (CLAUDE.md خريطة + قسم «مكوّنات
    الواجهة»، ARCHITECTURE §8، هذا الملف). قبول متحقَّق: إقلاع تطويري نظيف +
    dist:dir + النسخة المحزومة أقلعت بصفر أخطاء.
  - **قرار المالك**: اختبارات القبول اليدوية تُجمَّع لجولة واحدة لاحقة تغطي عدة دفعات
    (التحقق الحيّ الآلي يبقى شرطاً لكل دفعة).

---

## الدفعة 5 — «سطر يرى الويب» (اعتُمدت بقبول المالك 2026-07-11)

> من تحليل SWOT للميزتين (جلسة 2026-07-11): «المعاينة الجانبية + التحرير بالتأشير»
> و«الوكيل يستخدم المتصفح» تشتركان في **لبنة واحدة** — متصفح مدمج عبر `WebContentsView`
> (أصلي في Electron 33: ‏executeJavaScript للحقن + sendInputEvent للفعل + capturePage
> للقطات — **صفر اعتماديات جديدة**، قاعدة CLAUDE.md ‏5 قائمة). **الترتيب المعتمد**:
> المعاينة أولاً (مكاسب مؤكدة بمخاطر منخفضة وتوفير توكنز فعلي)، ثم أدوات الوكيل
> قراءةً ففعلاً (رهان أعلى يُبنى فوقها). التوقيت المرجّح: بعد النشر على GitHub.

**قرارات مثبّتة من التحليل (حدود النطاق)**:
- ربط «عنصر ⇐ ملف مصدر» يعتمد ذكاء النموذج (selector + ‏outerHTML مقتطع ⇒ يجده بـ
  Grep) — **لا** source maps ولا إضافات Babel (نمط Onlook — خارج الحجم بقرار نطاق).
- المعاينة حدودها صلبة: تحديد + وصف + إرسال للوكيل فقط — **ليست محرّراً مرئياً**
  (لا سحب/إفلات ولا تحرير CSS مباشر — حارس ضد زحف النطاق).
- **عزل أمني إلزامي**: webContents منفصل **بلا preload يكشف window.satr** + ‏partition
  مستقلة — المحتوى المعروض غير موثوق بالتعريف، وقلعة contextIsolation لا تُمس.
- أدوات الفعل (نقر/كتابة) **كانت مؤجلة لبوابة قرار بعد م-3؛ أُغلقت البوابة لاحقاً بتنفيذ
  م-4 ✅** — تهديد حقن البرومبت من صفحات الويب موثّق صناعياً؛ التخفيف المعتمد: إذن عربي
  لكل فعل + جلسة معزولة، ولم تلزم قائمة النطاقات بقرار م-4.
- مثال «النشر لمنصة كلاود»: الوكيل يجرّب CLI أولاً بطبيعته (أرخص وأثبت — الطرفية
  عنده أصلاً)، والمتصفح لفجوات ما لا CLI/API له.

- **م-1 ✅ (2026-07-11) — لوحة المعاينة المدمجة**: لوحة جانبية بـ WebContentsView (زرّ 🌐
  في الشريط — لا أمر `/`)، شريط عنوان مصغّر (URL يدوي + تحديث + رجوع/تقدم) + **التقاط
  عناوين localhost تلقائياً** من خرج الطرفيات (كل التبويبات + طرفية النموذج 🤖) بإشعار
  بزرّ «افتح المعاينة». نُفّذت: `electron/preview.js` (عزل كامل: بلا preload + partition
  مستقلة + http/https حصراً + رفض أذونات الويب) + `<satr-preview-panel>` (المكوّن 15 —
  إطار Shadow يبلّغ مستطيل العرض والعرض الأصلي يطفو فوقه) + `#midRow` (محادثة يميناً
  ومعاينة يساراً بمقبض عرض محفوظ). **حدّان موثّقان**: خوادم Bash الخفية لا تُرصد
  (bgprocs بلا URL — إدخال يدوي)، والعرض الأصلي فوق المنبثقات المركزية على الشاشات
  الضيقة. تحقق حيّ: مسبار 16/16 (فتح/صفحة عربية RTL بعنوانها/تنقل/رجوع/رفض غير http/
  مستطيلات/إغلاق) + إقلاع نظيف. **قبول المالك**: «سطر» يشغّل مشروع ويب ⇒ المعاينة
  تعرضه بجانب المحادثة والعربي فيه RTL سليم.
  - **م-1-ب ✅ (2026-07-11) — الوكيل يعرف المعاينة** (لقطة مالك من قبول م-1: النموذج
    شغّل الخادم بـ Bash الخفي وفتح **كروم** بـ Start-Process — لا يعلم بالمعاينة ولا
    تُرصد خوادمه الخفية): ‏systemPrompt ملحق (preset+append) يعرّفه ببيئة «سطر»
    ويمنعه من المتصفح الخارجي إلا بطلب صريح + أداة `open_preview(url)` في خادم MCP
    الداخلي تبثّ `preview_open` فتفتح القشرة اللوحة. تحقق حيّ: دور SDK حقيقي (haiku)
    استدعى الأداة والحدث وصل بعنوانه والدور سليم — + إقلاع نظيف.
  - **م-1-ج ✅ (2026-07-11) — تذكّر آخر مشروع + تحديث تلقائي** (طلب المالك: «النقر على 🌐
    يفتح المشروع مباشرة، ويتحدّث مع كل تعديل»): (1) آخر عنوان ناجح يُحفظ
    (`satr_preview_url`) والنقر على 🌐 يفتحه فوراً بدل الشاشة الفارغة (خادم متوقف ⇒ خطأ
    لطيف والحقل جاهز لإعادة المحاولة). (2) زرّ 🔄 «تحديث تلقائي» (مُفعّل افتراضياً،
    `satr_preview_autoreload`): القشرة تتتبّع `previewDirty` (يُضبط عند file_edit،
    يُصفّر ببدء الدور) وتستدعي `reloadIfLive()` عند اكتمال دور عدّل ملفات — المكوّن
    يعيد التحميل فعلياً فقط إن كان الوضع مفعّلاً والعرض حيّاً. **حدّ موثّق**: reload
    بعد اكتمال الدور — مشاريع ذات HMR (Vite) تتحدّث بنفسها فيُفضَّل إطفاؤه لها؛ ومشاريع
    ذات خطوة بناء منفصلة قد تسبق المعاينة اكتمالها (⟳ اليدوي متاح). تحقق حيّ: مسبار
    11/11 (تذكّر URL/إعادة فتح تحمّل الأخير/reload حين حيّ/لا reload حين مطفأ أو مغلق)
    + إقلاع نظيف.
  - **م-1-د ✅ (2026-07-11) — تذكّر لكل مجلد + رسالة خادم متوقف** (لقطة مالك: استأنف
    جلسة قديمة، النقر على 🌐 لم يفتح مشروعه — العنوان لم يكن محفوظاً قبل م-1-ج،
    وخادم الجلسة القديمة مات): (1) العنوان يُحفظ **لكل cwd** (`satr_preview_url::<cwd>`
    يقرأ #cwd — نمط terminal-panel) بلا fallback عام (يمنع تلوّث مشروع بعنوان آخر)،
    فلكل مشروع منفذه. (2) رسالة فشل واضحة تشرح أن الخادم يجب أن يعمل وتوجّه «اطلب من
    الوكيل شغّل المشروع» بدل رمز غامض — العرض يبقى حيّاً و⟳ يعيد المحاولة. **توضيح
    جوهري للمالك**: المعاينة **تعرض** خادماً حيّاً لا **تشغّله** (تشغيله مهمة الوكيل)؛
    استئناف جلسة لا يعيد إحياء خوادمها الميتة. تحقق حيّ: مسبار 7/7 (حفظ لكل cwd/
    مشروع آخر بلا تلوّث/العودة تحمّل عنوان المجلد/رسالة خادم متوقف) + إقلاع نظيف.
- **م-2 ✅ (2026-07-11) — التحرير بالتأشير**: زرّ 🎯 في المعاينة يبدأ وضع تحديد —
  `preview.startPick()` يحقن سكربتاً في الصفحة المعزولة عبر `executeJavaScript` يعيد
  **Promise يُحلّ عند نقر المستخدم** على عنصر (outline ذهبي يتتبّع المؤشر، يمنع تفعيل
  الروابط/الأزرار، Escape/إلغاء ⇒ null). لا preload في العرض ⇒ قيمة الـ Promise هي
  المسار الوحيد لإخراج البيانات (نظيف، بلا قناة). يلتقط `{selector تقريبي، tag،
  outerHTML مقتطع 600، نص 140}`، فيظهر شريط بملخّص العنصر + حقل «ماذا تريد أن يتغيّر»
  ⇒ حدث `preview-edit` ⇒ القشرة تركّب سياقاً منسّقاً وترسله **كدور عادي** (مسار send
  — صفر عقد محادثة جديد). **أمان**: outerHTML من صفحة غير موثوقة يُغلّف كـ «محتوى»
  ويُقتطع (حقن برومبت محتمل موثّق — م-2 وصف فقط، المستخدم يبادر ويرسل). IPC:
  `satr:previewPick`/`previewPickCancel`. تحقق حيّ: مسبار محرك 8/8 (نقر اصطناعي في
  عرض معزول يلتقط button بنصه العربي وصنفه ومُحدِّده + إلغاء ⇒ null + تنظيف تام) +
  مسبار واجهة 11/11 (شريط/حدث/إرسال/إلغاء) + إقلاع نظيف. **قبول المالك**: أحدّد
  زراً وأكتب «اجعله أخضر» ⇒ النموذج يجده في المصدر ويعدّله وبطاقة diff تظهر.
- **م-3 ✅ (2026-07-11) — أدوات تصفح قراءة للوكيل**: خادم MCP الداخلي (نمط المرحلة 16)
  أضاف أداتين للقراءة تعملان على العرض القائم (`open_preview` القائمة تخدم التنقّل —
  لم تُكرَّر): `read_page` (snapshot نصي من DOM: عنوان/عناوين/روابط/أزرار/حقول + مقتطف
  نص — يُغلّف كـ «محتوى صفحة للفحص لا للتنفيذ»، وعي بحقن البرومبت) و`screenshot`
  (لقطة PNG كمحتوى MCP نوع image — رؤية محرك SDK). المحرك في `preview.js`
  (`readPage`/`screenshot` — waitReady ينتظر انتهاء التحميل، وموديول مشترك بين main
  وagent مثل term.js). systemPrompt يوجّه الوكيل لاستعمالهما للتحقق من نتيجة تعديلاته
  وتصحيح نفسه **في اللوحة المرئية** (فلسفة «يعمل أمامك»). قراءة فقط — لا أفعال (م-4 خلف
  بوابة). تحقق حيّ (منطق محفوف — دعم image content): مسبار 9/9 = محرك مباشر (readPage
  يستخرج البنية بنصّها العربي + screenshot PNG غير فارغ) **+ دور SDK حقيقي (haiku)
  استدعى الأداتين والرؤية قبِلت اللقطة بلا خطأ** + إقلاع نظيف. **قبول المالك**: «افتح
  المعاينة وتأكد أن الهيدر يظهر» ⇒ الوكيل يقرأ/يرى ويصف الموجود فعلاً.
- **م-5 ✅ (2026-07-11) — تسجيل فيديو التصفح** (طلب المالك): زرّ ⏺ في المعاينة يسجّل
  جلسة التصفح فيديو webm قابل للتنزيل. **صفر اعتماديات**: `preview.captureFrame()`
  (capturePage ⇒ PNG base64 + أبعاد) يُطلب دورياً (~8 إطار/ث عبر satr:previewFrame) ⇒
  المكوّن يرسمه على `<canvas>` مخفي ⇒ `canvas.captureStream(8)` ⇒ `MediaRecorder`
  (webm/vp9 إن دُعم) ⇒ عند الإيقاف Blob ⇒ تنزيل `<a download>` (نفس نمط تصدير 4.8
  المثبّت — **لا CSP جديد**: تنزيل الـ anchor لا يخضع لـ media/connect-src). يسجّل عرض
  المعاينة وحده لا النافذة. الإغلاق يوقف التسجيل وينزّل ما سُجّل. **حدّ**: webm فقط
  (تحويل mp4 يحتاج ffmpeg — خارج صفر الاعتماديات). تحقق حيّ (منطق محفوف): مسبار سلسلة
  كاملة بعرض حقيقي (476×641، تسجيل 1.8s ⇒ webm نُزّل باسم satr-preview-*.webm؛ حارس
  onstop يضمن blob غير فارغ) + إقلاع نظيف.
- **م-4 ✅ (2026-07-11) — أدوات الفعل بالإذن**: أداتان `browser_click(selector)` +
  `browser_type(selector, text)` في خادم MCP الداخلي، على العرض القائم عبر
  `preview.clickElement/typeText` (executeJavaScript؛ selector يُهرَّب بـ JSON.stringify
  — لا حقن). النقر `el.click()` بعد scrollIntoView؛ الكتابة عبر **native value setter**
  ثم input/change events (توافق React/Vue). **الأمان (حرج، مثبّت)**: الأداتان تمرّان
  بـ `canUseTool` مثل Bash — مربع الإذن العربي **كل مرة** (لسن في alwaysAllowed)،
  bypassPermissions وحده يعفيها (لا acceptEdits — تحقق: canUseTool لأدوات MCP يُستدعى
  في default/acceptEdits). `toolDetail` يُظهر `selector` في الإذن فيرى المستخدم الهدف.
  **قرار نطاق**: قائمة النطاقات المسموحة **لم تلزم** — الإذن اليدوي لكل فعل أقوى منها
  (يعفي فعلاً واحداً لا نطاقاً كاملاً). systemPrompt يوجّه الوكيل لاستعمالهما بعد
  read_page. تحقق حيّ (منطق محفوف): مسبار محرك 8/8 (نقر يغيّر النص فعلاً + كتابة تضبط
  القيمة **وتُطلق حدث input** لتوافق React + not_found/not_editable/bad_selector) +
  إقلاع نظيف. **حدّ موثّق**: النص المكتوب لا يظهر كاملاً في مربع الإذن (selector فقط) —
  الوكيل يكتب عادةً ما طلبه المستخدم؛ حقن البرومبت من صفحة عبر read_page ⇒ الإذن اليدوي
  يكشف الفعل الشاذّ. **قبول المالك**: أطلب «جرّب زر الإرسال» ⇒ الوكيل ينقر بعد إذني.
- **ترقية تحكّم المتصفح — لقطة + ref حتمي (2026-07-12، Claude — العمل بالتوازي مع Codex)**:
  بحث موثّق أثبت تقارب الصناعة (Playwright MCP/browser-use/OpenAI CUA) على **لقطة شجرة
  الوصول بمُعرّفات ثابتة** بدل مُحدِّدات CSS الهشّة. نُفّذ: `browser_snapshot` (كل عنصر
  تفاعلي `[ref] role "name"` عبر سمة data-satr-ref — كفاءة رموز ~مئات مقابل آلاف)،
  و`browser_click`/`browser_type` بـ **ref حتمي** مع تراجع CSS (لا كسر عقد م-4)، +
  `browser_navigate` + `browser_wait_for`. systemPrompt يوجّه حلقة snapshot→act→snapshot
  (الـ ref يقدُم بعد كل فعل). كله في `preview.js` + كتلة أدوات المتصفح في `agent.js`
  (نطاق Claude — لا تلامس ملفات Codex). صفر اعتماديات، بلا preload (يطابق العزل). تحقّق
  حيّ (منطق محفوف): مسبار معزول 8/8 بخادم HTTP فعلي. مصادر: Playwright MCP، browser-use.
- **وضع تحكّم المتصفح (نمط Comet — 2026-07-12، Claude — طلب مالك؛ تكافؤ SDK/Codex
  معتمد)**: زرّ toggle بجوار الإرسال يمنح الوكيل صلاحية قيادة المعاينة بلا مربع إذن لكل
  فعل (تجربة Comet من Perplexity). علم `browserControl` يصل إلى محركَي SDK وCodex؛ كلاهما
  يملك رؤية الويب وأفعال المتصفح على لوحة المعاينة نفسها، بينما المحوّلات لا تملكها.
  **fail-safe**: التفويض يعفي أدوات المتصفح فقط — run_in_terminal والملفات تبقى تطلب إذناً
  (الفصل: قيادة متصفح ≠ تنفيذ أوامر). SDK يبوّب عبر `canUseTool`، وCodex عبر
  `codexmcp.js`/`requestPermission`؛ التفاصيل في CLAUDE.md قسم codexmcp. الوضع معطّل
  افتراضياً وتهديد حقن البرومبت موثّق، لذا يبقى اختياراً صريحاً.

---

## قيود ثابتة عبر كل الدفعات

- **أولوية الوكيل 3 ✅ — منفّذة؛ قبول المالك اليدوي باقٍ (2026-07-12) — تحقق + checkpoint للدور**:
  `.satr/verify.json` صريح بلا تخمين/تشغيل تلقائي، وأداة `verify_project` خلف إذن exec
  تشغّل الأوامر في الطرفية المرئية وتربط pass/fail بدليل Task Ledger. `checkpoints.js`
  يجمع edit IDs ويحفظ metadata/hashes فقط؛ آخر checkpoint الحي يُستعاد بالعكس داخل cwd
  عبر undo القائمة، بلا لمس Git history. بطاقة الدردشة تعرض الملفات والنتائج والمقارنة
  والاستعادة. تحقق: `test:verify` أخضر ومرصد الوكيل 12/12. لم يُعدّل `codex.js` أو المعاينة.

- **أولوية الوكيل 2 ✅ — منفّذة؛ قبول المالك اليدوي باقٍ (2026-07-12) — Task Ledger موحّد ودائم**: عقد
  `task_update` schema v1 + مخزن `electron/tasks.js` تحت `~/.satr/tasks/` بلا prompts؛
  Codex يطبّع `turn/plan/updated`، وClaude يطبّع `TodoWrite/Task*` و`system.task_*`،
  والمحوّلات تستعمل `update_task_ledger`. بطاقة الدردشة تعرض progress والاعتماديات
  والمالك والأدلة، مع إيقاف/استئناف صريحين واستعادة مع الجلسة. تحقق: `test:tasks`
  أخضر ومرصد الوكيل 12/12. لم تُمس المعاينة أو أدوات المتصفح.

- قواعد CLAUDE.md كلها (أمان، عربية أولاً، أقل اعتماديات، تحقق مدخلات في main.js).
- نموذج ARCHITECTURE.md: **لا يُنشأ `enterprise/` قبل نضج نقاط الربط** (الدفعة 3 تفتح ذلك بعد 1–2).
- الواجهة: CSP بلا inline (update-csp تلقائي)، حذار محارف التحكم في index.html.
- المحوّلات: المفاتيح عبر `keys.js`/بيئة النظام فقط، القيم لا تصل الواجهة أبداً.
- **دفعة الوكيل الحي ✅ (2026-07-19)**: تنفيذ مرئي موحّد، مهام PTY معمّرة وتبويبات 🛠،
  استعادة خادم المعاينة، envbrief مشترك، وأذونات متوازية بعدّاد ونطاق الدور.
- **دفعة المتصفح عضو مشترك ✅ (2026-07-19)**: نطاقات موثوقة تسد تسريب تحكم المتصفح،
  أفعال ولقطات مرئية وصادقة، حلقة أخطاء→إصلاح و🎯 محسّن، تنزيل/شهادة localhost، وعدة
  تحقق ذاتي متكافئة بين Claude وCodex.
- **دفعة مساعد إعداد المنصات والتكاملات ✅ (2026-07-19)**: سياسة فعل حساس `neverAlways`
  وحارس تسريب وميزانية مهمة، تعبئة جماعية غير سرية، نقل أسرار بين الحقول بلا رؤية
  النموذج، إدخال مستخدم داخل الحقل، تسليم خطوة قابل للاستئناف، أثر تصفح مرئي، وتفضيل
  `API/CLI` مع Task Ledger متعدد المنصات. تكافؤ SDK/Codex واختبار حي بصفحتين بلا اعتماديات.
- **دفعة تلميع الحلقة اليومية ✅ (2026-07-19)**: تحرير/إعادة إرسال بسيطة مع صور وحدّ
  no-rollback موثّق، إعادة المحاولة بعد الفشل/الإيقاف، شريط وعي دائم، مسودة لكل cwd،
  تثبيت وتسمية جانبيان للجلسات، ملخّص `file_edit`، بحث الخيط، واختصارات Ctrl+Alt.
  بلا أمر `/` أو اعتمادية جديدة؛ مخزن sessionmeta وحده أضاف IPC منقّى ومحدداً.
- **دفعة غرفة العمليات: دخول + شاهدها تعمل + ثقة الدمج ✅ (2026-07-19)**: زر من رسالة المستخدم
  يبذر المهمة بلا تشغيل، وافتراضي الفريق عامل واحد. أضيف `preview` اختياري إلى
  `.satr/verify.json` وتشغيل تكاملي مستقل بعد تحقق البصمة في worktree مؤقت عبر 🛠، مع تفرد وتنظيف
  عند الإيقاف/الدمج/الفريق الجديد/الإغلاق وبلا تلويث `devservers`. النتائج تعرض إجمالي الأثر، وفرق
  ملف واحد عند الطلب فقط بحد 256KiB/600 سطر، وقوائم مخاطر وملاحظات وتوصية كل مراجع. لا patch في
  البث، ولا أمر من renderer، وبوابة الدمج لم تتغير، وصفر اعتماديات جديدة.
- **مؤجّل لدفعات مستقلة**: تبويبات معاينة متعددة؛ مهام مجدولة/مراقبة دورية للصفحات؛
  autofill من ملف شخصي أو بيانات دفع؛ تسجيل/إعادة تشغيل الأتمتة وcodegen للخطوات؛ مقارنة
  لقطات بكسلية وتدقيق a11y كامل (تحتاج اعتماديات)؛ تحكم الوكيل اليدوي بالكوكيز/وضع القراءة؛
  إدخال صوتي عربي (إذن ميكروفون/دقة/اعتمادية)؛ تقويم هجري في الترويسة/التصدير؛ وتعديل
  الرسالة في مكانها مع تفريع جلسة حقيقي بدلاً من راحة «حرّر وأعد الإرسال» الحالية؛ Codex منفذاً
  أو فريقاً مختلطاً؛ قوالب مهام غرفة العمليات؛ مقارنة المخرجات جنباً لجنب؛ بث التفكير الحي؛ وإحياء
  معاينة تكاملية بعد إعادة تشغيل التطبيق.
