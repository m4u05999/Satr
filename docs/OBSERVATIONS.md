# ملاحظات مؤجَّلة — سجل «سجّل ولا تنفّذ»

> **قاعدة مالك (2026-08-13).** أي ملاحظة تظهر أثناء دفعة جارية **تُسجَّل هنا ولا
> تُنفَّذ**، ثم تُسحب في دفعة لاحقة يناسبها وسمها. الغاية مزدوجة: ألّا تضيع الملاحظة،
> وألّا تنتفخ الدفعة الجارية فيضيع حاجزها.
>
> العقد الكامل في `CLAUDE.md` قسم «سجل الملاحظات المؤجَّلة». يحرس **شكل** هذا الملف
> `npm run test:observations` (ضمن `test:full`).

## الحالة

| الرمز | المعنى |
|---|---|
| `مفتوحة` | لم تُعالَج بعد |
| `منجزة` | أُنجزت — **يجب** أن تحمل مرجع التزام |
| `مرفوضة` | قرار صريح بعدم التنفيذ — **يجب** أن تحمل سبباً |

الأوسمة المسموحة: `mobile` · `ui` · `ops-room` · `engines` · `preview` · `generation`
· `security` · `process` · `docs` · `perf`

الأنواع المسموحة: `تراجع أمني` · `عطل` · `تحسين` · `صقل`

**لا تُحذف ملاحظة أبداً** — المرفوضة تبقى بسببها، فتاريخ الرفض يمنع إعادة فتح نقاش
محسوم بعد أشهر.

---

## OBS-001 — الوكيل ينزلق إلى الإنجليزية في صفحة الدردشة
- **الوسم**: `ui`
- **النوع**: عطل
- **الحالة**: منجزة — `1bc5ffd` (2026-08-26، بعد `567aca4` و‏5 دفعات سابقة)
- **الدليل**: بلاغ مالك بلقطة شاشة (2026-08-13) — رُدَّ بجدول إنجليزي كامل داخل واجهة
  عربية. المصدر: `electron/envbrief.js` يطلب العربية إرشاداً بلا أي حارس يكشف المخالفة.
  **وبلاغ ثانٍ بلقطة (2026-08-14)**: المشكلة «بدأت تظهر كثيراً» — الوكيل يكتب
  البرومبتات والأوامر بالإنجليزية، ويفكّر بالإنجليزية، ويردّ أحياناً بالإنجليزية؛
  أي أن الانزلاق توسّع من ردٍّ عابر إلى **كل النواتج**. تكرار البلاغ في يومين
  متتاليين يرفع أولوية العصف الثلاثي الذي قرّره المالك.
- **المصدر**: دفعة قناة الحالة (F3)
- **الملاحظة**: كل عقد حرج آخر في «سطر» له حارس يعضّ (الأذونات، الظرف، القفل، الإيقاف،
  القائمة المغلقة)، أما اللغة فسطرٌ إرشادي — وهذا يناقض مبدأ §7.7.3 حرفياً: «الفشل مغلق
  بالبناء لا بالانتباه». الحدّ الفاصل بين «واجهة» و«بنية أساسية» يحتاج تعريفاً حرفياً
  وإلا تباعد القارئان. **قرار مالك: تُعالَج بعصف ثلاثي مستقل** لأن فيها مقايضة حقيقية
  (إجبار التفكير المعروض على العربية قد يضعف الاستدلال — تُقاس A/B لا تُحسم برأي).

  **خط الأساس المقيس (2026-08-14، مدخل العصف)**: `electron/langmetric.js` (مقياس نقي
  يقيس **النثر** بعد إقصاء ما يوجب العقدُ بقاءَه إنجليزياً — كود/مسارات/مصطلحات؛
  حارسه `test:langmetric` ضمن الطقم) + `scripts/language-probe.js` (حيّ، خارج الطقم،
  ستة أدوار حقيقية على النموذج الافتراضي بموجز `envbrief` الإنتاجي نفسه، ~$11.3).
  النتيجة: **الدور الواحد لا يُسقط العربية** — 92–99% نثراً عربياً في الستة كلها،
  بما فيها سيناريو ضغطٍ مدخلُه إنجليزية كثيفة (92%) وصفر بنية إنجليزية.
  **فالفرضية المرجّحة أمام العصف: الانزلاق انجرافُ سياقٍ في الجلسات الطويلة لا ضعف
  تعليمة** — لقطة 2026-08-14 نفسها من جلسة مخبّؤها ~1.77 مليون رمز. وهذا يوجّه
  العلاج نحو إعادة حقن التعليمة دورياً/بعد الضغط، أو كاشف انزلاق حيّ فوق
  `langmetric` — لا نحو تشديد الصياغة. **الفجوة القياسية الباقية**: إعادة إنتاج
  الانجراف متعدد الأدوار لم تُقس بعد (كلفتها أدوار كثيرة)؛ يقرر العصف طريقتها.

  **العصف الثلاثي جرى (2026-08-15)** — ثلاثة آراء مستقلة + نقد خصومي متبادل،
  الخام والحزمة في `D:\sater\prompts-lang-bs\DECISION-PACKAGE.md`. أبرز الثمرة:
  (أ) **فرضية الانجراف سقطت بالبيانات** — قياس رجعي لـ132 جلسة محفوظة بصفر كلفة:
  لا انحدار زمني بل تحسّن، والتوزيع ثنائي القمة، والجلسات المنهارة مقفولة على
  الإنجليزية **من الرسالة الأولى** (قفل انحداري ذاتي مرجَّح، وتبقى السببية لتجربة
  فصل ≤$35). (ب) **ثلاث فجوات تغطية مثبتة**: `openai-responses.js` بلا `envbrief`
  إطلاقاً · `claude-cli.js` كذلك · غلاف `memory.js` إنجليزي يُحقن كل دور.
  (ج) خطة مرحلية بإجماع: سدّ ⇒ قياس رجعي ⇒ تجربة سببية ⇒ مرساة ذيلية ⇒ حارس
  بوضع ظلّ ⇒ A/B للتفكير بمقياس موضوعي. **اعتمد المالك الحزمة والحدّ الحرفي
  (2026-08-15) وأمر بتنفيذ الدفعات 1–3.**

  **الدفعات 1–3 نُفِّذت (2026-08-15)** — الحدّ الحرفي في `docs/LANGUAGE-CONTRACT.md`:
  (1) سُدّت الفجوات الثلاث بعضّات مثبتة، و`langmetric` مجمَّد v2 مع `isSlip`.
  (2) القياس الرجعي القابل للإعادة: 36 جلسة ⇒ صفر «مختلطة» (ثنائية القمة مؤكدة)،
  والمنهارة الثماني كلها مقفولة من الرسالة الأولى.
  (3) تجربة الفصل (~$18.2) **نفت الآليتين معاً**: التلويث الخارجي (~120 ألف رمز)
  صمدت العربية أمامه 92%+، وبذرة القفل تعافى منها فوراً (‏0%→98.5%). **والهوية
  حسمت التشخيص**: المنهارة الثماني كلها جلسات عمل وكيلية (قيادة/منفّذون — إحداها
  جلسة القائد نفسها) نمطها ردود نهائية عربية و**سرد عمل بين الأدوات إنجليزي** من
  أول سطر. التشخيص المجمَّد: **فجوة نطاق في التعليمة** — «الشرح والردود» تُفهَم
  على الإجابات ويُعامَل سرد العمل كأنه داخلي. جلسات السؤال-والجواب لا تصاب أصلاً.
  **الدفعتان 4 و5 نُفِّذتا (2026-08-15، بأمر مالك)**: `langanchor.js` (عقد موسَّع
  يسمّي سرد العمل + مرساة ذيلية بقوية الدور الأول وما بعد الضغط، في SDK وCodex،
  والمعزولة خارجها) و`langshadow.js` (ظلّ عند `emitToWindow` — أرقام بلا نص).
  **بوابة القبول الحيّة**: مسبار وكيلي بذراعين أعاد إنتاج الداء (سرد 11.3% عربياً،
  تسلسل `100 100 0 0 0 0 100`) وأثبت الدواء (‏100% بالمرساة). حارسهما
  `test:langshadow` (‏21 فحصاً، في الطقم) بعضّتين مزروعتين مستعادتين.
  **المتبقي بقرار مالك**: الخروج من الظلّ بعد أسبوع بيانات (زر «أعد الصياغة
  بالعربية») والدفعة 6 (‏A/B للتفكير المعروض وبرومبتات المنفّذين).

  **الدفعة 6 — الخروج من الظلّ (‏`1bc5ffd`، 2026-08-26)**: عرضٌ بعد معايرة على
  **921 قياساً** لا تقديراً. انقسمت العشرون انزلاقة إلى مجتمعين متمايزين: تسعٌ
  `share` بحصص 0%–35% (سبعٌ منها 0%)، وإحدى عشرة `structure` بحصص **90%–96%**.
  فخرج ما لا لبس فيه وحده — أثرُه تاريخياً **~1% من الرسائل**. والممنوعات
  المجمَّدة سارية بحرفها: النقر هو الفعل، ولا ترجمة آلية (نصّ الطلب يوجب إبقاء
  الكود والمسارات والمصطلحات بالإنجليزية فيحفظ عقد «سطر» لا ينقضه).
  وأُصلح معه حقل `override` الذي كان **موثَّقاً بلا مستدعٍ** منذ 2026-08-17: في
  الظلّ يلوّث المعايرة صامتاً، وبعد العرض كان سيُظهر الزرّ على ردٍّ طلبه المستخدم
  بلغة أخرى بنفسه. قرار العرض دالة نقية (`langshadow.visibleSlip`) مختبَرة
  سلوكياً، بعضّتين مثبتتين.
- **ما لا تغطيه هذه الحالة رغم إغلاقها** (‏لئلا يُقرأ الإغلاق أوسع مما هو):
  1. **الدفعة 6 الأصلية** (‏A/B للتفكير المعروض وبرومبتات المنفّذين) لم تُنفَّذ —
     وهي «قرار مالك مستقل» في `docs/LANGUAGE-CONTRACT.md` لا شرطٌ لإغلاق العطل.
  2. **مجتمع `structure`** (‏11 انزلاقة) لم يُتحقَّق منه — انظر [OBS-057].
  3. الانزلاق نفسه لم يعد يُرصد بلاغاً منذ المرساة (‏2026-08-15): `codex·commentary`
     صفر انزلاق في 180 عيّنة. الإغلاق على هذا الأساس المقيس لا على انطباع.

## OBS-002 — برومبت المنفّذين لا يمنع «العضّة الاستدلالية»
- **الوسم**: `process`
- **النوع**: تحسين
- **الحالة**: منجزة — عرف العضّة مثبّت في `AGENTS.md` (الإخراج الحرفي/العدّ من الطرفين بـ`node`)
- **الدليل**: تقرير منفّذ الهاتف في دفعة F3 — أعاد عضّتيه بصيغة «لو أضفنا… لعاد بكائن
  غير صالح»، أي استدلالاً لا زرعاً. بينما أعاد منفّذ سطح المكتب عضّاته بأرقام زرع
  واستعادة فعلية. زرع القائد العضّتين بنفسه لاحقاً (‏`548fe23`).
- **المصدر**: دفعة قناة الحالة (F3)
- **الملاحظة**: نصّ العرف الحالي يقول «ازرع ⇒ تحقّق ⇒ أثبت السقوط ⇒ أعِد»، لكنه لا يطلب
  **إخراجاً حرفياً** للأمر ونتيجته. اقتراح: إلزام المنفّذ بلصق أمر الزرع وعدّ المطابقات
  ونصّ رسالة الفشل — فالاستدلال يصير مكشوفاً بالبناء لا بالمراجعة.
- **المنجز** (`922e6df`): أُضيف قسم «عرف العضّة» إلى `AGENTS.md`؛ يُلزم بالإخراج الحرفي لكل عضّة
  (أمر الزرع + عدّ الطرفين + نصّ رسالة الفشل + أمر الاستعادة) ويصرّح أن
  «استدلال بلا إخراج = عضّة غير مثبتة تُعاد».

## OBS-003 — تعليقان يتيمان في `mobilelink.js` بعد استخراج `mobilepending`
- **الوسم**: `mobile`
- **النوع**: صقل
- **الحالة**: منجزة (`9509a6c`)
- **الدليل**: `electron/mobilelink.js:415` تعليق «يحسم معلّقاً مرة واحدة ويسحبه» فوق
  `removeWaiter`، و`electron/mobilelink.js:506` تعليق «سطح المكتب حسم أولاً ⇒ يُزال
  المعلّق فوراً» فوق `status()`. كلاهما يصف دوالَّ انتقلت إلى `electron/mobilepending.js`.
- **المصدر**: مراجعة دفعة قناة الحالة (F3)
- **الملاحظة**: التعليق الثاني **مضلِّل لا زائد فقط** — يصف `withdraw` بينما يعلو
  `status()`، فقارئٌ لاحق قد يبني عليه فهماً خاطئاً لحارس الموافقة القديمة.
- **المنجز** (`9509a6c`): كانت **ثلاثة** لا اثنين — كشفت القراءة يتيماً ثالثاً:
  JSDoc «يعرض طلب إذن على الأجهزة المقترنة» بـ`@returns Promise<'allow'|…>` وهو
  يصف `offer` (‏`mobilepending.js:76`). الثلاثة كانت متراكمة فوق `status()` ولا
  واحد منها يصفها. قبل الحذف تُحقّق أن النصوص الثلاثة موجودة **حرفياً** فوق
  دوالها في `mobilepending.js` (‏`settle` و`offer` و`withdraw`) فلم يضع توثيق.
  وحُذف معها عنوان قسم «── المعلّقات ──» الذي بقي بلا محتوى (يعقبه عنوان قسم
  آخر مباشرة)، وأخذ `status()` تعليقاً يصفه هو. `test:mobile` ‏`9/9` بعدها.

## OBS-004 — مراقب المنفّذين يعدّ أغلفة pty فيعطي أرقاماً مضلِّلة
- **الوسم**: `process`
- **النوع**: صقل
- **الحالة**: منجزة — بقرار مالك ضمّها إلى دفعة «صقل متصفح سطر» (2026-08-15).
  أُضيف الدرس الثالث إلى رأس `D:\sater\executor.ps1` (الملف خارج المستودع فلا
  hash له؛ مرجع الدفعة `42d26a1`): العدّ بالفرز الفريد على قيمة `-Engine` لأن
  غلاف pty وعمليته يحملان المحرك نفسه فيُحسبان واحداً.
- **الدليل**: أثناء دفعة F3 أبلغ المراقب «executors still running: 3» لمنفّذين اثنين،
  لأن غلاف pty الذي يشغّل `executor.ps1` يطابق المرشِّح نفسه.
- **المصدر**: دفعة قناة الحالة (F3)
- **الملاحظة**: عولج جزئياً بالفرز الفريد على `-Engine <name>` (الغلاف والعملية يحملان
  المحرك نفسه). يبقى التوثيق ناقصاً في `executor.ps1` كي لا يعيد أحدٌ اكتشاف السبب.

## OBS-005 — عدّ مطابقات العضّة قد يكذب حين يحوي النمط شرطة مائلة خلفية
- **الوسم**: `process`
- **النوع**: عطل
- **الحالة**: منجزة — عرف العضّة مثبّت في `AGENTS.md` (الإخراج الحرفي/العدّ من الطرفين بـ`node`)
- **الدليل**: أثناء بناء حارس الملاحظات نفسه (2026-08-13): أعطى
  `echo "قبل: $(grep -c '^- \*\*الوسم\*\*: ` + "`ui`" + `$' file)"` القيمةَ **صفراً** بينما
  `sed` بالنمط نفسه طابق واستبدل فعلاً. السبب أن الشرطة المائلة الخلفية داخل `$( )`
  لا تبقى حرفية كما يُتوقَّع رغم الاقتباس المفرد.
- **المصدر**: بناء قاعدة سجل الملاحظات
- **الملاحظة**: خطر مباشر على **العرف الملزم** نفسه: العضّة تُقاس بعدّ المطابقات قبل/بعد،
  فأداة قياس تكذب تجعل «الزرع طُبِّق» ادّعاءً لا برهاناً — وهو تكرارٌ لخطأ سابق مثبَّت
  (عضّة لم تطابق الإزاحة فبدت ناجحة كذباً). العلاج المقترح: عدّ المطابقات في `python`
  أو `node` لا في `bash` حين يحوي النمط محارف هروب، والتحقق دائماً من **الطرفين**
  (اختفاء الأصل **و** ظهور المتحوّر) لا من طرف واحد.
- **المنجز** (`922e6df`): أُضيف قسم «عرف العضّة» إلى `AGENTS.md`؛ يلزم بعدّ المطابقات من الطرفين
  ويُحذّر من `grep -c` داخل `$( )` عند محارف الهروب، ويقدّم مثالاً بـ`python`.

## OBS-006 — سقوف صناديق الوسيط تفوق ذاكرة آلته بأربعين ضعفاً
- **الوسم**: `mobile`
- **النوع**: عطل
- **الحالة**: منجزة — `4f0492f` في `satr-enterprise`، بقرار مالك ضمّها إلى دفعة F1.
  `MAX_BOXES` ‏10000 ⇒ **96** (كل صندوق ممتلئ = 1MiB بالضبط، فالرقم هو سقف
  الميغابايت نفسه). وأُضيف فحص يربط الثوابت الثلاثة بالذاكرة المعلَنة في `fly.toml`
  فيحمرّ عند رفع أيٍّ منها وحده. اختبار الوسيط 97 ⇒ **103**.
  **تصحيح للتقدير أدناه**: «العلاج سطر واحد» صحيح، لكن البديل الأنظف ظاهرياً
  (ميزانية بايتات عامة) **رُفض عند التنفيذ**: يحتاج عدّاداً يُنقَص في أربعة مسارات
  إزالة (‏`sweep`/`prune`/`take`/التسليم المباشر)، وانحرافه يرفض حركةً مشروعة **إلى
  الأبد** — أي يستبدل عطلاً نادراً بعطل دائم.
- **الدليل**: `satr-enterprise/relay/mailbox.js` — `MAX_BOXES=10000` ×
  `MAX_FRAMES_PER_BOX=16` × `MAX_FRAME_BYTES=64KiB` = **9.77 GB** نظرياً، بينما
  `relay/deploy/fly.toml` يعلن `memory = "256mb"`.
- **المصدر**: مراجعة تكلفة الاستضافة قبل التجربة الحية (2026-08-13)
- **الملاحظة**: ليس خطراً **مالياً** (تكلفة الآلة ثابتة لا تتبع الذاكرة) بل **انقطاع
  خدمة** بـOOM. بلوغه يتطلب ~160 ألف طلب، أي نحو 44 ساعة من IP واحد تحت تحديد المعدّل
  القائم (60 طلباً/دقيقة)، أو أسرع موزَّعاً. العلاج سطر واحد: خفض `MAX_BOXES` إلى ما
  تسعه 256MB فعلاً مع هامش. لا يُنفَّذ الآن — الميزة قيد تجربة حية والسقف الحالي لا
  يكسر شيئاً في الاستعمال العادي.

## OBS-007 — نشر الوسيط لا يتحقق من تطابق نسخة PWA مع المستودع
- **الوسم**: `mobile`
- **النوع**: عطل
- **الحالة**: منجزة — `4f0492f` في `satr-enterprise`، بقرار مالك ضمّها إلى دفعة F1.
  نُفِّذ العلاج المقترح أدناه كما هو: `build.js` يقرأ `CACHE_NAME` ويطبعه (وغيابه خطأ
  قاتل)، والوسيط يعرض `/version`. **وتشديد واحد فوقه**: النسخة تُقرأ من `sw.js`
  **المخدوم فعلاً** لا من ثابت يُحقَن وقت البناء — فلا يستطيع الوسيط بنيوياً أن يعلن
  نسخةً لا يخدمها. ويطبع البناء أمرَي ما بعد النشر: `curl …/version` و
  `fly scale count 1 --yes`.
- **الدليل**: قبل تجربة F3 كان الوسيط يخدم `CACHE_NAME = 'satr-pwa-v6'` بينما المستودع
  عند `satr-pwa-v10` — أي أن الهاتف كان سيحمّل قشرة سابقة لثلاث دفعات بلا أي إشارة.
  المصدر الوحيد لملفات PWA هو `relay/deploy/build.js --pwa`، ولا شيء يربط النشر بإصدارها.
- **المصدر**: التجربة الحية لدفعة قناة الحالة (F3)
- **الملاحظة**: هذا **نمط «أُصلح ويبدو قائماً»** حرفياً — العطل الذي فرض «الشبكة أولاً»
  في `pwa/sw.js` أصلاً. الفارق أنه هنا على مستوى **النشر** لا الكاش: لا كاش الهاتف يخفي
  الجديد، بل الخادم نفسه لا يملكه. العلاج المقترح: يقرأ `build.js` قيمة `CACHE_NAME` من
  `pwa/sw.js` ويطبعها في خرجه، ويضيف الوسيط نقطة `/version` تعيدها، فيصير التحقق أمراً
  واحداً بعد كل نشر بدل الاعتماد على التذكّر.

## OBS-008 — رمز SAS لا يُعرض على سطح المكتب: الواجهة تَعِد بحارس غير مُنفَّذ
- **الوسم**: `mobile`
- **النوع**: تراجع أمني
- **الحالة**: منجزة — `2ff86f3`، بقرار مالك ضمّها إلى دفعة F1. اختير **بثّ SAS** لا
  إخفاء الخانة: `status()` في النقلين صار يحتفظ بـSAS المحسوب عند الاقتران ويعيده،
  فيلتقطه `main.js:972` بحارس `/^\d{6}$/` القائم وتعرضه `mobile-panel.js:184`.
  **ولم يتغيّر شيء في `main.js` ولا في الواجهة** — كانا يعملان أصلاً؛ الطرفان
  يحسبان والوسط مقطوع، فالعلاج وصلٌ لا بناء. وبذلك تفي اللوحة بوعد §2 («رمز تحقق
  قصير على الشاشتين») بدل أن تمنح ثقة كاذبة بخانة فارغة.
- **الدليل**: بلاغ مالك بلقطة أثناء التجربة الحية (2026-08-13): خانة «رمز التحقق المتوقع
  (SAS)» تعرض `——`. السبب من الكود: `src/ui/components/mobile-panel.js:184` يقرأ
  `status.sas`، و`electron/main.js:972` يقرأه من `handle.status()` — بينما
  `mobilelink.status()` و`mobilerelay.status()` **لا يعيدان الحقل إطلاقاً**. وشكل ردّ
  الاقتران مثبَّت بلا `sas` في `scripts/mobile-integration-test.js`
  (`'expiresAt,fingerprint,ok,url'`).
- **المصدر**: التجربة الحية لدفعة قناة الحالة (F3)
- **الملاحظة**: السبب الجذري بنيوي لا سهو: SAS يُشتق من `mobilePublic` الذي لا يُعرف إلا
  **بعد** أن يرسله الهاتف، فوقت عرض QR لا يمكن حسابه. الطرفان يحسبانه فعلاً
  (`sendPairedAck` عبر الوسيط، وردّ `/pair` محلياً) لكنه يصل **الهاتف وحده**، ولا مسار
  يبثّه إلى الواجهة بعد الاقتران.
  العقد في §2 من `MOBILE-CONTROL-PLAN.md` ينصّ على «رمز تحقق قصير SAS على الشاشتين»،
  فالمقارنة — وهي كل فائدته — **مستحيلة اليوم**. والأسوأ من غيابه أن اللوحة تعرض خانته
  فتمنح ثقة كاذبة. العلاج: إمّا بثّ حدث اقتران منقّى يحمل SAS إلى الواجهة، وإمّا إخفاء
  الخانة حتى يُنفَّذ. لا يُترك عنصرٌ يَعِد بحارس غير موجود.

## OBS-009 — لا حارس DOM حيّ لتطبيق الهاتف: البيانات تُختبر والبكسل لا
- **الوسم**: `mobile`
- **النوع**: تحسين
- **الحالة**: منجزة — `aec792d` (`scripts/pwa-dom-live-test.js`، **45 فحصاً**)، بقرار
  مالك ضمّها إلى دفعة F1. نُفِّذ العلاج المقترح أدناه: Chromium حقيقي يحمّل صفحة
  الهاتف — تُخدَم على `127.0.0.1` فتعمل بسياق آمن كالإنتاج — ويقيس
  `getComputedStyle(...).display` فعلياً.
  **وتشديد فوق المقترح**: لا يحمل الحارس قائمة لوحات مكتوبة يدوياً (كانت ستتقادم كما
  تقادم كل شيء آخر). يشتقّ اصطلاح `.X.active` من `styles.css` نفسه، ويفرض أن **لا
  عنصر يخلط الاصطلاحين** — وهو صياغة العطل بدقة: `!important` يغلب دائماً فيصير
  الإظهار مستحيلاً بالبناء — ثم يرفض أي عنصر مخفيّ خارج الاصطلاحين، فتدخل اللوحة
  الجديدة الحراسةَ تلقائياً.
  العضّة: إعادة `hidden` إلى `#statePanel` ⇒ **خمسة إخفاقات مستقلة**، منها
  «توقّعنا flex ووجدنا none». **يبقى الحدّ معلناً**: الحارس يقيس CSS والبنية، ولا
  يثبت أن `app.js` يضيف الصنف في اللحظة الصحيحة — ذاك يحرسه `sourceFunction` في
  حارس التكامل.
- **الدليل**: عطل لوحة الحالة (2026-08-13): كانت `#statePanel` تحمل `hidden` في
  `pwa/index.html` بينما `pwa/styles.css:388` يعرّف `.hidden{display:none !important}`
  فيغلب `.state-panel.active{display:flex}`. اللقطة كانت تصل وتُقبل وتُحلَّل بنجاح
  **ولا يظهر شيء على الهاتف**. أمسكه المالك بلقطة شاشة، لا اختبار.
- **المصدر**: التجربة الحية لدفعة قناة الحالة (F3)
- **الملاحظة**: أُصلح فوراً (استثناء «عطل يكسر الدفعة الجارية») بحذف `hidden` من
  الترميز — `.state-panel{display:none}` تكفي للإخفاء الافتراضي — وأُضيف
  `assertStatePanelVisible` ساكناً في حارس التكامل بعضّة مثبتة.
  **لكن الدرس أوسع من إصلاحه**: كل فحوص قناة الحالة — وحارس القائد منها — كانت تثبت
  أن اللقطة **تصل وتُقبل**، ولا واحد يثبت أنها **تُعرض**. حتى `sourceFunction` الذي
  بُني لدرس §5.5.5 يشغّل منطق الطرف لا عرضه. والفحص الساكن المضاف يمسك هذه الحالة
  بعينها لا كل حالات العرض.
  العلاج الحقيقي: حارس DOM حيّ لـ`pwa/**` بنمط `test:chat-rtl` و`test:task-ledger-ui`
  (‏Chromium حقيقي تحت CSP، يبثّ لقطة ويقيس `getComputedStyle(...).display` فعلياً).
  لا يوجد اليوم أي اختبار يحمّل صفحة الهاتف في متصفح.

## OBS-010 — تحديد معدّل الوسيط بالـIP يخنق كل المستخدمين معاً خلف بروكسي Fly
- **الوسم**: `mobile`
- **النوع**: عطل
- **الحالة**: منجزة — `653a0dd` في `satr-enterprise` (‏relay/server.js + relay/test.js)،
  بقرار مالك «أصلح قبل الإطلاق». اختبار الوسيط 88 ⇒ **97** بعضّتين مزروعتين ومستعادتين.
- **الدليل**: أثناء التجربة الحية (2026-08-13) ردّ الوسيط **HTTP 429** على أمر إيقاف
  حقيقي. المفتاح في `satr-enterprise/relay/server.js:131` هو
  `req.socket.remoteAddress` والحدّ `RATE_MAX_REQUESTS = 60` لكل دقيقة.
- **المصدر**: التجربة الحية لدفعة قناة الحالة (F3)
- **الملاحظة**: عاملان متراكبان.
  **(أ) استهلاك يتضاعف بعدد الأجهزة**: سطح المكتب يفتح حلقة استقصاء لكل جهاز مقترن
  ويدفع النبضة لكل جهاز. مع خمسة أجهزة: ‏10 طلبات/دقيقة استقصاءً + 15 نبضةً +
  ‏3–6 من الهاتف ≈ **31 في الخمول**، ويتجاوز الستين أثناء دور نشط لأن اللقطة تُبثّ عند
  كل `file_edit` و`task_update` وتغيّر حالة.
  **(ب) المفتاح خاطئ على Fly**: `remoteAddress` هو عنوان بروكسي Fly لا المستخدم، فكل
  العملاء يتشاركون مفتاحاً واحداً ويُطبَّق الحدّ على **المجموع الكلي**. اختيار
  `remoteAddress` كان مقصوداً وسليم النية (‏`server.js:130` يشرح أن `x-forwarded-for`
  قابل للانتحال) لكن أثره على Fly عكس المراد — بل يناقض سبب وجود الوسيط في §2:
  بُني ليحلّ **CGNAT**، وتحت CGNAT يتشارك مستخدمو المشغّل عنواناً فيخنق بعضهم بعضاً.
  **ما نُفِّذ فعلاً — وتصحيح للعلاج المقترح أعلاه**: كان المقترح «التحدید بمعرّف
  الصندوق **لا** بالـIP»، وهو **خطأ رُفض عند التنفيذ**: الاكتفاء بالصندوق مفتاحاً يفتح
  DoS كاملاً — مهاجم يرسل صندوقاً عشوائياً جديداً مع كل طلب فيحصل على مفتاح جديد ولا
  يُحدّ أبداً. الصواب **تركيب لا استبدال**:
  1. `boxLimiter` جديد بميزانية **120/دقيقة لكل صندوق** — الحدّ ذو المعنى لكل قناة،
     يعمل تحت CGNAT وبروكسي Fly، ومعرّفه 128 بت معتم لا يُنتحل بخلاف الترويسة.
  2. حدّ الـIP **يبقى فوقه** شبكةَ أمان ضد المجهول، ويُفحص الحدّان معاً fail-closed.
  3. معايرة من قياس حيّ: `RATE_MAX_REQUESTS` ‏60 ⇒ **600** و`STATIC_RATE_MAX` ‏120 ⇒
     **600**، لأن مستخدماً واحداً بخمسة أجهزة يستهلك ‏31/دقيقة مشروعةً — فحدّ 60 كان
     يقطع **أمر إيقاف حقيقي** لا هجوماً.

  العضّتان: حذف ميزانية القناة ⇒ يسقط «تجاوز ميزانية القناة يردّ 429» · وجعل الصندوق
  بديلاً عن الـIP ⇒ يسقط «صناديق عشوائية متتالية يوقفها حدّ الـIP». الثانية تحرس
  تحديداً ضد إعادة تنفيذ العلاج الخاطئ الذي كُتب في هذه الملاحظة أولاً.

  **يبقى مفتوحاً**: مراجعة تواتر النبضة حين تكثر الأجهزة (سطح المكتب يدفع نبضة لكل
  جهاز مقترن)، وإبطال الأجهزة غير المستعملة يقلّل الحمل خطياً.

## OBS-011 — اختبارات الجوال التسعة كلها خارج `test:full`
- **الوسم**: `process`
- **النوع**: تحسين
- **الحالة**: منجزة (`0a35c65`)
- **الدليل**: `scripts/full-suite.js` يعدّ 69 اختباراً وليس فيه أيٌّ من
  `test:mobile-crypto` · `mobile-envelope` · `mobile-pair` · `mobile-link` ·
  `mobile-tls` · `mobile-integration` · `mobile-relay` · `webpush-probe` ·
  `pwa-dom` (المُضاف في `aec792d`). تُشغَّل يدوياً بوصفها «الطقم الثمانية» وقد صارت
  تسعة.
- **المصدر**: دفعة Web Push (‏F1) — انتبه إليه القائد عند تسجيل الحارس الجديد
- **الملاحظة**: مبرَّر اليوم: الميزة خلف بوابة `mobileFeatureAvailable` قبل الإصدار،
  وبعضها بطيء ويحتاج Electron. **لكن المبرّر يسقط عند إطلاق «برو»**، ومجموعةٌ تُشغَّل
  «بالتذكّر» هي بالضبط ما أنتج OBS-007 (الوسيط يخدم `v6` والمستودع عند `v10`).
  والخطر ملموس: `test:mobile-integration` هو حارس الوصل الوحيد للميزة، فسقوطه في
  دفعة لا تمسّ الجوال لن يظهر إطلاقاً. العلاج المقترح: مجموعة `test:mobile` واحدة
  تجمع التسعة، تُستدعى من `full-suite` أو من CI عند لمس `pwa/**` أو `electron/mobile*`.
  **لا يُنفَّذ الآن** — يغيّر زمن الطقم الكامل ونطاقه، وهو قرار مالك مستقل.
- **المنجز** (`0a35c65`، بقرار المالك 2026-08-18): `scripts/mobile-suite.js`
  بنمط `opsroom-suite` (‏`execFileSync` بلا shell، يقف عند أول فشل) يجمع التسعة
  خلف `npm run test:mobile`، ومُضاف إلى `full-suite` بعد `test:opsroom-all`.
  الترتيب داخله: القطعية السريعة أولاً ثم ما يحتاج Electron/شبكة، فيسقط الأرخص
  أولاً. **مبرّر التأجيل سقط بالقياس لا بالرأي**: الطقم كامل **`9/9` في
  `15.1` ثانية** — كلفة لا تُذكر مقابل حارس وصل الميزة الوحيد
  (‏`test:mobile-integration`) الذي كان سقوطه في دفعة لا تمسّ الجوال لن يظهر.

## OBS-012 — العصف متعدد المحرّكات غير قابل للاكتشاف من داخل جلسة «سطر»
- **الوسم**: `engines`
- **النوع**: تحسين
- **الحالة**: منجزة — البنود الثلاثة بقرار مالك (2026-08-24) على دفعتين:
  `f6ca36f` للبند (ج) و`0fa072b` للبندين (أ) و(ب).
  **(أ) منجز**: `MULTI_OPINION_LINE` في `envbrief` يعرض الطرق الثلاث المتاحة فعلاً
  (‏`satr-diverge` · عصف غرفة العمليات · تشغيل محرّك آخر من الطرفية بأعلامه في
  `docs/AGENT-CLI-FLAGS.md`) بدل الاكتفاء بوصف العجز — وهو **جوهر العطل**: جواب
  الوكيل كان صحيحاً معمارياً لكنه اختار الوصف بدل الحلّ. السطر لكل المحرّكات
  والمحوّلات (المهارة محمولة وغرفة العمليات سطح تطبيق لا قدرة محرك)، ومعه فقرة
  «آراء متعددة على قرار واحد» في `satr-guide/features.md` بلغة المستخدم النهائي.
  **(ب) منجز**: `opsbrainstorm.js` فصل `REQUIRED_ENGINES` (‏SDK وCodex) عن
  `OPTIONAL_ENGINES` (‏`kimi-code`). Kimi ينضم رأياً ثالثاً حين يكون جاهزاً ويُتخطّى
  **بصمت** إن لم يكن — الاختيارية مقصودة لأن إلزامه كان سيُسقط الميزة عن كل من لم
  يثبّته: سلبُ قدرة قائمة ثمناً لقدرة جديدة. بوابة الجاهزية قرص فقط
  (`resolveKimiBin` + `authStatus`) بلا إطلاق عملية، بخلاف فحص Codex الذي يكلّف
  ~1.4ث (`OBS-043`). وحقن `keepAlive:false` يمنع رأي العصف من حجز أحد مقعدَي K2
  أو طرد جلسة المستخدم الخاملة.
  **درس تصميمي من التنفيذ**: أول محاولة اشتقّت عمر القناة من `browserControl===false`
  فأسقطت `test:kimi` — تلك علامة **كشف الأدوات** لا **عمر العملية**، وتحميلها معنى
  ثالثاً يخلط محورين مستقلين. العلاج علم صريح أحادي الغرض، ويحرسه `test:envbrief`
  بفحص يرفض عودة الاشتقاق.
  **(ج) منجز** ضمن جولة إغلاق قرار العصف: `SUBAGENT_NAMING_LINE` في `electron/envbrief.js` يحظر تسمية وكيل
  فرعي باسم محرّك آخر ونسبة رأي «نموذج مختلف» إليه، ويعطي البديل الحقيقي في السطر
  نفسه (‏`satr-diverge` أو عصف غرفة العمليات أو تشغيل المحرّك الآخر مع التصريح بذلك) —
  فالحظر وحده يترك الطلب المشروع بلا مخرج. يحرسه `test:envbrief` بثلاثة فحوص، ورابعٍ
  يثبت ألّا يتسرّب السطر إلى موجز المحوّلات التي لا تملك وكلاء فرعيين.
  **حدّ مُصرَّح به**: محوّل `claude-cli` الاحتياطي يُطبَّع `adapter` فلا يصله السطر رغم
  امتلاكه `Task` — مسار نادر، ولم يُوسَّع التطبيع لأجله كي لا يتغيّر تصنيف يستهلكه جرد
  الأدوات. **البندان (أ) و(ب) يبقيان مفتوحين**: توجيه `satr-guide` إلى اقتراح
  `/satr-diverge`، وتوسيع عصف غرفة العمليات إلى ثلاثة محرّكات. ولا يغيّرهما رفض
  `OBS-050` — فتلك ميزة سطح جديد، وهذه فجوة اكتشاف في القائم.
- **الدليل**: لقطة مالك (2026-08-14) — طلب من وكيل جلسة «عصفاً ثلاثياً كما يفعله
  القائد»، فأجاب أن كيمي وكودكس «محرّكان يُقادان من غرفة العمليات، لا أدوات تُستدعى
  من داخل جلسة». بينما القائد يشغّل الثلاثة هيدلس عبر `D:\sater\executor.ps1`
  بأداة الطرفية نفسها المتاحة للوكيل. و`electron/opsbrainstorm.js` **رأيان** فقط
  (‏SDK + Codex) لا ثلاثة، ومهارة `satr-diverge` المضمّنة — المبنية بالضبط لهذا —
  لم تُقترح على المستخدم.
- **المصدر**: سؤال مالك أثناء دفعة «العربية»
- **الملاحظة**: الفجوة **منتجية/معرفية لا قدرة نموذج**: جواب الوكيل صحيح معمارياً
  (المحرّكات ليست أدوات جلسة) لكنه اختار الوصف بدل الحلّ. الطرق الثلاث الممكنة
  اليوم: `/satr-diverge` · عصف غرفة العمليات (محرّكان) · تشغيل CLI هيدلس يدوياً
  (ودروسه الأربعة تعيش في ذاكرة القائد لا في المنتج). العلاج المقترح — قرار مالك:
  (أ) توجيه `envbrief`/`satr-guide` إلى اقتراح `/satr-diverge` عند طلب عصف متعدد
  الآراء، و/أو (ب) توسيع عصف غرفة العمليات إلى ثلاثة محرّكات بإدخال Kimi رأياً
  ثالثاً في `opsbrainstorm.js`. لا يُنفَّذ الآن — دفعة «العربية» جارية.
  **تصعيد (2026-08-17، بلاغ مالك بلقطة ثانية — جلسة `D:\alulama`)**: السلوك
  تطوّر من «وصف العجز» إلى **تمثيل العصف**: وكيل SDK أطلق ثلاثة وكلاء فرعيين
  (‏Claude نفسه بسياقات معزولة) **وسمّاهم «كودكس/كيمي/أوبس»** بشخصيات
  («بعقلية المهندس التنفيذي…») موهِماً المستخدم أنها النماذج الحقيقية — تنويع
  أسلوبي يُباع تنويعَ نماذج، والمستخدم اكتشفه بحدسه لا من الواجهة. يرفع أولوية
  العلاج، ويضيف مطلباً ثالثاً: (ج) توجيه صريح في `envbrief` بأن تسمية وكيل
  فرعي باسم محرّك آخر **محظورة** (صدق التسمية)، مع إرشاد وصفة CLI الهيدلس
  (‏`executor.ps1` أو نظيرها) لمن يريد النماذج الحقيقية من داخل جلسة.

## OBS-013 — تنازع التحكم في متصفح «سطر» بين المستخدم والوكيل
- **الوسم**: `preview`
- **النوع**: عطل
- **الحالة**: منجزة — دفعة «صقل متصفح سطر» (2026-08-15): مسبار استنساخ
  (‏`42d26a1` — S1/S2 صامتان، S2 نفّذ «حذف» بعد لقطة رأت «حفظ») ⇒ عصف ثلاثي
  بنقد متبادل ⇒ مسبار حاجز (`8caffc8`) ⇒ **عقد اللقطة** في `electron/preview.js`:
  كشف تدخّل المستخدم بـ`input-event` في العملية الرئيسية (حجب شامل v1 بقرار
  مالك، `input_changed`)، بصمة هدف `role+name+tag` تُقارن داخل نداء الفعل في
  **isolated world** (‏`target_changed` بالاسمين / `ref_removed`)، إبطال refs
  عند `endHandoff`/`startPick` (عطل ثالث اكتشفه العصف)، وشارة تنازع غير حاجبة
  في اللوحة. حارسه `test:preview-lease` (‏11 عضّة مستعادة) داخل `test:full`.
  الخام كله في `D:\sater\prompts-browser-bs\`.
- **الدليل**: بلاغ مالك (2026-08-15): أثناء قيادة الوكيل للمعاينة يستطيع المستخدم
  التفاعل معها **في اللحظة نفسها**، فيلتقط الوكيل لقطة لحالةٍ غيّرها المستخدم للتو
  ويبني خطوته التالية عليها — فيكمل «على شيء آخر». البنية تسمح بالسباق:
  `WebContentsView` واحد تصله أفعال الوكيل عبر أدوات MCP وأفعال المستخدم بالنقر
  المباشر، ولا وسم ملكية للدور ولا كشف لتغيّرٍ وقع بين اللقطة والفعل.
- **المصدر**: تخطيط دفعة «صقل متصفح سطر» (بلاغ مالك)
- **الملاحظة**: بند رئيس في دفعة المتصفح القادمة، وعلاجه **قرار تصميم يحتاج عصفاً**
  لا إصلاحاً بديهياً: قفل إدخال المستخدم أثناء فعل الوكيل يقتل مبدأ «المستخدم هو
  القائد» (المثبت في التسليم البشري)؛ وتركه يبقي السباق. مرشّحات للعصف: مؤشر
  ملكية دور صريح · إبطال refs عند **أي** تفاعل مستخدم (اليوم تُبطل عند التنقّل
  فقط) · بصمة DOM تُفحص بين اللقطة والفعل فيرفض الفعل على حالة متغيرة fail-closed.

## OBS-014 — بطء استجابة الوكيل عند قيادة متصفح «سطر»
- **الوسم**: `preview`
- **النوع**: تحسين
- **الحالة**: منجزة — دفعة «صقل متصفح سطر» (2026-08-15). مسبار الكمون
  (‏`42d26a1`) حسم: أدوات `preview.js` كلها <5ms — البطء في دورات النموذج
  وأحجام النتائج. ومحلل السجلات `scripts/browser-session-audit.js` قاس من 25
  جلسة حقيقية: **حصة اللقطات 99.7% من بايتات النتائج** ووسيط دور النموذج بعد
  `screenshot` ‏**12.1s** (‏P90 ‏38.4s) مقابل 2.6s بعد النقر. نُفِّذ: دلالة نتيجة
  صادقة (`dispatched/effect_observed/satisfied` — `satisfied` يعود فوراً بلا
  مهلة)، وتوجيه اللقطة الكاملة + تلميح 3× الآلي (بند مالك). **بقي عمداً بقرار
  قياسي**: 360ms و250ms و150ms (يحرسها عقد حي والأرقام لا تبرر لمسها)، وضغط
  اللقطات البصرية — انظر OBS-016.
- **الدليل**: بلاغ مالك (2026-08-15): استجابة الوكيل مع المتصفح «بطيئة» بشكل ملموس.
  لا قياس بعد — مواضع كمون معروفة بنيوياً تستحق مسباراً: دورة snapshot كاملة قبل كل
  فعل، وحد الانتظار التكيفي 360ms في `preview.js`، وذهاب-إياب الأذونات لكل فعل
  حساس، وحجم نتائج الأدوات العائدة للنموذج (رغم تحسينات H1 المقيسة: لقطات 7⇒1).
- **المصدر**: تخطيط دفعة «صقل متصفح سطر» (بلاغ مالك)
- **الملاحظة**: لا يُصلح شيء قبل **مسبار كمون** يفصل المكوّنات (زمن الأداة نفسها ·
  زمن دور النموذج · زمن الإذن البشري) — «بطيء» بلا قياس يقود إلى تحسين الموضع
  الخطأ. درس المشروع الثابت: مسبار أولاً ثم عقد ثم تنفيذ.

## OBS-015 — حارس التصميم يفحص مجلدات غير متتبَّعة لا علاقة لها بالمشروع
- **الوسم**: `process`
- **النوع**: عطل
- **الحالة**: منجزة — `3df7a85`، بقرار مالك ضمّها إلى دفعة «صقل متصفح سطر»
  (2026-08-15). نُفِّذ العلاج المقترح مع تدقيق: الحصر على **غير المتتبَّع فقط**
  (قائمة `PROJECT_ROOTS` أو ملف في الجذر مباشرة)، والمتتبَّع المعدَّل يبقى
  مفحوصاً كله لأنه ملك المشروع بالتعريف. العضّة المستعادة أثبتت الطرفين:
  `foreign-probe/bad.css` بمخالفة `z-index: 9999` الحرفية ⇒ صمت ونجاح، و
  `src/ui/obs015-bite.css` بالمخالفة نفسها ⇒ عضّ بالرسالة الحرفية. السطر الوقتي
  في `.git/info/exclude` لم يعد ضرورياً لكنه غير ضار.
- **الدليل**: بوابة إصدار 2.16.0 (2026-08-15) سقطت بـ
  `hadiah/styles.css: numeric z-index outside tokens` من
  `scripts/opsroom-ui-test.js:1149`. المصدر `changedFiles()` (‏`opsroom-ui-test.js:24-27`)
  يضمّ `git ls-files --others --exclude-standard`، فيلتقط **أي** مجلد غير متتبَّع
  يضعه المستخدم في جذر المستودع — و`hadiah/` مشروع منفصل للمالك (856ك.ب) لا يدخل
  حزمة «سطر» أصلاً (خارج `build.files`).
- **المصدر**: بوابة إصدار 2.16.0
- **الملاحظة**: ضمّ غير المتتبَّع **مقصود وصحيح** (الملف الجديد يجب أن يُفحص قبل
  إضافته)، لكن نطاقه واسع: لا يميّز «ملف مشروع جديد» من «مجلد أجنبي في الجذر».
  والأثر ليس إزعاجاً فقط — **يمنع الإصدار** حتى يتدخل أحد. عولج وقتياً بسطر في
  `.git/info/exclude` (محلي، بلا لمس `.gitignore` المشترك وبلا حذف). العلاج المقترح:
  حصر الفحص بمسارات المشروع المعروفة (‏`src/` · `electron/` · `scripts/` · `site/` ·
  `pwa/`) بدل «كل ما ليس متتبَّعاً» — فيبقى الحارس عاضّاً على ما يملكه المشروع
  ويصمت عمّا لا يشحنه. **لا يُنفَّذ الآن** — دفعة الإصدار جارية.

## OBS-016 — اللقطات البصرية تهيمن على كلفة التصفح الوكيلي (99.7% من البايتات)
- **الوسم**: `preview`
- **النوع**: تحسين
- **الحالة**: منجزة — ترميز تكيفي للقطات النموذج مع سقف عرض وتوجيه بنيوي أولاً؛
  مرجع الأساس `18b11ff`، ويلحق القائد مرجع التزام الدفعة عند الدمج (2026-09-03).
- **المنجز**: تمرّر أداتا المحرّكين `modelImage:true` إلى `preview.js`؛ فلا يُصغّر
  إلا العرض الذي يتجاوز `SHOT_MAX_EDGE=1280` مع إبقاء النسبة والطول (حتى سقف
  `MAX_FULL_HEIGHT`). بعد ذلك يُرمّز الناتج PNG وJPEG بجودة `SHOT_JPEG_QUALITY=72`
  ويُعاد الأصغر مع `mimeType` المطابق، ويمرّره كل من `agent.js` و`codexmcp.js`.
  بقيت مصغّرة المستخدم `agent_screenshot` ‏PNG بعرض ≤360px وحجم ≤512KiB. وتوسّع
  عقد 🎯 الخلفي بنسخة عرض PNG في `base64` ونسخة نموذج في
  `modelBase64/modelMimeType`؛ استهلاك نسخة النموذج في الواجهة مذكور أدناه لأنه خارج
  ملكية هذه الدفعة. كما تُستهلك `page_metrics` للقطة الكاملة في تنبيه القصّ عند 20000px.
- **توصيل الواجهة (2026-09-03)**: `preview-panel.js` تمرّر `dataUrl` (PNG عرض) و`model`
  (‏`media_type` + `data` للنموذج) عند توفر `modelBase64`؛ `app.js` تُدخل الصورة في
  المؤلف عبر `addImageData(dataUrl, model)`؛ و`composer.js` يبقي المصغّرة من العرض
  ويرسل للنموذج بيانات `model`. غياب `model` = السلوك القديم حرفياً.
- **تصحيح بعد المراجعة الخصومية (2026-09-03)**: كان ادعاء الخفض السابق بأكثر من 90%
  يخلط أثر التصغير على الضلع الأطول بأثر الصيغة، ويقيس canvas ضوضاء منحازاً إلى JPEG.
  القياس الحي الجديد عند نافذة 700×760 على fixture واجهة عربية فعلية (نص/أزرار/ألوان
  مسطّحة)، بعد تطبيق سقف العرض نفسه، هو:

  | العيّنة | PNG (بايت) | JPEG q72 (بايت) | المختار |
  |---|---:|---:|---|
  | صفحة كاملة | 71,378 | 79,217 | PNG — 71,378 |
  | عنصر | 33,637 | 38,020 | PNG — 33,637 |
  | نافذة العرض | 34,813 | 39,737 | PNG — 34,813 |

  لذلك لا يُنسب خفض ثابت للصيغة. يحرس `test:preview-member-live` قياس الصيغتين واختيار
  الأصغر، ومطابقة الرأس لـ`mimeType`، وبقاء عرض `full_page` ≥ عرض النافذة حين يكون
  ≤1280px، وPNG المصغّرة. ويوجّه وصف الأدوات إلى `browser_snapshot` أولاً للبنية وإلى
  اللقطة البصرية للحكم على التخطيط فقط.
- **الدليل**: محلل السجلات `scripts/browser-session-audit.js` على 25 جلسة حقيقية
  (‏187 ملفاً، 627 خطوة متصفح): بايتات النتائج 24.2MiB حصة اللقطات منها **99.7%**
  (‏`screenshot` وسيطها 96.6KiB وP90 ‏338.5KiB)، ووسيط دور النموذج بعد `screenshot`
  ‏**12.1s** (‏P90 ‏38.4s) مقابل 2.6s بعد `browser_click`. الخام في
  `dist/browser-session-audit.json`.
- **المصدر**: دفعة «صقل متصفح سطر» (منفّذ ب)
- **الملاحظة**: هذا أكبر بند كلفة/كمون متبقٍ بعد الدفعة، وقرار الحزمة تأجيله حتى
  توفر الأرقام — وقد توفرت. مرشّحات: تقليص لقطة `capturePage` (‏JPEG بجودة أدنى
  بدل PNG؟ تصغير الأبعاد؟)، توجيه أقوى نحو `browser_snapshot` البنيوية (وسيطها
  2.9KiB فقط) قبل اللجوء للبصرية، ولقطة تفاضلية بعد حسم عقدها مع أجيال refs.
  التلميحان الجديدان (3× وضيق اللوحة) كانا خطوة أولى؛ الضغط الفعلي أُنجز في هذه الدفعة.

## OBS-017 — كودكس «يُنجز ولا يتوقف» وشجرته اليتيمة تنجو من قتل pty
- **الوسم**: `process`
- **النوع**: عطل
- **الحالة**: مفتوحة
- **الدليل**: دفعة «صقل متصفح سطر» (2026-08-15)، مرتين في يوم واحد: في جولة
  النقد سجل 811KB مع 12 «collab: Wait»، وفي التنفيذ أنهى عمله كاملاً
  (‏`exec-report.md` عند 04:56، الكود مستقر منذ 04:51) ثم واصل ضخ السجل
  ‏1.30⇒1.94MB في 40 دقيقة بلا لمس ملف. وبعد إيقاف مهمة pty بقيت **شجرة ~34
  عملية يتيمة** (‏`codex`/`codex-code-mode-host`) حتى قُتلت يدوياً بـ`taskkill /T`.
- **المصدر**: دفعة «صقل متصفح سطر» (مراقبة القائد)
- **الملاحظة**: علاجان مرشّحان في `D:\sater\executor.ps1`: (أ) حاجز مهلة صمت —
  إن لم يتغير أي ملف في الشجرة ولا نما التقرير خلال N دقائق بعد وجود
  `exec-report.md` يُقتل المنفذ بشجرته؛ (ب) تسجيل PID الجذر عند البدء وقتل
  شجرته صراحةً في نهاية السكربت (‏`taskkill /T` على PID المسجل لا الاعتماد على
  موت pty). ومنع `spawn_agent` في البرومبت خفف الظاهرة ولم يمنعها.
  **تحديث (2026-08-17، بعد التكرار الثالث في دفعة الفارسية)**: نُفِّذ العلاج (أ)
  في `D:\sater\executor.ps1` — معامل `-SilenceTimeoutMinutes` (افتراضي 6،
  ‏0 يعطّل) بجوب مراقبة موازية لا تمسّ مسار الأنبوب المجرَّب: صمتُ السجل أطول
  من المهلة ⇒ قتل شجرة عمليات المحرك التي بدأت بعد الإقلاع (فرز اسم +
  StartTime — حدّ معلن: محرك يدوي متزامن بنفس الاسم قد يُقتل معه)، وسطر
  توثيقي في السجل. الملف خارج git؛ **الإثبات الحي أول اصطياد فعلي في تشغيل
  قادم — تبقى مفتوحة حتى حينه**.

## OBS-018 — قراءات المعاينة المتبقية في main world قابلة للتعطيل من صفحة عدائية
- **الوسم**: `preview`
- **النوع**: صقل
- **الحالة**: منجزة — عنقود صقل الواجهة `d2c971b` (2026-08-25)
- **الدليل**: تقرير منفّذ أ (بند «رصدتها ولم أنفّذها»): بعد نقل الأفعال إلى
  isolated world بقيت `SNAPSHOT_FN` و`RECT_FN` و`PICK_SCRIPT` وقياس البصمة وقت
  اللقطة في main world — صفحة تخرّب `document.querySelector[All]`/`getAttribute`
  تعطّل **القراءة** (لا الفعل) أو تحدث `target_changed` كاذباً.
- **المصدر**: دفعة «صقل متصفح سطر» (منفّذ أ)
- **الملاحظة**: خارج العقد المجمَّد عمداً (حُصّن الفعل والبصمة وقت التنفيذ —
  الأخطر). نقل القراءة كلها إلى `AGENT_WORLD_ID` امتداد طبيعي بدفعة صغيرة،
  والبصمة موثقة أصلاً «كاشف انجراف لا برهان أمني» في تعليق `SEMANTICS_SRC`.
- **الإغلاق (2026-08-25)**: نُقلت إلى `runIsolated` تسع قراءات — `SNAPSHOT_FN`
  (ومعها حساب البصمة)، و`READ_SCRIPT`، و`WAIT_FN`، و`RECT_FN`، وقياس طول الصفحة في
  `screenshot`، و`PICK_SCRIPT` **ومعه إلغاؤه** (‏`__satrPick` عالميّ عالمِه فنقله وحده
  كان سيكسر الإلغاء). **واتّسع النطاق المعلن** إلى `ACTION_CONTEXT_FN` و
  `ACTION_TARGET_FN`: وُجدتا في main world أثناء التنفيذ، وهما قراءتان يُبنى عليهما
  قرار أمني (`isSubmit`/`crossOriginPost`/`targetUrl`)، فصفحة تخرّب `closest` أو
  `form.action` كانت تُخفي حساسية الفعل عن البوابة. ضُمّتا لأنهما من عائلة الملاحظة
  حرفياً («نقل القراءة كلها») ولأن مكسبهما أعلى من الثلاثة الأصلية.
  **بقي في main world عمداً** (موثَّق فوق `AGENT_WORLD_ID`): `browser_evaluate` —
  غرضه سياق الصفحة نفسه فنقله يُبطله؛ ومسبار الأفعال `PROBE_*` — مجموعة متسقة تُنصَّب
  وتُقرأ معاً ونقل جزئي يكسرها؛ و`SECRET_DONE_FN`؛ و`SCROLL_FN`/`FLASH_FN` (فعلان لا
  قراءة، وأثر تخريبهما تعطيل وميض أو تمرير لا قرار خاطئ — سُجّلا في OBS-051).
  الحارس: `test:preview-lease` كسب خمسة فحوص على الصفحة العدائية القائمة (لقطة/
  `read_page`/`wait_for`/مستطيل لقطة العنصر/سياق البوابة) تفشل حتماً لو عادت أيٌّ منها
  إلى main world. **حدّ مقيس**: فحص لقطة العنصر يثبت حلّ العنصر ومستطيله لا الالتقاط
  نفسه، لأن `capturePage` يحتاج نافذة مرسومة وهي مخفية في الاختبار.

## OBS-022 — مقياس اللغة أعمى عن الفارسية: نثر فارسي خالص يُسجَّل «ملتزماً بالعربية»
- **الوسم**: `engines`
- **النوع**: عطل
- **الحالة**: منجزة — `567aca4` (2026-08-26)
- **الدليل**: عصف الفارسية (2026-08-16، رأي أوبس — مثبت بتشغيل فعلي): نص فارسي
  خالص (154 حرفاً قوياً) أعطى `share=0.9625` وحكم `ok` من `langmetric.isSlip` —
  أعلى من نص عربي قيس بجانبه (0.9571). السبب `langmetric.js:19`: نطاق
  `ARABIC_RE` ‏`U+0600–06FF` يشمل الحروف الفارسية (پ چ ژ گ) والأرقام ‏۰–۹.
- **المصدر**: عصف الفارسية (`D:\sater\prompts-farsi-bs\archive\opinion-opus.md`)
- **الملاحظة**: أثره قائم اليوم بلا أي فارسية: جلسة فارسية عابرة تلوّث بيانات
  معايرة سجل الظلّ صامتة. العلاج المرشّح (درجة أوبس 2): `scriptOf(prose)` تعيد
  `ar|fa|mixed` بإشارات موجبة، ويرفع `METRIC_VERSION` إلى 3 — **بعد قراءة
  حصيلة v2 المستحقة 2026-08-22 حصراً** (قاعدة عدم مقارنة الأرقام عبر إصدارين).
- **المنجز** (`567aca4`): قُرئت الحصيلة أولاً (‏906 رسالة/12 يوماً) فانفتحت البوابة،
  ثم نُفِّذ العلاج المرشّح كما وُصف. الإشارات الموجبة زوجا `ك/ک` و`ي/ی` وپچژگ وأرقام
  الطرفين وZWNJ، **مكتوبةً بـ`\u` لا حرفيةً** لأن ZWNJ عديم العرض وزوجَي الكاف/الياء
  لا تفرّقهما العين في المصدر. و`mixed` يُوسم ولا يُدان (عربيٌّ يقتبس فارسية حالة
  مشروعة)، والغموض يُحسم إلى `ar` لأن كلفة الخطأين غير متماثلة — وسمُ عربيٍّ سليم
  «فارسياً» إنذارٌ كاذب يُعطِّل الحارس.
- **قيدٌ لم يكن في الملاحظة وظهر عند التنفيذ**: رفع الإصدار كان سيُهدر الـ906
  المتراكمة بقاعدة «لا تُقارن عبر إصدارين»، فيعيد ساعة معايرة الخروج من الظلّ إلى
  الصفر. فصُمّم v3 **توافقياً**: `arabicShare` والعتبات و`structuralSlips` لم تُمَسّ،
  وفحص الخط يقع بعد بوابتَي `no_prose`/`short` وقبل الحصّة — فحكم كل نثر عربي مطابق
  لـv2 حرفياً، ولا يتغيّر إلا الفارسي الخالص.
- **الحارس**: `test:langmetric` يحمل نسخة v2 **مجمَّدة بعتباتها مكتوبة حرفياً** (لا
  مستوردة، وإلا تحرّكت مع الإنتاج فقارَن الشيءَ بنفسه) ويقارن الحكم عيّنةً عيّنة.
  **مُثبَت أنه يعضّ**: تحريك `SHARE_THRESHOLD` أسقط التوافق، وحذف فحص الفارسية أسقط
  الإصلاح. ومعه في `test:langoverride` استُبدل وتد `METRIC_VERSION = 2` الأعمى —
  الذي أطلق النار على هذه الدفعة كما صُمّم — بالثابت الذي كان يدافع عنه: يجوز تحريك
  الرقم **بشرط برهان التوافق**. الوتد الأعمى كان سيمنع الإصلاح الصحيح ويُلتفّ عليه
  بحذفه، وذلك أسوأ ما يصيب حارساً.

## OBS-023 — `kimi.js` خارج منظومة المرساة اللغوية
- **الوسم**: `engines`
- **النوع**: عطل
- **الحالة**: منجزة — `567aca4` (2026-08-26)
- **الدليل**: عصف الفارسية (رأي أوبس، وتحقق القائد): `kimi.js` لا يستورد
  `langanchor` — يأخذ `CONTRACT_LINE` عبر `envbrief` فقط، بلا مرساة ذيلية ولا
  صيغة قوية للدور الأول/ما بعد الضغط، والآن بلا `langoverride` أيضاً (دفعة
  الفارسية وصلت SDK وCodex حصراً بملكيتها المعلنة).
- **المصدر**: عصف الفارسية
- **الملاحظة**: تفاوت بين المحركات الأصيلة الثلاثة في عقد حرج — محادثات Kimi
  الوكيلية الطويلة معرضة لانزلاق السرد الذي عالجته دفعة OBS-001 في نظيريه.
  العلاج: نفس نمط الحقن (المرساة + التجاوز) في مسار برومبت `kimi.js`، بدفعة
  لغة قادمة مع OBS-022.
- **دليل مقيس أُضيف عند التنفيذ**: حصيلة سجل الظلّ الأولى (‏906 رسالة/12 يوماً) لم
  تحمل لـKimi إلا **صفّين**، وصفّ سرده الوحيد كان انزلاقاً بحصّة 5% — بينما
  `codex·commentary` صفر انزلاق في 180 عيّنة. عيّنة `n=1` إشارةٌ لا برهان، لكنها
  متسقة مع التشخيص.
- **المنجز** (`567aca4`): `langanchor` و`langoverride` في مسار برومبت `kimi.js`
  بنمط `codex.js` نفسه — مرساة ذيلية **آخرَ كتلة** (وتلك علّة تسميتها ذيلية: الأقرب
  لما يقرؤه النموذج)، قوية لأول دور وبعد الضغط عبر `markCompactedSession`، وخارج
  `/compact` كبقية كتل السياق. والسياقات المعزولة (`browserControl:false`) خارجها
  ببوابة الذاكرة نفسها لا ببوابة جديدة.
- **الحارس**: فحص **سلوكي** في `test:kimi` على البرومبت المرسَل فعلاً — الموضع (آخر
  كتلة) والصيغة (بايتاً ببايت من `langanchor`) والبوابة (المعزول خارجها) — لا فحصاً
  نصّياً على المصدر. **مُثبَت أنه يعضّ**: حذف سطر الحقن أسقطه. ومعه فحص نصّي في
  `test:langoverride` يمنع **سقوط Kimi الصامت** من المنظومة مرة أخرى.

## OBS-025 — `test:executionteam` هشّ تحت حمل الطقم الكامل ويمرّ مستقلاً
- **الوسم**: `process`
- **النوع**: صقل
- **الحالة**: منجزة (`0a35c65`)
- **الدليل**: مرتان موثّقتان في يومين (بوابتا دفعة المتصفح ودفعة الفارسية):
  `test:opsroom-all` يسقط عند `executionteam-test.js` (‏`started.ok=false` —
  فشل بدء فريق 3 عوامل بworktrees حقيقية) داخل `test:full` الكامل والتطبيق
  مفتوح، **وينجح فوراً عند الإعادة المستقلة** في المرتين. النمط يطابق سوابق
  الهشاشة المسجّلة في ذاكرة المشروع (تنظيف EPERM تحت الضغط).
- **المصدر**: بوابات `test:full` (دفعتا المتصفح والفارسية)
- **الملاحظة**: ليست تراجع كود — لم يُمسّ `executionteam` في الدفعتين. مرشّحا
  علاج: إعادة محاولة داخلية واحدة في `opsroom-suite` لفشل `worktree add`
  البيئي، أو ترتيب `executionteam` أول المجموعة قبل احتدام الحمل. حتى العلاج:
  فشل `opsroom-all` وحيداً في بوابة كاملة يُعاد مستقلاً قبل أي استنتاج.
- **المنجز** (`0a35c65`): طبقتان. **التشخيص أولاً**: كان
  `assert.strictEqual(started.ok, true)` بلا رسالة فيطبع «`false !== true`»
  ويخفي السبب، بينما `start()` يعيد `error` و`teamPublic` يحمل `worker.error`
  لكل عامل (‏`executionteam.js:321`) — صار يطبعهما، فالمرة القادمة تُعرف علة
  البيئة بدل تخمينها. **ثم إعادة محاولة معلَنة**: قائمة `RETRYABLE` مغلقة في
  `opsroom-suite` (‏`test:executionteam` وحده) و`MAX_RETRIES=1`، مع سطر تحذير
  صاخب عند التعثّر وذكر المُعاد في خاتمة الطقم — فلا يُخفى شيء. **الدليل**:
  مسبار حقن فشل (نسخة مؤقتة من المشغّل بسكربت غير موجود) طبع سطر إعادة المحاولة
  ثم أسقط الطقم برمز `1` — أي أن العثرة تُعاد **و**التراجع الحقيقي يفشل مرتين
  فيسقط؛ ثم `test:opsroom-all` كامل `11/11` بلا إعادة محاولة في المسار السعيد.
  أُزيل بذلك التنبيه الشفهي («أعِد تشغيله منفرداً») لأنه صار سلوكاً مبرمجاً.

## OBS-024 — قمع التوزيع أحادي اللغة قسراً: المثبّت يفرض العربية على كل ويندوز
- **الوسم**: `process`
- **النوع**: صقل
- **الحالة**: مفتوحة
- **الدليل**: عصف الفارسية (رأي أوبس): `build/installer.nsh:12,16` يفرض
  `$LANGUAGE=1025` في `preInit`/`customUnInit` مهما كانت لغة النظام، وصفحات
  `site/` كلها `lang="ar"`.
- **المصدر**: عصف الفارسية
- **الملاحظة**: مقصود اليوم (هوية «البيت العربي» + درس `multiLanguageInstaller`
  الموثق) ولا يُغيَّر الآن — يُسجَّل حداً معلناً: أي وعد مستقبلي بلغة ثانية
  داخل المنتج يمرّ أولاً بفتح هذا القمع (مثبّت وموقع) وإلا كان وعداً مكسوراً
  من أول تنزيل. يقرأه من يخطط دفعة م-ب/م-ج قبل أي كود.

## OBS-020 — الوكيل يجهل سلوك إغلاق المعاينة ولا يملك أداته فيجيب خطأً
- **الوسم**: `preview`
- **النوع**: عطل
- **الحالة**: منجزة — `72ca14c`، بقرار مالك ضمّها ذيلاً لدفعة «صقل متصفح سطر»
  (2026-08-16). نُفِّذ العلاج المقترح كاملاً: أداة `close_preview` في السطحين
  (SDK يبثّ `preview_close` فتستدعي القشرة `previewEl.close()` — مسار زر ✕
  نفسه؛ وCodex/Kimi عبر `deps.closePreview`) بتصنيف navigate، وفقرة الحقيقة في
  `envbrief` تمنع الإجابة الخاطئة وتوجّه إلى 🧹 لصفحة نظيفة. **درس من العضّة**:
  حارس `test:envbrief` عدّي لا اسمي (إعادة تسمية أداة لا يمسكها)؛ الماسك الاسمي
  الفعلي هو `test:satr-guide` (انحراف الكتالوج المولَّد) — اكتفي بهما معاً.
- **الدليل**: بلاغ مالك بلقطة (2026-08-16، أثناء بوابة القبول الحية لدفعة «صقل
  متصفح سطر»): سأل الوكيلَ إغلاق المعاينة لأن ✕ «لا يقفل والصفحة تعود عند إعادة
  الفتح»، فأجاب: «لا أملك أداة لإغلاق معاينة سطر؛ علامة ✕ **تخفيها فقط** وتحفظ
  الصفحة والجلسة» واقترح إغلاق التطبيق بالكامل. **معلومته خاطئة**: المسار المتتبع
  `pvClose` ⇒ `satr:previewClose` ⇒ `preview.close()` (‏`preview-panel.js:656` ⇒
  `main.js:2812`) **يدمّر** WebContentsView كلياً. عودة الصفحة سببها ميزة «تذكّر
  آخر مشروع» المقصودة (`satr_preview_url::<cwd>` — إعادة الفتح تستعيد آخر عنوان)
  وpartition الدائمة تحفظ الكوكيز.
- **المصدر**: البند 4 من دفعة «صقل متصفح سطر» (تجربة المالك الحية)
- **الملاحظة**: ثلاث فجوات مركبة: (أ) لا أداة `close_preview` في طقم أدوات
  المتصفح (‏`open_preview` بلا مقابل) فطلب «أقفلها من عندك» غير قابل للتلبية؛
  (ب) `envbrief`/`satr-guide` لا يوثقان سلوك ✕ والتذكر فيهلوس الوكيل تفسيراً؛
  (ج) تجربة المستخدم نفسها: «يُغلق ثم يعود» يُقرأ عطلاً — قد يستحق ✕ خيار
  «إغلاق ونسيان العنوان» أو توضيحاً. العلاج المقترح: أداة `close_preview`
  (تصنيف navigate) + فقرة حقيقة في envbrief (✕ يدمر العرض، الكوكيز تبقى،
  والفتح التالي يستعيد آخر عنوان — لصفحة نظيفة وجّه المستخدم إلى 🧹 مسح
  التخزين) + إعادة `gen:satr-guide`، مع تحديث حارس الجرد والاختبارات.

## OBS-021 — علم التسليم البشري يعلق مرفوعاً فترفض كل أدوات المعاينة بلا مخرج
- **الوسم**: `preview`
- **النوع**: عطل
- **الحالة**: منجزة — على مرحلتين ضمن استثناء «عطل يكسر بوابة القبول»:
  مسار التعافي `677df7e` (‏✕ يفكّ التسليم العالق)، ثم **الجذر** بعد أن أثبتت
  تجربة المالك الثانية تكرار العلوق أثناء الاستخدام الفعلي: إشارة إجهاض لكل
  نداء `tools/call` في خادم `codexmcp` (‏`res.on('close')` قبل اكتمال الرد ⇒
  `raceWithAbort` يحسم انتظاري التسليم والإذن بالإلغاء فيمرّ `finally` ويُفكّ
  العلم — لا وعد يتيماً بعده). الفحص الحي الجديد في `test:codexmcp` (‏150):
  نداء handoff يُدمَّر عمداً ⇒ العلم يُفكّ وحده وتعود الأدوات فوراً، بعضّة
  مستعادة (تعطيل `raceWithAbort` ⇒ سقوط بنص «لا علوق يتيم» الحرفي).
- **الدليل**: بلاغ مالك بلقطتين (2026-08-16، تجربة حية على حساب Google): بعد
  تسليم بشري لرفع صورة الملف الشخصي صار الوكيل يرد «المعاينة عالقة في وضع
  التحكم اليدوي، ترفض جميع أوامر المتصفح» عبر أدوار مكتملة متتالية، وبقيت
  الحالة **حتى بعد إغلاق المستخدم اللوحة بيده**. من الكود: `close()` كان يستدعي
  `clearSensitiveState()` (يفكّ طلب السر فقط) ولا يستدعي `endHandoff()`؛ و
  `cleanup()` في `codex.js:1640` يحسم `pendingHandoffs` بالإلغاء لكنه لا يعمل
  إلا عند موت عملية `app-server` **المعمّرة عبر الأدوار** — فدورة handoff تنقطع
  دون حسم (الأرجح: مهلة أداة MCP لدى codex تقطع النداء بينما `dispatch` في
  `codexmcp.js` ينتظر `await tool.handler` للأبد فلا يصل `finally{endHandoff()}`)
  ويبقى العلم مرفوعاً بلا أي مخرج مستخدم أو وكيل.
- **المصدر**: البند 4 من دفعة «صقل متصفح سطر» (تجربة المالك الحية)
- **الملاحظة**: **المنجز** (`الالتزام المرافق لهذه الدفعة`): `preview.close()`
  صار يستدعي `endHandoff()` — إغلاق اللوحة فعل مستخدم قاطع يفكّ التسليم العالق
  (بديهة المستخدم التي جرّبها المالك صارت حقيقة)، بعضّة مستعادة في
  `test:preview-lease`. **المتبقي للجذر**: (أ) مسبار يستنسخ انقطاع نداء MCP
  (مهلة/إلغاء دور) ويثبت بقاء `dispatch` معلقاً؛ (ب) ربط إجهاض بـ
  `req.on('close')` في خادم `codexmcp` يحسم التسليم/الإذن المعلق عند موت
  الطلب؛ (ج) التحقق من نظير المسار في SDK وKimi (‏`kimi.js:966` يحسم عند
  نهاية الدور — يبدو سليماً)؛ (د) مؤقّت أمان أقصى للتسليم؟ يقرره العصف/المسبار.

## OBS-019 — `test:rtl-preview` هشّ بيئياً: يمسح صف الشاشة كاملاً
- **الوسم**: `process`
- **النوع**: صقل
- **الحالة**: منجزة (`0a35c65`)
- **الدليل**: دفعة «صقل متصفح سطر»: فشل `rtl-preview-fix-test` بنمط
  `x:1282,width:3` ثابت، وتجربتان ضابطتان مستقلتان (منفّذ أ ثم القائد) شغّلتاه
  على نسخة `HEAD` النظيفة **ففشل بالفشل نفسه حرفياً** — السبب بيئي: الاختبار
  يبحث عن البكسل الأحمر في صف من **الشاشة كاملة** فأي نافذة أخرى تعرض أحمر
  عند ذلك الصف (أو تغطي نافذته) تفسد القياس.
- **المصدر**: دفعة «صقل متصفح سطر» (منفّذ أ + تحقق القائد)
- **الملاحظة**: البوابة خارج `test:full` عمداً (تحتاج نافذة مرئية) لكن هشاشتها
  تجعل «أعد التشغيل على سطح نظيف» طقساً شفهياً. مرشّحان: قصر المسح على حدود
  نافذة الاختبار نفسها (`getContentBounds`) بدل الشاشة، أو لون علامة نادر
  (magenta ‏`#FF00FF`) يقل تصادمه مع واجهات حقيقية.
- **المنجز** (`0a35c65`): **المرشّحان معاً** — المسح صار محصوراً بعرض نافذة
  الاختبار (`contentBounds.x` إلى `x+width` بعد ضرب معامل التحجيم)، والعلامة
  magenta بدل الأحمر الشائع، ومعهما `win.moveTop()` قبل كل قياس وتمييز صريح في
  الخرج بين «لم تُرصد العلامة داخل النافذة» (محجوبة/شاشة مقفلة/بلا سطح مرئي)
  و«العلامة خارج الموضع» — الفرق الذي كان يستهلك دقائق تشخيص في كل عثرة.
  **الدليل الحي**: `3/3` حالات مطابقة تماماً (`0/0` · `380/380` · `560/560`)
  تحت `locale=ar` **ومع نافذتين أخريين مفتوحتين** — وهو الظرف نفسه الذي كان
  يُسقط الاختبار. يبقى خارج `test:full` عمداً (يحتاج سطحاً مرئياً).

## OBS-026 — تجربة المالك تجري على النسخة المثبّتة فتُقاس ميزة غير مُصدَرة بكود قديم
- **الوسم**: `process`
- **النوع**: عطل
- **الحالة**: منجزة (`9509a6c`)
- **الدليل**: 2026-08-18، بعد أن أغلق المالك كل النوافذ وأعاد التشغيل «من جديد»:
  العملية المستضيفة كانت
  `C:\Users\Unknown\AppData\Local\Programs\Satr\Satr.exe` بإصدار `2.16.0`
  وملف `resources\app.asar` بتاريخ `2026-08-15 02:22` — أي **قبل دفعة «صقل
  متصفح سطر» كلها**، ومعها `17` التزاماً في `main` بعد وسم `v2.16.0`. تأكيد
  سلوكي مستقل: أداة `close_preview` موجودة في `electron/agent.js:1423` على
  القرص و**غائبة كلياً** من جرد أدوات جلسة الوكيل داخل النسخة المثبّتة
  (‏`ToolSearch select:` أعاد «No matching deferred tools found»).
- **المصدر**: جلسة قيادة 2026-08-18 (تشخيص القائد قبل بوابة قبول المتصفح)
- **الملاحظة**: هذا هو **السبب الفعلي** لفساد ثلاث تجارب حية سابقة نُسبت إلى
  «التباس النوافذ»: إغلاق النوافذ وإعادة الفتح **لا يمكن** أن يجدي، لأن كل
  نافذة من النسخة المثبّتة تعيد تحميل نفس `app.asar` القديم. التشخيص الشفهي
  السابق كان خاطئاً، والعلاج الشفهي («وحّد النوافذ») كان يعيد المالك إلى نفس
  الحفرة. المسكّن المطبَّق في الجلسة: رفع `version` إلى `2.16.1` قبل التجربة
  فصار سطر «إصدار سطر» في ⚙ علامة تمييز بصرية بين نافذة `npm start` والنسخة
  المثبّتة. **العلاج المقترح للسحب**: مؤشر بيئة تشغيل صريح في الواجهة —
  `app.isPackaged` يفرّق «مثبّتة» عن «تطوير» ويُعرض في ⚙ (أو شارة صغيرة في
  الشريط)، فلا يبقى التمييز معتمداً على رقم إصدار قد يتطابق. ومعه قاعدة صريحة
  في عقد التجارب الحية: **أي ميزة غير مُصدَرة تُجرَّب من `npm start` حصراً**،
  ولا تُقبل بوابة قبول على نسخة مثبّتة أقدم من آخر التزام.
- **المنجز** (`9509a6c`): الشقّان معاً. **المؤشر**: `satr:appVersion` يعيد
  `packaged` من `app.isPackaged` (توسعة additive — علامة منطقية فقط، لا مسار
  تنفيذ ولا اسم ملف يعبر إلى renderer)، و⚙ يعرض شارة «نسخة تطوير» بجانب الرقم.
  يُعلَّم **الاستثناء وحده**: المثبّتة هي القاعدة فغياب الشارة يعني مثبّتة، وغياب
  الحقل (نسخة أقدم من العملية الرئيسية) يبقي الشارة مخفية fail-safe فلا يُدّعى
  «تطوير» بلا يقين. **والقاعدة**: فقرة ملزمة في `docs/DEFINITION-OF-DONE.md §4`
  تنص أن بوابة القبول الحية لميزة غير مُصدَرة تُجرى من `npm start` لا من النسخة
  المثبّتة — مؤشر بلا قاعدة مدوّنة يُنسى بعد أسابيع.
- **الحد المصرَّح به**: التحقق هنا بنائي ومنطقي (‏`node --check` + `design-guard`
  + تطابق المعرّف بين `index.html` و`topbar.js`)؛ **لم يُتحقَّق بصرياً بعد** لأن
  نافذة التطوير القائمة تشغّل كود ما قبل هذا الالتزام وكانت تجربة المالك جارية
  فيها. يقع التحقق البصري تلقائياً عند أول إعادة تشغيل للنافذة.

## OBS-027 — عقد اللقطة حجب أدوات القراءة فأغلق الحلقة على الوكيل
- **الوسم**: `preview`
- **النوع**: عطل
- **الحالة**: منجزة (`6faa3a4`)
- **الدليل**: بلاغ المالك الحي 2026-08-18 (جلسة `D:\alulama`): «الوكيل لا يمكنه
  التحكم في المتصفح». سجل الجلسة على القرص حسم التشخيص — **20 رفضاً متتالياً**
  برسالة `input_changed` شملت `open_preview` و`browser_snapshot` و`close_preview`
  و`screenshot` و`browser_navigate`، وكلها ليست أفعالاً. الجذر خطأ عقد بين
  وحدتين: `agent.js:1081` و`codexmcp.js:765` يستدعيان
  `preview.leaseError(toolName, input)` ظانَّين أنه يميّز نوع الأداة، بينما كان
  التوقيع `function leaseError()` بلا معاملات فيتجاهل الاسم صامتاً.
- **المصدر**: تجربة المالك الحية لبوابة قبول دفعة «صقل متصفح سطر»
- **الملاحظة**: الأثر أسوأ من حجب: **حلقة مغلقة لا يملك الوكيل كسرها** — الرسالة
  تطلب `browser_snapshot` جديدة واللقطة نفسها مرفوضة، ولا مخرج للمستخدم غير إعادة
  تشغيل التطبيق. المبدأ المستخلص: **اللقطة والقراءة والتنقّل مخرج التنازع لا
  ضحيته**؛ عقدٌ يحجب علاجه يصير مصيدة. **المنجز** (`6faa3a4`): العلاج في الدالة لا
  في الغلافين (فيُصلحان معاً بلا تكرار) — العقد يحكم أفعال `act` وحدها عبر
  `browserorigin.classifyBrowserTool`، والاستدعاء الداخلي بلا اسم يبقى محكوماً لأنه
  لا يقع إلا داخل فعل. حماية الأفعال لم تضعف: الفحص الثاني داخل كل فعل قائم
  و`pressKey` ما زال يستهلك العقد.
- **درس الحارس**: `test:preview-lease` كان **أخضر كاذباً** — يختبر منطق `preview.js`
  حيث `leaseGate` داخل الأفعال الثمانية، ولا يشغّل منطق الغلاف المستهلِك. وهو تكرار
  حرفي لدرس عطل «التحكم من الجوال» الثامن. أُضيفت عضّات تستدعي الدالة المصدَّرة
  **بالشكل نفسه الذي يستدعيها به الغلافان** (13 اسم أداة)، وأُثبت أنها تمسك العطل:
  بإرجاع الدالة مؤقتاً لسلوكها المعطوب سقط الاختبار عند `browser_snapshot` بالضبط.
- **بوابة القبول** (جلسة `7ad95229`، بعد الإصلاح): `14` نداء أداة متصفح و**صفر
  رفض**، والنقر نُفِّذ فعلاً (`dispatched=true · effect_observed`). **حدّ مصرَّح
  به**: التمرير ليس إدخالاً ملتزماً (`COMMITTED_INPUT_TYPES` = mouseDown/rawKeyDown/
  keyDown)، فالتجربة أثبتت زوال الحجب ولم تستثر التنازع نفسه؛ سلوكه مغطّى قطعياً في
  `test:preview-lease` وينتظر مشاهدة حية بنقرة أثناء عمل الوكيل.

## OBS-028 — `browser_set_viewport` يقبل عرضاً أكبر مما تتسع له اللوحة
- **الوسم**: `preview`
- **النوع**: صقل
- **الحالة**: منجزة — دفعة «تغذية الأدوات الراجعة» `493a29e` (2026-08-24). ورد العيب نفسه من
  مصدر ثانٍ مستقل (تقرير قائد فريق خارجي) فصار بنداً معتمداً في تلك الدفعة، وسُحبت هذه
  الملاحظة إليه بقرار مالك. المعتمد هو المرشّح الأول والأرخص المذكور أدناه — التحذير
  الصريح في نتيجة الأداة — مع تسمية السبب: اللوحة تبلّغ وضع محاكاة الأجهزة النشط مع
  المستطيل، فتقول الأداة «موبايل/لوحي مفعّل» أو «اللوحة ضيقة» وتذكر العلاج. لم يُؤخذ
  مرشّح توسيع اللوحة مؤقتاً ولا مسار CDP (محاكاة لا عرض فعلي — يُسقط مكسب التصميم).
- **الدليل**: جلسة `7ad95229` (2026-08-18): طلب الوكيل `1440×900` فأعادت الأداة
  `ok:true` مع `actual` أضيق، ثم صرّح بنفسه: «لوحة المعاينة عرضها الفعلي 844px ولا
  تتسع لـ1440 — فلا أستطيع الحكم بصرياً وحده»، وتحوّل إلى `browser_evaluate` ليقيس
  التجاوز في DOM.
- **المصدر**: تجربة المالك الحية (ملاحظة رصدها الوكيل عن «سطر» لا عن مشروعه)
- **الملاحظة**: ليس عطلاً في التنفيذ — التصميم متعمّد: المعاينة تضبط viewport
  **فعلياً** بعرض العرض الأصلي (لا محاكاة CDP) كي تتفاعل media queries حقيقةً، وهذا
  مكسب. لكن سقفه عرض اللوحة، فاختبار سطح مكتب واسع مستحيل بينما اللوحة ضيقة، و
  `ok:true` وحده يوحي بنجاح كامل. الوكيل هنا كان يقظاً وقرأ `actual`، ولا يُعوَّل
  على اليقظة. مرشّحات: تحذير صريح في نتيجة الأداة حين `actual.width < requested.width`
  (الأرخص والأصدق)، أو توسيع اللوحة مؤقتاً للقياس ثم إعادتها، أو مسار CDP اختياري
  معلَن أنه محاكاة لا عرض فعلي.

## OBS-029 — `browser_console` يخلط تحذيرات غلاف «سطر» بتحذيرات صفحة المستخدم
- **الوسم**: `preview`
- **النوع**: صقل
- **الحالة**: منجزة — عنقود صقل الواجهة `d2c971b` (2026-08-25)
- **الدليل**: جلسة `7ad95229`: خلص الوكيل إلى أن «الكونسول نظيف على الشاشات الثلاث —
  التحذير الوحيد من غلاف Electron الخاص بـ«سطر» (CSP) ورسالة React DevTools، ولا شيء
  منهما من التطبيق».
- **المصدر**: تجربة المالك الحية (ملاحظة رصدها الوكيل عن «سطر»)
- **الملاحظة**: الوكيل ميّز المصدر بنفسه هذه المرة، لكن الضجيج يستهلك رموزاً في كل
  قراءة ويخاطر بأن يطارد وكيلٌ أقل يقظةً تحذيراً ليس من مشروع المستخدم أصلاً — وهو
  ضدّ غرض الأداة (تشخيص ما بناه). مرشّح: وسم مصدر كل رسالة (`page` مقابل `host`)
  وترشيح الأخير افتراضياً مع إبقائه خلف خيار صريح، لا حذفه كي لا يضيع تشخيص حقيقي.
- **الإغلاق (2026-08-25)**: نُفِّذ المرشّح — كل رسالة تُوسم عند الالتقاط بـ`scope`،
  و`getConsole({includeHost})` يرشّح `host` افتراضياً ويعيد `hostHidden` فيُذكر العدد
  في نتيجة الأداة، و`include_host=true` يعيدها (‏SDK وCodex/Kimi معاً).
  **حدّ مُصرَّح به — أضيق مما قد يُفهم من عنوان الملاحظة**: التصنيف **يقيني** بمخطّط
  المصدر وحده (`devtools:`/`chrome-extension:`/`chrome:`/`about:`…)، وكل ما جاء عبر
  `http(s)` يبقى `page` **مهما كان مضيفه** — لأن مورد CDN خارجي جزء طبيعي من صفحة
  المستخدم، ومقارنة الأصل كانت ستحجبه. لذلك رسالة مثل «React DevTools» تبقى ظاهرة:
  هي فعلاً من bundle الصفحة، ونسبتها إلى «سطر» كانت استنتاج الوكيل لا حقيقة المصدر.
  عُوِّض ذلك بتوجيه صريح في وصف الأداة: ما يظهر لك من الصفحة المعروضة نفسها فلا
  تنسبه إلى «سطر». المصدر المجهول يُعدّ `page` عمداً (الإظهار الخاطئ يكلّف سطراً،
  والحجب الخاطئ يكلّف عطلاً).

## OBS-030 — الزر المعطَّل في حوار الأسئلة يبدو قابلاً للنقر
- **الوسم**: `ui`
- **النوع**: صقل
- **الحالة**: منجزة — عنقود صقل الواجهة `d2c971b` (2026-08-25)
- **الدليل**: لقطة المالك (2026-08-18، جلسة `7ad95229`): الخيارات الثلاثة كلها بلا
  تحديد وزر «إرسال الإجابة» ذهبي بارز كأنه جاهز. الفحص يثبت أن **المنطق سليم**:
  `src/ui/components/question-dialog.js:203` يضبط
  `this._submit.disabled = !answeredAll || !this._groups.length || this._sending`،
  والنمط الوحيد للحالة المعطّلة هو `.q-actions .submit:disabled { opacity: .5 }`
  (السطر 56).
- **المصدر**: مراجعة القائد للقطة المالك (2026-08-18)
- **الملاحظة**: العيب في الإشارة لا في المنع. `opacity:.5` على زر ذهبي فوق خلفية
  داكنة يُبقيه أبرز عناصر الحوار، فيضغطه المستخدم ولا يحدث شيء ⇒ يظنّ الحوار
  معطّلاً أو التطبيق عالقاً. تسجيل تصحيحي: أول قراءة للقطة استنتجت أن الزر مفعّل
  فعلاً — الكود نفى ذلك، والباقي حقيقي. مرشّح: خفض التشبّع لا العتامة وحدها (رمادي
  محايد بحدّ باهت) وتلميح «اختر إجابة أولاً»، فتصير الحالة مقروءة بلا نقر تجريبي.
- **الإغلاق (2026-08-25)**: نُفِّذ المرشّح حرفياً — الحالة المعطّلة صارت
  `--surface-3` بنص `--text-faint` وحدّ `--border-dim` و`opacity:1`، ومعها تلميح
  «اختر إجابة أولاً» يظهر بالمنع ويختفي بالإجابة (ولا يظهر أثناء الإرسال لأن المنع
  حينها لسبب آخر). الحارس في `test:question-dialog`. **درس قياس مثبَّت**: `background`
  ضمن `transition` في `controlsSheet`، وrAF تجوع في نافذة الاختبار المخفية فلا يتقدّم
  الانتقال و`getComputedStyle` يعيد قيمة البداية — فقياس المظهر في الاختبارات الحية
  يعتمد خصائص فورية (اللون، عرض الحدّ، العتامة) لا خاصيةً متحركة.

## OBS-031 — نسخة التطوير والنسخة المثبّتة لهما مخزنا تفضيلات منفصلان
- **الوسم**: `process`
- **النوع**: تحسين
- **الحالة**: منجزة — المرشّح (ب): شارة «نسخة تطوير» في الشريط العلوي (39f87b6)
- **الدليل**: 2026-08-18: ارتدّ النموذج إلى `Default (recommended)` في نافذة
  `npm start` بينما تفضيل المالك المثبَّت هو Opus. الحفظ سليم
  (`src/ui/app.js:462/542` — `satr_model_<engine>` في localStorage)، لكن الجذر أن
  `%APPDATA%\Satr` (المثبّتة) و`%APPDATA%\Electron` (‏`npm start`) مجلدا userData
  **منفصلان**، وكلاهما يحوي `Local Storage` فعلاً، وأوقات تعديلهما تتبع النافذتين.
  `electron/main.js:266` يضبط `setAppUserModelId` فقط ولا يوحّد `userData`.
- **المصدر**: تحقيق القائد في ملاحظة المالك (2026-08-18)
- **الملاحظة**: ليس عطلاً في الحفظ بل انقساماً في هوية التخزين. الأثر أوسع من
  النموذج: الثيمة والمسودات وعرض اللوحات ومجلد المشروع والمهارات المعطّلة — كلها
  تبدأ افتراضية في كل تجربة تطوير، فتُقاس التجربة الحية على إعدادات ليست إعدادات
  المستخدم و«يعمل عندي» يصير غير قابل للتكرار. **يتقاطع مع OBS-026**: البيئتان
  تختلفان في الكود **وفي البيانات** معاً. مرشّحان: توحيد `app.setPath('userData')`،
  أو قبول الانفصال عمداً مع إعلانه في شارة «نسخة تطوير» كي لا يُفسَّر ارتداد إعداد
  على أنه عطل.
- **المنجز** (`39f87b6`): شارة «نسخة تطوير» في الشريط العلوي (`#topbarDevBadge`) تظهر فقط عند `app.isPackaged === false` عبر القناة القائمة `satr:appVersion`، مع `title` يشرح انفصال التفضيلات والمجلد عن النسخة المثبتة.
- **سبب رفض المرشّح (أ)**: المالك يشغّل النسختين (مثبّتة و`npm start`) معاً كثيراً؛ مشاركة `userData` واحد بين نسختي Chromium حيّتين تعني قفل profile وتلف LevelDB محتمل، وهو أخطر من الانفصال المرئي.

## OBS-032 — المعاينة تنطفئ سواداً أثناء الحوارات بلا أي إشارة
- **الوسم**: `preview`
- **النوع**: صقل
- **الحالة**: منجزة — عنقود صقل الواجهة `d2c971b` (2026-08-25)
- **الدليل**: لقطة المالك (جلسة `7ad95229`): أثناء حوار «يسأل النموذج» ظهرت منطقة
  المعاينة سوداء فارغة بينما إطار اللوحة وشريط عنوانها في مكانهما.
- **المصدر**: مراجعة القائد للقطة المالك (2026-08-18)
- **الملاحظة**: السلوك مقصود — `preview.holdForDialog` يُخفي `WebContentsView` لأنه
  طبقة نظام فوق كل DOM فيحجب الحوار (عُولج ذلك أصلاً بعد لقطة مالك سابقة). لكن
  الإخفاء صامت، فيقرأ المستخدم السواد عطلاً لا تنحّياً مؤقتاً. مرشّح: نص خافت مكان
  العرض «المعاينة مخفية مؤقتاً أثناء الحوار» — سطر واحد يحوّل ما يبدو عطلاً إلى
  سلوك مفهوم.
- **الإغلاق (2026-08-25)**: نُفِّذ المرشّح — لافتة `#pvHeldNote` داخل `#pvBox` بنمط
  لافتة الترحيب القائمة، يضبطها `setHeld` وحده. **مقصورة على سبب الحوار**: نصّها يعد
  بالعودة «فور ردّك» فلا يصلح لسبب `drawer`، ولا تظهر قبل بدء عرض أصلاً (لافتة الترحيب
  مكانها حينئذ)، وتُخفى عند إغلاق اللوحة. لزم معها سطر `.pv-hint[hidden]{display:none}`
  لأن `display:flex` في قاعدة الصنف تغلب سمة `hidden` وحدها.

## OBS-033 — حوار الأسئلة يغطّي النص الذي بُني عليه السؤال
- **الوسم**: `ui`
- **النوع**: صقل
- **الحالة**: منجزة — عنقود صقل الواجهة `d2c971b` (2026-08-25)
- **الدليل**: لقطة المالك (جلسة `7ad95229`): الحوار الوسطي غطّى جدول «يُحفظ/يُتجاهل»
  وفقرات الانضباط والفحص الآلي — وهي حرفياً ما بُني عليه السؤال المطروح.
- **المصدر**: مراجعة القائد للقطة المالك (2026-08-18)
- **الملاحظة**: يُجيب المستخدم بلا أن يرى ما يجيب عنه، فيختار بالذاكرة أو يلغي.
  مرشّحات: إزاحة الحوار إلى أسفل الخيط بدل توسيطه، أو إبقاء التمرير خلفه متاحاً،
  أو تكرار مقتطف السياق داخل الحوار نفسه. الأخير أضمن لأنه لا يعتمد على تخطيط
  الشاشة.
- **الإغلاق (2026-08-25)**: نُفِّذ المرشّح الثالث — `ask()` يقبل `context` اختيارياً
  تمرّره القشرة من `chat.lastAssistantText(600)` (الإجابة النهائية أولاً وإلا سجل
  التفكير، و**ذيل** النص لأنه الأقرب إلى السؤال)، ويُعرض بـ`textContent` كبقية بيانات
  النموذج داخل صندوق مقيّد الارتفاع أعلى الأسئلة. غياب `context` يُبقي الحوار كما كان
  حرفياً — يحرسه فحص مستقل في `test:question-dialog`.

## OBS-034 — لا تنبيه عند الكتابة في مستودع بلا commits
- **الوسم**: `security`
- **النوع**: تحسين
- **الحالة**: منجزة — تنبيه مرة واحدة لكل مشروع (2026-09-03، دفعة موجة أوبس فوق
  `2e1dedb`؛ **يستبدل القائد هذا المرجع برقم التزام الدمج** — المنفّذ لا يلتزم).
- **الدليل**: 2026-08-18، مشروع `D:\alulama` (تحقّق القائد مباشرةً):
  `commits: 0` و`worktrees: 1` بينما ثلاثة محركات تكتب في الشجرة نفسها. شخّص وكيل
  المشروع الحالة بأنها «قاعدة شرف بلا أي آلية إنفاذ ولا أي إمكانية استرجاع».
- **المصدر**: بلاغ المالك من جلسة `D:\alulama` (2026-08-18)
- **الملاحظة**: يبدأ «سطر» الكتابة في أي مجلد بلا أن ينبّه أن لا شبكة استرجاع خلف
  `file_edit`: لقطات التراجع ذاكرية وتموت بإغلاق التطبيق، وcheckpoint يفقد
  `restorable` بعدها (موثّق في CLAUDE.md)، فيبقى `git` هو الأرضية الوحيدة الباقية —
  وهي غائبة. تمييز لازم عند العلاج: **commit يعطي كشفاً واسترجاعاً، ولا يعطي
  إنفاذ ملكية**؛ المنع الحقيقي عزل الكتابة (worktree لكل محرك + ملكية مسارات)، وهو
  ما تفعله `worktrees.js`/`executionteam.js` داخل غرفة العمليات ولا يناله من يشغّل
  محرّكات خارجها. مرشّح: تنبيه غير حاجب مرة واحدة لكل مشروع عند أول دور كتابة في
  مستودع صفر-commit أو مجلد ليس مستودعاً أصلاً. صنف الخطر عام لا يخصّ مشروعاً بعينه.
- **المنجز**: تنبيه غير حاجب مرة واحدة لكل مشروع، بلا قناة IPC جديدة وبلا تعديل في
  `main.js`. **الحقل**: `gitdiff.changes` صار يعيد `head:boolean` additive (مشتقاً من
  `hasHead` نفسه الذي يحكم حساب الفروقات — لا مصدر حقيقة ثانٍ)، ويصحب `repo:false`
  دائماً `head:false`؛ ومعالج `satr:gitChanges` يمرّر الردّ كما هو فلا يلزم سطر تنقية.
  **الواجهة**: `warnIfNoGitSafetyNet` في `app.js` تُستدعى في فرع `file_edit` **بعد**
  عرض الفرق وبلا `await` فلا تؤخّر الدور، وتخزّن الحالة في `satr_git_safety_notice::<cwd>`.
  **fail-open للصمت عمداً**: ردّ فاشل أو بلا حقل `head` (بناء أقدم) لا يُنبّه ولا
  يُسجَّل — لا ادّعاء بغياب شبكة استرجاع بلا دليل صريح. والمستودع السليم يصمت **بلا
  كتابة مفتاح** كي يبقى الفحص قائماً لجلسة لاحقة؛ ثمنه استعلام واحد لكل مشروع في
  الجلسة (كلفة فتح لوحة ± مرة)، وقُبل لأن إسكات تنبيه أمان أسوأ من مسح واحد.
  التمييز في نصّ الملاحظة محفوظ: الإشعار يَعِد بشبكة استرجاع لا بإنفاذ ملكية.
  **الحارس**: `npm run test:gitdiff` (قطعي، بمستودعات git حقيقية) — يغطي `head` في
  الحالات الثلاث وعدم تراجع الحقول القائمة، ومنطق التنبيه **مستخرَجاً من `app.js`
  وقت التشغيل** لا نسخةً موازية. مُثبَت أنه يعضّ: حذف `head` أسقطه، وكتابة مفتاح
  للمستودع السليم أسقطته أيضاً.

## OBS-035 — إلغاء سؤال النموذج يُقرأ «امضِ بتخمينك» ولا مخرج نصّي للمستخدم
- **الوسم**: `engines`
- **النوع**: عطل
- **الحالة**: منجزة (`d55003d`)
- **الدليل**: بلاغ المالك (2026-08-18): «عندما أغلق البطاقة يقوم الوكيل بالاختيار
  بنفسه ثم يكمل». الكود يفسّره: `electron/agent.js:2214` يردّ
  `{behavior:'deny', message:'لم يُختَر جواب صالح'}`، و`agent.js:1017` يردّ
  `'أُلغي الطلب'` — كلتاهما جملة محايدة لا توجّه النموذج إلى التوقّف أو إعادة
  السؤال.
- **المصدر**: بلاغ المالك أثناء اعتماد حزمة ملاحظات 2026-08-18
- **الملاحظة**: الأثر أسوأ من عدم السؤال أصلاً: القرار الذي طُلب من المستخدم
  يُتَّخذ نيابةً عنه صامتاً بينما يوهم السؤالُ بأن رأيه أُخذ. العلاج المقترح شقّان
  **يحترمان عقد «مؤشرات فقط» بحرفه**: (1) رسالة رفض موجِّهة بدل المحايدة — «لم يجب
  المستخدم؛ لا تفترض إجابة ولا تختر نيابةً عنه — اطرح السؤال نصّاً وتوقّف»، وهي
  تغيير نصّي رخيص يعالج الجذر وحده؛ (2) زر ثالث «أجب بنصّي» يغلق الحوار بـ`deny`
  **ويُدرج نصاً في المؤلّف بلا إرسال** (نمط شريحة الاقتراح القائم)، فالنص الحر لا
  يمرّ عبر `updatedInput` إطلاقاً ولا يقترب من `buildQuestionAnswer` — وهذا ما
  يجعله ممكناً بينما خيار «أخرى» داخل القائمة ليس كذلك. ملاحظة تكافؤ: محرك
  **Codex** يدعم أصلاً حقول `requestUserInput` النصية وخيار «أخرى»، فالفجوة قائمة
  بين المحركات لا في المنتج ككل. **تصحيح مسجَّل**: صنّف القائد غياب «أخرى» أولاً
  بأنه «ليس عيباً — قرار أمني موثّق»، وهو تبرير بالعقد بدل النظر في الأثر؛ العقد
  سليم والعيب أن المخرج البديل غير موجود.
- **المنجز** (`d55003d`): الشقّان كما اقتُرحا. (1) `QUESTION_UNANSWERED_MESSAGE`
  يُستعمل في مسار الإجابة الفارغة وفي إجهاض الطلب معاً؛ ونطاقه **مقصور على مسار
  السؤال** — «أُلغي الطلب» تبقى صحيحة في مساري الإذن والموصّل لأن الدور هناك لا
  يكمل أصلاً، فلا يُفرض توجيه في غير موضعه. (2) زر «أجب بنصّي» يغلق السؤال بإجابة
  فارغة ثم يبثّ `question-write`، وتمهّد القشرة المحرّر ببادئة «بخصوص سؤالك …» عبر
  `composer.insertPrompt` (بلا إرسال، بلا دهس ما كتبه المستخدم، وبلا مساس بالمرفقات
  — بخلاف `restoreTurn`). النص الحر لا يعبر IPC السؤال ولا يقترب من
  `buildQuestionAnswer`.
- **الحارسان**: `test:askquestion` يفحص نص الرسالة ومسارَي استعمالها، و
  `test:question-dialog` يثبت أن الزر يرسل `selections` فارغة **بلا أي نص**، ويبثّ
  `question-write` بنص السؤال ووسمه، وأن حالته لا تتسرّب إلى سؤال لاحق. أُثبت أن
  الحارس يمسك التراجع: بإرجاع الرسالة المحايدة سقط فوراً ثم عاد أخضر بالاستعادة.
  ودرس صغير مسجَّل: الفحص الساكن أمسك أولاً **تعليقاً** يقتبس الرسالة القديمة ليشرح
  سبب استبدالها، فضُبط ليفحص شكل الكود (`message: '…'`) لا النثر.

## OBS-036 — `test:promocapture-live` يسجّل صفر بايت تحت حمل الطقم الكامل
- **الوسم**: `process`
- **النوع**: صقل
- **الحالة**: منجزة — `209aef9` (RETRYABLE مغلقة بسقف 1 وإعلان صاخب وذكر في الخاتمة، ومعها فشل promocapture-live الصريح «لم تصل إطارات»). أول استعمال فعلي للآلية كان `OBS-101` بعد ساعة
- **الدليل**: بوابة 2.16.1 (2026-08-18): سقط وحيداً من `72` مجموعة بالخطأ
  `فشل stream/MediaRecorder` وحمولته `stop:{size:0, head:[]}` رغم
  `stopped.ok:true` و`start.tracks:1` و`frameRate:30` — أي أن المسار كله نجح ولم
  تصل **إطارات**. إعادته مستقلاً فوراً نجحت: `bytes=14958 · stream=ok · close=ok`.
  وقرينة حاسمة: `test:promo-studio` التالي له في الطقم نفسه **نجح** وأنتج
  `558497 bytes` — فالترميز سليم والعطب في التقاط النافذة وحده.
- **المصدر**: بوابة `test:full` قبل إصدار 2.16.1
- **الملاحظة**: ثالث اختبار بصري يسقط بيئياً تحت الحمل بعد `OBS-019`
  (‏`rtl-preview`) و`OBS-025` (‏`executionteam`) — والنمط واحد: قياس يحتاج سطحاً
  مرسوماً أو موارد نظام، يتزاحم مع اختبارات Electron الحية المتتالية فتُحرم نافذة
  الالتقاط من الرسم. لا تراجع كود: لم تُمسّ `promocapture.js` في هذه الدفعة.
  مرشّح مباشر: نقل آلية **إعادة المحاولة المعلَنة** من `opsroom-suite` إلى
  `full-suite` بقائمة `RETRYABLE` مغلقة تضم الاختبارات البصرية الحساسة — نفس
  التصميم بالضبط (إعادة واحدة، إعلان صاخب، ذكرها في الخاتمة، والتراجع الحقيقي يفشل
  مرتين فيسقط). مرشّح مكمّل: تأكيد أن نافذة الالتقاط في المقدمة ومرسومة قبل بدء
  التسجيل، وفشل صريح برسالة «لم تصل إطارات» بدل خطأ عام.

## OBS-037 — نطاق اللغة يُبنى على «الفارسية» بينما الحاجة كل لغات الحرف العربي بلهجاتها
- **الوسم**: `engines`
- **النوع**: تحسين
- **الحالة**: مفتوحة
- **الدليل**: قرار المالك 2026-08-18 عند اعتماد حزمة الملاحظات: «أريد تكبير المهمة
  بحيث أضمّ جميع اللغات التي تستخدم الحروف العربية وبحسب لهجة كل بلد»، ومعه رصد
  محدَّد: «الفارسية الموجودة حالياً هي الإيرانية وليست **الدَّرية** الأفغانية».
  الأثر البنيوي قائم في الكود: `test:farsi-content` ومشهدا `ui:audit` ‏46/47
  ومسار `langoverride` كلها مبنية على وسم لغة واحد (`fa`) لا على **عائلة خط**.
- **المصدر**: قرار المالك أثناء إطلاق 2.16.1
- **الملاحظة**: التمييز جوهري لا تجميلي — الدَّرية (`fa-AF`) والفارسية الإيرانية
  (`fa-IR`) تشتركان في المحارف وتختلفان في المفردات والأرقام المعتادة وبعض
  الاصطلاحات، وكذلك الأردية والبشتو والكردية السورانية والسندية والأويغورية:
  كلها **حرف عربي، لغات مختلفة**، ولكل منها ذخيرة محارف إضافية وقواعد اتصال
  ونقاط ZWNJ خاصة. بناء الطبقة على «فارسية» واحدة يخلق دَيناً يتضاعف مع كل لغة
  تُضاف لاحقاً. **أثر على القرار السابق**: كان م-ب مؤجلاً حتى ما بعد 2026-08-22
  ووجود مستخدم فارسي حقيقي؛ هذا القرار يوسّع نطاقه فيلزم إعادة تأطيره قبل التنفيذ
  لا بعده. الحزمة المرجعية في `D:\sater\prompts-farsi-bs\DECISION-PACKAGE.md`
  تحتاج مراجعة على هذا الأساس. **وملاحظات المالك على لقطتَي `46/47` لم تُفصَّل
  بعد** — تُجمع قبل أي دفعة، فاعتمادهما ما زال معلّقاً ولا يُعدّ حاجزاً للإصدار.
- **حصيلة العصف الثلاثي (2026-08-19)** — الحزمة في
  `D:\sater\prompts-lang-family\DECISION-PACKAGE.md` والآراء في `archive/`:
  - **قرارا المالك**: الوكيل أولاً والواجهة لاحقاً · الأقرب تقنياً أولاً · والمرساة
    عربية باسم اللغة بلغتها (`endonym`) والقياس يحكم لاحقاً.
  - **إجماع ثلاثي** رغم الزوايا المتضادة: المفتاح `locale` لا «عائلة خط» · جدول
    بيانات واحد · حارس واحد data-driven · `kimi.js` بوابة لا ذيل · لا كشف ضمني
    سلطةً · `METRIC_VERSION` لا يُمسّ قبل 2026-08-22.
  - **الخط لا يعيق شيئاً** (ثلاثة فحوص `cmap` مستقلة تلاقت): **681** نقطة
    و**252/256** من `U+0600-06FF` — الأردو 11/11 · البشتو 13/13 · السوراني 6/6 ·
    السندي 12/12 · الأويغوري 6/6، بصفر بايت إضافي.
  - **حقيقة تخالف الانطباع**: الفرق التقني بين `fa-AF` و`fa-IR` داخل المنتج **صفر**
    عند الخط والاتجاه والمقياس ومتّجه المحارف — الفرق معجمي لا محرفي. لذلك
    الدَّرية ليست «أرخص لغة ثانية» بل **الصفّ الذي لا يختبئ خلف فروق المحارف**،
    فنجاحها يثبت أن الطبقة تعمل بالتسمية والاختيار لا بالصدفة الرسمية.
  - **حدّ معلن**: الأردو ستُرسم Naskh لا Nastaliq (الخط يغطيها فلا مربعات، لكن
    `Noto Nastaliq Urdu` وحده يتجاوز 600ك.ب مقابل 191ك.ب لكل خطوطنا).
- **المنجز — الدفعة 0 (‏2026-08-19)**: عطل مقيس أُصلح بحارسه. كان
  `detectExplicitRequest` يعيد `null` لكلتا اللغتين اللتين سمّاهما المالك:
  «أجبني **بالأردو**» (الاسم الشائع فعلياً) لأن ملاحقة `(?![ء-ي])` تسقط عند الواو
  ما لم يسبق الجذرَ الأطولُ في البدائل، و«أجبني **بالدَّرية**» لأن الشدّة بين الدال
  والراء تمنع المطابقة — بينما ينجح النادر «الأردية». العلاج: `stripDiacritics`
  للمطابقة وحدها (بترميز صريح لا محارف حرفية) + جذور `دري|أردو|اردو|أوردو|اوردو`
  بالأطول أولاً + `dari` في `EN_LANG`. وأُخرج مفكّك `cmap` من
  `scripts/farsi-content-test.js` إلى `scripts/lib/woff2cmap.js` (نقل حرفي) ليستعمله
  الحارس المُعمَّم بدل «قارئين يتباعدان بصمت». **الدليل**: `test:langoverride`
  ‏84→**96** فحصاً، ونصفها الجديد **سلبي** (النفي والسؤال والترجمة والمحتوى على
  الجذور الجديدة) لأن توسيع قائمة مغلقة يلتقط كل شيء إن لم يُحرَس؛ وحُقن التراجعان
  فعلياً فاحمرّ الحارس بتأكيدين يسمّيان الحالة (`أجبني بالدَّرية → null` و
  `أجبني بالأردو → null`) ثم عاد أخضر. `langmetric`/`langshadow`/`envbrief`/
  `farsi-content` خضراء بلا تعديل.

## OBS-038 — حارس الصمت يقيس صمت السجل لا صمت العملية، فيقتل منفّذاً يعمل

- **الوسم**: `process`
- **النوع**: عطل
- **الحالة**: منجزة — العلاج في أداة خارج المستودع، فمرجعها بصمة النسخة
  المصلَحة `sha256:aed7db502966` لا رقم التزام (`executor.ps1` ليس في Git).
- **المصدر**: رصد المالك أثناء الدفعة 0 لمسابير الاستوديو: «لماذا انقطع أوبس عدة
  مرات، هذه ملاحظة يحتاج أن نظيف حل لها»
- **الدليل**: `D:\sater\executor.ps1:79-101` · وبصمة القتل الحرفية في
  `prompts-studio-bs/logs/probe-opus.md` و`finish-opus.md`:
  `== حارس الصمت (OBS-017): لا سطر جديد منذ 15.0 دقيقة — قتل شجرة claude* ==`
- **ما وقع**: قُتل منفّذ opus **مرتين** أثناء الدفعة 0 لمسابير الاستوديو، كلتاهما
  عند `15.0` دقيقة بالضبط (‏`05:21:37→05:36:39` ثم `06:20:58→06:36:03`)، فضاع
  تقريره مرتين رغم اكتمال عمله التقني وحفظ نتائجه على القرص. جولتاه النصّيتان
  السابقتان نجتا لأنهما انتهتا في `4` دقائق بـ`claude result: success`.
- **العلة ثلاث طبقات**:
  1. **تناقض نثر/كود**: التعليق في `executor.ps1:31` يقول «مهلته 45 دقيقة» والكود
     يضع `15` — نمط «فحص الكود لا النثر» نفسه (‏OBS-035).
  2. **الحارس يقيس الشيء الخطأ**: يفحص `LastWriteTime` لملف السجل، أي **صمت
     السجل**؛ ومهمة تنتظر أداة طويلة (تسجيل ‏20×60ث، تحليل ffmpeg) تصمت في السجل
     وهي في أوج عملها. الفصل بين «صامت» و«ميت» مفقود بالتصميم.
  3. **النجاة كانت بالصدفة**: codex وkimi يطبعان تدريجياً فيُنعشان الملف؛ وopus في
     وضع البثّ يصمت أثناء انتظار أداة طويلة.
- **العلاج المطبَّق**: (أ) تصحيح `15→45` مطابقةً للتوثيق. (ب) والأهم — قبل القتل
  يقيس الحارس **نشاط معالج شجرة المحرك** (المحرك وكل ما أطلقه نزولاً عبر
  `Win32_Process`/`ParentProcessId`)، وتزايده يعيد ضبط العدّاد؛ فلا يقع القتل إلا
  عند **صمت السجل وصفر نشاط معاً**، وهو غرض OBS-017 الأصلي.
- **درسان من الإصلاح نفسه، كلاهما مقيس**:
  - **النسخة الأولى من الإصلاح كانت أسوأ من العطل**: قياس CPU بأسماء عامة
    (`electron/node/ffmpeg`) التقط **«سطر» نفسه** (رُصد `132` عملية حيّة) فيبدو
    التطبيق نشاطاً للمحرك ويُبطل الحارس كلياً. العلاج: الشجرة لا الأسماء.
  - **العتبة ضُبطت بقياس حيّ لا بالتقدير**: عتبة `2.0s`/فحص بدت معقولة، لكن قياس
    codex أثناء مسبار فعلي أعطى `1.12s` في `20s` (المحرك ينتظر ردّ الشبكة فأغلب
    وقته I/O لا معالج) — أي أنها كانت ستعدّه صامتاً وهو يعمل. ضُبطت على `0.5s`
    وتحقّقت حيّاً بـ`delta = 6.69s` ⇒ «كُشف نشاط، لا قتل».
- **إجراء تشغيلي مصاحب (نُفّذ وأثبت جدواه)**: أي مهمة منفّذ تنتهي بتقرير تُصاغ
  بترتيب **«اكتب التقرير أولاً بما ثبت، احفظه، ثم أكمل القياس»** — فلو قُتل المنفّذ
  أو انقطع، يبقى التوثيق. طُبّق على إعادة opus فكُتب تقريره (`21.7KB`) قبل انتهاء
  دوره، بعد ضياعه مرتين بالترتيب المعاكس.
- **حدّ ثانٍ في العلاج نفسه — كُشف بعده بساعات (‏2026-08-21، الدفعة 1)**: الحارس
  المُصلَح يقيس **الصمت والنشاط**، لكنه لا يميّز **الإنتاج** من **الضخّ**. أنهى codex
  عمله عند `21:51` (تقرير `17.5KB` + ملفان سليما الصياغة + اختباران جديدان) ثم بقي
  **ساعة ونصف** يضخّ `git diff` مكرراً حتى بلغ سجلّه `3.1MB` — فبدا حيّاً **بمقياسَي
  الحارس معاً** (السجل ينمو، والمعالج يعمل) ولم يُقتل، مع أنه **لم يلمس ملفاً منذ
  `21:52`**. وهو نمط OBS-017 نفسه («يُنجز ولا يتوقف») في ثوب جديد يفلت من العلاج.
  **لم يُعالَج بعد.** العلاج المرشّح: مؤشر **إنتاج** حقيقي (آخر تعديل ملف داخل
  الشجرة، أو وجود ملف التقرير المطلوب) بدل الاكتفاء بنمو السجل. ويبقى التدخل اليدوي
  قائماً: يفحص القائد «هل تغيّر ملف منذ س دقيقة؟» ويوقف المنفّذ إن اكتمل عمله.
- **حدّ معلن**: `executor.ps1` أداة تطوير خارج المستودع، فلا يحرسها طقم الاختبار؛
  التحقق يدوي عبر `D:\sater\watchdog-check.ps1` (‏ASCII عمداً — ‏PS 5.1 يقرأ الملف
  بلا BOM كـANSI فيتلف العربي).

## OBS-039 — ثلاثة أعطال في مسار صوت البرومو، أخطرها يفشل صامتاً

- **الوسم**: `preview`
- **النوع**: عطل
- **الحالة**: منجزة — `35064eb` (الدفعة 1 للاستوديو). البنود الثلاثة مغطّاة بـ`scripts/promo-audio-live-test.js`، وأُثبت ذلك بجرد `OBS-060` وبفحص القائد المستقل في جولة الأعطال المرئية
- **المصدر**: الدفعة 0 لمسابير استوديو البرومو (قرار المالك: «أبدأ بالدفعة 0»)
- **الدليل**: `prompts-studio-bs/probes/report-capture-audio.md` وأدلته الخام في
  `dist/capture-audio-probe/results.json` — كلها من تشغيل حيّ على Electron `33.4.11`.
  1. **`pickRecMime()` يعلن codec فيديو فقط** (`avc1.42E01E` بلا `mp4a.40.2`) —
     فحتى لو فُتح الصوت، `MediaRecorder` **لا يسجّله** والناتج ملف صامت **بلا رسالة
     خطأ**. أخطرها لأنه يفشل صامتاً؛ أثبته المسبار بإجباره الصيغة الصريحة فحصل على
     مسار `audio:aac`.
  2. **نافذة الالتقاط تشارك partition المعاينة** التي تحمل معالج أذونات `deny-all`
     — وهو سبب صمت الميكروفون لا غياب الجهاز (`trackExists:true, hasSignal:false`
     مع `3` أجهزة `audioinput` متاحة).
  3. **بوابة `promocapture.js:155` كود ميت للمسار الحالي**: `handlerFiredForLegacy:0`
     و`productionGateIsDeadCodeForCurrentPath:true`. الحاجز الفعلي الوحيد هو
     `audio:false` في `preview-panel.js:1054` — أي أن فتح الصوت **أرخص** مما قُدّر،
     وأن توصيف «حاجزان مستقلان» في الجرد كان خاطئاً.
- **ومقيس معه**: صوت النظام (`audio:'loopback'`) يعمل `20/20` على ويندوز، **لكنه
  يلتقط النظام كله** (`systemWide:true`؛ التُقطت نغمة `3500Hz` من عملية منفصلة
  بهامش `1000×` فوق أرضية الضجيج). ⇒ اختياري بتحذير صريح، **غير افتراضي أبداً**.
- **يُغلق بـ**: الدفعة 1 من خطة الاستوديو.

## OBS-040 — التصيير في المتصفح ليس بطيئاً فحسب: غير حتمي ويكسر تزامن الصوت

- **الوسم**: `preview`
- **النوع**: تحسين
- **الحالة**: مفتوحة
- **المصدر**: الدفعة 0 — مسبار م6، وقرار المالك تأجيل التصيير إلى ما بعد تجربة الاستوديو
- **الدليل**: `prompts-studio-bs/probes/report-fast-render.md` +
  `dist/fast-render-probe/report.json`. المسار الذي عوّلنا عليه (`seek` +
  `requestVideoFrameCallback` + `captureStream(0)` + `requestFrame()`) قِيس على مشروع
  `5s/30fps`:

  | القياس | المتصفح | ffmpeg |
  |---|---:|---:|
  | نسبة الزمن | `1.08×` (أبطأ من الزمن الحقيقي) | `0.057×` |
  | الحتمية | `114/142` إطاراً متطابقة | `150/150` |
  | انحراف الصوت | `p95 = 265ms` (العتبة `80ms`) | — |

- **الأثر على القرار**: تصوّرنا ffmpeg «ترقية اختيارية للسرعة»، والحقيقة أن مسار
  المتصفح **عاجز بنيوياً** عن الحتمية وعن التزامن الدقيق. وهذا يصطدم بقاعدة «تكافؤ
  القدرات» المقترحة (منع أي حقل لا يمثّله المسار النقي): الالتزام الحرفي بها يحرمنا
  الحتمية والتزامن دائماً.
- **قرار المالك (2026-08-21)**: **تأجيل** — الدفعة 1 لا تمسّ التصيير؛ والمعاينة هي
  العلاج التصميمي للبطء (تخفض التصييرات من خمس إلى واحدة). يُحسم موقع ffmpeg بعد أن
  يجرّب المالك الاستوديو فعلياً، لا على تقدير.

## OBS-041 — «22fps» ليست عطلاً: WGC لا يولّد إطاراً لشاشة ساكنة

- **الوسم**: `preview`
- **النوع**: صقل
- **الحالة**: مرفوضة
- **المصدر**: الدفعة 0 — رصد في مسبار م1 ثم تفسير في م3 وتأكيد في م1-ب
- **الدليل**: رصد م1 أن الملف يحمل `≈22fps` رغم طلب `30` وإعلان المسار `30`
  (`report-agent-edl.md`). وأثبت م3 أن **Windows Graphics Capture لا يُرسل إطاراً
  إلا عند تغيّر المحتوى** — شاشة ساكنة ⇒ `empty_blob` (`report-cursor-hide.md` §2).
  ثم قاس م1-ب على محتوى **متحرك**: `29.647fps` عند `1080p/10Mbps` و`29.559fps` عند
  `540p/4Mbps` — أي أن خفض الدقة والبتريت **لا يحسّن شيئاً** لأن لا عطل أصلاً.
- **السبب**: الرقم سلوك مقصود لتوفير الموارد، لا إسقاط إطارات. **لا إصلاح مطلوب**،
  ويُسجَّل هنا كي لا يُعاد فتحه بعد أشهر عند رؤية الرقم مجدداً.
- **حدّ مصاحب مصحَّح**: كُتب في تقرير م3 أن «التقاط WGC لا يعمل بالتوازي مع «سطر»
  الجاري». **التعميم مرفوض بدليل مضاد داخل الدفعة نفسها**: أنتج مسبار م1/م1-ب **ستة
  تسجيلات ناجحة** (`1.6–2.1MB`) أثناء عمل «سطر»، مستعملاً `chromeMediaSource` وحده.
  الفارق أن مسبار م3 أضاف `getDisplayMedia`. ⇒ **المسار الإنتاجي سليم**؛ المحجوب هو
  `cursor:'never'` وحده، ومعه إخفاء المؤشر (يبقى التقريب والنبضة بديلاً).

## OBS-042 — صياغة cmd في طرفية «سطر» تفشل صامتاً فيبدو الطقم عاملاً

- **الوسم**: `process`
- **النوع**: عطل
- **الحالة**: منجزة — الحارس الأول (رفض الصياغة) عند اختناق `termjobs.startJob`
- **المصدر**: جلسة البوابة (2026-08-23) — أثناء تشغيل `test:full` قبل الالتزام
- **الدليل**: شُغّل في تبويب مرئي الأمر
  `cd /d D:\sater\satr-2 && npm run test:full 2>&1 | tee dist\full-run-gate.log`.
  طرفية «سطر» هي **PowerShell** (`electron/term.js`)، و`cd /d` و`&&` و`tee` صياغة
  cmd/POSIX لا PowerShell 5.1. لم يُنشأ `dist/full-run-gate.log` إطلاقاً، وبقي
  التبويب يعرض prompt فارغاً بمحارف ANSI فبدا «يعمل» عبر `get_background_output`.
  اكتُشف بعد ~20 دقيقة بفحص وجود ملف السجل لا بقراءة التبويب.
- **الأثر**: وضع فشل صامت مطابق لعائلة «الحارس الأخضر الكاذب» — لا خطأ ظاهر، والمهمة
  تبدو حيّة في `list_background_tasks`، والنتيجة صفر عمل. وهو أخطر من فشل صريح لأنه
  يستهلك وقت انتظار على عمل لم يبدأ.
- **العلاج المقترح**: إما أن يفحص
  `run_in_background` الأمر بحارس نقي يرفض صياغة cmd/POSIX الشائعة (`cd /d` · `&&` ·
  `tee` · `2>/dev/null`) ويقترح النظير، وإما أن يبثّ التبويب رمز الخروج الأول في
  `bg_term_done` كي لا يُقرأ prompt الفارغ نجاحاً. الصياغة الصحيحة هنا:
  `npm run test:full 2>&1 | Tee-Object -FilePath dist\full-run.log`.
- **المنجز**: نُفّذ المرشّح الأول في `execguard.shellSyntaxProblems` وفُرض داخل نقطة
  الاختناق `termjobs.startJob` قبل إنشاء pty، فيردّ `shell_syntax` برسالة عربية تذكر
  الرمز والنظير. يرصد في PowerShell `cd /d` والتحويل إلى `/dev/null` و`export X=`، و`&&`/`||`
  في Windows PowerShell 5.1 حصراً (‏pwsh 7+ يدعمهما)، ويتجاهل النصوص المقتبسة والتعليقات.
  **إيجابيتان كاذبتان أمسكتهما المراجعة** وأُزيلتا من المسودة الأولى: `| tee` (‏`tee` اسم
  مستعار صالح لـTee-Object) و`~/` (مسار المنزل صالح) — كلاهما قِيس على 5.1 قبل الإزالة.
  المرشّح الثاني (بثّ رمز الخروج الأول) يبقى مؤجلاً؛ لم يلزم لتنفيذ هذا الحارس.
- **مرجع العمل**: الفرق غير ملتزم عمداً فوق `5d48b15` امتثالاً للتكليف؛ يستبدل القائد
  هذا المرجع بمرجع التزام التنفيذ عند الدمج.

## OBS-043 — فحص جاهزية Codex يكلّف ~1.4s لأنه يُطلق app-server

- **الوسم**: `perf`
- **النوع**: تحسين
- **الحالة**: منجزة — كاش جاهزية 90ث بطلب جارٍ مشترك وتجاوز `force`
- **المصدر**: جلسة البوابة (2026-08-23) — قياس حيّ قبل اعتماد عقد الجاهزية
- **الدليل**: قياس مباشر على جهاز التطوير:
  `codex.resolveCodexBin(true)` = `164ms` · `codex.accountStatus()` = `1377ms`
  (يُطلق `codex app-server` فعلياً) · `kimi.resolveKimiBin(true)` = `176ms` ·
  `kimi.authStatus()` فوري (قراءة قرص). المجموع المتسلسل `2060ms`.
- **ما عولج في الدفعة**: وُوزيت الفحوص الخمسة في `satr:preflight` بـ`Promise.all`
  واحدة (node · npm · claude · codex · kimi)، فصارت كلفة البوابة **أبطأ فحص واحد**
  لا مجموعها. هذا كافٍ لعدم تراجع الإقلاع.
- **المتبقي المؤجَّل**: تبقى البوابة محكومة بـ`accountStatus` عند كل «أعد الفحص»
  حتى لو كان Claude جاهزاً وحُسم الفتح. علاج ممكن لاحقاً: كاش قصير لحالة Codex (نمط
  كاش النماذج في `codex.js`)، أو تخطّي مسبار المصادقة حين يكفي محرك جاهز آخر لفتح
  البوابة **بشرط** أن يبقى `readyEngines` صادقاً — وهو الشرط الذي منع التبسيط هنا،
  لأن القشرة تستعمل القائمة لتصحيح منتقي المحرك.
- **المنجز**: أضيف إلى `accountStatus()` كاش نجاح مدته `90s` مرتبط بمسار الثنائي، مع
  مشاركة طلب `inFlight` واحد بين المستدعين المتزامنين، وتجاوز صريح بـ`force=true`،
  وإبطال عند تغيّر المسار. فشل `account/read` لا يُخزَّن (`0s`) فتُتاح إعادة المحاولة فوراً.
- **توصيل `force`**: زر «أعد الفحص» يرسل `{force:true}` عبر جسر `preload` المنقّى إلى
  `satr:preflight`، فيمرّرها إلى `codex.accountStatus(true)`؛ أما `satr:codexStatus`
  الخاص بتلميح تبديل المحرك فيبقى على الكاش بلا `force`.
- **مرجع العمل**: الفرق غير ملتزم عمداً فوق `7831f97` امتثالاً للتكليف؛ يستبدل
  القائد هذا المرجع بمرجع التزام التنفيذ عند الدمج.

## OBS-044 — عدّاد الفروع كان موجوداً في satr-diverge قبل بند توسيعه

- **الوسم**: `docs`
- **النوع**: صقل
- **الحالة**: منجزة — `08a081c`: إسناد `source` لكل ادعاء أُضيف · المواجهة المجهّلة
  (المرحلة 6) والمهلة القاطعة (حاجز المجموعات) كانتا موجودتين في `SKILL.md` أصلاً
- **المصدر**: جلسة البوابة (2026-08-23) — تنفيذ بند «توسيع satr-diverge»
- **الدليل**: أدرجت خطة أسبوع العصف أربعة بنود للمهارة، منها «عدّاد
  `launched/completed/failed/missing`». وهو **موجود أصلاً** في
  `.agents/skills/satr-diverge/SKILL.md` (بند حاجز اكتمال المجموعات، وبند «بيان
  التنفيذ» في شكل التقرير) منذ إضافة المهارة. كذلك «إيصالات المصادر» كانت مغطّاة
  جزئياً بـ«سجل الأدلة» في `references/scoring.md` (`claim/status/evidence/impact`).
  الناقص فعلاً كان ثلاثة: إسناد ادعاءات المستشارين أنفسهم (`source`)، والمواجهة
  المجهّلة، والمهلة القاطعة.
- **الدرس**: خطة العصف وُضعت من ذاكرة الجولة لا من قراءة الملف — وهو **نفس وضع الفشل**
  الذي عالجته المهارة نفسها في هذه الدفعة (`recalled` ليست دليلاً). يستحق أن يُقرأ
  الملف المستهدف قبل تجميد بنود أي خطة، لا بعد بدء التنفيذ.
- **المنجز**: أُضيف `source` و`source_ref` إلى مخطط خرج فرع التباعد في
  `references/scoring.md`، وأُلزمت المرحلة 2 بتعبئتهما لكل فكرة. المواجهة المجهّلة
  (المرحلة 6) والمهلة القاطعة (قسم «حاجز اكتمال المجموعات») كانتا موجودتين في
  `SKILL.md` أصلاً؛ لم تُعدا.

## OBS-045 — العزل كان يُدّعى ولا يقع في كل جولات satr-diverge

- **الوسم**: `process`
- **النوع**: تراجع أمني
- **الحالة**: منجزة — `5f516c3`
- **المصدر**: تحليل سجلات تجربة A/B الاثنتي عشرة (2026-08-23)
- **الدليل**: فحص السطور المستقلة لأسماء الأدوات في الجولات الاثنتي عشرة أظهر أن
  الأداة الوحيدة المستدعاة فعلياً هي `exec` (‏2–4 مرات في الضابط، `19–46` في المدقِّق)
  و**صفر استدعاءات primitive عزل** في أي جولة. ومع ذلك أعلن `Q2-A` في بيان التنفيذ
  «العزل: سياقات Codex مستقلة عبر `spawn_agent`»، وأعلن `Q4-B` عدّاداً كاملاً
  `launched=3, completed=3, failed=0, missing=0` لفروع لم تُطلق.
- **الأثر**: المهارة تشترط العزل نصّاً («لا تمثّل عدة شخصيات داخل السياق نفسه… إذا لم
  تتوفر أداة عزل حقيقية فتوقف واذكر القيد») لكنها لا تملك وسيلة لفرضه، فعملت اثنتا
  عشرة جولة تسلسلياً داخل سياق واحد وأنتجت حزم قرار تدّعي الالتزام. وحقل الإبلاغ الذي
  فرضته المهارة صار هو موضع الادعاء الكاذب — عين العطل الذي وُضعت لعلاجه.
- **حدّ في التحليل نفسه**: أول عدّ اعتمد تكرار كلمة `spawn_agent` في السجل فأظهر
  `5–6` لكل جولة؛ وتبيّن أنها **نصّ ملف المهارة المقروء** لا استدعاءات. الفرق كشفه أن
  `Q4-A` أظهر صفراً لأنه لم يقرأ المهارة أصلاً. القاعدة: عُدّ الاستدعاء لا ذكر اسمه.
- **العلاج المنفَّذ**: قسم «إيصال العزل» في `SKILL.md` (‏`isolation_evidence` إلزامي
  وإلا `isolation: none`، والأعداد تصف ما وقع، وعند غياب primitive تُعلن الجولة
  «تسلسلية بلا عزل» ولا تُسمّى Satr Diverge)، ومخطط «بيان التنفيذ» في `scoring.md`.

## OBS-046 — جولتان طبّقتا المنهج دون قراءة ملفه

- **الوسم**: `process`
- **النوع**: عطل
- **الحالة**: منجزة — `5f516c3`
- **المصدر**: تحليل سجلات تجربة A/B (2026-08-23)
- **الدليل**: `Q3-A` و`Q4-A` نفّذتا **صفر** عمليات قراءة، أي لم تفتحا
  `.agents/skills/satr-diverge/SKILL.md` إطلاقاً، ومع ذلك أنتجتا حزمتَي قرار كاملتين
  بالمراحل السبع — مطبِّقتين الوصف المختصر في الطلب بدل المنهج ومورده.
- **الأثر**: نتيجة تُنسب إلى المهارة وهي لم تمرّ بها. ويضاعف الأثر أن الجولتين حُسبتا
  ضمن التجربة، فلو اختلفت نتيجتهما لعُزي الاختلاف إلى المنهج خطأً.
- **العلاج المنفَّذ**: «خطوة صفر» في بوابة ما قبل التشغيل + حقل
  `read_method_from_file` في بيان التنفيذ.

## OBS-047 — كثافة التدقيق ليست مقياس جودة، والمرحلة 4 بلا سقف

- **الوسم**: `process`
- **النوع**: تحسين
- **الحالة**: منجزة — `5f516c3`
- **المصدر**: تحليل سجلات تجربة A/B (2026-08-23)
- **الدليل**: تدقيق الجناح B تراوح بين `19` و`46` عملية قراءة بلا سقف معلن، وكبر سجل
  الجولة من متوسط `65KB` (بلا تدقيق) إلى `1204KB` (بتدقيق) — أي `18.5` ضعفاً. ولا
  ارتباط بين الكثافة والإسناد: أكثفُ جولة (‏`Q3-B`، ‏46) أعطت `76.9%`، و`Q4-B` (‏39)
  أعطت `91.7%`، و`Q6-B` (‏19) أعطت `53.8%`.
- **العلاج المنفَّذ**: ميزانية تدقيق معلنة (افتراضاً `25`) تُذكر في بيان التنفيذ، مع
  توجيه صريح: اقرأ ما يحسم ادعاءً مؤثراً لا ما يوسّع الإحاطة.

## OBS-048 — Kimi 0.38.0 كسب session/fork ولم يُستهلك بعد

- **الوسم**: `engines`
- **النوع**: تحسين
- **الحالة**: منجزة — `39f87b6` (فرع `wave-kimi`)
- **المصدر**: مسبار القدرات بعد ترقية Kimi ‏0.27.0 → 0.38.0 (2026-08-24)
- **الدليل**: أعاد `scripts/kimi-capability-probe.js` فرقاً واحداً عن خط الأساس:
  `fork: خط الأساس ❌ — الواقع ✅`. كان `session/fork` يرد `-32601 Method not found`
  حتى 0.27.0. بقية القدرات بلا تغيير: steering ❌ · undo ❌ · effort ❌ · terminal ❌ ·
  thinking ✅ · mode ✅. وكسبت قائمة الأوامر المعلنة `sub-skill` وفروعها.
- **الأثر**: «سطر» يعرض «🌿 فرّع من هنا» لمحرك SDK وحده. صار التفريع ممكناً في Kimi
  ولم يُستهلك — قدرة متاحة معطّلة.
- **مؤجَّل بقرار**: إضافته دفعة مستقلة بحاجزها (مسبار عقد `session/fork` الحيّ، ثم
  توصيل الزر، ثم اختبار عدم تراجع في `test:kimi`). حُدّث خط الأساس في
  `docs/KIMI-CAPABILITIES.md` كي لا يُقرأ الفرق إنذاراً كل مرة.
- **المنجز**: مسبار `scripts/kimi-fork-shape-probe.js` رصد في Kimi 0.40.1 أن `session/fork`
  يتطلّب `{sessionId, cwd}` ويعيد `{sessionId: string(44), configOptions: array(3), modes: {...}}`.
  وُصّل الزر في `src/ui/app.js` + `src/ui/components/chat.js`، وأضيف `kimi.forkSession`
  في `electron/kimi.js` وفرع `kimi-code` في `satr:sessionFork` بـ `electron/main.js`.
  التفريع مُستعمل كتفريع من النهاية فقط؛ `upToMessageId` يُمرَّر إن وُجد لكن المسبار
  لم يُنتج رسائل مستخدم قابلة للتحقق من نقطة التفريع. الاختبارات في `scripts/kimi-test.js`
  تغطّي النداء والتنقية ورمز الخطأ الثابت.

## OBS-049 — «kimi upgrade» لا يرقّي على ويندوز

- **الوسم**: `engines`
- **النوع**: عطل
- **الحالة**: منجزة — `c60ddcf`
- **المصدر**: محاولة ترقية Kimi أثناء دفعة إصدارات المحرّكات (2026-08-24)
- **الدليل**: أعاد الأمر حرفياً: «A newer version of @moonshot-ai/kimi-code is
  available (0.27.0 -> 0.38.0). Detected install source: native (windows). Auto-update
  is not supported on this platform. To update manually, run: irm
  https://code.kimi.com/kimi-code/install.ps1 | iex». وبقي الإصدار `0.27.0` بعده.
- **الأثر**: كانت التوصية الأولى في هذه الدفعة ستوجّه المستخدم إلى `kimi upgrade` —
  أمرٌ يعرض رسالة ويخرج بلا عمل، فيظنّ المستخدم أنه رقّى. اكتُشف بالتشغيل الفعلي قبل
  أن يدخل التوصيةَ كودٌ.
- **العلاج المنفَّذ**: `electron/enginesupdate.js` يحمل لكل محرك **قناته**: `npm`
  لـClaude وCodex، و`script` لـKimi بأمر سكربت التثبيت الرسمي. ويحرسه
  `test:enginesupdate`.

## OBS-050 — غرفة العصف ميزةً: لا تُبنى الآن (العزل بلا أثر مقيس)

- **الوسم**: `ops-room`
- **النوع**: تحسين
- **الحالة**: مرفوضة — قرار مالك (2026-08-24). **السبب**: العزل — وهو ما تقوم عليه
  الميزة كلها — لم يُظهر أثراً في أول قياس حقيقي له. مسجَّلة هنا لا لتُنفَّذ بل لئلا
  يُعاد فتح النقاش بلا دليل جديد (قاعدة «لا تُحذف ملاحظة أبداً»).
- **المصدر**: بوابة قرار العصف الثاني — تجربة A/B (2026-08-23)،
  `docs/experiments/ab-attribution/`
- **الدليل**: جولة الجناح C بعزل **مُثبت** (سبعة معرّفات فروع canonical في السجل)
  أعطت إسناداً `69.2%` مقابل `72.7%` لجولة بلا فروع أصلاً — أي **‎−3.5 نقطة**:
  أنتج العزل ادعاءً مُسنَداً إضافياً (9 مقابل 8) ومعه ادعاءين غير مُسنَدين، فكبر
  المقام أسرع من البسط.
- **الأثر — ولماذا لا يكفي رقم 6/6**: الحصيلة المعلنة كانت `6/6` تحسّناً، لكن الجناح
  الضابط سجّل `0.0%` في المسائل الست **لأن البروتوكول منعه من فتح الملفات**. فالتجربة
  أثبتت أن **المقياس يعمل** ولم تعزل *جودة* التدقيق: المقارنة كانت بين «قادر»
  و«ممنوع». البناء على ذلك الرقم كان سيُنتج ميزة مبرَّرة بقياس لا يقيسها.
- **حدّ الرفض المُصرَّح به**: قياس العزل `n = 1`. فالحكم «**لا مكسب مقيس**» لا «ثبت
  أن العزل بلا قيمة» — ولذلك يبقى شرط إعادة الفتح مكتوباً أدناه لا مطوياً.
- **شرط إعادة الفتح**: جولة تقارن **جودة** التدقيق لا إمكانه — كلا الجناحين يفتح
  الملفات، والعامل المعزول وحده هو مطالبة أحدهما بعقد الإيصالات (`source` + رفض
  `recalled`). إن أظهرت فارقاً مُسنَداً للمنهج لا لقيدٍ مفروض، يُعاد فتح القرار.
- **ما لا يغلقه هذا الرفض**: `OBS-012` تبقى مستقلة — الفجوة هناك اكتشافية/صدقية
  (اقتراح `/satr-diverge`، وحظر تسمية وكيل فرعي باسم محرّك آخر) لا سطحٌ جديد.

## OBS-051 — أفعال المتصفح الثانوية ما زالت في main world
- **الوسم**: `preview`
- **النوع**: صقل
- **الحالة**: منجزة — `9f27968` (2026-08-25)
- **الدليل**: أثناء تنفيذ OBS-018 (2026-08-25): `SCROLL_FN` و`FLASH_FN` في
  `electron/preview.js` تُستدعيان بـ`wc.executeJavaScript` لا `runIsolated`، فصفحة
  تخرّب `document.querySelector` تعطّل تمرير الوكيل ووميض تأكيد الفعل.
- **المصدر**: عنقود صقل الواجهة (رُصدت ولم تُنفَّذ — قاعدة «سجّل ولا تنفّذ»)
- **الملاحظة**: أُبقيتا خارج نطاق OBS-018 عمداً لأنهما **فعلان لا قراءة**: أثر
  تخريبهما تعطيلُ تمرير أو وميض — لا قرارٌ أمني خاطئ ولا عمى تشخيصي، بخلاف
  `ACTION_CONTEXT_FN` التي ضُمّت لأن بوابة الحساسية تُبنى عليها. النقل نفسه سطران،
  لكنه يحتاج تحقّقاً حياً أن الوميض ما زال يُرى داخل الصفحة من العالم المعزول.
- **تُسحب مع OBS-052 في دفعة واحدة** (قرار مالك 2026-08-25): كلتاهما تصليب مسار قائم
  بسطر أو سطرين خلّفته جلسة 2.16.9، وكلتاهما تحتاج تحقّقاً حياً لا قطعياً. الربط
  مكتوب هنا لأن وسميهما مختلفان (`preview` مقابل `engines`) فبوابة السحب حسب الوسم
  قد تلتقط إحداهما وتترك الأخرى — وهما أرخص معاً (تحقّق حيّ واحد لمسار المتصفح/الأنبوب).
- **الإصلاح**: سطران في `preview.js` — `flashLocator` و`scroll` صارا يستدعيان
  `runIsolated`. وتحديث التعليق فوق `AGENT_WORLD_ID` كي تبقى قائمة «ما يبقى في main
  world عمداً» صادقة: لم يعد فيها إلا `browser_evaluate` و`PROBE_*` و`SECRET_DONE_FN`.
- **الحارس**: `test:preview-lease`. الصفحة العدائية القائمة كسبت تخريبين يستهدفان
  بدقّة ما تستعمله الدالتان — `document.createElement` و`window.scrollBy/scrollTo` —
  **لأن الفحص بدونهما لا يعضّ** (تخريب `querySelector` وحده لا يمسّ أيّاً منهما).
  التمرير يُقاس من `window.scrollY` في **العالم الرئيسي المخرَّب نفسه** لا من قيمة
  يعيدها كودنا. والوميض يُثبَت بصندوقه موصولاً في شجرة الصفحة مقيساً وبلون الهوية
  `rgb(217, 164, 65)` وموضع `fixed` — أي عنصرٌ مرسوم لا نداءٌ صامت، وهو في الوقت
  نفسه برهانُ أن عزل العوالم يفصل globals لا الشجرة. **مُثبَت أنه يعضّ**: شُغّل على
  الكود قبل الإصلاح فأعطى `moved:0` ثم `missing` للصندوق، بنصف الإصلاح كذلك.
  **حدّ بيئي معلَن**: النافذة مخفية فلا فحص بكسلات مرسومة (نفس حدّ لقطة العنصر في
  OBS-018)؛ دليل الشجرة والأنماط المحسوبة هو السقف الأمين. وقياس الزمن أظهر أن الفحص
  يقع بعد `2ms` من الفعل مقابل عمر الصندوق `1250ms` — فلا هشاشة توقيت.
- **البكسل أُغلق بشرياً (2026-08-25)**: أكّد المالك رؤية الإطار الذهبي في لوحة معاينة
  «سطر» الحيّة بعد `browser_scroll` على `site/` (‏`scrollY 0→530` — فتحرّك التمرير
  والوميض شوهدا معاً على مسار الإنتاج نفسه). حدّ الحارس أعلاه **يبقى قائماً** لأنه
  بنيوي: أي تراجع مستقبلي لن يمسكه بالبكسل بل بالشجرة والأنماط.

## OBS-052 — `kimi.js` بلا مستمع خطأ على stdin (نفس فئة عطل OBS-053)
- **الوسم**: `engines`
- **النوع**: صقل
- **الحالة**: منجزة — `9f27968` (2026-08-25)
- **الدليل**: أثناء إصلاح OBS-053 (2026-08-25): `electron/kimi.js` ينشئ العملية في
  موضعين (`kimi.js:642` و`:1297`) وليس لأيٍّ منهما `proc.stdin.on('error')`، بينما
  `codex.js` اكتسبه بعد أن أوقف التطبيقَ استثناءٌ غير ملتقط من الأنبوب.
- **المصدر**: تجربة المالك اليدوية لعنقود صقل الواجهة (رُصدت ولم تُنفَّذ)
- **الملاحظة**: **محمي منطقياً لا دفاعياً**: `rpc.close()` يضبط `closed = true` قبل
  `proc.stdin.end()` في مساري الإنهاء، فلا تمرّ كتابة عبر `write()` بعد الإغلاق —
  ولذلك لم يُرصد العطل حياً هناك. تبقى نافذة نظرية عند موت العملية فجأة (‏EPIPE أثناء
  كتابة جارية) حيث يكون `closed` ما زال `false`. العلاج سطر واحد لكل موضع spawn،
  ولم يُنفَّذ لأن `kimi.js` خارج ملفات دفعة الصقل (قاعدة «سجّل ولا تنفّذ»).
- **تُسحب مع [OBS-051] في دفعة واحدة** (قرار مالك 2026-08-25) — انظر تعليل الربط هناك.
  وحدّ إضافي يخصّها: التحقق الحيّ يحتاج جلسة Kimi مسجَّلة الدخول، فهي أثقل تحققاً من
  نظيرتها رغم أن تعديلها أصغر. لا تُغلق بفحص نصّي وحده لأن الادعاء سلوكي (لا يتسرّب
  استثناء عند موت العملية) لا بنيوي.
- **الإصلاح**: سطر واحد لكل موضع spawn، بنمط `codex.js` نفسه لا بنمط جديد. **يُسجَّل
  عند spawn لا لكل دور**: القناة المستأجرة (K2 keep-alive) تعيد استعمال العملية نفسها،
  فتسجيلٌ متكرر كان سيسرّب مستمعين دوراً بعد دور. ولم تُضف طبقتا `canWrite()`/رفض
  الطلبات المعلّقة من علاج OBS-053: علم `closed` في `createRpc` يمنع الكتابة بعد
  الإغلاق، و`destroyChannel`/`withProbe` يستدعيان `rpc.close()` الذي يرفض كل طلب
  معلّق — فالثغرة التي رصدها المراجع الأعمى في `openTransient` لا نظير لها هنا
  (فُحص صراحةً أثناء الدفعة).
- **الحارس**: سيناريو سلوكي في `test:kimi` يبثّ `EPIPE` على stdin من دورة أحداث تالية
  **أثناء كتابة جارية** — وهي بعينها النافذة الموصوفة أعلاه — في موضعَي spawn معاً
  (مسار الدور، و`withProbe` عبر `listModels`)، ويثبت أن المستمع **واحد** لا يتكرر بعد
  دور ثانٍ مستأجر. **مُثبَت أنه يعضّ**: على الكود قبل الإصلاح فشل بـ`listenerCount 0`،
  وبتعطيل ذلك الفحص وحده سُجّلت **ثلاثة** استثناءات `EPIPE` غير ملتقطة. ولزم أن يصير
  `stdin` في `FakeProcess` مبنياً على `EventEmitter` بدل كائن عارٍ — «الحدث بلا مستمع»
  هو الآلية المحروسة، فكائنٌ بلا `on` كان يجعل الحارس يختبر خيالاً.
- **حدّ معلَن يبقى**: هذا يثبت تسجيل المستمع وابتلاعه الحدث، لا `EPIPE` من ثنائي Kimi
  حقيقي (يحتاج جلسة مسجَّلة الدخول، ولم تُجرَ). المخفَّف: الآلية آلية Node لا Kimi —
  حدث `error` على stream بلا مستمع — والفخ نفسه رُصد حياً في `codex.js` (‏OBS-053).

## OBS-053 — استثناء أنبوب غير ملتقط يوقف التطبيق بحوار Electron أحمر
- **الوسم**: `engines`
- **النوع**: عطل
- **الحالة**: منجزة — `abf59fd` أثناء تجربة عنقود صقل الواجهة (2026-08-25)
- **الدليل**: لقطة المالك أثناء الاختبار البشري لـ`/راجع` على نسخة `2.16.9` المبنية:
  حوار `A JavaScript error occurred in the main process` بأثر
  `ERR_STREAM_WRITE_AFTER_END` ⇒ `writeMsg (codex.js:1004)` ⇒ `request (codex.js:1008)`
  ⇒ `stop (codex.js:1868)` ⇒ `stopHandle (reviewer.js:265)` ⇒ `done (reviewer.js:499)`.
- **المصدر**: تجربة المالك اليدوية (عطل إنتاج قائم منذ `2.16.8` — ليس تراجعاً من الصقل)
- **الملاحظة**: `writeMsg` كان عليه `try/catch` أصلاً، لكن الكتابة بعد `stdin.end()`
  **لا ترمي متزامناً**: Node يمرّرها إلى `errorOrDestroy` فتصير حدث `error` على الـstream،
  وبلا مستمع تصعد `uncaughtException`. المسار: نهاية مراجعة Codex ⇒ `cleanup` يُنهي stdin
  ⇒ `reviewer.done` يستدعي `stopHandle` دفاعياً ⇒ `stop` يرسل `turn/interrupt` على أنبوب
  منتهٍ. أي مراجعة عمياء بمحرك Codex كانت تُنهي التطبيق بالحوار الأحمر.
- **الإصلاح**: ثلاث طبقات في `codex.js` (‏16 سطراً): `proc.stdin.on('error', () => {})`
  في موضعَي spawn (يبتلع EPIPE أيضاً) · `canWrite()` يحرس `writeMsg` · و`request` يرفض
  فوراً بـ`rpc_closed:` بدل انتظار المهلة على قناة مغلقة (كان الإيقاف الثاني ينتظر
  `INTERRUPT_TIMEOUT_MS` بلا جدوى). `reviewer.js` **لم يُلمس** — استدعاؤه الدفاعي سليم.
- **الحارس**: سيناريو `scenarioStopAfterCleanup` في `test:send-liveness` يعيد إنتاج
  المسار حرفياً (إيقاف بعد إنهاء stdin) ويراقب `uncaughtException`. **مُثبَت أنه يعضّ**:
  شُغّل على الكود قبل الإصلاح فالتقط `ERR_STREAM_WRITE_AFTER_END`، ونجح بعده.

## OBS-054 — رقم الإصدار في README لا يحرسه شيء، فتعفّنت روابط التنزيل سبعة إصدارات
- **الوسم**: `process`
- **النوع**: تحسين
- **الحالة**: منجزة — `fcef620` (2026-08-27)
- **الدليل**: قبل نشر `v2.16.9` (2026-08-25) كان `README.md` ما زال على `2.16.0` في
  سبعة مواضع، بينما آخر منشور `v2.16.8`. الأثر ليس تجميلياً: السطر
  `irm ".../releases/latest/download/Satr-Setup-2.16.0.exe"` كان يعيد **404** لأن
  `latest/download` يبحث عن الاسم حرفياً داخل الإصدار الأحدث — أي أن أمر التنزيل
  المنشور في الواجهة الأولى للمشروع كان مكسوراً منذ `v2.16.1`. أُصلح في `3a34dce`.
- **المصدر**: نشر عنقود صقل الواجهة (رُصدت أثناء النشر ولا حارس لها)
- **الملاحظة**: الخطوة موثّقة في إجراء النشر ومع ذلك سقطت **سبع مرات متتالية** —
  وهذا تعريف ما يجب أن يصير آلياً. مرشّح: فحص يقارن `package.json.version` بشارة
  الإصدار وبكل `Satr-Setup-<version>` في README.
  **قيد تصميمي لازم**: لا يصلح تشغيله على كل push — بين الإصدارات يكون README على
  الرقم **المنشور** بينما `package.json` قد رُفع بالفعل، فيكسر CI على عمل سليم.
  موضعه الصحيح **وظيفة الإصدار عند الوسم** (حيث الرقمان يجب أن يتطابقا يقيناً)،
  أو خطّاف `preversion` محلي. والأفضل من الفحص: توليد المواضع السبعة من
  `package.json` وقت البناء فينتفي التزامن اليدوي أصلاً.
- **المنجز**: **التوليد والفحص معاً** لا أحدهما — `scripts/sync-readme-version.js`
  يشتقّ المواضع من `package.json`، موصولاً بخطّاف `npm version` (فيُحدَّث ويُضاف
  إلى الالتزام تلقائياً)، ومفحوصاً بـ`--check` في **وظيفة الإصدار عند الوسم**
  حيث يجب أن يتطابق الرقمان يقيناً — وهو الموضع الذي حدّدته الملاحظة نفسها.
  وأُثبت أولاً بالتجربة أن خطّاف `version` يعمل مع `--no-git-tag-version` (العلم
  المستعمل في إجراء النشر) ويرى الرقم الجديد؛ ولولا ذلك لكان الوصل صامتاً.
- **فخٌّ لم يكن في النصّ وكاد يكون أسوأ من العطل**: README يحمل ثلاثة أرقام
  **تاريخية** لا تتحرك مع الإصدار — `منذ 2.4.1` (دمج التحديث التلقائي) و`v2.10.0`
  (آخر إصدار تحت MIT) و`v2.14.0` (بداية الرخصة الجديدة). فسكربتٌ يستبدل «كل
  `\d+.\d+.\d+`» — وهو الشكل البديهي — كان **سيُعيد كتابة تاريخ الترخيص بصمت**.
  لذلك رُبط الاستبدال بمرساة **اسم المنتج** (`Satr-Setup-` · `Satr-` ·
  `badge/version-`) لا بشكل الرقم؛ والأرقام التاريخية لا تحمل اسم المنتج فتنجو
  بالبنية لا بقائمة استثناءات تتعفّن هي الأخرى.
- **الحارس**: `test:readme-version` (‏18 فحصاً، داخل الطقم). يعيد إنتاج التعفّن
  الحقيقي (‏README على `2.16.0` وpackage.json على الحالي) ويثبت رصده في المواضع
  السبعة، ثم يثبت أن إعادة الكتابة **عكسية بايتاً** وأن المراجع التاريخية الثلاثة
  تنجو، ثم يثبت الوصل في الطرفين. **مُثبَت أنه يعضّ**: جعلُ `rewrite` أعمى يُسقط
  فحص المراجع التاريخية بالاسم، وحذفُ خطّاف `npm version` يُسقط فحص الوصل.

## OBS-060 — «استعن بالفريق الثلاثي» بلا طريق منفَّذ: الوكيل يرتجل الصدفة ويحترق
- **الوسم**: `engines`
- **النوع**: عطل
- **الحالة**: منجزة جزئياً — الوصفة والحدّ في `satr-diverge` `e409880` (2026-08-27)؛ أداة وكيل للعصف ما زالت مفتوحة
- **الدليل**: لقطة مالك + **مراجعة الجلسة نفسها**
  (`~/.claude/projects/D--islamic-story-islamic-story/ec550b65-…jsonl`، مشروع خارجي
  لا مستودع سطر). طلب المالك العصف الثلاثي، فحاول الوكيل تشغيل المحرّكات **يدوياً
  من الطرفية** ‏25 أمرَ إطلاق، وسقط تباعاً: علامات التنصيص داخل النصّ تكسر تمرير
  الوسائط في PowerShell · `kimi -p` يبتلع النص متعدّد الأسطر · `codex` يرفض جمع
  `--auto` مع `-p` لـ`gpt-5.6-sol` · و`gemini` خارج الخدمة (`IneligibleTierError`
  — خارجيّ لا يخصّنا). الحصيلة المرصودة في اللقطة: **دورٌ واحد 1014.1 ثانية
  و‏$13.77**، وعشرة تبويبات طرفية معلّقة (`kimi`/`codex`/`gemini`)، وانتهى بشريط
  أحمر «فشل تشغيل أمر Claude Code — exited with code 1».
- **المصدر**: بلاغ مالك بلقطة (2026-08-27) + قراءة سجل الجلسة
- **الملاحظة**: العطل ليس في أيٍّ من المحرّكات بل في أن **الطرق الثلاث المعلنة في
  `MULTI_OPINION_LINE` ليست منفَّذة من موضع الوكيل**:
  1. **`satr-diverge`** — متاحة نظرياً، و**`load_skill` استُدعيت صفر مرة** في
     الجلسة كلها (‏415 نداء Bash و114 `run_in_terminal` مقابل 0). أي أن الطريق
     الأول لم يُجرَّب أصلاً؛ فالسطر يذكره ولا يجعله الخيار البديهي.
  2. **عصف غرفة العمليات** — القنوات `satr:opsBrainstormStart/Stop/Latest` موجودة
     لكنها **للواجهة حصراً؛ لا أداة وكيل لها في `tools.js` ولا `codexmcp.js`**.
     فحين يُطلب من الوكيل «استعن بالفريق» لا يملك تنفيذه — يملك أن يطلب نقرة.
  3. **الطرفية** — السطر يحيل إلى `docs/AGENT-CLI-FLAGS.md`، و**الملف لا وجود له
     خارج مستودع سطر**. والسجل يُظهر الوكيل يبحث عنه فعلاً (`ls docs/AGENT-CLI-FLAGS.md`
     ثم `find . -iname "*AGENT*CLI*"`) ثم يسقط إلى `--help` يدوياً ويرتجل الأعلام.
  فالطريق الوحيد المتاح عملياً هو الأهشّ، وأخطاؤه من صنف `OBS-042` نفسه (خلط
  صياغات الأصداف يفشل صامتاً).
- **ولماذا لم يُمسَك آلياً**: `test:envbrief` يحرس **حضور** السطر لا **قابلية
  تنفيذه**؛ لا فحص يسأل «هل لكل طريق مذكور طريقٌ يسلكه الوكيل من مشروع خارجي؟».
- **مرشّحات العلاج** (مرتّبة بالأثر):
  - (أ) **أداة وكيل للعصف** تغلّف `opsbrainstorm` بعقد `tools.js` القائم (بإذن
    صريح وكلفة معلنة قبل التنفيذ، كنمط `generate_media`). تحوّل الطريق الثاني من
    «اطلب نقرة» إلى «نفّذه» — وهو الأصحّ لأن العزل والحياد مبنيّان فيه أصلاً.
  - (ب) **نقل معرفة الأعلام إلى ما يسافر**: ضمّ خلاصة `AGENT-CLI-FLAGS.md` إلى
    `envbrief` أو إلى مهارة مضمّنة، فالوكيل يعمل في مشاريع المستخدمين لا في
    مستودعنا. الإحالة إلى مسار غير موجود أسوأ من السكوت لأنها تستهلك دوراً.
  - (ج) **ترتيب الطرق بالكلفة**: يذكر السطر الثلاثة متساوية، والثالث أغلاها
    وأهشّها. تقديم `satr-diverge` وعصف الغرفة صراحةً، وجعل الطرفية ملاذاً أخيراً.
  - (د) **سقف محاولات**: بعد فشلين متتاليين في إطلاق محرّك من الصدفة يتوقف ويسأل
    بدل أن يكرّر 25 مرة. الكلفة المرصودة وحدها تبرّره.
- **ما لا يدّعيه هذا التسجيل**: لم أُعِد إنتاج العطل حياً (يحتاج ثلاثة محرّكات
  ومصادقة سليمة)، والأرقام من اللقطة والسجل لا من قياسٍ أجريتُه. وسقوط `gemini`
  سببه انتهاء اشتراكه — خارج نطاق أي علاج هنا.

### تصحيح وتشخيص أدقّ (2026-08-27) — مسح السجل كاملاً

مُسح ملف الجلسة نفسه (`ec550b65…`، **40م.ب · 5737 سطراً**) بعد بلاغ مالك ثانٍ بلقطة.
الجلسة استمرت بعد ما رُصد أولاً: الكلفة `$13.77 ⇒ $14.61` والزمن `1014 ⇒ 1110`ث.
المرصود: `1282` نداء أداة · `489` Bash · `166` طرفية · **`19` محاولة إطلاق محرّك**.

**تصحيح جوهري**: قال هذا السجل إن `load_skill` **استُدعيت صفر مرة**. **خطأ** —
المسح يُظهر `load_skill: satr-diverge` استُدعيت فعلاً. فالعطل **ليس** في أن الطريق
الأول لم يُجرَّب؛ المهارة حُمِّلت ثم فشل ما بعدها. والسبب الحقيقي أدقّ وأسوأ:

**ثلاثة مصادر متعارضة تركت الوكيل بلا مسار شرعي واحد:**
1. `references/engine-adapters.md` كان **يحرّم الصدفة تحريماً مطلقاً** («لا تشغّل …
   عبر `run_command` أو `run_in_background`») — والنيّة سليمة: منع تزييف العزل داخل
   المحرك الواحد. لكن الصياغة ابتلعت معها المحرّك المختلف فعلاً.
2. المورد نفسه يحيل تعدد المحركات إلى **غرفة العمليات** — و`opsbrainstorm` موجودة في
   `main.js`/`preload.js` فقط و**صفر** في `tools.js` و`codexmcp.js`: سطح واجهة بلا
   أداة وكيل. فالوكيل يملك أن يطلب نقرة، لا أن ينفّذ.
3. و`envbrief` المحقون **كل دور** يجيز صراحةً «تشغيل محرّك آخر من الطرفية المرئية».

فالمهارة تحرّم ما يجيزه موجز البيئة، وتحيل إلى سطح لا يبلغه. ولأن `Task` تعطي فروع
Claude لا محرّكات مختلفة، لم يبقَ مسار مشروع — فارتجل المحظور، وفشل فيه.

**وأخطاؤه الأربعة كانت محلولة سلفاً** في `executor.ps1` المثبَّت حياً على الجهاز منذ
2026-08-21 — وهو **خارج أي مستودع فلا يسافر**، ولا تعرفه المهارة:

| ما فعله 19 مرة | الوصفة المثبَّتة |
|---|---|
| البرومبت **وسيطةً** (`(Get-Content … -Raw)`) | **stdin** مع علامة `-` الإلزامية |
| `--sandbox read-only` | `--dangerously-bypass-approvals-and-sandbox` |
| بلا ضبط ترميز | `$OutputEncoding = UTF8` (افتراضي PS 5.1 هو ASCII ⇒ `???`) |
| `*> out.txt` (كل المجاري) | `2>&1` + `EAP=Continue` + تطبيع `ErrorRecord` |

ومعها خامسة رآها المالك في اللقطة: **حاجز الصمت** (‏`OBS-017`) — تبويبا codex ٣ و٤
بنقاط حمراء و«انتهت جلسة الطرفية». ورُصد في السجل أنه حاول **تسطيح الأسطر**
(`-replace "\`r\`n"," "`) — أي أنه لامس المشكلة الصحيحة ولم يعرف علاجها.

- **المنجز**: `references/engine-adapters.md` صار (أ) يعلن أن غرفة العمليات **سطح
  واجهة لا أداة وكيل** فيتوقف الوكيل ويطلب النقرة بدل البحث عن بديل، (ب) يفرّق بين
  تزييف العزل (ممنوع كما كان) وتشغيل محرّك مختلف فعلاً (مسار مشروع بشرطه)، (ج) يحمل
  **الوصفة الخمس مضمَّنة نصّاً** — بلا إحالة إلى `executor.ps1` أو أي مسار من
  مستودعنا لأنها تعمل في مشروع المستخدم (‏`OBS-060` نفسها)، (د) وسقف **محاولتين**
  لكل محرك ثم إبلاغ المستخدم، فلا حلقة ارتجال.
- **الحارس**: `test:skills` كسب سبعة فحوص — إعلان حدّ الغرفة، وجود القسم، العلم
  وعلامة stdin معاً، تحذير الترميز، `EAP=Continue`، سقف المحاولات، وصفر تسريب مسار.
  **مُثبَت أنه يعضّ**: إعادة `--sandbox read-only` مكان العلم الصحيح تُفشِل الطقم.
- **ما لا يغلقه**: **أداة وكيل للعصف** (مرشّح العلاج (أ) الأصلي) لم تُنفَّذ — تبقى
  غرفة العمليات نقرةَ مستخدم. والوصفة **لم تُشغَّل حيّاً من مشروع خارجي بعد**؛ هي
  منقولة من `executor.ps1` المثبَّت لا من تشغيل جديد. ولا حارس يمنع `envbrief`
  والمهارة من التعارض مجدداً — التوفيق نصّي لا مفروض آلياً.

## OBS-059 — المعاينة تطفو فوق الحوارات التي لا تمرّ بمنسّق الأسطح
- **الوسم**: `preview`
- **النوع**: عطل
- **الحالة**: منجزة — `2805da5` (2026-08-27)
- **الدليل**: لقطة مالك (2026-08-27، على `2.16.11` المنشورة): حوار **«ما الجديد —
  v2.16.11»** مفتوح، ومحتوى المعاينة (صفحة `site/` على `127.0.0.1:4600`) يظهر
  **فوقه ومن خلاله** — بطاقات «سور 01/02/03» ترتسم عبر جسم الحوار فيتعذّر قراءته.
  السبب في الكود لا في التخمين: `src/ui/lib/update-toast.js` يفتح الحوار ويغلقه
  بـ`dialog.hidden = false/true` مباشرةً (‏`openNotes`/`closeNotes`) **بلا أي
  استدعاء لـ`surfaceCoordinator.setDialog`** — وهو وحده ما يستدعي
  `previewEl.holdForDialog(true)`.
- **المصدر**: بلاغ مالك بلقطة بعد نشر 2.16.11
- **الملاحظة**: ليست حالة مفردة بل **صنف**. `WebContentsView` طبقةُ نظامٍ فوق كل
  DOM — وهو حدّ معلن في `CLAUDE.md` — ولذلك بُني `holdForDialog` أصلاً حين اختبأ
  مربع الإذن خلف المعاينة. لكن الحماية **تُكتسب بالتسجيل لا بالبنية**: ستة أسطح
  تستدعي `setDialog` (‏ops · permission · question · elicitation · verify-config ·
  promo-studio)، وكلُّ سطحٍ جديد يُفتح بـ`hidden=false` أو `showModal()` يسقط منها
  صامتاً. والمرصود اثنان حتى الآن:
  1. حوار «ما الجديد» (`update-toast.js`) — **مؤكَّد باللقطة**.
  2. عارض اللقطة المكبّر (`chat.js:1136`، `showImageLightbox`) — يستدعي
     `showModal()` ولا أثر له في `app.js`؛ **بنيوي لم يُصوَّر بعد**.
- **قيد يفسّر السقوط**: `update-toast.js` وحدة في `lib/` لا ترى `surfaceCoordinator`
  (المنسّق مغلق داخل `app.js`). فالنمط القائم لبقية الحوارات هو بثّ حدث DOM
  (‏`perm-visible` مثلاً) يلتقطه `app.js` — وهو ما لم يفعله هذا الحوار.
- **العلاج المرشّح**: (أ) بثّ حدث بنمط `perm-visible` من الحوارين ووصلُه في
  `app.js` — أقلّ تغيير وأقرب إلى القائم؛ (ب) **الأقوى**: جعل الحماية بنيوية بدل
  التسجيل — مراقب DOM على مستوى المستند يرصد أي عنصر حواري ظاهر (‏`<dialog open>`
  أو سطح بصنف معلوم) ويحجب المعاينة تلقائياً، فلا يعود السطح الجديد يسقط بالنسيان.
  الثاني يعالج الصنف والأول يعالج حالتين — والاختيار قرار مالك.
- **ولماذا لم يُمسَك آلياً**: لا حارس يقول «كل سطح حواري يجب أن يحجب المعاينة».
  والاختبار الحيّ للمعاينة يقيس الموضع والمرآة لا التراتب مع حوار — وقياس
  التراتب يحتاج لقطة شاشة حقيقية لأن `WebContentsView` خارج DOM أصلاً.
- **المنجز**: العلاج (ب) — الحماية صارت **بنيوية لا تسجيلية**.
  `src/ui/lib/preview-shield.js` وحدةٌ نقية تراقب DOM وتحجب طبقة العرض الأصلي عند
  ظهور أي سطح حواري (`dialog[open]` · `[role="dialog"]` · `[aria-modal="true"]`)،
  موصولةً في `app.js` بمفتاح حجب **مستقل** `modal`: لو تشارك الدرعُ والمنسّق
  مفتاحاً واحداً لأفرج إغلاقُ أحدهما عن حجب الآخر فبرز العرض فوق حوار حيّ.
  والتسجيل يبقى لما يحتاج أكثر من الحجب (تعليق اللوحات وإعادة التركيز) لا لأصل
  الحماية. ووُسّع سطر `OBS-032` المفسِّر للسواد ليغطي السببين — المستخدم يرى
  السواد ذاته ويلزمه التفسير ذاته سواء أُعلن الحوار أم اكتُشف.
- **فخُّ تنفيذٍ لم يكن في النصّ**: `#notesCard` يحمل `role="dialog"` **دائماً**
  بينما المخفيّ أبوه `#notesDialog`. فالتنفيذ البديهي — فحص `[hidden]` على العنصر
  نفسه — كان سيرى الحوار مفتوحاً أبداً فيحجب المعاينة بلا سبب. العلاج
  `checkVisibility()` لأنه يقرأ الحساب النهائي بما فيه الأب.
- **الحارس**: `test:preview-shield` (‏20 فحصاً، حيّ تحت CSP صارم، داخل الطقم).
  يشغّل الوحدة الإنتاجية على ترميز الحوار المنسوخ حرفياً من `index.html` (وحارسٌ
  يقارنهما فلا ينحرف)، ويغطي الحالتين المرصودتين (`hidden=false` و`showModal()`)
  و**التراكب** (إغلاق حوار بينما آخر مفتوح لا يفكّ الحجب) وعدم الإنذار الكاذب على
  سطح عادي. **مُثبَت أنه يعضّ**: استبدال فحص الرؤية بفحص `[hidden]` يُسقطه
  بالاسم، وحذف `shield.start()` يُسقط فحص الوصل.
- **وهشاشةٌ عادت في بناء الحارس نفسه**: أول نسخة من fixture انتظرت
  `requestAnimationFrame` فعلّقت أبداً — نافذة `show:false` لا تُشغّل rAF. وهي
  الهشاشة المسجَّلة سابقاً في `question-dialog`؛ استُبدلت بنبضة `setTimeout`
  لأن `MutationObserver` مَهمة صغرى فتكفيها نبضة كبرى واحدة.

## OBS-058 — إخراج `structure` من الظلّ ينتظر بيانات v4، لا كوداً
- **الوسم**: `ui`
- **النوع**: تحسين
- **الحالة**: مفتوحة
- **الدليل**: بعد `2e641d6` صار المقياس `v4` بقاعدة جداول مصقولة، بينما الأحد عشر
  صفَّ `structure` المتراكمة (‏2026-08-14 ← 2026-08-26) قيست بـ`v2`. وقاعدة «لا
  تُقارن الأرقام عبر إصدارين» تمنع معايرة الجديدة عليها. فالعرض يبقى محصوراً في
  `share`/`script` كما ثبّته `1bc5ffd`.
- **المصدر**: دفعة OBS-057 (خطوة تالية مسجَّلة لا منفَّذة)
- **الملاحظة**: **ليست عملاً برمجياً بل انتظارَ قياس** — ولذلك سُجّلت بدل أن
  تُنفَّذ: لا يوجد ما يُكتب اليوم. تنضج حين يتراكم قدرٌ من صفوف `v4` يماثل ما
  عايرنا عليه أول مرة (‏921 قياساً/12 يوماً)، ثم تُقرأ حصيلتها بالتقرير القائم —
  وهو يعرض عمود «يُعرض/في الظلّ» فيكفي لاتخاذ القرار بلا أداة جديدة.
- **شرط الإنضاج**: أن يظهر مجتمع `structure` في `v4` **أصغر بوضوح** من نظيره في
  `v2` (‏11 من 921). فإن بقي بحجمه فالصقل لم يعالج الصنف والفرضية تحتاج مراجعة،
  وإن تلاشى فالسؤال يُغلق بلا عرضٍ أصلاً. **الاحتمالان مذكوران عمداً** كي لا
  يُقرأ الانتظار وعداً بالعرض.
- **ما لا يفتحه**: لا يمسّ `share`/`script` المعروضين، ولا الممنوعات المجمَّدة.

## OBS-057 — مجتمع `structure` بقي في الظلّ لأن السجل لا يحمل نصّاً يُتحقَّق منه
- **الوسم**: `ui`
- **النوع**: تحسين
- **الحالة**: منجزة — `2e641d6` (2026-08-27)
- **الدليل**: معايرة الخروج من الظلّ (‏`1bc5ffd`، 2026-08-26): من عشرين انزلاقة في
  921 قياساً، **إحدى عشرة** سببها `structure` وحصصها `90%–96%` — أي ردود عربية في
  جوهرها أُدينت بسطرين بنيويين إنجليزيين. أهي لقطة المالك الأصلية (جدول إنجليزي
  كامل داخل ردٍّ عربي — وهي بالضبط ما صُمّم `structuralSlips` لمسكه)، أم جداول
  تقنية مشروعة رؤوسها إنجليزية كما يوجب العقد نفسه؟ **لا سبيل للجواب من السجل**:
  «أرقام بلا نص» قرار عصفٍ مجمَّد، وهو صحيح — لكنه يجعل هذا المجتمع غير قابل
  للتحقق بأثر رجعي.
- **المصدر**: دفعة الخروج من الظلّ (رُصدت وأُجِّلت بقرار معايرة صريح)
- **الملاحظة**: هذه **مقايضة لا عطل**: قاعدة «أرقام بلا نص» تحمي الخصوصية وتمنع
  تحوّل ملف مراقبة إلى تسريب، وثمنها أن صنفاً من الأحكام لا يُعايَر إلا بعيّنة
  تحمل نصاً. فالعلاج ليس تخفيف القاعدة على السجل الدائم.
  مرشّحات: (أ) وضع تشخيص **مؤقت وصريح** يطلبه المالك، يكتب عيّنة محدودة تحمل
  الأسطر البنيوية المُدينة وحدها (لا الرسالة) إلى ملف منفصل يُحذف بعد المعايرة؛
  (ب) توليد عيّنة اصطناعية من أنماط الجداول الشائعة في ردود «سطر» وقياس
  `structuralSlips` عليها — أرخص وبلا أي بيانات حقيقية، لكنه يقيس التصميم لا
  الواقع؛ (ج) رفع `STRUCTURAL_THRESHOLD` من 2 إلى 3 وقياس أثره رجعياً على الـ11.
  الثاني أولاً: يفصل «هل التصميم صحيح؟» عن «هل الواقع يطابقه؟» بصفر كلفة خصوصية.
- **ما لا يفتحه**: لا يعيد النظر في عرض `share`/`script` — ذاك محسوم بالبيانات.
- **المنجز** (`2e641d6`): نُفِّذ المرشّح (ب) فأجاب سؤالاً **وأبطل آخر**.
  - **ما أُجيب**: هل التصميم صحيح؟ **لا**. العيّنة كشفت إيجابية كاذبة حقيقية —
    جدول مقارنةٍ عربيٌّ بحصّة `0.91` يُدان بسبب `| share | same | same |` و
    `| script | absent | ar/fa/mixed |`، وهي أسماء حقول وقيم يوجب العقد نفسه
    بقاءها إنجليزية. أي أن الحكم كان **يعاقب الجواب الصحيح**. فصار إبقاء
    `structure` في الظلّ مسنَداً بدليل لا بشبهة.
  - **العلاج**: الفارق ليس اللغة بل **الجملة** — يُشترط لصف الجدول تتابعُ ثلاث
    كلمات إنجليزية داخل خلية واحدة، والأنبوب يقطع التتابع. لصفوف الجداول وحدها؛
    العناوين والبنود نثرٌ بطبيعتها وهي نمط شكوى المالك فتبقى بحكمها. `METRIC_VERSION`
    صار `4`. ولقطة المالك المحفوظة ما زالت مُمسَكة — الساقط صفُّ عناوينها وحده.
  - **ما صار لاغياً**: السؤال الأصلي «أهذه الأحد عشر صحيحة أم كاذبة؟» لم يُجَب
    **ولن يُجاب**: قيست بالقاعدة القديمة، فلا هي قابلة للاسترجاع ولا صالحة
    لمعايرة الجديدة. المرشّحان (أ) و(ج) سقطا معها — (ج) بالذات كان سيرفع العتبة
    بلا علاج للصنف، وإيجابيتي الكاذبة كانتا سطرين فما كان ليمنعهما.
  - **الخطوة الباقية ليست هذه الملاحظة**: إخراج `structure` من الظلّ يحتاج بيانات
    v4 طازجة — انظر [OBS-058].
- **درسٌ في الحارس نفسه**: نسخة v2 «المجمَّدة» في `test:langmetric` كانت تستورد
  `structuralSlips` من الإنتاج، فلما اكتسبت الاستثناء صارت تقارن الشيء بنفسه في
  الفرع البنيوي وتمرّ دائماً. لم يمسكها إلا **فحص اختلافٍ صريح** أُضيف بعده (يثبت
  أين يجب أن يختلف الإصداران لا أين يتفقان فقط). **نسخةٌ تستورد فرعها من الإنتاج
  ليست مجمَّدة** — والقاعدة الآن محروسة نصّياً في `test:langoverride`.

## OBS-056 — سقوط `test:termjobs` تحت الحمل **يعلّق** الطقم بدل أن يُسقطه
- **الوسم**: `process`
- **النوع**: عطل
- **الحالة**: منجزة — `42185fc` (2026-08-27)
- **الدليل**: بوابة دفعة «الخروج من الظلّ» (2026-08-26): سقط `test:termjobs` عند
  `scripts/termjobs-test.js:93` برسالة «خرج النداء الأول متشابك» (تشابك قفل FIFO
  في `runCapture`)، **ثم بقي الطقم معلّقاً عند [36/75] أكثر من 1000 ثانية** حتى
  أُوقف يدوياً. وأعيدت المجموعة مستقلة فنجحت فوراً. الملفان `term.js` و`termjobs.js`
  لم يُمسّا في تلك الدفعة، والطقم نفسه مرّ 75/75 قبلها بساعة.
- **المصدر**: بوابة `test:full` لدفعة الخروج من الظلّ
- **الملاحظة**: عَرَضان مستقلان يجب ألّا يُخلطا:
  1. **الهشاشة** — مسجَّلة سابقاً لـ`termjobs` لكن بعَرَض آخر (تنظيف EPERM، أُصلح
     في `21ad2f1`). هذا عَرَض جديد: تشابك التقاط رغم قفل FIFO، ولا يظهر إلا تحت
     حمل الطقم الكامل مع التطبيق مفتوحاً. مرشّح السبب: `runCapture` يعتمد علامتَي
     بداية/نهاية في مجرى pty مشترك، وتحت ضغط الجدولة قد تتداخل مع صدى صدفة بطيء.
  2. **الأخطر — التعليق**: فشل الاختبار لم يُنهِ العملية. مهمة pty حيّة تبقي
     Node حياً بعد `assert` فينتظر المشغّل إلى الأبد. أثره أن **بوابة خضراء
     كاذبة تصير بوابة صامتة**: لا نجاح ولا فشل، بل جلسة تُهدَر حتى ينتبه بشر —
     وهو نمط `OBS-042` نفسه (لا تقرأ صمتاً نجاحاً) بوجه آخر.
- **العلاج المرشّح**: (أ) مهلة لكل مجموعة في `scripts/full-suite.js` تُنهي المعلَّق
  بفشل صريح بدل انتظار أبدي — يعالج **كل** اختبار معلَّق لا termjobs وحده؛
  (ب) `process.exit` صريح في نهاية `termjobs-test.js` بعد التنظيف، أو `unref`
  للـpty الباقي؛ (ج) تشخيص التشابك نفسه. الأول أعلى قيمة لأنه يحوّل صنفاً كاملاً
  من الأعطال الصامتة إلى أعطال ناطقة.
- **المنجز**: (أ) وحده — `SUITE_TIMEOUT_MS` مع تجاوزات للمجمَّعة وتجاوز بيئي
  محدود، وقتلُ شجرةٍ عند التجاوز، وطباعةُ أبطأ ثماني مجموعات في كل تشغيل.
  **الأرقام مقيسة لا مخمَّنة**: كتبتُ أولاً 300ث/900ث تخميناً، ثم قِستُ تشغيلاً
  كاملاً فظهر أن أبطأ مجموعة `test:opsroom-all` ‏121.6ث و`opsroom-all-live` ‏51.0ث
  و`handoff-bar-live` ‏25.2ث وما عداها دون 20ث — فثُبّتت المهل بين **4× و9×**
  المرصود. وطباعةُ المدد ليست زينة: بها يصير أي تعديل لاحق للأرقام مسنَداً.
- **(ب) لم يُنفَّذ ومبرَّرٌ تركُه**: `termjobs-test.js` يملك أصلاً `catch` يستدعي
  `process.exit(1)`، و**خطؤه طُبع فعلاً** في الحادثة — فالمعلِّق ليس Node بل حفيدٌ
  يتيم خارج سجل `term.js`. إصلاحه يستلزم إعادة إنتاج التشابك أولاً، والمهلة تجعله
  فشلاً ناطقاً خلال أربع دقائق بدل انتظار مفتوح. (ج) يبقى مفتوحاً بلا حاجة عاجلة.
- **الحارس**: `test:full-timeout` (جديد، داخل الطقم). **وقيمته أنه كشف خطأً في
  نفسه**: بُني أولاً بحفيدٍ عادي فمات مع أبيه، فكان فحص `killTree` يمرّ على عمليةٍ
  ميتة أصلاً — أخضر كاذب صنعتُه بيدي. القياس أظهر أن الحفيد **المنفصل**
  (`detached`) وحده ينجو من قتل الابن، وهو شكل pty فعلاً؛ فصار الفحص يثبت النجاة
  أولاً ثم يثبت أن `killTree` تُنهيها. **مُثبَت أنه يعضّ**: تعطيل `killTree` يُسقطه.
  ومعه غُلِّف `main()` خلف `require.main` كي يصير الملف قابلاً للاستيراد — بدونه
  كان الاختبار سينسخ المنطق فيقارن الشيء بنفسه.

## OBS-055 — `satr-accept` بلا ترس أوسط: إما البروتوكول كاملاً أو لا شيء
- **الوسم**: `process`
- **النوع**: تحسين
- **الحالة**: منجزة — `3636eaa` (2026-08-25، سُحبت بقرار مالك في الجلسة نفسها)
- **الدليل**: دفعة OBS-051/052 (‏`9f27968`، 2026-08-25) — أول استعمال للمهارة بعد
  شحنها. طابقت «متى لا تُستعمل» فصُرف النظر عنها بحق (سطران لكل ملف، وجانبها المرئي
  الوحيد صار حارساً آلياً يعضّ). لكن بقيت **فجوة واحدة حقيقية**: الحارس يشتغل بنافذة
  مخفية فلا يثبت البكسل المرسوم. أغلقها المالك في **عشر ثوانٍ** — `browser_scroll`
  على `site/` داخل لوحة معاينة «سطر» العاملة، فرأى الإطار الذهبي (‏`scrollY 0→530`).
  لا بناء `dist:dir`، ولا بيئة مضبوطة، ولا خطوات مرقّمة.
- **المصدر**: أول تطبيق لـ`satr-accept` بعد شحنها (رُصدت ولم تُنفَّذ)
- **الملاحظة**: المهارة اليوم ثنائية: بروتوكول كامل (بناء + بيئة + جدول خطوات) أو
  إحالة إلى الطقم القطعي. والحالة الشائعة بينهما: **حارس آلي يغطي معظم الادعاء وتبقى
  فجوة بصرية واحدة** — وعلاجها نقرة واحدة في التطبيق العامل لا جلسة. غياب هذا الترس
  يدفع إلى أحد خطأين: أداء الطقس كاملاً لدفعة سطرين، أو إغلاق الفجوة بالاستنتاج
  («الشجرة والأنماط تكفي») وهو ما تحظره القاعدة 4 صراحةً.
  المقترح: بند «فحص مصغّر» في `SKILL.md` — شرطه أن يكون الادعاء المتبقي **واحداً**
  وقابلاً للعرض في نسخة عاملة بلا بناء؛ وتبقى قاعدة «هو ينقر لا أنت» كما هي.
  **ما لا يغلقه**: هذا لا يوسّع «متى لا تُستعمل» ولا يخفّف البروتوكول لدفعة واجهة
  حقيقية — الترس الأوسط للفجوة المفردة لا للسطح الجديد.
- **الإصلاح**: قسم «الترس الأوسط — فحص مصغّر» في `SKILL.md` بشروط ثلاثة تمنعه من
  الانفلات إلى بديل رخيص عن البروتوكول (ادعاء مفرد · نسخة عاملة بلا بناء · معيار
  مكتوب سلفاً)، وسقوط أيّها يعيد إلى البروتوكول كاملاً؛ و«هو ينقر لا أنت» مُعادة
  التثبيت داخل القسم بلا تخفيف. ومعه مثال مقيس من دفعة `9f27968` وحدٌّ صريح أن
  الفحص المصغّر يغلق الادعاء **لدفعته** لا لكل تراجع مستقبلي. والقاعدة مثبّتة في
  `AGENTS.md` و`CLAUDE.md` كنظيرتها.
  **وعُدّل الوصف عمداً**: هو ما يقرّر متى تُحمَّل المهارة، وحدّ «دفعة من سطرين» كان
  سيصرف الوكيل عن التحميل في الحالة عينها التي يخدمها الترس — فيبقى حبراً لا يُقرأ.
- **الحارس**: `test:skills` كسب أربعة فحوص للقسم وشرطَيه ولقاعدة النقر، وخامساً
  للوصف. **مُثبَت أنه يعضّ**: حذف ذكر الترس من الوصف يفشل، وتغيير عنوان القسم يفشل.

## OBS-061 — توثيق الاتجاه يوصي بـ`plaintext` بينما الكود المقيس يحسمه إحصائياً
- **الوسم**: `docs`
- **النوع**: عطل
- **الحالة**: منجزة — `50a29c6` رصداً، والإصلاح في هذه الدفعة (قرار مالك 2026-08-27)
- **الدليل**: `docs/DESIGN-SYSTEM.md:182` يقول حرفياً «النص البشري أو المختلط
  `dir="auto"` أو `unicode-bidi: plaintext`»، ونظيره في `CLAUDE.md` (قاعدة 3)
  يقول «النصوص المختلطة تستخدم `dir="auto"` أو `unicode-bidi: plaintext`».
  وهذا **عكس** ما أثبته القياس: `src/ui/lib/text-dir.js:4-18` يوثّق أن `plaintext`
  يحسم من أول حرف قوي فترسو الفقرة العربية البادئة برمز لاتيني LTR كاملة، وأن
  العلاج المعتمد حسمٌ إحصائي (`ar*2 >= lat`). و`scripts/arabic-rtl-probe.js` يقيسه
  بالبكسل. أي أن التوثيق يوصي بالعلّة التي أُصلحت في 2026-07-18.
- **المصدر**: رُصد أثناء دفعة `browser_readability` (2026-08-27) — سُجّل ولم يُنفَّذ
- **الملاحظة**: الخطر ليس نظرياً: المهارة المضمّنة `satr-design-ar` والوكيل نفسه قد
  يقرآن الوثيقة فيعيدان زرع العطل في مشروع المستخدم. ولا يكفي حذف الجملة، لأن
  `dir="auto"` **ما زال مستعملاً عمداً وصحيحاً** في مواضعه: الحاويات والنصوص
  القصيرة (`chat.js:742, 796, 967, 1213`)، بينما الحسم الإحصائي مقصور على الفقرات
  وعناصر القوائم وفقاعة المستخدم. فالمطلوب تمييز الحالتين لا قلب التوصية:
  **فقرة/عنصر قائمة/عنوان ⇒ حسم إحصائي صريح؛ حاوية أو نص قصير بلا رموز بادئة ⇒
  `dir="auto"` مقبول**. وحدّ يُذكر معه: `getComputedStyle(el).direction` لا يكشف
  العطل، فالمراجعة بالعين أو بالخاصية المحسوبة لا تجزئ.
- **ولماذا لم يُمسَك آلياً**: `test:design-guard` يفحص الألوان وz-index والزوايا
  والمسافات والسمات المضمّنة — ولا يقرأ النثر. ولا حارس يقارن توصية وثيقة بسلوك
  الكود المقيس، فتعفّن التوثيق صنفٌ لا يمسكه أي طقم اليوم.
- **الإصلاح** (أذن به المالك 2026-08-27): التمييز أعلاه مثبَّت في **ثلاثة** مواضع لا
  اثنين — `docs/DESIGN-SYSTEM.md` §7 و`CLAUDE.md` قاعدة 3 و`AGENTS.md` قاعدة 3.
  الثالث لم يكن مرصوداً عند التسجيل وظهر عند الإصلاح: نسخة ثالثة من القاعدة نفسها،
  وتركها كان سيُبقي التوصية الخاطئة قائمة في الملف الذي يقرأه الفريق.
  الصياغة الجديدة تفرّق ثلاث حالات بدل قاعدة واحدة: **حسم إحصائي صريح** للفقرة
  وعنصر القائمة والعنوان وخلية الجدول والفقاعة · و`dir="auto"` **مقبول ويبقى**
  للحاوية والنص القصير بلا رمز بادئ (فلا تُطارَد مواضعه القائمة) · و**اتجاه واحد
  للكتلة** متعددة الأسطر. ومعها الحدّ الذي يمنع تكرار العطل: `getComputedStyle`
  لا يكشفه، والدليل موضع أول محرف بالبكسل.
- **ما لا يغلقه**: لا حارس آلي يقارن توصية وثيقة بسلوك الكود، فتعفّن التوثيق يبقى
  صنفاً لا يمسكه طقم اليوم — أُغلق النصّ لا الصنف.

## OBS-062 — مسبار Kimi يطبع تقريره ولا يخرج، فيحجب سلسلة المسابير كلها بصمت
- **الوسم**: `process`
- **النوع**: عطل
- **الحالة**: منجزة — `4758f2a` (2026-08-27)
- **الدليل**: تشغيل حيّ 2026-08-27. سلسلة خمسة مسابير في مهمة `run_in_background`
  واحدة (‏`drift-round1`): طبع `kimi-capability-probe.js` تقريره كاملاً ثم **توقّف
  كل شيء**. الذيل ينتهي عند سطر التقرير الأخير بلا `--- exit=… ---` وبلا فاصل
  المسبار التالي، و`list_background_tasks` يُبقي `term_2` حياً و`recent_exits`
  فارغاً، و`Get-Process node` يُظهر العملية حيّة بعد أكثر من 14 دقيقة بينما **لا
  عملية `kimi` باقية** — أي أن الطفل خرج والمسبار نفسه هو العالق. فلم يعمل أيٌّ من
  الأربعة التالية (‏claude-models · codex-mcp-panel · codex-account · codex-steer).
- **السبب الجذري** (مقروء من الملف): `scripts/kimi-capability-probe.js` ‏271 سطراً
  فيها **صفر `process.exit`** و**صفر `unref`**؛ الإغلاق `(async () => { … })()` عند
  السطر 267 ينتهي ثم يبقى مقبض حيّ (أنابيب `kimi acp` أو مؤقّت) يُبقي حلقة الأحداث
  دائرة. والنمط الصحيح موجود في المستودع نفسه: `scripts/arabic-rtl-probe.js` يجمع
  `process.exit(0)` و`guard` بمهلة `.unref()` و`uncaughtException`.
- **المصدر**: بلاغ مالك أثناء جولة مسابير الانحراف (‏«لقد انتهت الطرفية ولم تكتشف ذلك؟»)
- **ما ليس عطلاً هنا**: `wait_for_background_task` **لم تكذب** — العملية لم تخرج فعلاً
  فأعادت `running` بحق. الخلل في المسبار لا في أداة الانتظار. أما الخطأ البشري/الوكيلي
  المصاحب فهو أن الذيل قُرئ بعد أول مهلة و**كان يحمل الدليل** (غياب سطر `exit` وغياب
  الفاصل التالي) فأُعيد الانتظار بدل تشخيصه — نظير `OBS-042` حرفياً: لا تقرأ صمتاً
  نجاحاً، وافحص أثراً ملموساً.
- **الفجوة المنهجية الأوسع**: `full-suite.js` كسب `SUITE_TIMEOUT_MS` بعد `OBS-056`
  لأن مجموعة معلّقة كانت تُهدر الجلسة. **والمسابير خارج الطقم عمداً فلا تملك هذا
  الحارس**: تشغيلها اليدوي المتسلسل بلا مهلة لكل خطوة يجعل تعليق واحد يحجب البقية بلا
  إشارة. الصنف نفسه، والعلاج نفسه، ومكانه غير مغطّى.
- **مرشّحات العلاج**:
  - (أ) تطبيق نمط `arabic-rtl-probe.js` على كل مسبار: `process.exit` صريح + مهلة
    `unref` + `uncaughtException`. يعالج السبب الجذري.
  - (ب) مشغّل مسابير بمهلة لكل خطوة و`killTree` (إعادة استعمال منطق `full-suite.js`)،
    فيصير التعليق فشلاً صريحاً وتكمل السلسلة بدل أن تُحجب.
  - (ج) عند التشغيل المتسلسل: لا تُعِد الانتظار قبل قراءة الذيل والتحقق من **فاصل
    الخطوة التالية** لا من طباعة الخطوة الحالية.
- **راكب معه (لا يستحق ملاحظة مستقلة)**: خط الأساس داخل المسبار متأخّر عن
  `docs/KIMI-CAPABILITIES.md` — الوثيقة تقول `session/fork` **مدعوم منذ 0.38.0**
  بينما يعلن المسبار فرقاً «خط الأساس ❌ — الواقع ✅» في كل تشغيل، فيصير تحذيراً
  يُتجاهَل. ومعه خطأ إملائي في السطر 263: «خط الأساب» ⇐ «خط الأساس».
- **السبب الجذري الدقيق** (أدقّ مما سُجّل أولاً): ليس مجرّد غياب `process.exit`، بل
  **سلسلة مؤقّتات لا تنتهي** عند السطر 178:
  `const check = () => { if (gotChunk) return resolve(); setTimeout(check, 50); };`
  ومعها `setTimeout(resolve, 3000)` — الثاني **يحلّ الوعد ولا يوقف السلسلة**. فحين لا
  تصل أول كتلة إجابة، تعيد `check` جدولة نفسها كل 50ms أبداً وتبقى حلقة الأحداث
  دائرة. طبقة RPC كانت سليمة (تمسح مؤقّتاتها)، والطفل `kimi` خرج فعلاً.
- **الإصلاح** — أربع طبقات:
  1. **السبب**: مقبضان (`pollTimer`/`capTimer`) يُمسح كلٌّ منهما عند أول حسم أيّهما سبق.
  2. **الدفاع**: `process.exit(0)` صريح + مهلة كلية `unref` + `uncaughtException`
     (نمط `arabic-rtl-probe.js`)، وقتل الطفل بلا مؤقّت معلَّق (‏`taskkill /T /F` على
     ويندوز لأن الثنائي قد يكون خلف `cmd /c` فقتل الأب يترك الحفيد).
  3. **الصنف**: `scripts/probe-runner.js` جديد — **مهلة لكل مسبار** + `killTree` بنمط
     `full-suite.js` حرفياً. أي مسبار يعلّق يصير فشلاً صريحاً وتكمل السلسلة. يسدّ
     الفجوة لبقية المسابير الاثني عشر التي لا تملك خروجاً صريحاً بلا تعديلها عمياءً.
  4. **الحارس**: `test:probe-runner` (‏40 فحصاً، داخل `test:full`) — **مُثبَت أنه
     يعضّ** بـfixture يعلّق عمداً (`fixtures/hanging-probe.js`)، وقد أمسك فعلاً عطلاً
     في المشغّل نفسه أثناء كتابته (`path.basename` كان يجرّد المجلد الفرعي).
- **تصحيح الراكب**: الوصف الأول قال «خط الأساس داخل المسبار متأخّر» وهو غير دقيق —
  خط الأساس **يُقرأ من الوثيقة** لا من ثابت، والخلل أن المحلّل يقارن بمساواة تامة
  `=== 'مدعوم'` بينما الوثيقة تكتب الحالة مؤهَّلة `**مدعوم منذ 0.38.0**`. العلاج
  مطابقة **ببادئة** (‏«غير مدعوم» لا يطابقها لأنه يبدأ بـ«غير»). والخطأ الإملائي
  «خط الأساب» صُحّح.
- **التحقق الحيّ**: المسبار الذي بقي حياً أكثر من 14 دقيقة صار يخرج في **32 ثانية**
  برمز 0، ويطبع «لا يوجد فرق عن خط الأساس» بدل الفرق الكاذب، وبلا عملية يتيمة.
- **ما لا يغلقه**: 12 مسباراً آخر ما زالت بلا `process.exit` صريح، و34 من 38 بلا مهلة
  حارسة. المشغّل يمنعها من حجب غيرها، **لكنه لا يُصلح خروجها** — كلٌّ منها يبقى عطلاً
  يُعالَج عند لمسه. ولا حارس يمنع مسباراً جديداً من الولادة بلا خروج صريح.

## OBS-063 — SDK صار يعلن قدرات كل نموذج، و«سطر» يسقطها ويعتمد على التخفيض الصامت
- **الوسم**: `engines`
- **النوع**: تحسين
- **الحالة**: منجزة — مرشّح (أ) (2026-09-03، دفعة موجة أوبس فوق `cb68ce4`؛ **يستبدل
  القائد هذا المرجع برقم التزام الدمج** — المنفّذ لا يلتزم). مرشّح (ب) يبقى مؤجّلاً
  عمداً بالسبب المدوّن أدناه.
- **الدليل**: جولة مسابير الانحراف 2026-08-27 على Claude Code `2.1.241` (‏SDK
  `0.3.176`). `scripts/claude-models-probe.js` أعاد **6** نماذج (كانت 5 في عقد دفعة B
  المقيس على `2.1.220`) — انضم `claude-opus-4-8`، وتغيّرت أطوال وصف
  `default`/`opus[1m]` من 59 إلى 57. والأهم: كل نموذج صار يحمل **تسعة حقول** لا ثلاثة:
  `value · displayName · description · resolvedModel · supportsEffort ·
  supportedEffortLevels · supportsAutoMode · supportsFastMode · supportsAdaptiveThinking`.
- **المصدر**: جولة مسابير ما بعد ترقية المحرّكات (رُصد ولم يُنفَّذ)
- **الملاحظة**: عقد `satr:claudeModels` في `main.js` قائمة سماح مغلقة على
  `{value,label,description}` — فالحقول الستة الجديدة **تُسقَط قبل renderer**. وهذا
  fail-closed سليم أمنياً ولا يكسر شيئاً، لكنه يعني أن «سطر» يملك معلومة ولا يستعملها.
  الأثر العملي في الجهد: `CLAUDE.md` يقول اليوم «الـSDK يخفّضه صامتاً إن لم يدعمه
  النموذج» — أي أن المستخدم يختار جهداً من ⚙ فيُسقَط بلا إشعار. صار SDK يعلن
  `supportsEffort` و`supportedEffortLevels` صراحةً، فيمكن أن يعرض المنتقي المستويات
  المدعومة للنموذج المختار وحدها بدل قائمة ثابتة يُتجاهل بعضها.
- **مرشّحات العلاج**:
  - (أ) توسعة قائمة السماح بـ`supportsEffort`/`supportedEffortLevels` **فقط** (لا
    الحقول الستة كلها) وربطها بمنتقي الجهد. أصغر تغيير بأوضح أثر.
  - (ب) `supportsFastMode`/`supportsAutoMode` يبقيان مؤجّلين — لا سطح لهما اليوم،
    وإضافتهما بلا مستهلك تكرّر عيب «حقل مجمَّد بلا مستهلك».
- **حدّ**: الحقول الجديدة غير موثّقة في `sdk.d.ts` المثبَّت بالضرورة كعقد مستقر —
  يُتحقَّق منها قبل البناء عليها، ولا تُمرَّر خاماً إلى renderer.
- **تقدّم جزئي (2026-09-03، بلاغ مالك بلقطة)**: صار `resolvedModel` يُستهلك داخل
  `main.js` (‏`officialClaudeName`) لاشتقاق التسمية الرسمية في منتقي النماذج —
  «Fable» الخام صارت «Fable 5.1 (1M context)» والافتراضي يذكر نموذجه المحلول —
  بلا تمرير الحقل خاماً إلى renderer (العقد العام `{value,label,description}` كما
  هو، يحرسه `test:claude-models`). مرشّح (أ) — حقول الجهد — يبقى مفتوحاً.
- **المنجز** (مرشّح أ، 2026-09-03): `agent.js` صار يمرّر `supportsEffort` و
  `supportedEffortLevels` داخلياً إلى `main.js` فقط، و`sanitizeClaudeEffortLevels`
  يشتق منهما حقلاً عاماً اختيارياً واحداً `effortLevels` منقّى بقائمة قيم **مغلقة**
  `['low','medium','high','xhigh','max']` (تطابق اتحاد `sdk.d.ts`) وبترتيب القائمة لا
  ترتيب SDK؛ يُضاف فقط عند `supportsEffort === true` ومصفوفة غير فارغة بعد التنقية،
  ولا يعبر `supportsEffort` نفسه ولا الحقول الثلاثة الباقية. و`rebuildEfforts` في
  `app.js` يعرض للنموذج المُعلِن مستوياته وحدها مسبوقة بـ«الافتراضي»، والاختيار المحفوظ
  خارجها يسقط إلى الافتراضي **بإشعار عربي** بدل التخفيض الصامت؛ ومعه إصلاح لازم: دورة
  شريط الوعي صارت تُشتق من خيارات المنتقي الفعلية لا من `EFFORT_CYCLE` الأوسع، وإلا
  ضبط التدوير قيمة بلا خيار مقابل فأفرغ الحقل. غياب الحقلين (‏CLI أقدم) يُبقي العقد
  والقائمة الثابتة حرفياً. يغطيه `test:claude-models` الموسَّع (تنقية العقد + منتقي
  الجهد الفعلي مستخرجاً من `app.js` داخل DOM مصغّر)، وأُثبت أنه يعضّ بعشر طفرات مستعادة.
- **المقيس حياً عند التنفيذ** (‏Claude Code `2.1.258`، ‏SDK `0.3.176`، مسبار قراءة فقط
  بلا دور): ستة نماذج — خمسة تعلن `supportsEffort:true` بالمستويات الخمسة كاملةً، و
  **`haiku` لا يعلن حقول جهد إطلاقاً** (أربعة مفاتيح فقط). وعدد الحقول ليس تسعة دائماً
  كما رُصد في 2026-08-27: `claude-fable-5-1[1m]` و`sonnet` بثمانية (بلا `supportsFastMode`).
- **حدّ مُصرَّح به يبقى مفتوحاً**: غياب الحقلين لا يميّز «نموذج بلا جهد» عن «CLI أقدم»،
  فغُلِّب التوافق الخلفي — أي أن `haiku` ما زال يعرض القائمة الثابتة كاملةً ويُخفَّض
  اختياره صامتاً. علاجه يحتاج إشارة تمييز من upstream (أو استنتاج نسخة CLI) ولم يُبنَ
  على تخمين.

## OBS-064 — لا مسبار يعلّق: أربع «تعليقات» كانت مهلة قياس قصيرة، وعقد المسبار هشّ أمام تباين النموذج
- **الوسم**: `process`
- **النوع**: عطل
- **الحالة**: منجزة تشخيصاً — `254866e` (2026-08-27): لا عطل تعليق أصلاً؛ بقي عقد هشّ بانتظار قرار مالك
- **الدليل**: جرد مخارج المسابير 2026-08-27 عبر `probe-runner` بمهلة `100`ث على عشرة
  مسابير تفتقر `process.exit`. النتيجة: **خمسة خرجت نظيفةً** (‏`browser-guard` 5ث ·
  `rtl-bounds` 5ث · `preview-contention` 3ث · `preview-latency` 16ث ·
  `browser-loop-live` 22ث)، و`fast-render` خرج برمز 1 وهو **حكمه الصادق** لا عطلاً
  (‏`fastSpeedRatio 1.115` مقابل `ffmpegSpeedRatio 0.056`، و`videoDeterministic:false`،
  و`p95DriftMs≈265` — يؤكّد `OBS-040` حرفياً)، وأربعة تجاوزت المهلة.
- **الحقيقة الوحيدة المثبتة**: `browser-loop-kimi-probe` **يعلّق فعلاً** — طبع خطأ
  `browser_loop_kimi_contract_failed` ثم استمر حتى المهلة رغم امتلاكه `app.quit()`.
  وبعد الخطأ لا عمل مشروعاً متبقياً، فالاستمرار تعليق لا بطء. ومعه **فشل عقد مريب**
  في التقرير نفسه: `clickRefs` كلها `sN:e200` عبر ست لقطات — ست نقرات على الفهرس
  نفسه. إمّا خلل في المسبار أو انحراف حقيقي في حلقة المتصفح؛ لم يُشخَّص بعد.
- **السبب المرجَّح (غير مثبت)**: `finally` ينفّذ
  `await new Promise((r) => fixture.server.close(r))` قبل `app.quit()`، و`server.close()`
  ينتظر **تصريف الاتصالات المفتوحة** — وهو الحدّ نفسه الموثّق في نواة مدير جولة
  TestSprite. اتصال MCP حيّ من Kimi يمنع التصريف فلا يُبلَغ `app.quit()` أصلاً.
  يلزم قياس قبل البناء عليه.
- **إيجابيات كاذبة في القياس نفسه — الدرس الأهم**: وُسمت ثلاثة أخرى «تجاوز المهلة»
  فقرأتُها «تعليقاً»، وهو **خطأ قراءة لا خطأ أداة**. `language-agentic-probe` و
  `language-drift-probe` يستدعيان `spawnSync('claude', …)` فيستهلكان أدواراً حقيقية —
  وتصنيفهما «رخيصين» في الجرد كان خطأً — وكلاهما `function main()` **متزامنة** بـ
  **صفر** `setTimeout`/`setInterval`/`new Promise`/`listen`، فلا يملكان مقبضاً يُبقيهما
  حيّين بعد `main()`. أي أنهما **لا يستطيعان التعليق**؛ كانا داخل نداء `claude` حين
  قتلتهما المهلة. و`cursor-hide-probe` يشغّل PowerShell وffmpeg فمئة ثانية قد لا تكفيه.
  القاعدة: **«تجاوز المهلة» ليس «تعليقاً»** — الفصل بينهما يحتاج مهلة أوسع من العمل
  الطبيعي، أو دليلاً أن العمل انتهى (خطأ مطبوع، تقرير مكتمل) والعملية باقية.
- **ولماذا لم يُمسَك آلياً**: الجرد الساكن كذب في **الاتجاهين** — غياب `process.exit`
  لا يعني تعليقاً (خمسة خرجت بـ`app.quit()`)، ووجود `app.quit()` لا يعني خروجاً
  (‏`browser-loop-kimi` يملكه ويعلّق). صنف `OBS-005` نفسه: العدّ الساكن يكذب.
- **مرشّحات العلاج**:
  - (أ) تشخيص `browser-loop-kimi-probe`: هل `server.close()` هو المعلِّق؟ العلاج
    المعتاد `server.closeAllConnections()` قبله، أو `app.exit()` بدل `app.quit()` في
    مسار الفشل. **بعد القياس لا قبله.**
  - (ب) فشل العقد `e200` المتكرر يستحق تشخيصاً مستقلاً — قد يكون انحراف حلقة متصفح.
  - (ج) إعادة قياس الثلاثة بمهلة أوسع (‏600ث) لحسم «بطء أم تعليق» — تستهلك أدواراً.

### تصحيح بعد التشخيص (2026-08-27) — الادعاء المركزي كان خاطئاً

قال هذا السجل إن `browser-loop-kimi-probe` «يعلّق فعلاً». **ليس كذلك.** أربع فرضيات
اختُبرت كلٌّ منها معزولةً، وسقطت جميعاً، ثم حسم التشغيل الحقيقي:

| الفرضية | كيف اختُبرت | النتيجة |
|---|---|---|
| `server.close()` ينتظر تصريف الاتصالات | إعادة بناء تسلسل التفكيك بلا Kimi | ❌ الـcallback في **1ms** |
| قناة keep-alive تُبقي مقبضاً حيّاً | دور Kimi قصير + التفكيك نفسه | ❌ خرج في **12ث** |
| اتصال `codexmcp` من نداء أداة متصفح | الدور نفسه مع `browser_snapshot` | ❌ خرج في **20ث** |
| مسار الاستثناء (‏`app.quit` ثم `app.exit`) | رمي مُصطنع بعد الدور | ❌ خرج في **18ث** برمز 1 |

**الحسم**: تشغيل المسبار الحقيقي بـ`SATR_BROWSER_LOOP_PAIRS=1` و`SATR_BROWSER_LOOP_RUNS=2`
ومهلة `480`ث ⇒ **نجح في 151 ثانية برمز 0**. كل دور ‏`~72–77`ث (‏`duration_ms` ‏77429 و
‏71950)، فبالإعدادات الافتراضية (‏`PAIRS=2` و`RUNS=3` ⇒ أربعة أدوار بستّ نقرات لكلٍّ)
يحتاج **خمس إلى عشر دقائق**. ومهلة القياس كانت **100 ثانية**.

**فالحصيلة النهائية: صفر مسبار يعلّق.** الأربعة الموسومة «تجاوز المهلة» كانت أبطأ من
مهلة قصيرة، لا معلَّقة. والجرد الساكن الذي أطلق الجولة كذب في الاتجاهين، ثم كذب قياسي
المتسرّع فوقه — وقعتُ في الفخّ **مرتين**: مرة حين قرأت «تجاوز المهلة» تعليقاً لثلاثة،
ومرة حين استثنيت هذا الرابع بحجّة «طبع خطأً فلا عمل متبقياً» — وهي حجّة تفترض أن الخطأ
وقع في النهاية، ولم يكن لديّ ما يثبت موضعه.

**والعائد ليس صفراً — بقي عطل حقيقي واحد**: عقد المسبار **هشّ أمام تباين النموذج**.
الفشل المرصود أول مرة كان `snapshots: 6` بينما `expectedSnapshots = RUNS*2+1 = 7` — أي
أن Kimi تخطّى لقطةً واحدة (غالباً بعد النقرة الأخيرة، ولا حاجة لها فعلاً)، وكل ما عداه
مطابق. وفي إعادة التشغيل جاء `snapshots: 5` مقابل متوقَّع `5` فمرّ. فالعقد يساوي
`!==` على عدد اللقطات، وهو سلوك نموذج لا عقد منصّة.

**وتصحيح ثانٍ**: وسم `clickRefs` كلها `e200` بأنه «مريب» كان خطأً — الـfixture فيه
‏`ELEMENTS-1 = 199` زر «عنصر» + زر `#target`، فالهدف هو العنصر **200** دائماً والجيل
وحده يتغيّر. و`s1:e200 … s4:e200` هو الصواب بعينه. بل إن وضع delta أثبت عمله: refs
تتزايد داخل الجيل الواحد (`s6:e200→e203`) وبايتات اللقطات `32170 ⇒ 6443`.

- **مرشّح العلاج المتبقي**: تخفيف عقد اللقطات من مساواة تامة إلى مدى (‏`>= RUNS*2`
  و`<= RUNS*2+1`) كي لا يفشل القياس على تحسين مشروع من النموذج؛ أو إبقاء الصرامة
  وتوثيق أن الفشل قد يكون تبايناً لا انحرافاً. **قرار مالك** — لم يُنفَّذ.
- **ما لا يغلقه**: لم يُقَس أثر `SATR_BROWSER_LOOP_*` على الكلفة، ولم يُعَد قياس
  الثلاثة الأخرى (`cursor-hide` و`language-agentic` و`language-drift`) بمهلة كافية —
  اثنان منها يستهلكان أدوار `claude` حقيقية. بقاؤها غير مقيسة **معلَن لا مسكوت عنه**.

## OBS-065 — `run_in_background` يكسر الأوامر الطويلة، والتفكير يُعرض ولا يُحفظ
- **الوسم**: `engines`
- **النوع**: عطل
- **الحالة**: مفتوحة
- **الدليل**: **سجل تفكير الوكيل** في جلسة تباعد حقيقية (‏`01a045a9`، محرك Codex،
  مجلد `D:\sater\Training`، 2026-08-28) — نقله المالك حرفياً:
  «طول الأمر تجاوز ما تتحمله طرفية PowerShell، فتعطّل PSReadLine وبقيت القشرة عند
  مؤشر `>>`». ومعه: «أداة سجل الطرفية قصّت بداية سطر Kimi الطويل، لذلك لم يظهر
  مفتاح `frame` للتحقق الآلي». وأُكِّد الأول بالكود: `term.js` يملك اللصق المُقوّس
  (`\x1b[200~`) ويستعمله `runCapture` وحده، بينما `termjobs.js:193` يكتب خاماً عبر
  `writeTerm` — فالحماية في الملف نفسه ولا تصل نصف مستعمليها.
- **المصدر**: بلاغ مالك بسجل تفكير الوكيل (لا لقطة)
- **المنجز في هذه الدفعة**: العطل الثاني (قصّ الذيل) **أُصلح** — `readBuffer` كان
  يقصّ بايتات خاماً فيشطر محرفاً عربياً **ويقطع السطر الأول بلا علامة فيبدو كاملاً**؛
  صار يحاذي حدّ UTF-8، ويُسقط السطر الجزئي، ويعلن `truncated` و`droppedBytes` وسطر
  «قُصّ N بايت من بداية السجل». يحرسه `test:term-longline` (داخل `test:full`).
- **العطل الأول — أُصلح (2026-08-28، دفعة تالية) وتشخيصه الأول كان خاطئاً**:
  - **الطول ليس العلّة — نُفي بالقياس**: مسبار على pty حقيقي مرّر أمراً **7986 محرفاً**
    (‏67 سطراً ملتفاً على طرفية 120×30) بالكتابة الخام، فوصل سليماً ونُفّذ وأعاد رمز
    خروجه الصحيح. فجملة «طول الأمر تجاوز ما تتحمله طرفية PowerShell» في سجل التفكير
    كانت **تفسير الوكيل لنفسه لا قياساً** — ودوّنّاها دليلاً فبنينا عليها مرشّحات علاج
    تعالج عرَضاً لا سبباً. **الدرس**: سجل التفكير دليلٌ على ما جرى، لا على سببه.
  - **العلّة الحقيقية**: `term.sanitizeCommand` يحذف `\n` **بلا بديل**، فسطور الأمر
    تلتصق: `$b = @'⏎نص⏎'@⏎Write-Output $b` ⟶ `$b = @'نص'@Write-Output $b`. والناتج
    **جملة غير مكتملة**، وهي المعنى الوحيد لمِحَثّ `>>` الذي عَلِقت عنده المهمة. أي أن
    العطل **إفسادٌ صامت للأمر** لا حدّ سعة — ويفسّر البلاغ حرفياً لأن وصفة التباعد
    تمرّر برومبت here-string متعدد الأسطر.
  - **العلاج المنفَّذ — مرشّح رابع لم يكن مدوَّناً**: الأمر يُمرَّر في **وسائط spawn**
    (`powershell.exe -EncodedCommand <base64 UTF-16LE>`) فلا يمرّ بمحرِّر السطر إطلاقاً،
    ومعه `term.sanitizeScript` يحفظ الأسطر الجديدة. قياس مقارِن بجملة غير مكتملة:
    المسار القائم **عَلِق حتى المهلة** عند `>>`، بينما وسائط spawn خرجت برمز 1 ورسالة
    `TerminatorExpectedAtEndOfString` صريحة.
  - **ولماذا سقط المرشّح (أ)** رغم أنه وُصف «الأقرب إلى الصواب»: يعمل فعلاً، لكنه يورث
    الاعتماد على `ExecutionPolicy`. جهاز التطوير `CurrentUser=RemoteSigned` فمرّ، بينما
    افتراضي ويندوز للعميل `Restricted` يحجب `& 'file.ps1'` — عطلٌ كانت بيئة التطوير
    ستُخفيه عن المستخدمين، نمط درس مرآة RTL نفسه. ويضيف قرصاً وتنظيفاً وBOM بلا مقابل.
  - **سقف مرصود أثناء الإصلاح**: صدى الأمر يضاعف طوله داخل السكربت، فرصد الحارس حيّاً
    `Cannot create process, error code: 206` (‏تجاوز سطر أوامر ويندوز) لأمر 7000 محرف.
    عولج بقصّ الصدى عند 400 محرف وفحص base64 قبل spawn مقابل 30000 ⇒ `script_too_long`.
  - **الحارس**: `test:term-longline` صار 30 فحصاً، و**بِتّته مُثبَتة**: أُعيد المسار
    القديم فسقط عند `انتهت مهلة: bg_term_done للمهمة متعددة الأسطر` — أي أنه يلتقط
    العلق الميداني نفسه لا مجرد غياب دالة.
  - **حدّ معلَن**: `cmd` وصدف POSIX تبقى على الكتابة إلى السطر (العطل خاص بـPSReadLine،
    وتحويل `bash` إلى `-c` يُسقط `.bashrc` فيتغيّر PATH — تغيير غير مقيس).
- **العطل الثالث — حُسم بالرفض (2026-08-28، جولة `satr-diverge` كاملة)**: عُرض السؤال
  «أين وكيف يُحفَظ تيار التفكير؟» على منهج القرار المتباعد — ثلاثة فروع معزولة، ناقد
  أعمى على 15 فكرة، مصحّحان متوازيان، مواجهة مجهّلة، ثم ناقد خصومي جديد. **الحكم:
  لا تُبنى الميزة.** أربعة اعتراضات قاتلة، كلها مسنودة:
  1. **المشكلة انتفت في المسار الافتراضي** قبل أن تُبنى: أُصلحت في هذه الدفعة نفسها،
     و`defaultShell()` يعيد `powershell.exe` على كل ويندوز 10+.
  2. **المقياس بلا نقطة صفر**: `buildPwshJobScript` يضيف 4 أسطر **بالتصميم**،
     و`pwshSingleQuote` يضاعف الاقتباس، وفرع `sanitizeCommand` يحذف الأسطر **عمداً** —
     فالفرق البنيوي غير صفري في المسار السليم، وأرضية الضجيج تساوي الإشارة.
  3. **العائد صفر إزاء حارس قائم**: `test:term-longline` يمسك الانحدار في CI **قبل
     الشحن** بدل أن يُقرأ سجلٌّ بعد أن تعلق مهمة المستخدم.
  4. **معدّل الوقوع 1 من 65** ملاحظة من هذا الصنف بالضبط.
  ويسنده قرار المالك القائم في `OBS-057`: قاعدة «أرقام بلا نص» **مقايضة لا عطل**،
  و«العلاج ليس تخفيف القاعدة على السجل الدائم».
  **وادّعاء غير مثبت في صلب المرشّح**: ما كشف عطل هذه الدفعة فعلاً **مسبار قياس على
  pty حقيقي**، لا عدّاد فرق — والعدّاد يفترض أن الشكّ وقع سلفاً على حدّ التسليم.
  **المنفَّذ بدلاً منها** (البديل الأرخص، بقرار مالك): `term.structuralDelta` دالة
  نقية **بلا مستدعٍ في الإنتاج** — يحرسها فحص صريح — وجدولُ ثوابت في
  `test:term-longline` يثبّت التحويل المعلَن لكل فرع صدفة رقماً. صفر قرص وصفر خصوصية
  وصفر سطح إنتاج، والبِتّة مُثبَتة: إضافة سطر واحد إلى الغلاف تُسقط الحارس.
  **معيار إعادة الفتح**: عطلٌ واحد يعجز `test:full` عن مسكه **ويحتاج نصّ التفكير**
  لتشخيصه — بدليل عجزٍ مقيس لا بشبهة حاجة.
- **وصف العطل الثالث كما رُصد أصلاً**: **تفكير الوكيل يُعرض ولا يُحفظ**.
  «سطر» يبثّه حيّاً فيراه المستخدم، لكن التخزين ملك المحرك: ملف `rollout` لدى Codex
  يكتبه **مشفّراً** (‏`encrypted_content` ×71 وصفر `summary_text` في الجلسة نفسها).
  فبعد انتهاء الجلسة يتعذّر تشخيص «لماذا» — وهذه الدفعة كلها خرجت من تفكير نقله
  المالك يدوياً، ولولا نقله لبقيت الأعطال الثلاثة مجهولة. المرشّح: حفظ تيار
  `commentary` محلياً بحدوده وحجب أسراره، وهو دفعة مستقلة بحاجزها.
- **وثلاثة حدود محرّكات رُصدت عرَضاً — حُسمت كلها بالفحص (2026-08-28)**:
  - **Kimi ‏0.38.0 يرفض جمع `--prompt` مع `--plan` — مؤكَّد بقياس حيّ**:
    `error: Cannot combine --prompt with --plan.` ورمز خروج `1`. ولا بديل يفرض القراءة
    فقط في وضع `-p`: جرد `--help` يعرض `--yolo` و`--auto` وكلاهما **يوسّع** الصلاحيات.
    ⇒ دُوّن في وصفة `satr-diverge` كي لا يُضاف العلم غريزةً فيُسقط الفرع قبل أن يبدأ.
  - **اسم نموذج Claude**: `--model opus` مقبول، فليس عطلاً — أُسقط.
  - **دالة الترميز الغائبة**: كانت في مِشْغَل الوكيل لا في المهارة — أُسقط.

## OBS-066 — `npm` العارية تُحجب بـExecutionPolicy، والزرّ المنفِّذ يفشل صامتاً
- **الوسم**: `process`
- **النوع**: عطل
- **الحالة**: منجزة — مسار مفتاح API في البوابة + force
- **الدليل**: بلاغ مستخدم بلقطة (فشل التثبيت)، ثم **إعادة إنتاج حيّة** على جهاز
  التطوير بلا تغيير أي إعداد:
  ```
  powershell -ExecutionPolicy Restricted -Command "npm --version"
    ⇒ npm.ps1 cannot be loaded because running scripts is disabled on this system
  powershell -ExecutionPolicy Restricted -Command "npm.cmd --version"
    ⇒ 11.17.0
  ```
  والسبب البنيوي مقيس: `Get-Command npm` يرتّب `C:\Program Files\nodejs\npm.ps1`
  (‏ExternalScript) **قبل** `npm.cmd`، و`ExecutionPolicy` تحجب ملفات السكربت لا
  الملفات الدفعية. وافتراضي ويندوز للعميل `Restricted`.
- **المصدر**: بلاغ مالك + تحليل وكيل، ثم تحقّق مستقل بالقياس
- **الأثر — أوسع من البوابة**: تسعة مواضع كانت تشحن `npm` العارية، وأحدها
  **يُنفَّذ لا يُعرض**: `enginesupdate.js` يشغّل أمر التحديث فعلاً في طرفية
  PowerShell عبر `termjobs`، فزرّ «حدّث المحرك» كان **يفشل صامتاً** على كل جهاز
  بالسياسة الافتراضية. أي أن العطل يصيب مستخدمين مثبّتين لا مستخدمين جدداً فقط.
- **المنجز**: `readiness.NPM_BIN` مصدراً واحداً (`npm.cmd` على ويندوز، `npm` على
  غيره) يستهلكه `enginesupdate.js`؛ وتحديث النصوص في `gate.js` و`app.js` و
  `codex.js`. ويحرسه `readiness-test.js` بفحص سببي لا نصّي: أوامر التثبيت
  والتحديث تبدأ بـ`npm.cmd` على ويندوز، **ولا `npm` عارية في أي من الملفات
  الخمسة الشاحنة**.
- **تصحيح لتحليل سابق**: قيل إن العلاج مشروط بأن يصير «سطر» هو المنفّذ لا
  المستخدم. **غير صحيح** — الأمر المنسوخ يدوياً بـ`npm.cmd` يعمل تحت `Restricted`
  كما يثبت القياس أعلاه. فتسقط فئة العطل كلها بلا أتمتة ولا واجهة جديدة.
- **ما بقي مفتوحاً** (دفعة البوابة، بقرار مالك «الاثنان معاً» 2026-08-29): كشف
  **سبب** الفشل بدل عرض وصفة واحدة عمياء · أزرار تنفيذ تعيد استعمال عقد
  `satr:engineUpdateRun` القائم · تقرير تشخيص للنسخ. هذه الثلاثة باقية عمداً ولم
  تنفّذها دفعة مسار المفتاح.
- **ما لا يفتحه**: لا Node مضمَّن ولا تثبيت claude داخل `~/.satr` — قرار مؤجَّل
  بثمنه المعلن (‏+30–40م.ب وانتقال مسؤولية الترقية إلينا).
- **مرجع العمل**: الفرق غير ملتزم عمداً فوق `15bf25a` امتثالاً للتكليف؛ يستبدل
  القائد هذا المرجع بمرجع التزام التنفيذ عند الدمج.

## OBS-067 — استئناف جلسة يغيّر مجلد عمل المستخدم صامتاً ويثبت
- **الوسم**: `ui`
- **النوع**: عطل
- **الحالة**: مفتوحة — الإصلاح مُنفَّذ؛ دفاع الواجهة وتنظيف الأرشيف مؤجَّلان
- **الدليل**: بلاغ مالك بلقطة (جلستان متتاليتان، الثانية في مجلد أعمق لم يختره)، ثم
  تشخيص بالقياس على أرشيفه الفعلي:
  - أداة `Bash` في Claude Code تملك صدفة معمّرة، فحين ينتقل الوكيل بـ`cd` يسجّل CLI
    المجلد الجديد في كل سطر تالٍ. **قياس على جلسة حقيقية**: انزاح `cwd` ‏**37 مرة**
    داخل جلسة واحدة (‏`7ad95229`، ‏3217 سطراً) بدأت في `D:\alulama` وآخر قيمة
    مسجَّلة `D:\alulama\packages\erp-poc`.
  - و**ملفّانا كانا يقرآن الحقل بقاعدتين متعاكستين**: `sessions.js:104` يأخذ **أول**
    `cwd` (للسرد)، و`buildMessages` كان يأخذ **آخره** بتعليق «آخر cwd في الملف هو
    الأحدث» — وهي جملة صحيحة عن **الصدفة** وخاطئة عن **المشروع**.
  - فعند الاستئناف يكتب `src/ui/app.js:1894-1895` القيمة المنزاحة في حقل المجلد
    **وفي `localStorage`**، فيتغيّر مجلد العمل صامتاً ويبقى بعد إعادة التشغيل، وتُولد
    كل جلسة تالية في المجلد الخطأ.
- **المصدر**: بلاغ مالك بلقطة + تشخيص مستقل بالقياس
- **جرد الضرر (مقيس، 156 جلسة)**: **لا ضرر بالملفات**. فحص بنية المرآة لم يجد أي
  مجلد منزاح يحوي نسخة من بنية جذره ⇒ لم تُكتب ملفات في مسار مضاعف. ولا ذاكرة مشروع
  ولا سجل مهام يتيم للمجلدات المنزاحة. من 16 جلسة «بدأت في مجلد فرعي» كان 11 مسارات
  أدوات مقصودة (‏Temp ومسابير وworktrees)، واثنتان مشروعاً شرعياً
  (`D:\islamic story\islamic-story` مستودع git مستقل داخل مجلد حاوية) ⇒ **الحالة
  المؤكَّدة واحدة** هي المُبلَّغ عنها.
- **حدّ الخطورة — معلن**: نصف قطر الانفجار محكوم **بموضع `cd` الذي وقع فعلاً**. في
  كل ما رُصد كان المجلد المنزاح **سليل** الجذر، فالأثر تضييقُ مجال رؤية الوكيل لا
  توسيعه (حرّاس `files.js`/`inject.js`/`tools.js` تحصر داخل cwd). لكن `cd` صاعداً —
  إلى `D:\` مثلاً — كان سيولّد العكس. فالعطل لم يكن حميداً بطبعه بل بالمصادفة.
- **المنجز**: `buildMessages` يأخذ **أول** `cwd` فيتفق الطرفان ومعهما اسم مجلد الأرشيف
  الذي يشتقّه Claude Code نفسه. يحرسه `test:sessions-cwd` (‏11 فحصاً، داخل
  `test:full`) — **وبِتّته مُثبَتة**: إعادة السطر القديم تُسقطه بالقيمة المنزاحة
  نفسها من البلاغ.
- **والمعقل الثاني — نُفِّذ أيضاً (بطلب مالك)**: السلوك الصامت لم يكن العلّة بل
  **مُضخِّمها**، ويبقى ضاراً بعد إصلاحها في حالة لا تحتاج انزياحاً أصلاً: من يفتح
  جلسة مشروع آخر **ليقرأها** كان مجلد عمله يتبدّل بلا إشعار ويبقى متبدّلاً بعد إعادة
  التشغيل. والتطبيق يملك حارساً لهذه الحالة (‏`send()` يُعلم ويبدأ جلسة جديدة حين
  يغيّر المستخدم المجلد بنفسه) لكنه **لا يشتعل هنا بنيوياً** لأن `sessionCwd` يُزامَن
  فوراً مع القيمة الجديدة فيتساوى الطرفان.
  استُخرجت المسارات الثلاثة المتطابقة (‏Claude/Codex/Kimi) في `applyResumedCwd`
  واحدة: تبدّل ثم **تُعلم بزرّ «↩ أعِد مجلدي»** عارضةً المجلدين. القاعدة المعلنة
  **«أخبِر ولا تسأل»** — مربع تأكيد مرفوض عمداً لأنه يضع احتكاكاً على فعل يتكرر
  عشرات المرات ويعاقب الحالة الشائعة السليمة. والتراجع يصفّر الجلسة صراحةً لأن جلسات
  Claude Code مرتبطة بمجلدها، فيُقال ذلك فوراً بدل أن يُكتشف عند أول إرسال. وفرق حالة
  الأحرف لا يُزعج (ويندوز لا يميّزها)، وأول تشغيل بلا مجلد سابق لا يُعلم.
- **ما بقي مفتوحاً**: (أ) «مشروع» وهمي واحد بقي في الأرشيف (`erp-poc`) ويلوّث أي
  تجميع بالمشروع؛ (ب) قيمة `localStorage` لدى المالك ما زالت منزاحة حتى يصحّحها من
  الشريط.
- **ما لا يفتحه**: اشتقاق المجلد من اسم مجلد الأرشيف — يتجنّبه الكود عمداً
  (`sessions.js`) لأن ترميز الاسم هشّ.

## OBS-068 — لوحة الجلسات تبتر الأرشيف، والبحث يرشّح النافذة لا الأرشيف
- **الوسم**: `ui`
- **النوع**: عطل
- **الحالة**: مفتوحة — البتر والتجميع ومرشّح الأدوات ووسم جلسات الأدوات (ب) مُنفَّذة؛
  العناوين المتشابهة (أ) قِيست ورُفض علاجها، ويبقى (ج) `erp-poc` قرارَ مالك
- **الدليل**: بلاغ مالك بلقطة («أبحث عن جلسات مشاريع سابقة وتهت»)، ثم جرد أرشيفه
  الفعلي:
  - **156 ملفاً** (‏143 بمحادثة فعلية)، واللوحة تعرض **100** ⇒ **43 محجوبة**.
  - أقدم جلسة معروضة **قبل 17 يوماً**، وأقدم ما على القرص **قبل 26 يوماً**.
  - **22 مشروعاً متميّزاً** داخل المئة المعروضة، وأحدها (`satr-2`) يأخذ **36%**.
  - **41 عنواناً من 100** يشترك في أول 30 محرفاً مع عنوان آخر.
- **العلّة المركزية**: `sessions.js` كان يقصّ عند `MAX_SESSIONS = 100` **قبل** أن
  يصل شيء إلى الواجهة، و`_render()` في اللوحة يرشّح `this._data` المحمَّل. أي أن حقل
  البحث **مرشّحٌ فوق نافذة لا بحثٌ في أرشيف** — فما يبحث عنه المستخدم قد لا يكون في
  مجموعة البحث أصلاً، واللوحة تقول «لا نتائج مطابقة» فيظن أنه أخطأ الكلمة.
- **والسقف كان يحمي من كلفة غير موجودة — مقيس**: مسح الأرشيف كاملاً بقراءة رأس
  64ك.ب لكل ملف = **62ms** لـ156 ملفاً (‏337ms أول مرة بكاش بارد). ولا يُقلَّل
  `HEAD_BYTES`: ‏16ك.ب ⇒ 125/156 التقاطاً مقابل 64ك.ب ⇒ **143/156** — التوفير 10ms
  والخسارة 18 جلسة.
- **المصدر**: بلاغ مالك بلقطة + جرد وقياس مستقلان
- **المنجز**: `MAX_SESSIONS` ‏100 ⟶ **1000** (‏أسوأ حالة ~0.4ث بنفس المعدّل) ·
  **تجميع بالمشروع** برؤوس قابلة للطيّ (‏المثبّتة أولاً، ثم المشروع الحالي مفرود
  والبقية مطوية، والبحث يفرد المطابق) فتصير 22 مجموعة بدل 100 صفّ متشابك ·
  **مرشّح «أخفِ جلسات الأدوات»** مفعّل افتراضياً، والمثبّتة تنجو منه دائماً ·
  وعدّاد يعلن «‏N من M» فلا يكون الإخفاء صامتاً.
- **حدّ الكشف معلن (صار احتياطاً بعد البند ب)**: كان الكشف **بيقين من المسار**
  (`~/.satr/worktrees` أو `Temp`) بلا تخمين من نمط اسم مجلد — لأن مجلدات مثل
  `<project>-opus` و`<project>-wt` مجلدات **حقيقية على قرص المستخدم** لا يميّزها شيء،
  فتخمينها كان سيخفي عمله هو. صار المسار **احتياطاً للجلسات القديمة** وحدها، والأساس
  وسمُ الجلسة وقت إنشائها (البند ب أدناه). ولم يُزَل الاحتياط: كل جلسة سبقت الوسم بلا وسم.
- **(أ) العناوين المتشابهة — قِيست ورُفض علاجها (2026-09-02)**: ثلاث استراتيجيات
  قُيست على 146 عنواناً حقيقياً، وكلها **أسوأ من القائم أو بلا أثر**:
  - الحالية (تسطيح + قصّ 90): **116 فريداً · 47 مكرّراً**.
  - أول سطر + إسقاط markdown: **105 فريداً · 55 مكرّراً** ⬇ أسوأ. السبب في العيّنة:
    `[عامل منفّذ معزول…] نفّذ المهمة التالية بتعديل…` يصير `[عامل منفّذ معزول…]` وحده،
    فيُفقد ما يميّزه وتتطابق أربعة صفوف كانت مختلفة.
  - إضافة إسقاط البادئات المعروفة: **بلا أي تحسّن** (‏105/55).
  - تمديد العنوان المخزَّن 90 ⟶ 500 محرفاً: **39 ⟶ 35** مكرّراً فقط. ومن مجموعات
    التكرار عند 90 توجد **ثلاث** فيها اختلاف لاحق، ومواضعه عند المحارف 147 و297 و298
    — بعيداً عن أي طول عرض معقول (اللوحة تقصّ سطرين).
  **والسبب الحقيقي**: **29 رسالة متطابقة حرفياً** — نفس الطلب أُرسل مرات. فالعناوين
  لا تُصلَح باشتقاق أفضل لأن الطلبات نفسها واحدة.
  **ومصدرها**: من 47 مكرّراً يكشف مرشّح الأدوات **10** فقط؛ والباقي جلسات وكلاء
  ومسابير جرت **داخل مجلدات مشاريع حقيقية** (`شغّل الأمر: echo تشخيص` ×6 و`تعمل ما
  أهم ثلاثة أخطاء` ×4 من طقم الاختبارات في `satr-2`). أي أنه **أثر جهاز المطوّر** إلى
  حدّ بعيد: أرشيف مستخدم عادي لا يشغّل `test:full` لن يحمل هذه.
  ⇒ **لا تغيير في اشتقاق العنوان**. العلاج الوحيد المسنود هو وسم جلسات الأدوات وقت
  إنشائها (البند ب)، وهو يغطي جزءاً لا كلاً.
- **(ب) وسم جلسات الأدوات وقت إنشائها — منفَّذ (2026-09-03، دفعة موجة أوبس فوق
  `44ac91f`؛ **يستبدل القائد هذا المرجع برقم التزام الدمج** — المنفّذ لا يلتزم)**.
- **المنجز**: حقل `kind` **additive** في `electron/sessionmeta.js` بقائمة مغلقة
  (`['tool']`) و`setKind(sessionId, kind)` بتنقية `SAFE_SESSION` نفسها وكتابته الذرية ·
  والوسم يقع في **خمسة مواضع إنشاء** حين يعرف كلٌّ منها `session_id` من حدث المحرك:
  `executor.js` و`reviewer.js` (داخل `applyReviewEvent` — سياسة العمى المشتركة، فتغطي
  هيئة القضاة و`reviewOnce` معاً بنسخة واحدة) و`orchestrator.js` و`opsbrainstorm.js`
  و`opsplanner.js` · واللوحة تقرأ **الوسم أولاً** ثم تتراجع إلى `TOOL_PATH`.
- **قراران في التنفيذ**:
  1. **الوسم لا يمرّ من renderer**: قائمة سماح `set` بقيت `pinned/title` وحدهما، و
     `satr:sessionMetaSet` في `main.js` **لم يُمَسّ** — فلا تستطيع الواجهة إخفاء جلسة
     مستخدم بادّعاء أنها أداة. و`satr:sessionMetaList` يمرّر `sessionmeta.list()` كما هو
     فيعبر الحقل الجديد بلا سطر واحد في `main.js`.
  2. **حصّة `MAX_TOOL_ENTRIES = 200`** داخل سقف الـ500: دورة غرفة عمليات واحدة قد تسم
     نحو عشر جلسات، فبلا حصّة كانت الوسوم تملأ المخزن خلال أسابيع ثم يعيد `set` خطأ
     `limit` **صامتاً** (اللوحة تتجاهل الفشل: `if (!result || !result.ok) return;`)
     فيتعطّل زرّ التثبيت. الإخلاء FIFO ويقتصر على **الوسوم الخالصة** (بلا تثبيت ولا
     عنوان)، فقرارات المستخدم الصريحة لا تُطرد أبداً. وثمنه معلن: وسمٌ أُخلي يعود صاحبه
     للظهور إن لم يكشفه المسار.
- **ما بقي مفتوحاً**: (ج) «مشروع» وهمي واحد (`erp-poc`) من `OBS-067` ما زال في
  الأرشيف — **قرار مالك** (حذفه لمسٌ لأرشيف الجلسات، وهو خارج ما تفعله هذه الدفعة).
- **التحقق**: `test:sessions-panel` (‏Electron حيّ، **10** فحوص، داخل `test:full`)
  يشغّل المكوّن الإنتاجي تحت CSP: التجميع، وإخفاء الأدوات وكشفها، وترتيب المثبّتة
  والمشروع الحالي، والطيّ **يُخفي صفوفاً فعلاً**، و**البحث يصل جلسة داخل مجموعة مطوية**
  — وهو جوهر العطل المُبلَّغ. وأُضيف له فحص البند ب: **ثلاث جلسات في مجلد المشروع
  الحقيقي نفسه** — موسومة تُخفى، وتوأمها غير الموسوم يظهر، وموسومة مثبّتة تنجو —
  فلا يمكن اجتيازه بتخمين اسم المجلد. ومعه عقد ساكن يمنع عودة السقف الصغير أو تقليل
  `HEAD_BYTES`، ويثبّت أن الوسم **يُقرأ قبل** `TOOL_PATH` وأن قائمة سماح `set` لم تكسب
  مفتاحاً. و`test:sessionmeta` كسب نصفاً ثانياً: وحداتِ `setKind` (القائمة المغلقة،
  حجبه عن renderer، بقاؤه مع التثبيت/التسمية، لا كتابة قرص للوسم المتكرر، والحصّة
  المتدحرجة مع بقاء زرّ التثبيت عاملاً)، و**فحص وصل** يشغّل الوحدات الخمس الإنتاجية
  بمحرّك مزيّف يبثّ `system` ذا `session_id` ويثبت أن كلاً منها وسم جلستها فعلاً.
  **مُثبت أنه يعضّ**: حذف سطر الوسم من كلٍّ من الملفات الخمسة أسقط `test:sessionmeta`
  خمس مرات باسم الموضع، وحذف قراءة الوسم من اللوحة أسقط `test:sessions-panel` عند
  عقده الساكن، ثم — بتعطيل العقد الساكن مؤقتاً لبلوغ الطبقة الحيّة — عند الفحص الحيّ
  برسالة «الجلسة الموسومة أداةً ظهرت رغم الإخفاء».

## OBS-069 — لقطات صفحة الهبوط تحمل تسميات المحرّكات القديمة بعد جولة إعادة التسمية
- **الوسم**: `ui`
- **النوع**: تحسين
- **الحالة**: منجزة — تحديث fixture اللقطات وإعادة توليدها (2026-09-03، دفعة موجة أوبس فوق
  `de93127`؛ **يستبدل القائد هذا المرجع برقم التزام الدمج** — المنفّذ لا يلتزم).
- **الدليل**: جولة إعادة التسمية 2026-09-02 غيّرت تسميات قائمة «المحرك» في
  `src/index.html` و`app.js` وسجلّ المحوّلات (مبدأ «العلامة + طريقة الدخول»)، بينما
  `scripts/fixtures/site-shots.html:53` ما زال يعرض «SDK — بث وأذونات حية»، ولقطات
  `site/assets/` المولَّدة منه تعرضها للزوار.
- **المصدر**: جولة إعادة التسمية (رُصد ولم يُنفَّذ — توليد اللقطات ثقيل ويدوي
  ‏`npm run site:shots` وخارج نطاق الجولة)
- **الملاحظة**: عند أول تحديث قادم لصفحة الهبوط: حدّث fixture اللقطات ثم أعد
  `site:shots` (حتمي — الزمن مجمَّد) كي تطابق اللقطات الواجهة الفعلية.
- **إضافة (2026-09-03، تقرير منفّذ Opus في دفعة المنصات)**: `scripts/site-shots.js` لا
  يلتقط المقطع الجديد `#platform-interest` (لا مدخل له في `SHOTS`) — يُضاف مع التحديث نفسه.
- **المنجز**: `scripts/fixtures/site-shots.html` — خيارات المحرك الأربعة منسوخة حرفياً من
  `src/index.html:206–209` (‏`Claude — اشتراك Claude Code` · `Codex — اشتراك ChatGPT` ·
  `Claude CLI — احتياطي` · `Google Gemini — مفتاح API`) بدل الخيار الواحد القديم.
  و`scripts/site-shots.js` كسب مدخل `#platform-interest`، مع تعميم مقطع `sitePage` بحقلَي
  `section` و`anchorScroll`: الرسوّ بإزاحة `anchorOffset` الإنتاجية لا `scrollIntoView`،
  فتُظهر اللقطة ما يراه الزائر بعد إصلاح ‏OBS-071. الناتج `dist/platform-interest-preview.png`
  ‏`1440×1400` (‏264ك.ب)، ورسوّه المقيس: رأس المقطع `86` · حافة الرأس `73` · أسفل المقطع
  `1394` داخل نافذة `1400` — المقطع كامل بلا قصّ.
- **نتيجة التشغيل الواحد** (`npm run site:shots`): **5 من 9** لقطات `site/assets` عادت
  **متطابقة بايتاً** (‏`app-term` و`app-preview` وثلاث لقطات التوليد) — دليل مباشر على أن
  المسار حتمي فعلاً. تغيّرت أربع: `app-chat` و`app-tasks` بالمتوقَّع (تسمية المحرك)، و**`app-ops`
  و`app-judges` بانحراف خارج نطاق الملاحظة**: fixture غرفة العمليات لم يتغيّر لكنه يشغّل
  المكوّن الإنتاجي، والمكوّن أُعيد بناؤه بعد توليد اللقطات (‏`b091715`/`243a64b`/`6cd1cd4` —
  «المسار الموجّه»). فكانت الصفحة تعرض للزوار **تبويبات حُذفت نهائياً** (‏العمل/النتائج/السجل)
  وتاريخاً هجرياً استُبدل بميلادي — أي النسخة الأشدّ من العطل نفسه الذي تصفه هذه الملاحظة.
  اللقطتان الجديدتان تعرضان شريط المحطات الخمس الحالي (متحقَّق بصرياً قبل/بعد).

## OBS-071 — إزاحة المراسي الثابتة `-60` في صفحة الهبوط تُخفي رأس المقطع خلف الرأس الملتفّ على الجوال
- **الوسم**: `ui`
- **النوع**: صقل
- **الحالة**: منجزة — إزاحة مشتقّة من ارتفاع الرأس الفعلي (2026-09-03، دفعة موجة أوبس فوق
  `de93127`؛ **يستبدل القائد هذا المرجع برقم التزام الدمج** — المنفّذ لا يلتزم).
- **الدليل**: تقرير منفّذ Opus (دفعة المنصات 2026-09-03) بقياس حي في Chromium عند
  390px: الرأس `position: fixed` يلتف إلى ~151px، بينما معالج المراسي في `site/js/main.js`
  يستعمل إزاحة ثابتة `-60` لكل روابط الموقع؛ بعد نقر `#platform-interest` يستقر رأس
  المقطع عند ~61–68px فيقع الوسم العلوي عند حافة الرأس تماماً (‏145 و152 في تشغيلين).
  سلوك عام لكل مراسي الموقع لا خاصية المقطع الجديد.
- **المصدر**: تقرير منفّذ (رُصد ولم يُنفَّذ — تغيير الإزاحة يمسّ كل المشاهد المعتمدة)
- **تحفّظ على القياس**: `lenis.scrollTo` لا يكمل بثبات في نافذة offscreen (تشغيلان أعطيا
  نتيجتين)؛ يُحسم بنقرة يدوية على جهاز حقيقي قبل أي تعديل.
- **مرشّح العلاج**: اشتقاق الإزاحة من ارتفاع الرأس الفعلي (`header.offsetHeight`) بدل
  الثابت، ثم إعادة `site:shots` لأن المشاهد المعتمدة تتأثر.
- **المنجز** (‏`site/js/main.js`): الثابت `-60` استُبدل بـ`anchorOffset()` =
  ‏−(`header.offsetHeight` + `ANCHOR_GAP` = 12)، **تُقاس وقت النقر** لا وقت التحميل كي تتبع
  التفاف الرأس وتغيّر حجم النافذة. ومعالج المراسي صار مشتركاً (`initAnchors`) بين مسار Lenis
  ومسار reduced-motion الذي **لم يكن له معالج أصلاً** فكان يعتمد قفزة المتصفح الأصلية — أي أنه
  كان يبتلع ارتفاع الرأس كاملاً لا `91` بكسل فقط.
- **القياس** (‏Chromium حي على `site/index.html`، تمرير مباشر بالإزاحة التي تحسبها **دالة
  الإنتاج نفسها**، فلا اعتماد على إكمال `lenis.scrollTo` في نافذة offscreen — وبذلك يرتفع
  التحفّظ المسجَّل أعلاه. «مخفي» = حافة الرأس السفلى − رأس المقطع، على
  `#platform-interest` و`#download` و`#features`):
  - **390px** (ارتفاع الرأس `151`، الإزاحة الجديدة `-163`): قفزة المتصفح الأصلية بلا معالج ⇒
    مخفي `150–151` · بـ`-60` ⇒ رأس المقطع `60–61` ومخفي `90–91` وأول نص عند `140–147`
    أي على حافة الرأس `151` تماماً (يطابق `145` و`152` المرصودين) · بعد الإصلاح ⇒ رأس المقطع
    `163–164` ومخفي `0` وأول نص `243–248`.
  - **1280px** (ارتفاع الرأس `73` لا `60`، الإزاحة الجديدة `-85`): قفزة أصلية ⇒ مخفي `72–73` ·
    بـ`-60` ⇒ مخفي `12–13` — أي أن **العطل كان يمسّ سطح المكتب أيضاً** بدرجة أخفّ لأن الثابت
    القديم كان أقصر من الرأس هناك كذلك · بعد الإصلاح ⇒ مخفي `0`.
  - الأرقام متطابقة في المسارين (‏Lenis وreduced-motion)، و**نقرة حقيقية** على
    `a[href="#download"]` تحت `--force-prefers-reduced-motion` رست عند `164` (‏390px) و`85`
    (‏1280px) بمخفي `0` — مسار الإنتاج كاملاً من طرف إلى طرف.

## OBS-072 — `runCapture` يتشابك خرجه تحت bash: بروتوكول علامتَي الالتقاط مبنيّ على PowerShell 5.1
- **الوسم**: `engines`
- **النوع**: عطل
- **الحالة**: مفتوحة
- **الدليل**: أول تشغيل لـ`platform-trial.yml` (‏`33697105032`، 2026-09-03) على
  `ubuntu-latest`: كل شيء نجح (‏`npm ci`، الصياغة، 11 اختباراً نقياً، تعبئة `linux-unpacked`)
  عدا `test:termjobs` الذي سقط عند `scripts/termjobs-test.js:93` برسالة
  `خرج النداء الأول متشابك` — أي أن pty عمل والصدفة أُطلقت، لكن التقاط النداء الأول تداخل.
- **المصدر**: CI تجريبي (قياس لا إصدار — قرار المنصات 2026-09-03)
- **الملاحظة**: `term.runCaptureNow` يفترض PowerShell (اللصق المقوّس، `$LASTEXITCODE`/`$?`،
  علامة `END:<code>:<ok>`)؛ فرع `sh`/`bash` القائم يطابق النمط القديم فقط. هذا هو **منفذ
  `shell`** في قاعدة المنافذ الأربعة (`docs/PLATFORMS-DECISION-2026-09.md`)، ولا يُعالج إلا
  حين يُفتح `term.js` لسبب آخر أو تتحقق عتبة ماك.

## OBS-073 — node-pty يفشل بـ`posix_spawnp failed` على macOS: الصدفة لا تُطلق أصلاً
- **الوسم**: `engines`
- **النوع**: عطل
- **الحالة**: مفتوحة
- **الدليل**: التشغيل نفسه على `macos-latest` (‏darwin-arm64): كل شيء نجح بما فيه تعبئة
  `mac-arm64` غير الموقّعة، عدا `test:termjobs` الذي سقط عند `termjobs-test.js:42` بعد
  `[term] فشل تشغيل الصدفة: posix_spawnp failed.` — قبل أي خرج.
- **المصدر**: CI تجريبي (قياس لا إصدار)
- **الفرضية المرجّحة (غير مثبتة)**: `node_modules/node-pty/prebuilds/darwin-arm64/spawn-helper`
  (موجود في الحزمة، 50KB) يفقد صلاحية التنفيذ بعد `npm ci` — حالة موثقة لدى node-pty تُنتج
  هذه الرسالة حرفياً. تُحسم بخطوة `chmod +x node_modules/node-pty/prebuilds/darwin-*/spawn-helper`
  قبل الاختبار في التشغيل التالي؛ إن نجحت فهي سطر في **منفذ `installers`**، وإن لم تنجح
  فالعلّة في الـprebuild نفسه تحت `npmRebuild:false`.
- **التشخيص المضاف (2026-09-03)**: أُضيفت خطوتان في `platform-trial.yml` قبل
  `test:termjobs` — `تشخيص node-pty` تطبع `SHELL` وصلاحيات `prebuilds/*/` وتُجري
  فحص pty صغير، ثم `إصلاح صلاحية spawn-helper (تجريبي)` تُطبّق `chmod +x` وتُعيد
  الفحص — كلتاهما `continue-on-error: true` ومُدرجتان في `GITHUB_STEP_SUMMARY`؛
  النتيجة تُحدّد ما إذا كانت المشكلة صلاحية أم عطب أعمق في الـprebuild.

## OBS-070 — معرّفات نماذج NVIDIA NIM وGroq تنتظر إثبات المسبار الحيّ بمفتاحين
- **الوسم**: `engines`
- **النوع**: تحسين
- **الحالة**: منجزة — أُغلقت 2026-09-02 بجولة الإثبات الحي (التزام جولة المسبار
  التالية لالتزام `3a18704` مباشرةً)
- **الدليل**: جولة «النماذج المجانية» 2026-09-02 سجّلت `nvidia` و`groq` فوق مصنع
  `openai-compatible.js`، ومعرّفات النماذج الأولى كانت من التوثيق المنشور لا من
  استدعاء حيّ. **وقد أثبت الإغلاقُ صحةَ القاعدة حرفياً**: بعد إنشاء المالك المفتاحين
  رفض السلكُ المعرّفين المدوَّنين كليهما — نموذج NVIDIA بلغ نهاية عمره
  (`410 Gone` بتاريخ `2026-08-26`) ونموذج Groq `404 model_not_found`.
- **المنجز**: استُعلمت قائمتا `GET /models` الحيتان، وفُحص 8 مرشّحين دردشةً وجولةَ
  أداة، ودخل الكتالوج **المثبت حصراً**: ‏NVIDIA ‏`nemotron-3-super-120b-a12b`
  (افتراضي) و`deepseek-v4-pro-0813` · ‏Groq ‏`gpt-oss-120b` (افتراضي) و`gpt-oss-20b`
  و`qwen3.8-27b` و`allam-2-7b` (عربي، دردشة فقط — يرد 400 على tools والمصنع يتدهور
  رشيقاً). الخاتمة الرسمية: `finish_reason=tool_calls` بوسائط JSON سليمة للمنصتين،
  و`401` للمفتاح الفاسد.
- **المصدر**: جولة «النماذج المجانية» (قرار مالك: منصتان لا ثماني)
- **ثلاثة دروس مقيسة للمسابير القادمة**: (1) مفاتيح `~/.satr/keys.json` مشفّرة
  بـ`safeStorage` ومفتاح فكّها في `Local State` داخل `userData` — مسبار node خام أو
  Electron باسمه الافتراضي يقرأ مجلداً آخر فيفشل الفكّ **صامتاً**؛ العلاج
  `app.setPath('userData', appData/Satr)` قبل `whenReady` (مطبق في المسبار).
  (2) النماذج الاستدلالية (gpt-oss) تصرف رموزاً في التفكير قبل نداء الأداة — سقف
  `max_tokens` الضيق ينهي الدور `length` قبل النداء فيبدو دعم الأدوات غائباً زوراً.
  (3) طبقة NIM المجانية تتقلب (إقلاع بارد، `ECONNRESET` عارض) — أعد المحاولة قبل
  الحكم «لا يدعم»؛ `kimi-k3` و`deepseek-v4-flash` على NIM أثبتا الدردشة لا الأدوات
  فبقيا خارج الكتالوج.

## OBS-074 — وسوم الأدوات تقضم سعة تثبيت المستخدم: `set()` لا يخلي شيئاً عند الفيض فيفشل التثبيت صامتاً
- **الوسم**: `ui`
- **النوع**: صقل
- **الحالة**: منجزة — `184127b` (‏Codex، دفعة صقل 2026-09-03)
- **الدليل**: المراجعة الخصومية (Opus، 2026-09-03) بمسبار على المخزن الحقيقي بملف مؤقت:
  200 وسم أداة + 300 تثبيت/تسمية = 500 ⇒ التثبيت 301 يعيد `limit`، واللوحة تتجاهله
  (`sessions-panel.js` يتجاهل `ok:false`) فيبدو زر التثبيت معطّلاً. السقف المعلن 500 صار
  عملياً 300 لقرارات المستخدم. (الجانب المطمئن مُثبت: الموسومة **المثبّتة** تنجو من الإخلاء.)
- **المصدر**: مراجعة خصومية لما دُمج (رُصد ولم يُنفَّذ)
- **مرشّح العلاج**: في `sessionmeta.set()` عند بلوغ `MAX_ENTRIES` لمُدخَل جديد، أخلِ أقدم وسم
  خالص أولاً (نفس منطق إخلاء `setKind`) بدل إعادة `limit`؛ وأن تُظهر اللوحة فشل التثبيت.
- **المنجز**: `set()` يخلي أقدم وسم **خالص** واحداً (بلا تثبيت ولا عنوان — قرار المستخدم محصّن
  ولو حمل وسم أداة) بترتيب `setKind` نفسه، ويعيد `limit` فقط حين لا وسم خالص يُخلى، ويستعيد
  المُخلى ذرّياً عند فشل `persist()`. واللوحة تعرض `.meta-status` عربياً يميّز `limit` من
  `write_failed` (‏`role=status` و`aria-live`، بـtokens وadoptedStyleSheets). الحارس
  `test:sessionmeta` ‏4 عقود جديدة، وعضّته مثبتة: «التثبيت رقم 301 لم يطرد أقدم وسم خالص».

## OBS-075 — تفريع Kimi يطلق عمليتَي `kimi acp` ويعتمد على سرد جلسات مسقوف بـ200
- **الوسم**: `engines`
- **النوع**: صقل
- **الحالة**: منجزة — `a966aea` (‏Kimi K3، دفعة صقل 2026-09-03)
- **الدليل**: المراجعة الخصومية (Opus، 2026-09-03): `main.js` يستدعي `kimi.forkSession` بلا
  `cwd`، فيستدعي `kimi.js` ‏`rawSessionList()` (‏`withProbe` = عملية) ثم `withProbe` ثانية
  للتفريع — تأخّر ~2–4ث لكل تفريع؛ ومن تجاوز 200 جلسة Kimi (`MAX_SESSIONS` و10 صفحات) وفرّع
  جلسة خارجها يعود `session_not_found` مغلَّفاً بـ`kimi_fork_failed` العام بلا تمييز.
- **المصدر**: مراجعة خصومية لما دُمج (رُصد ولم يُنفَّذ)
- **مرشّح العلاج**: تمرير cwd الجلسة المتحقَّق منه من `main.js` (لا من renderer) لتجنّب السرد،
  أو تمييز `session_not_found` برسالة مرشدة.
- **المنجز** (الاثنان، و`kimi.js` بلا تغيير لأن `forkSession` كان يقبل `cwd` أصلاً): خريطة
  `kimiSessionCwd` في `main.js` تتغذى من مصدرين تملكهما العملية الرئيسية فقط — cwd المُتحقَّق
  منه عند `satr:send` (حدث `system/init`) وسرد `satr:listKimiSessions` — وتُفحص بـ`statSync`
  قبل التمرير؛ الحمولة من renderer **لا تُقرأ منها cwd** (حقن `cwd` فيها يُتجاهل — محروس).
  غياب المدخل يسقط إلى السلوك القديم لا إلى مصدر مخترع. و`session_not_found` صار
  `kimi_session_not_found` برسالة عربية تذكر سقف الـ200 والعلاج. المقيس بالحارس: عمليتان → **واحدة**
  مع cwd، واثنتان بلا cwd (لا تراجع). حدّ معلن: الخريطة ذاكرية، فأول تفريع بعد إعادة التشغيل
  لجلسة لم تُسرد بعد يعود للعمليتين.

## OBS-076 — `pickEngineSwitch` وُسّعت واختُبرت ولا مستهلك لها في الإنتاج
- **الوسم**: `process`
- **النوع**: صقل
- **الحالة**: منجزة — `64b43f3` (‏Opus، دفعة صقل 2026-09-03)
- **الدليل**: المراجعة الخصومية (Opus، 2026-09-03): `grep` على `electron/` و`src/` لا يعطي
  مستدعياً واحداً لـ`readiness.pickEngineSwitch` خارج `scripts/readiness-test.js`؛ منطق التبديل
  الفعلي نسخة ثانية في `app.js` (‏`applyGateEngineSwitch`). النسختان متكافئتان اليوم،
  والمختبَرة هي **غير** المستعملة — مصدرا حقيقة يتباعدان بصمت.
- **المصدر**: مراجعة خصومية لما دُمج (رُصد ولم يُنفَّذ)
- **مرشّح العلاج**: إمّا أن يستهلك `app.js` الدالة النقية (تمرير نتيجتها عبر preflight)،
  وإمّا حذفها مع اختبارها.
- **المنجز — بالخيار الثالث الذي كشفته القراءة**: النسختان **لم تكونا متكافئتين** كما ظنّت
  الملاحظة: `applyGateEngineSwitch` تقيّد البديل بما هو موجود فعلاً في `<select>` (‏`kimi-code`
  قد لا يكون معروضاً بعد لأن `loadProviders` غير متزامنة مع `gate-ready`) — قيد DOM لا تعرفه
  دالة نقية، فاستبدالها بها كان سيُعيد العطل. حُذفت `pickEngineSwitch` واختباراتها الاثنا عشر،
  وصار `test:readiness` يستخرج الدالة الحقيقية من `app.js` **وقت التشغيل** ويشغّلها في DOM
  مصغّر (نمط `claude-models-test` و`gitdiff-test`): 65/65 → 79/79، منها 26 على الدالة
  المستعملة التي كانت بصفر تغطية. عضّتان كشفتا ضعفاً في الحارس نفسه أثناء إثباته: `includes`
  يطابق استدعاءً معطَّلاً بتعليق (صار بداية سطر)، وإسقاط `includes(current)` لم يعضّ حتى أُضيف
  سيناريو «جاهز مختار ليس الأسبق ترتيباً». حدّ معلن: مرساتان نصّيتان في `app.js` (تعليقا
  «المحركات التي يحكمها عقد الجاهزية» و«بوابة أول التشغيل») — إعادة صياغتهما تُسقط الحارس
  **صراحةً** لا صامتاً.

## OBS-077 — مفتاح مشفّر لا يُفكّ كان يفتح البوابة، ووصلة تفريع Kimi كانت ترفض الفراغ الذي تمرره الواجهة
- **الوسم**: `security`
- **النوع**: عطل
- **الحالة**: منجزة — `c7d23e7` (‏`keys.hasUsableValue` في preflight + حارس وصلة التفريع عبر حمولة preload) و`f861cce` (شرط الفراغ)
- **الدليل**: المراجعة الخصومية (Opus، 2026-09-03) لما دُمج:
  1. `keys.names()` في `electron/keys.js` تستعمل `hasValue()` الذي يفحص وجود النصّ المشفّر
     بلا فكّ. مفتاح منسوخ من جهاز آخر أو ملف تعريف ويندوز متغيّر (`enc:true` لا يفكّه
     DPAPI) يُدرَج في الأسماء ⇒ `satr:preflight` يضيف مزوّده إلى `keyProviders` ⇒ البوابة
     تفتح على مفتاح فارغ عملياً، وكل دور يفشل بـ«لم يُضبط مفتاح» حتى إعادة التشغيل. قبل
     دفعة البوابة كان التباين تجميلياً في ⚙؛ بعدها صار **قرار فتح المنتج**.
  2. `preload.sessionFork(sessionId, upToMessageId, title, engine)` تمرّر `upToMessageId: ''`
     لجلسات Kimi (تفريع من النهاية)، ومعالج `satr:sessionFork` في `electron/main.js` كان يرفض
     الفراغ بـ`SAFE_SESSION` فتموت ميزة `OBS-048` في كل نقرة — و`test:kimi` أخضر لأنه يستدعي
     `forkSession` مباشرةً بلا المرور بالمعالج.
- **المصدر**: مراجعة خصومية Opus (2026-09-03) — بندان `medium` و`high`
- **المنجز**: أُضيفت `keys.hasUsableValue(name)` التي تفكّ سرّياً وتعيد `boolean` فقط،
  ويستعملها `satr:preflight` بدل `names()` وحدها؛ و`scripts/keys-test.js` (‏`test:keys`) يحقن
  `safeStorage` مزيّفاً يرمي عند الفكّ. وشرط الفراغ صُحّح في المعالجين (Kimi وSDK)، ومعه
  حارس وصلة في `scripts/kimi-test.js` يستخرج `handleKimiSessionForkRequest` الحقيقي ويمرّر
  الحمولة كما يبنيها preload — عضّتان مثبتتان بالإخراج الحرفي.
- **الدرس** (مكرَّر ومقيس): ثلاثة من أعلى خمسة بنود في المراجعة نجت من `test:full` الأخضر
  لسبب واحد — الحارس يختبر الوحدة النقية أو يزيّف الجسر بدل أن يعبره. الاختبار الأرخص
  المفقود دائماً هو مرور واحد عبر الحمولة التي يبنيها `preload` فعلاً.

## OBS-078 — بعد `browser_handoff` على claude.ai صارت كل `open_preview` تنجح ثم تردّ الأدوات «المعاينة غير مفتوحة» فوراً
- **الوسم**: `preview`
- **النوع**: عطل
- **الحالة**: منجزة — `542efe5` (السبب مثبت: صفحة callback تقتل WebContents؛ open_preview صار يؤكّد)
- **الدليل**: جلسة 2026-09-03 (فجراً) في «سطر» نسخة تطوير على `main` ‏`3cf1f3d`: تسلسل
  `open_preview(github.com/settings/installations)` ⇒ `browser_handoff` (تسجيل دخول GitHub) ⇒
  `browser_snapshot`/`read_page`/`screenshot` كلها عملت ⇒ `browser_navigate(claude.ai/settings/connectors)`
  ⇒ `browser_handoff` ثانية (تسجيل دخول claude.ai) عادت «استلم المستخدم» ⇒ **كل نداء بعدها**
  (`browser_snapshot` · `browser_wait_for` · `screenshot`) يردّ `المعاينة غير مفتوحة — استخدم
  open_preview أولاً`، بينما `open_preview` نفسها تردّ «فُتحت المعاينة» — على claude.ai **وعلى
  github.com** أيضاً (ثلاث محاولات). أي أن العرض يُدمَّر بعد الفتح مباشرةً أو أن حالة
  `handoffActive`/`held` بقيت معلّقة فتُغلق العرض.
- **المصدر**: جلسة القائد (رُصد ولم يُشخَّص — لم تُفحص سجلات main ولا حالة `holdForDialog`)
- **مرشّحات التشخيص**: (أ) هل يبقى `handoffActive` مرفوعاً بعد استلام ثانٍ في الجلسة نفسها
  (تسليمان متتاليان بلا دور بينهما)؟ (ب) هل يُدمَّر العرض عند `navigate` إلى origin غير
  موثوق أثناء التسليم؟ (ج) إعادة إنتاج: تسليمان متتاليان على origin مختلفين ثم `open_preview`.
  العلاج المؤقت للمستخدم: إغلاق لوحة المعاينة بزر ✕ وإعادة فتحها أو إعادة تشغيل «سطر».

## OBS-079 — `killTree` في مشغّل الطقم لا تعضّ على POSIX: `process.kill(-pid)` بلا `detached`
- **الوسم**: `process`
- **النوع**: عطل
- **الحالة**: مفتوحة
- **الدليل**: `scripts/full-suite.js` (‏دالة `killTree`) — بلاغ Cowork (وكيل خارجي، لينكس تحت
  xvfb، 2026-09-03) في وصف فرع `cowork/ci`: قتل مجموعة الطفل على POSIX بـ`process.kill(-pid)`
  لا يبلغ الشجرة لأن العملية لا تُنشأ بـ`detached:true`؛ وجرّب `detached:true` فظهر تسلسل
  قتل **غير مفسَّر** لخمس مجموعات تالية على لينكس فسُحب حتى يُفهم. **غير متحقَّق منه محلياً**
  (لا POSIX على جهاز التطوير)؛ على ويندوز `taskkill /T` مسار مستقل فلا أثر.
- **المصدر**: تقرير Cowork (رُصد ولم يُنفَّذ)
- **مرشّح العلاج**: حين تُعتمد بوابة لينكس فعلاً: `detached:true` مع فهم تسلسل القتل، أو
  `killTree` عبر `pkill -P` تكرارياً؛ والحارس `test:full-timeout` يجب أن يُثبت العضّ على POSIX.

## OBS-080 — `test:reviewmerge` يفشل داخل `git worktree` وينجح في استنساخ عادي
- **الوسم**: `process`
- **النوع**: عطل
- **الحالة**: مفتوحة
- **الدليل**: `scripts/reviewmerge-test.js` — بلاغ Cowork (لينكس، 2026-09-03): داخل worktree
  يكون `.git` **ملفاً** (مؤشر `gitdir:`) لا مجلداً، وافتراض المجلد في الاختبار أو في `worktrees.js`
  يُسقطه. **غير متحقَّق منه محلياً** — منفّذونا يعملون في worktrees على ويندوز وينجح عندهم
  `test:opsroom-all`، فالعطل قد يكون خاصاً بمسار POSIX أو ببيئة Cowork؛ يلزم إعادة إنتاج
  مضبوطة قبل أي تعديل.
- **المصدر**: تقرير Cowork (رُصد ولم يُنفَّذ)
- **مرشّح العلاج**: إعادة الإنتاج في worktree على ويندوز ولينكس؛ إن ثبت فالاكتشاف الصحيح
  لجذر المستودع هو `git rev-parse --git-common-dir` لا `fs.statSync('.git').isDirectory()`.

## OBS-081 — `eval:agent` لا يسمّي المهمة المتخطّاة على POSIX (الوصف الأصلي كان معكوساً)
- **الوسم**: `process`
- **النوع**: صقل
- **الحالة**: مفتوحة — **بوصف مصحَّح**؛ الدعوى الأصلية («أخضر كاذب») سقطت بالقياس
- **الدليل**: `scripts/agent-eval.js` — بلاغ Cowork (لينكس، 2026-09-03) قال إن المهمة
  `powershell-command` تُتخطّى **صامتةً** فيطبع «12/12» عنواناً و11 تنفيذاً، ووصفها
  **حارساً أخضر كاذباً**.

  **والقياس نفى ذلك** (جولة الأعطال المرئية، 2026-09-04 — تحقّق القائد ثم جرد أوبس
  المستقل): السلوك اليوم **أحمر قاطع** لا أخضر كاذب —
  `scripts/evals/tasks.json:148` فيه `"platforms": ["win32"]`، و`agent-eval.js:581-582`
  يسجّل `status:'متخطاة'`، و`:546` يطبع **`11/12 ناجحة، 1 متخطاة`**، و`:632`
  `if (summary.failed || summary.skipped) process.exitCode = 1;`.
  **ولم ينحرف الوصف بعد الرصد بل لم يطابق يوماً**: الحقل وحلقة التخطّي دخلا في
  الالتزام نفسه `fdb83d7` (2026-07-12) — أي أن البلاغ قرأ الكود خطأً لا أن الكود تغيّر.
- **المتبقي الحقيقي — أصغر بمراتب**: لا يُطبع سطر **باسم** المهمة المتخطّاة وسببها،
  فيُعرف العدد ولا يُعرف أيّها. ويبقى سؤال تصميم مستقل: أيصحّ أن يُسقط تخطٍّ **مشروع
  بالمنصة** رمز الخروج إلى 1، أم يُفصَل «متخطّى معلن» عن «فاشل»؟
- **المصدر**: تقرير Cowork (رُصد ولم يُنفَّذ) — **صُحّح بجرد صدق السجل** (`OBS-060`
  ومعه فحص القائد المستقل)
- **مرشّح العلاج**: التخطّي يُطبع صراحةً «⏭ متخطّاة على <platform>: <سبب>» ويُعدّ في البسط
  والمقام كما فعل `full-suite.js` في `cowork/ci` (النمط نفسه، ‏OBS-042).

## OBS-082 — ذيول جولة Cowork الأولى: مرشّح ffmpeg الويندوزي في سكربتَي البرومو، ووصف الوصلة في ARCHITECTURE، وفحص CI المطلوب على `main`
- **الوسم**: `docs`
- **النوع**: صقل
- **الحالة**: مفتوحة
- **الدليل**: (1) `scripts/promo-render.js:48` و`scripts/promo-assemble.js:27` يحملان المرشّح
  الثابت `D:\sater\tools\ffmpeg.exe` الذي أُزيل نظيره من `scripts/genmedia-probe.js` في
  `cowork/hygiene` (والملفان لهما `FFMPEG` بينما المسبار يقرأ `FFMPEG_PATH` — توحيد الاسم
  قرار مالك). (2) `docs/ARCHITECTURE.md:101` يصف الوصلة بـ`ee.registerIpc(ipcMain)`، والأدقّ
  بعد `cowork/ipc-seam`: «ipcMain المحروس عبر `features.init({ ipcMain })`». (3) وظيفة
  `tests` في `.github/workflows/release.yml` تُبلّغ ولا تمنع حتى تُضاف فحصاً مطلوباً في حماية
  `main` (‏`gh api .../branches/main/protection`) — مؤجَّل بقرار مالك حتى تشغيلين ناجحين.
- **المصدر**: تقرير Cowork «تغيير لزم في ملف لا أملكه» (رُصد ولم يُنفَّذ)
- **مرشّح العلاج**: الثلاثة سطور صغيرة؛ (3) يُنفَّذ بعد قياس تشغيلين على Actions لا قبله.
- **تحديث 2026-09-03 (مساءً)**: أول تشغيل حقيقي `33772062720` **أسقط كل اختبار Electron حيّ**
  (‏24 مجموعة) بـ`FATAL: setuid_sandbox_host` ثم `SIGTRAP` — افتراض Cowork «مستخدم غير root فلا
  حاجة لتعطيل sandbox» خاطئ على `ubuntu-latest` = 24.04 حيث AppArmor يمنع user namespaces
  غير المميّزة. العلاج `6d65ddc`: خطوة `sysctl -w kernel.apparmor_restrict_unprivileged_userns=0`
  (تُبقي namespace sandbox عاملاً — أدق من `ELECTRON_DISABLE_SANDBOX`). التشغيل الثاني
  `33772458296` **أخضر في 4m17s**. عدّاد شرط (3): **تشغيل ناجح 1 من 2**. وذيل رابع من
  السجل: `actions/setup-node@v4` يُوسم «Node.js 20 deprecated — forced to run on Node.js 24» —
  تحذير على الـaction نفسه لا على `node-version`، ويُعالَج برفع الـaction إلى v5 حين يُلمس الملف.

## OBS-083 — `test:reviewmerge` هشّ على لينكس: الدفعة تُعلَن `failed` قبل أن تبلغ كل عقد الزوايا حالتها
- **الوسم**: `ops-room`
- **النوع**: عطل
- **الحالة**: منجزة — `71a66f4` (المسند صار «الدفعة وكل عقدة زاوية طرفية» — إغلاق بنيوي)
- **الدليل**: بوابة لينكس على Actions — التشغيل `33772458296` (‏`6d65ddc`) أخضر 84/84، ثم
  `33772992487` (‏`d8e3b4c`، **تغيير توثيقي محض**) سقط في `scripts/reviewmerge-test.js:297`:
  `assert(failed.reviews[0].lenses.every((node) => node.state === 'failed'))` داخل سيناريو
  «الحدث المحظور يقع في كل عقدة زاوية فتُوقَف الثلاث». `completedReview` ينتظر حالة **الدفعة**
  الطرفية، و`aggregateLensState` يعيد الأسوأ فتصير الدفعة `failed` فور فشل **أول** زاوية بينما
  قد تكون الأخريان `running` لحظة الفحص — سباق زمني يخفيه ويندوز الأسرع. على ويندوز الطقم
  90/90 ثابت. **غير حتمي** ⇒ يهدّد صدق عدّاد «تشغيلين ناجحين» لشرط الإصدار (‏OBS-082 بند 3).
- **المصدر**: جلسة القائد (رُصد ولم يُنفَّذ)
- **مرشّح العلاج**: قرار تصميم قبل السطر: (أ) **إنتاجي** — لا تُعلَن الدفعة طرفية حتى تبلغ كل
  عقد الزوايا حالة طرفية (صدق العرض: الواجهة لا ترى «دفعة فاشلة بزوايا تعمل»)، في `reviewer.js`
  وهو ملف عدم تراجع فيلزمه حارسه؛ أو (ب) **اختباري** — `completedReview` ينتظر «كل الزوايا طرفية»
  لا «الدفعة طرفية». (أ) أصح دلالياً؛ (ب) لا يغيّر عقد الحدث. وقبل أيّهما: إعادة التشغيل مرتين
  على Actions لقياس معدّل الهشاشة لا الاكتفاء بعينة واحدة.

## OBS-084 — Agent SDK متأخر 82 إصداراً (0.3.176 → 0.3.258) وفيها تغييرات كاسرة وأخرى تفكّ قيد rewind
- **الوسم**: `engines`
- **النوع**: تحسين
- **الحالة**: مفتوحة
- **الدليل**: `package.json` (‏`@anthropic-ai/claude-agent-sdk ^0.3.176`) مقابل `latest=0.3.258` في سجل
  npm (2026-09-02). رادار سطر ٠٠١ (متحقَّق من CHANGELOG المصدر): الكاسر — 0.3.233 أزال أدوات
  Todo/Task افتراضياً مع Fable 5+ (تُعاد بـ`CLAUDE_CODE_ENABLE_TODO_TOOLS=1`؛ يمسّ Task Ledger
  في `electron/agent.js`)، 0.3.234 حذف `bypass_permissions_disabled` من `ExitReason`، 0.3.217
  خفّض عمق subagents المتداخلة إلى 1. الجديد المهم — `rewind_conversation` (0.3.186)،
  `resumeSessionAt` + `resumeDropsTurn` (0.3.223)، `permissionPrompts:'none'` (0.3.258)، وإصلاح
  `mcp_status` الذي كان يبلّغ اتصالاً وهمياً (يمسّ لوحة `/موصلات`).
- **المصدر**: رادار سطر ٠٠١ (Cowork، 2026-09-03) — رُصد ولم يُنفَّذ
- **مرشّح العلاج**: ترقية على فرع بحاجز: `test:full` + مسابير SDK الحية الخمسة (fork-rewind،
  claude-models، elicitation، sdk-background، sdk-polish) + فحص أدوات Todo وقائمة النماذج؛
  ثم ملاحظة مستقلة لتجربة مسار rewind الجديد (`resumeSessionAt` + `forkSession` + `rewindFiles`).

## OBS-085 — مجهول مالي: هل يُحاسَب دور سطر من رصيد Agent SDK الشهري ($20 لـPro) أم من حصة Claude Code العادية؟
- **الوسم**: `engines`
- **النوع**: تحسين
- **الحالة**: مفتوحة
- **الدليل**: شروط Anthropic (‏`code.claude.com/docs/en/legal-and-compliance`، قُرئت 2026-09-03):
  الاشتراك مسموح على «ثنائي Claude Code غير المعدَّل» بشروط ثلاثة يلتزمها سطر (`electron/agent.js`
  يوجّه `pathToClaudeCodeExecutable` إلى الثنائي المثبَّت؛ الدخول عبر `claude login`؛ لا وساطة).
  ومنذ 2026-05-13 (مصدر ثانوي — VentureBeat) الاستخدام البرمجي للاشتراك من رصيد «Agent SDK»
  منفصل ($20 Pro / $100 Max5 / $200 Max20) بأسعار API لا يُرحَّل. إن احتُسب دور سطر منه استنفده
  مستخدم Pro في أيام، ويجب أن تقوله البوابة. **غير مقيس**. تنظيف مصاحب **نُفّذ 2026-09-03**:
  `electron/main.js` (‏`sdkReviewEngineAvailable`) كان يقرأ `~/.claude/.credentials.json` لفحص
  الوجود؛ صار يستعلم `claude auth status` عبر `claudeauth.probe` (السلسلة الثلاثية async،
  والمستدعيان في preflight الفريق والحلقة ينتظرانها)، و`test:reviewmerge` يحرس غياب أي قراءة
  لـ`.credentials.json` من `main.js`. سطر الآن **صفر لمس** لملفات اعتماد Claude.
- **المصدر**: رادار سطر ٠٠١ (F1) + تحليل القائد — رُصد ولم يُنفَّذ
- **مرشّح العلاج**: مسبار على حساب Pro حقيقي يقرأ `rate_limit_event`/`SDKRateLimitInfo`
  (‏`seven_day_overage_included`، `model_scoped` منذ 0.3.191) قبل وبعد دور سطر؛ ثم سطر في
  البوابة و`features.md` بحسب النتيجة. فقرة الموقف التعاقدي في `README.md` (مع هذه الدفعة).

## OBS-086 — حدود Groq المجانية (8K token/دقيقة · 30 طلب/دقيقة) تصطدم بها جلسة وكيلية من أول مخرج CLI طويل
- **الوسم**: `engines`
- **النوع**: تحسين
- **الحالة**: منجزة — `1ba763a` (تراجع مسقوف على 429 + سطر الحدود في البوابة)
- **الدليل**: `console.groq.com/docs/rate-limits` (رادار ٠٠١): النماذج المجانية الأربعة تتشارك
  30 RPM · 1,000 RPD · 8K TPM · 200K TPD؛ وسحبت Groq نماذج Llama وQwen3-32b من المجاني في
  يوليو/أغسطس. NIM ~40 RPM بلا مسار زيادة. محوّلنا (`electron/adapters/openai-compatible.js`)
  لا يعالج 429 بتراجع ولا يعرض الحدود عند إدخال المفتاح في البوابة (`src/ui/components/gate.js`).
- **المصدر**: رادار سطر ٠٠١ (F5) — رُصد ولم يُنفَّذ
- **مرشّح العلاج**: (أ) 429 ⇒ backoff بمهلة `Retry-After` ورسالة عربية تسمّي الحدّ؛ (ب) سطر
  الحدود في شاشة المفتاح المجاني؛ (ج) اقتراح `/ضغط` أو قصّ مخرجات الطرفية تلقائياً على Groq.

## OBS-087 — ChainDrop: hooks مزروعة في `.claude/settings.json` تُنفَّذ من داخل سطر حين يفتح مستودع الآخرين
- **الوسم**: `security`
- **النوع**: تحسين
- **الحالة**: منجزة جزئياً — البند (أ) في `926b932` (`electron/hookguard.js`: تنبيه عربي غير حاجب مرة واحدة لكل مشروع، fail-open، ولا يعرض أمر الخطّاف)؛ **البند (ب) — بصمة تكوين خوادم MCP — ما زال مفتوحاً**
- **الدليل**: دودة npm (2026-08-04، Microsoft/Elastic — رادار ٠٠١): 444+ حزمة تزرع persistence في
  `.claude/settings.json` + `.claude/setup.mjs` + `.vscode/tasks.json` عبر كل الفروع وتسرق
  أنماط credentials. lockfile سطر نظيف (keyv 4.5.4، cacheable-request 7.0.4 خارج النطاق).
  لكن `electron/agent.js` يحمّل `settingSources=['user','project','local']` فأي `SessionStart`
  hook في مستودع المستخدم يعمل من داخل سطر. وبالتوازي: اختطاف `~/.claude.json` لتوجيه MCP
  إلى proxy محلي (Mitiga) — سطر يعرض قائمة MCP في `/موصلات`.
- **المصدر**: رادار سطر ٠٠١ (F8) — رُصد ولم يُنفَّذ
- **مرشّح العلاج**: (أ) تحذير عربي غير حاجب قبل أول دور في مشروع فيه `SessionStart` hooks أو
  `setup.mjs` تحت `.claude/` لم يُوافَق عليها من قبل (بصمة لكل مشروع في `~/.satr/`)؛ (ب) بصمة
  لتكوين كل خادم MCP وتنبيه عند تغيّره خارج سطر؛ (ج) `CLAUDE_MIN_RECOMMENDED` اليوم 2.1.197 ≥
  2.1.163 (سلسلة CVE أُصلحت فيها) ✓.

## OBS-088 — تفسير ميكانيكي لـOBS-001 (رسوّ النموذج على لغة الإطار) يقترح تجربة re-anchor رخيصة بعد كل tool result
- **الوسم**: `engines`
- **النوع**: تحسين
- **الحالة**: مفتوحة
- **الدليل**: ورقة «Code-Switching Reveals Language Anchoring» (arXiv 2606.19668 — رادار ٠٠١،
  لم تشمل العربية): الحالات الداخلية تبقى راسية على لغة الإطار والإدخال المختلط يزيد التدهور
  — متسق مع تشخيص OBS-001 (مخرجات CLI الإنجليزية هي الإطار). أداة القياس عندنا جاهزة:
  `electron/langmetric.js` و`test:langmetric`/`test:langshadow`. وبالتوازي QIMMA (TII،
  2026-04-21) أول لوحة coding بتعليمات عربية — مفتوحة على GitHub.
- **المصدر**: رادار سطر ٠٠١ (F6) — رُصد ولم يُنفَّذ
- **مرشّح العلاج**: تجربة مضبوطة — إعادة ترسيخ عربية قصيرة تُحقن بعد كل `tool_result` (لا تشديد
  للعقد) وقياس الانزلاق بـlangmetric لكل نموذج على حدة؛ وتشغيل مجموعة QIMMA البرمجية على
  نماذج سطر الخمسة؛ وتحذير في الواجهة عند اختيار Nemotron (لا يعلن العربية بين لغاته السبع).

## OBS-089 — MCP 2026-07-28: البروتوكول صار stateless و`elicitation/create` استُبدل بنمط MRTR — يصلنا عبر ترقية SDK/Codex لا بتغيير في سطر
- **الوسم**: `engines`
- **النوع**: تحسين
- **الحالة**: مفتوحة
- **الدليل**: `modelcontextprotocol.io/specification/2026-07-28/changelog` (رادار ٠٠١): حُذف
  handshake `initialize` و`Mcp-Session-Id`، وطلبات الخادم (elicitation/sampling/roots) صارت
  Multi Round-Trip Requests (‏`resultType:"input_required"`). سطر لا يتحدث MCP مباشرة (لا
  `@modelcontextprotocol/sdk` في الاعتماديات) — المحرّكات تحمله (Codex 0.147+)، لكن
  `electron/elicitation.js` ولوحة `/موصلات` تعتمدان شكل الرسائل الذي يمرّره SDK.
- **المصدر**: رادار سطر ٠٠١ (F7) — رُصد ولم يُنفَّذ
- **مرشّح العلاج**: مربوط بـOBS-084: بعد ترقية SDK/Codex أعد `test:elicitation` و`test:codex-mcp-panel`
  والمسبار الحي `elicitation-probe`، وسجّل ما تغيّر شكله.

## OBS-090 — ACP صار معياراً لتضمين الوكلاء (Kiro/Codex/Claude Code لها adapters) — spike لتقييمه مساراً ثانياً
- **الوسم**: `engines`
- **النوع**: تحسين
- **الحالة**: مفتوحة
- **الدليل**: Agent Client Protocol 1.0 (2026-06-24) ثم 1.6، وv2 alpha (2026-07-21) يوحّد
  `session/load`/`session/resume` ويضيف `auth/*`؛ Zed يوزّع `@zed-industries/claude-code-acp`،
  وKiro CLI وCodex لهما ACP (رادار ٠٠١ F4). سطر يغلّف ثلاثة وكلاء بثلاث تكاملات مخصّصة
  (`electron/agent.js` · `codex.js` · `kimi.js` — والأخير ACP أصلاً).
- **المصدر**: رادار سطر ٠٠١ — رُصد ولم يُنفَّذ
- **مرشّح العلاج**: spike مقيس: تشغيل `claude-code-acp` عبر عميل ACP الموجود في `kimi.js`
  وقياس ما يضيع (أذونات SDK، الأحداث، rewind، المهام الخلفية) مقابل ما يُكسب (Kiro/Gemini/
  OpenCode بتكلفة واحدة). تعارض محتمل مع بند Anthropic (OBS-085): الاشتراك مربوط بالثنائي
  غير المعدَّل — يُفحص أولاً.

## OBS-091 — GitHub Sponsors متاح للسعودية، وelectron-builder 26.13 أصلح كاذب «التطبيق لا يمكن إغلاقه» في NSIS
- **الوسم**: `process`
- **النوع**: تحسين
- **الحالة**: مفتوحة
- **الدليل**: قائمة الدول المدعومة في Sponsors تشمل السعودية (رادار ٠٠١ F9)؛ لا `FUNDING.yml`
  في المستودع اليوم. وelectron-builder 26.13.0 أصلح مطابقة اسم العملية في NSIS — يهمّ سطر لأنه
  يشغّل عمليات CLI فرعية أثناء التحديث (`package.json` على 25.1.8؛ الترقية مربوطة بـOBS-095).
- **المصدر**: رادار سطر ٠٠١ — رُصد ولم يُنفَّذ
- **مرشّح العلاج**: تفعيل Sponsors + `.github/FUNDING.yml` (يكمّل Ko-fi)؛ وترقية electron-builder
  ضمن ترقية Electron لا منفردة.

## OBS-092 — xterm.js: BiDi مفتوح منذ 2017 وPR الشامل رُفض؛ المسار الأرجح libghostty — يمسّ الـbuffer الذي يقرأ منه عارض سطر المزدوج
- **الوسم**: `ui`
- **النوع**: تحسين
- **الحالة**: مفتوحة
- **الدليل**: xterm.js#701 (2017، آخر نشاط 2026-07-05) · PR #5623 أُغلق 2026-01-26 («I have
  doubts this type of thing can be contributed externally») · اقتراح #5686 استبدال
  Buffer/Parser بـlibghostty (2026-08-15) بلا قرار · 6.1.0-beta.304 فيها إصلاح IME/RTL
  composition. عارض BiDi في `src/ui/components/terminal-panel.js` يقرأ Buffer API مباشرة.
- **المصدر**: رادار سطر ٠٠١ (F3) — رُصد ولم يُنفَّذ
- **مرشّح العلاج**: قياس اعتماد الـdual-renderer على internals الـbuffer (قائمة الواجهات
  المستعملة)، وتجربة 6.1.0-beta على فرع بحارس `test:xterm-csp`/`test:terminal-tabs`.

## OBS-093 — node-pty 1.2.0-beta: فشل CreateProcessW صار حدث exit بدل uncaughtException
- **الوسم**: `ui`
- **النوع**: تحسين
- **الحالة**: مفتوحة
- **الدليل**: سلسلة 1.2.0-beta.15 (2026-08-03 — رادار ٠٠١): فشل spawn عبر ConPTY يظهر حدثاً
  عادياً وتُغلق pipe handles. المثبّت 1.1.0 (`package.json`)، ومسار spawn في `electron/term.js`.
  نفس فئة الانهيار الصامت التي يرصدها غياب معالج `uncaughtException` عام.
- **المصدر**: رادار سطر ٠٠١ — رُصد ولم يُنفَّذ
- **مرشّح العلاج**: الترقية عند 1.2.0 stable مع إعادة فحص prebuilds لـElectron الهدف (OBS-095)؛
  حتى ذلك الحين معالج عام يغطّي.

## OBS-094 — Claude Code 2.1.232+: تفريع subagents افتراضي وحقول شجرة جديدة لا يقرؤها عارض سطر، وموافقة usage-credits لمستخدمي Pro لا تمرّ عبر واجهته
- **الوسم**: `ui`
- **النوع**: تحسين
- **الحالة**: مفتوحة
- **الدليل**: CHANGELOG Claude Code (رادار ٠٠١ F1/F2): subagent forking افتراضي (2.1.232)،
  الرسائل تحمل `parent_agent_id`/`spawn_depth`/`is_backgrounded`؛ و2.1.251 أضاف موافقة
  usage-credits لـFable على Pro و`/usage` يعرض Spend limit. بطاقة الوكيل في
  `src/ui/components/chat.js` تعتمد `parent_tool_use_id` وحده، ولا مسار لعرض consent prompt
  في سطر. (نص Help Center الأصلي للخطط لم يُجلب — مصدر ثانوي.)
- **المصدر**: رادار سطر ٠٠١ — رُصد ولم يُنفَّذ
- **مرشّح العلاج**: مربوط بـOBS-084: بعد الترقية مسبار يبثّ subagent متداخلاً ويقيس ما تعرضه
  البطاقة؛ وعرض حالة الحدود/الـcredits لكل نموذج في شريط الوعي حين يمرّرها SDK.

## OBS-095 — Electron 33 خارج الدعم منذ 2025-04-29 (المدعوم 44/43/42): Chromium 130 بلا رقع تحت معاينة تحمّل خوادم تطوير
- **الوسم**: `security`
- **النوع**: تحسين
- **الحالة**: مفتوحة
- **الدليل**: صفحة الإصدار `releases.electronjs.org/release/v33.4.11` تحمل «has reached
  end-of-support» (رادار ٠٠١، متحقَّق من المصدر). CVEs أغسطس 2026 (CVE-2026-70604 بدرجة 7.4 وأخرى)
  لا تصل إلى 33. سطوح سطر المحدّدة: `electron/preview.js` يرفض كل أذونات الويب ولا
  `registerSchemesAsPrivileged` — فالثغرات المسمّاة أقل انطباقاً، والفجوة الحقيقية Chromium
  نفسه. قيد الترقية: `npmRebuild:false` يعتمد prebuilds `node-pty 1.1.0` المثبتة على 33 —
  توافقها مع 44 غير مقيس؛ ويلزم electron-builder 26.x.
- **المصدر**: رادار سطر ٠٠١ (F8) — رُصد ولم يُنفَّذ؛ **قرار مالك**: مؤجَّل إلى ما بعد قراءة عتبة
  المنصات (~2026-09-10)
- **مرشّح العلاج**: spike على فرع: Electron 44 + electron-builder 26 + فحص prebuilds + `test:full`
  كاملاً + `test:rtl-preview` (نافذة مرئية) + بناء مثبّت تجريبي. وهي الخطوة اللازمة لماك أيضاً.

## OBS-096 — خريطة توقيع مطوّر فرد في السعودية: Azure مغلق، SignPath يرفض FSL، Certum المسار الواقعي — وwinget بلا شهادة هو الطريق الأقصر للتوزيع
- **الوسم**: `process`
- **النوع**: تحسين
- **الحالة**: مفتوحة
- **الدليل**: (رادار ٠٠١ F9، متحقَّق جزئياً) Azure Artifact Signing يقبل الأفراد من أمريكا وكندا
  فقط · SignPath Foundation يشترط رخصة OSI وFSL-1.1-MIT ليست منها · Certum Open Source €49–69
  لكن قبول FSL تحت «مفتوح المصدر» **غير مؤكد** · winget لا يشترط التوقيع ويتجاوز حظر تنزيل
  المتصفح · Microsoft Store يوقّع MSIX بنفسه بشرط عمل spawn داخل الحاوية. السياق: 53 تنزيلاً
  إجمالاً ⇒ المشكلة توزيع (`docs/PLATFORMS-DECISION-2026-09.md`).
- **المصدر**: رادار سطر ٠٠١ — رُصد؛ **winget نُفّذ بقرار مالك 2026-09-03**
- **مرشّح العلاج**: (أ) manifest عبر `wingetcreate` وPR إلى `microsoft/winget-pkgs` — **مُرسَل**:
  `microsoft/winget-pkgs#429004` («New package: Moxa.Satr version 2.16.15»)؛ الملفات الأربعة
  (version/installer/en-US/ar-SA، schema 1.10) محفوظة في `winget/manifests/m/Moxa/Satr/2.16.15/`
  و`winget validate` نجح محلياً. المعرّف الدائم `Moxa.Satr` (قرار مالك). **نتيجة خط الفحص
  (2026-09-03 مساءً)**: CLA وُقّع من جلسة المالك، والفحوص 01–07 خضراء، و**08. Installation
  Validation فشل** بوسم `Validation-Shell-Execute` (البوت: «failed to install during dynamic
  validation… verify that the application installs correctly using winget cli»). **ما استُبعد**:
  فرضية «المثبّت يطلق التطبيق في الوضع الصامت» — قالب `app-builder-lib/templates/nsis/
  installSection.nsh` (المثبّت assisted لا oneClick) لا يستدعي `doStartApp` صامتاً إلا مع
  `--force-run`. **ما لم يُحسم**: سبب فشل ShellExecute في بيئة winget (رفع صلاحيات؟ رمز خروج؟
  `$LANGUAGE=1025` في `preInit`؟) — الوسم شائع (12 PR مفتوحاً به اليوم) ويُحلّ عادةً بمراجعة
  يدوية أو بإعادة إنتاج محلية. **إعادة الإنتاج على عدّاء GitHub ويندوز نظيف** (سير يدوي
  `.github/workflows/winget-install-check.yml`، التشغيلان 33796759651 و33796955694): التشغيل
  المباشر `Satr-Setup.exe /S` عبر CreateProcess ⇒ **exit=0 في 8ث**، وسُجّل «سطر — Satr | 2.16.15
  | Moxa» ولم يُطلَق التطبيق؛ وmanifest الـPE يعلن `asInvoker` (لا رفع صلاحيات). أمّا `winget
  install --manifest` فوجد الحزمة وتحقق من البصمة وسجّل «Starting: …\Temp\WinGet\Moxa.Satr.2.16.15\
  Satr-Setup-2.16.15.exe with arguments '/S'» ثم **علق 20 دقيقة** حتى مهلة السير — أي أن عطل
  البوابة أُعيد إنتاجه، وموضعه ShellExecute لا المثبّت. **الفرضية العاملة**: winget يكتب علامة
  الويب (Zone.Identifier) على ما ينزّله، وShellExecute يستشير SmartScreen للملف الموسوم **غير
  الموقّع** فيعلّق حواره جلسة بلا واجهة؛ CreateProcess لا يستشيره. تصحيح مصاحب: `AppsAndFeaturesEntries`
  صارت بالقيم الفعلية (`DisplayName: سطر — Satr`، `ProductCode: be4b5aae-94c3-5c0d-bec7-9e26b831dbed`)
  ودُفعت إلى فرع الـPR. **الفرضية ثبتت** (التشغيل الثالث 33799413776): بعد 300ث من تعليق
  `winget install --manifest` كانت عملية `smartscreen` حيّة (pid 8172) وتحت `winget` نافذة
  «Window Dialog»، والملف الذي نزّله winget يحمل `Zone.Identifier` — أي حوار SmartScreen على
  مثبّت **غير موقّع** موسوم بعلامة الويب، لا يُغلق في جلسة بلا واجهة؛ وفي التشغيل نفسه أنهى
  CreateProcess على النسخة غير الموسومة في 7ث بـexit=0. ودليل ثانٍ مستقل في التشغيل نفسه: نسخة
  من الملف زُرعت عليها `Zone.Identifier` يدوياً ثم أُطلقت بـ`Start-Process -Verb Open` (‏ShellExecute)
  رُفضت فوراً بـ«An error occurred trying to start process … canceled by the user» — الردّ
  المعياري حين تتعذّر موافقة SmartScreen في جلسة بلا واجهة. **الخلاصة**: المثبّت سليم صامتاً،
  والعلة توقيع غائب + علامة ويب. **العلاج الجذري توقيع المثبّت** (Certum — بند ب): تحقّقتُ من صفحات
  Certum (2026-09-03): «Open Source Code Signing in the Cloud» ‏€49 موجود (الرادار أصاب)، **للأفراد
  فقط** بمشروع علني يُظهر العلاقة، والتحقق آلي + هوية + فاتورة خدمات، **وتُلغى الشهادة إن وُقّعت بها
  برمجية تُوزَّع تجارياً** — يمسّ خطة «برو» فلا يُشترى قبل جواب مكتوب. مسودة الرسالة (أربعة أسئلة:
  أهلية FSL · بند التوزيع التجاري · التحقق للأفراد في السعودية · التوقيع الآلي من Actions أم OTP)
  أُنشئت **مسودةً في Gmail المالك** ليرسلها بيده؛ والمسار المؤقت طلب مراجعة يدوية من مشرفي winget بهذا الدليل — **نُشر بإذن المالك
  وباسمه** (‏`microsoft/winget-pkgs#429004 (comment 5533029257)`، 2026-09-03 22:36Z): القياس
  على العدّاء النظيف ورابط التشغيل وطلب المراجعة اليدوية، مع التصريح بأن التوقيع على الخارطة.
  المتبقي: انتظار ردّ المشرفين (أيام)، وإن أُغلق خمولاً يُعاد بمثبّت موقّع. **درس**: الوسم `Validation-Shell-Execute` لا يعني عطلاً في المثبّت؛ التمييز
  بين CreateProcess وShellExecute على ملف موسوم هو مفتاحه. بعد القبول: قسم «التثبيت عبر winget»
  في README وسير إصدار يحدّث الحزمة آلياً (`wingetcreate update`) مع كل وسم؛
  (ب) رسالة إلى Certum عن FSL قبل أي شراء (مسودة جاهزة عند القائد)؛ (ج) تجربة `target: appx`
  مع اختبار PTY داخل الحزمة.

## OBS-097 — قناة اكتساب مجانية: issues RTL مفتوحة في Claude Code وCodex وVS Code أصحابها جمهور سطر الأول
- **الوسم**: `docs`
- **النوع**: تحسين
- **الحالة**: مفتوحة
- **الدليل**: (رادار ٠٠١ F10 — **صُحّح بـ`gh issue view` 2026-09-03**): `claude-code#48559`
  **مغلق** (‏stale منذ 2026-04، آخر تعليق يروّج `rtlwrap`) و`codex#5827` **مغلق** (صاحبه نشر إضافة
  `agents-rtl`) — الرادار عدّهما مفتوحين. المفتوحة فعلاً ثلاث: `codex#30129` (‏RTL في محادثات
  Codex، 2026-06-25، 3 تعليقات — **الملائم الوحيد**)، و`codex#31903` (عبري، **macOS**) و
  `vscode#296495` (‏**macOS**، صفر تعليق) — وسطر ويندوز فقط فالتعليق فيهما مضلّل. لا قسم
  «Known RTL issues» في `README.md`.
- **المصدر**: رادار سطر ٠٠١ — رُصد ولم يُنفَّذ؛ **قرار مالك** (صوت عام)
- **مرشّح العلاج**: تعليق مهذّب واحد بصورة قبل/بعد في `codex#30129` (مسودة عند القائد)، وقسم
  «حدود RTL المعروفة ومقارنة» في README؛ وفي claude-code **issue جديدة** بعنوان دقيق
  (PowerShell + نص مختلط على ويندوز) بدل التعليق على المغلقة — لأن الحرّاس الآليين يغلقون الخامل.
  **درس على الرادار**: حالة issue تُقرأ من API لحظة الكتابة لا من ذاكرة التقرير.

## OBS-098 — «ar-terminal» (إضافة VS Code سعودية) أول تنفيذ علني لنهج الـoverlay نفسه — الميزة في الجودة لا في وجود الطبقة
- **الوسم**: `docs`
- **النوع**: تحسين
- **الحالة**: مفتوحة
- **الدليل**: (رادار ٠٠١ F4) `marketplace.visualstudio.com · ar-terminal` — xterm.js للمنطق +
  طبقة DOM ترسم العربية بمحرك المتصفح وتذكر Claude Code؛ 3,023 تثبيتاً، مستودع بنجمتين بلا
  نشاط منذ 2026-03-29. نهج سطر في `src/ui/components/terminal-panel.js` (عارض BiDi النافذي)
  و`docs/PHASE8-DESIGN.md`.
- **المصدر**: رادار سطر ٠٠١ — رُصد ولم يُنفَّذ
- **مرشّح العلاج**: تثبيتها وتشغيل حالات سطر الصعبة عليها (التحديد، المؤشر، الاتجاه المختلط،
  شذوذ ConPTY) وتوثيق الفروق في README مقارنةً صريحة.

## OBS-099 — قسما «حساب Claude» و«حساب Codex» في ⚙ لم يعملا قط: سباق `queueMicrotask` مع مبدّل حالة اللوحة
- **الوسم**: `ui`
- **النوع**: عطل
- **الحالة**: منجزة — `c8f2433` (حدث settings-open صريح + حارس حيّ test:settings-account)
- **الدليل**: (مقيس على نسختين، 2026-09-04) `src/ui/app.js:632-638` يسجّل مستمع ⚙ ثم يقرأ
  `settingsPop.hidden` داخل `queueMicrotask`؛ و`src/index.html:344` يحمّل `app.js` **قبل**
  `topbar.js:368`، فمستمع app.js يُسجَّل أولاً وتُصرَّف الـmicrotask في نقطة تفتيش المهام
  الدقيقة **قبل** مستمع `topbar.js:121` الذي يبدّل `hidden` ⇒ يقرأ الشرط اللوحة مغلقة عند
  الفتح ويخرج، فلا تُستدعى `refreshClaudeAccountView`/`refreshCodexAccountView` إطلاقاً.
  ثلاثة أدلة متقاطعة: (1) `scripts/claude-models-probe.js` أعاد
  `accountPublicFieldsPresent: {email:true, organization:true, subscriptionType:true}` على
  الجهاز نفسه ⇒ القناة سليمة؛ (2) لقطتا المالك تُظهران الصفوف الثمانية بـ«—» بينما
  `setAccountField` (‏`app.js:192`) **يُخفي الصف كلياً** عند غياب القيمة، وحالة القسم باقية
  على نصّها الأولي في `index.html:79` («يُحدّث عند فتح الإعدادات») لا «جارٍ التحديث…» ولا
  «تعذّر التحديث» ⇒ الدالة لم تُستدعَ لا أنها فشلت؛ (3) «إصدارات المحرّكات» و«النشاط المحلي»
  **مملوءان** في اللقطة نفسها، وهما من مستمعَي `topbar.js:274` و`350` اللذين يقرآن الحالة
  بعد التبديل. الإغلاق بزر ✕ أو بالنقر خارج اللوحة لا يمرّ بمستمع `#settingsBtn` أصلاً.
- **المصدر**: تجربة قبول MSIX (‏OBS-096 بند ج) — ظهر بحثاً عن علة حاوية فتبيّن أنه عام:
  النسخة العادية (‏`D:\sater\satr-2`) تُظهر الحقول الثمانية فارغة كنسخة الحزمة تماماً.
  ميزتان مبنيتان بلا وصل فعلي منذ دفعتَي B (2026-07-25) وC4 (2026-07-27) — نظير درس
  `awaitPairing` («موصول لكن غير مربوط»).
- **مرشّح العلاج**: أن يبثّ `topbar.js` حدثاً صريحاً عند **الفتح** من داخل `setSettingsOpen`
  ويستمع إليه `app.js`، بدل استنتاج الحالة من ترتيب مستمعين غير مضمون. الحارس يجب أن يكون
  حيّاً (‏Chromium ينقر ⚙ فعلاً ويثبت تغيّر `claudeAccountState` عن نصّه الأولي) لأن الفحص
  الساكن يرى الاستدعاء موجوداً — وهو موجود فعلاً لكنه لا يُنفَّذ. **حدّ مُصرَّح به**: لم
  يُقَس بعد ما إذا كان الإصلاح يكشف عطلاً ثانياً خلفه في مسار `codexStatus/Limits/Usage`.

## OBS-100 — تسجيل دخول لسطر: مؤجَّل بقرار مالك، والبديل لـ«برو» مفتاح ترخيص لا نظام حسابات
- **الوسم**: `process`
- **النوع**: تحسين
- **الحالة**: مفتوحة (مؤجَّلة بقرار مالك 2026-09-04 أثناء إعداد رفع المتجر)
- **الدليل**: سأل المالك أثناء ملء `Submission Options` عمّا إذا كان يجب أن يسجّل مستخدمو
  سطر الدخول، بحجة أن ذلك يفيد «برو» لاحقاً. الأسباب الخمسة التي أجّلته: (1) يناقض سياسة
  الخصوصية المكتوبة والمعتمدة في المتجر قبل ساعة («no servers, no data») فيستلزم إعادة
  كتابتها وتغيير إعلانات `Properties` وربما التصنيف العمري؛ (2) «المراجع لا يستطيع الدخول»
  من أشهر أسباب رفض المتجر، وسطر اليوم يعمل فور التثبيت وهي **ميزة** في المراجعة؛
  (3) سياسة المتجر ‏10.8.1 تُلزم بنظام تجارة المتجر (وعمولته) لأي اشتراك **يُستهلك داخل
  التطبيق**، فبيع «برو» يُصمَّم بوعي لا يُرتجل؛ (4) سطر مفتوح المصدر (‏FSL) فبوابة الدخول
  تُلتَف عليها ببناء نسخة — القيمة الحقيقية في خدمة خلفية لا في بوابة؛ (5) 53 تنزيلاً
  إجمالاً حتى اليوم، فبناء نظام حسابات يسبق إثبات الطلب، والنشر في المتجر نفسه هو أداة
  القياس (`docs/PLATFORMS-DECISION-2026-09.md`).
- **المصدر**: جلسة رفع MSIX إلى Microsoft Store — رُصد ولم يُنفَّذ بقرار مالك
- **مرشّح العلاج**: عند تفعيل «برو» — **مفتاح ترخيص** يُلصق في مركز المفاتيح القائم
  (`electron/keys.js` + قسم ⚙) بدل نظام حسابات: لا خادم حسابات ولا كلمات مرور ولا استعادة
  ولا تسجيل دخول يعطّل مراجعة المتجر؛ والتحقق لاحقاً بنداء واحد إلى خدمة صغيرة إن لزم.
  وقناة تواصل بلا مساس بالخصوصية: زرّ اختياري «اشترك في تحديثات سطر» يفتح صفحة الموقع.
  **حدّ مُصرَّح به**: لم يُقَس بعد أثر بيع «برو» خارج المتجر على قبول التطبيق فيه — يُراجَع
  نصّ ‏10.8.1 قبل ربط أي شراء بالتطبيق المنشور.

## OBS-101 — `test:preview-member-live` يتجاوز 240ث تحت الحمل ويمرّ في 4.7ث منفرداً
- **الوسم**: `process`
- **النوع**: صقل
- **الحالة**: مفتوحة
- **الدليل**: بوابة جولة «الأعطال المرئية» (2026-09-04) على فرع الدمج: سقطت المجموعة
  بـ`تجاوز 240ث` **ثم طبعت نجاحها** بعد قتل شجرتها — أي أن العمل اكتمل والمهلة سبقته.
  وإعادتها **منفردة فوراً** أعطت `4.7` ثانية (قياس `Stopwatch` في الطرفية المرئية) ⇒
  فرق **×51**، وهو أعلى من أن يُفسَّر بنمو المحتوى (أضافت دفعة `OBS-078` تسعة وتسعين
  سطر فحص إلى `scripts/preview-member-live-test.js`).
  وموضعها في الطقم قرينة: تسبقها مباشرةً `test:browser-member-live` وهي اختبار Electron
  حيّ آخر يفتح معاينة.
- **المصدر**: بوابة `test:full` لجولة الأعطال المرئية (القائد)
- **الملاحظة**: **رابع اختبار بصري يتعثّر بيئياً تحت الحمل** بعد `OBS-019` (‏`rtl-preview`)
  و`OBS-025` (‏`executionteam`) و`OBS-036` (‏`promocapture-live`) — والنمط واحد: قياس
  يحتاج سطحاً مرسوماً أو موارد نظام يتزاحم مع اختبارات Electron حية متتالية.
  **ولم يُشخَّص**: لم يُقَس أهو تزاحم موارد أم علوق فعلي في تهيئة `WebContentsView`
  بعد اختبار معاينة سابق — والفرق ×51 يرجّح العلوق لا البطء، فلا يُدّعى التشخيص.
- **مرشّح العلاج**: أُضيف إلى `RETRYABLE` في `scripts/full-suite.js` بالآلية التي
  بناها `OBS-036` (إعادة واحدة، إعلان صاخب، ذكرها في الخاتمة، والتراجع الحقيقي يفشل
  مرتين فيسقط) — **علاج عَرَض معلن لا تشخيص**. التشخيص يبقى مفتوحاً: قياس تسلسل
  الاختبارَين على حدة، وفحص تحرير `WebContentsView` بين المجموعتين.
