# تقرير فحص RTL بصري — سطر

**تاريخ التشغيل:** 2026-08-12T00:08:11.412Z  
**اللغات المقارنة:** افتراضي النظام (en-US) مقابل `--lang=ar` (ar)  
**المقاسات:** 1400x900، 1100x760  
**الأسطح المفحوصة:** topbar، empty-chat، composer، terminal، files، git، settings، ops-room، preview

## ملخص

- عدد الأسطح المفحوصة: **18** (9 أسطح × 2 مقاس).
- عدد الفروق المُكتشفة: **1**.
- أخطر ثلاثة:

1. **settings** (1400x900): "settingsPop" أبعاد مختلفة: default 398×820 vs ar 393×808.

## منهجية المقارنة

1. شُغّل التطبيق الحقيقي (electron/main.js) مرتين: باللغة الافتراضية وبـ `--lang=ar`.
2. لكل مقاس، أُخذت لقطة لكل سطح بعد فتحه برمجياً (نقرات محاكاة).
3. قُيست `getBoundingClientRect` لعناصر رئيسية في كل سطح.
4. قورنت العناصر بين اللغتين:
   - العرض والارتفاع يجب أن يتطابقا (+2 بكسل).
   - الحواف الأفقية والرأسية يجب ألا تزح (+2 بكسل).
   - لا عنصر يُقصّ خارج نافذة المحتوى.
   - لا تداخل غير متوقع بين عناصر رئيسية.
5. بالنسبة لـ **المعاينة**، رُصد العرض الأصلي (WebContentsView) بلون مؤشر من لقطة الشاشة للتحقق من انعكاس RTL.

> ملاحظة: HTML الأساسي للتطبيق يبقى `dir="rtl"` في الحالتين؛ الفرق الوحيد هو إعداد `--lang` الذي يغيّر سلوك Chromium للطبقات الأصلية والخطوط/الأشرطة الافتراضية.

## جدول النتائج

| السطح | المقاس | الحكم | الخطورة | ملاحظات |
|---|---|---|---|---|
| topbar | 1400x900 | مطابق | — | مطابق هندسياً |
| empty-chat | 1400x900 | مطابق | — | مطابق هندسياً |
| composer | 1400x900 | مطابق | — | مطابق هندسياً |
| terminal | 1400x900 | مطابق | — | مطابق هندسياً |
| files | 1400x900 | مطابق | — | مطابق هندسياً |
| git | 1400x900 | مطابق | — | مطابق هندسياً |
| settings | 1400x900 | مختلف | مزعج | "settingsPop" أبعاد مختلفة: default 398×820 vs ar 393×808. |
| ops-room | 1400x900 | مطابق | — | مطابق هندسياً |
| preview | 1400x900 | مطابق | — | مطابق هندسياً |
| topbar | 1100x760 | مطابق | — | مطابق هندسياً |
| empty-chat | 1100x760 | مطابق | — | مطابق هندسياً |
| composer | 1100x760 | مطابق | — | مطابق هندسياً |
| terminal | 1100x760 | مطابق | — | مطابق هندسياً |
| files | 1100x760 | مطابق | — | مطابق هندسياً |
| git | 1100x760 | مطابق | — | مطابق هندسياً |
| settings | 1100x760 | مطابق | — | مطابق هندسياً |
| ops-room | 1100x760 | مطابق | — | مطابق هندسياً |
| preview | 1100x760 | مطابق | — | مطابق هندسياً |

## تفاصيل الفروق

### settings — 1400x900 (مزعج)

"settingsPop" أبعاد مختلفة: default 398×820 vs ar 393×808.
"settingsPop" موضع مختلف: default (16,54) vs ar (19,64).
"settingsHead" أبعاد مختلفة: default 386×50 vs ar 381×49.
"settingsHead" موضع مختلف: default (27,55) vs ar (30,65).
"effortField" أبعاد مختلفة: default 190×35 vs ar 187×34.
"effortField" موضع مختلف: default (43,146) vs ar (46,155).
"keysMgr" أبعاد مختلفة: default 354×112 vs ar 349×110.
"keysMgr" موضع مختلف: default (43,818) vs ar (46,817).

- اللقطة الافتراضية: `dist/rtl-audit/default/settings-1400x900.png`
- اللقطة العربية: `dist/rtl-audit/ar/settings-1400x900.png`
- فرضية السبب: ربما فرق في أبعاد النافذة الأصلية أو ترتيب العرض عند تغيير --lang.

## ما لم يُفحص ولماذا

- **المحادثة الحية بمحتوى:** تتطلب تشغيل دور SDK/Codex ومفاتيح API، وهو خارج نطاق الفحص البصري بدون شبكة.
- **مربع الأذونات / حوار السؤال / حوار Elicitation:** يتطلب وصولاً برمجياً لحالة الدور؛ لم تُعرض أثناء الفحص.
- **عارض الملفات (file-viewer):** يتطلب فتح ملف باختيار مستخدم؛ يمكن فحصه في دفعة لاحقة.
- **الوضع الفاتح:** جميع اللقطات التقطت بالثيمة الافتراضية (داكن)؛ لم يُجرَب الوضع الفاتح.
- **شريط التمرير الأفقي:** لم تُقاس دقة موضع الشريط بين اللغتين إلا ضمنياً عبر عدم القصّ.

## توصية الفرز

### إصلاح فوري (يمنع الاستخدام)

- لا شيء.

### يؤجَّل / يُراجع

- settings (1400x900) — مزعج

## مسارات اللقطات

- الافتراضي: `dist/rtl-audit/default/`
- العربية: `dist/rtl-audit/ar/`

---
*أُنتج تلقائياً بواسطة scripts/rtl-visual-audit.js*
