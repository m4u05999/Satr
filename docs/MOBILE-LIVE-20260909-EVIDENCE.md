# دليل تجربة الجوال الحية — 2026-09-09

أُجريت التجربة بتفويض المالك لقيادة الوكيل واجهة PWA بنفسه، عبر معاينة نسخة القبول `dist/final-batch-preview-v15` وصفحة الإنتاج وجلسات محركات حقيقية. هذه تجربة آلية حية إضافية، وليست قبولاً بشرياً ينقر فيه المالك، ولا تجربة هاتف فعلي. الدليل الأولي محفوظ في `dist/mobile-live-20260909/`؛ تُنقل نتائجه وحدوده هنا حتى لا يُقرأ أي PASS على أنه قبول شامل.

## البيئة وطريقة الرصد

- التطبيق `2.16.21`، وقشرة PWA المبنية التي أُجريت عليها التجربة تحمل كاش `v15`. تعديل المصدر إلى كاش `v16` جاء لاحقاً لهذه النتائج الحية.
- مجلد العمل مستقل: `D:\sater\satr-2\dist\mobile-live-20260909\workspace`. اتصل السائق بواجهة نسخة القبول عبر CDP المحلي `127.0.0.1:9478`.
- خدمة PWA والاقتران والأذونات من `mobilelink` الإنتاجي على `https://127.0.0.1:62504`. لا خادم نقل مقلّد ولا حقن قرار عبر IPC ولا استبدال للمحرك. السائق يرسل الطلب من مؤلّف الواجهة وينقر أزرار PWA الفعلية.
- رصد `window.SatrCrypto.seal/open` يمرّر الوسيطات والـPromise والبايتات الأصلية، ويسجل فقط أنواع الرسائل والقرار والحالة وتطابق الأمر والدور والوقت وأسماء أدوار بديلة `run-N` وعدّادات المهام. لا مفاتيح أو PIN أو قيمة اقتران في التقرير. سجل `seal` يسبق اكتمال التعمية، لذلك لا يُعد وحده دليلاً على الإرسال أو الوصول.
- رصد سطح المكتب يستمع إلى `window.satr.onEvent` بحقوله المحدودة. وصول `mobile_decision` أقوى من ظهور نقرة: `main.js` لا يبثه إلا بعد قبول حل إذن الطلب الجاري.

## تعثرات تجهيز منفصلة عن الإصلاح

1. رفضت المعاينة شهادة عنوان LAN `10.34.40.53:62504` بـ`ERR_CERT_AUTHORITY_INVALID`. استثناء الإنتاج في `preview.js` يقبل الشهادات الذاتية لـ`localhost/127.0.0.1` فقط؛ لم يُعطّل TLS عاماً. غيّر وسيط التجربة عنوان الاتصال وحده، بما فيه `payload.url`، إلى loopback داخل الذاكرة. بقيت بقية بيانات الاقتران مطابقة؛ `pairing-evidence.json` يثبت `onlyEndpointChanged:true` و`secretsLogged:false`. هذا لا يثبت قبول شهادة LAN على هاتف.
2. في المعاينة ذات المخزن الحالي فشل `indexedDB.open` حتى لاسم قاعدة تجريبي بـ`UnknownError: Internal error`. منع حجز العدّادات إرسال القرار كما ينبغي. بعد عزل `userData` لنسخة القبول نجح IndexedDB وحفظ `CryptoKey` على الأصل نفسه ومع حارس الأذونات نفسه، ثم نجح الاقتران وتطابق SAS بحسب قياس القائد. هذا يحصر العائق في بيئة التخزين السابقة، ولا يثبت أن قفل الملف هو سببه المحدد. لم يُخفّف حارس العدّادات ولم يُنسب التعثر إلى إصلاح OBS-147/148.
3. أنشأت محاولة أولى `denied.txt` من دون `mobile_decision`؛ لذلك استُبعدت من نجاح الرفض. يستخدم الدليل النهائي ملفاً جديداً هو `denied-fresh-session.txt`، ويتحقق من غيابه قبل الطلب وبعد رفضه.
4. محاولة مهام لاحقة بمحرك SDK/Haiku لم تُقبل: أفاد قياس القائد `Prompt too long: 226834/200000`. الملف المنقح `scenario-after-haiku-failed.json` يسجل `ok:false` و`live_wait_timeout` مع `result/spawn_error/proc_done`؛ لا يحتوي نص المحرك الكامل. لا تُعد هذه المحاولة نجاحاً، وأُعيدت المهام عبر SDK/Opus `[1m]`.

## الرفض الحقيقي — term34

الأمر: `node dist/mobile-live-20260909/scenario.cjs deny`. تقرير `scenario-deny.json` يحمل `ok:true`. بدأ دور Codex في مجلد التجربة، ووصلت بطاقة «تنفيذ أمر» تعرض إنشاء `denied-fresh-session.txt`. نُقر رفض PWA، ثم سجل سطح المكتب `mobile_decision` بقيمة `deny`، وبقي الملف غائباً عند الفحص النهائي. السائق لا يكتفي بغياب الملف أو بنقرة؛ يشترط قرار سطح المكتب داخل مقطع أحداث المحاولة الحالية.

تعليق التقاط اللقطة قبل القرار لم يُحسب دليلاً: مربع الإذن حجب BrowserView فتوقفت محاولة screenshot، ونُفذت النقرة مباشرة ثم أكمل السائق. لذلك يظهر سجل `clicked_mobile_deny` بعد حدث `mobile_decision` بنحو414ms؛ ليس ترتيباً صالحاً لحساب زمن النقرة. لا ندّعي أن لقطة محفوظة أثبتت كل خطوة. تقرير الرفض ينتظر `proc_done` وغياب الملف؛ لا يحمل قياساً مستقلاً لـ`run.done` في هذا الدور.

## السماح ثم إيقاف الدور — term35

الأمر: `node dist/mobile-live-20260909/scenario.cjs allow`. تقرير `scenario-allow.json` يحمل `ok:true`. عرضت بطاقة PWA أمر PowerShell الذي ينشئ `approved-confirmed.txt` ثم ينتظر120s. وصل `mobile_decision=allow`، وظهر الملف بالمحتوى `MOBILE_ALLOW_CONFIRMED` حرفياً. بعد ذلك نُقر زر الإيقاف في PWA.

| الدليل | epoch بالمللي ثانية | النتيجة |
|---|---:|---|
| محاولة أمر الإيقاف الصاعد | 1788916334134 | run-4 |
| حدث سطح المكتب `proc_done` | 1788916334892 | يسبق الإقرار |
| إقرار `command_result` بعد الفك الإنتاجي | 1788916335165 | `stopped`, `commandMatches=true`, `runMatches=true` |
| قراءة نص التأكيد | 1788916335225 | «أكّد الحاسوب انتهاء الدور الجاري.» |

وصل الإقرار بعد محاولة الإيقاف بـ1031ms، وبعد `proc_done` بـ273ms. القراءة النهائية لـPWA أظهرت النص المؤكد ومرحلة «توقف». لا يوجد إقرار توقف أقدم في السجل المحفوظ يمكن أن يكون استُعمل بدلاً منه.

وفحص القائد عملية Codex المحددة: PID=`53704`، ParentProcessId=`57972`، Creation=`1788916313388`؛ كانت حية قبل الإيقاف، ثم عاد استعلام Win32_Process لذلك PID فارغاً بعده. هذا إثبات خروج تلك العملية، ولا يثبت خروج كل descendants منفردة. اكتمال `run.done` مستدل عليه من إقرار الإنتاج وبوابته: `handleMobileStop` ينتظر `active.done`، و`Codex.done` ينتظر خروج العملية وتنظيف التوصيلات. لم يراقب المسبار Promise مباشرة، ولم يستعمل `proc_done` وحده لإثبات تأكيد الإيقاف.

## المهام في دور لاحق للمشروع نفسه — term37

الأمر: `node dist/mobile-live-20260909/scenario.cjs after`. أُجريت المحاولة الناجحة بمحرك SDK/Opus `[1m]` بحسب تشغيل القائد؛ JSON يثبت `engine:"sdk"` ولا يسجل اسم النموذج. تقرير `scenario-after.json` يحمل `ok:true`، ويشترط إطار حالة جديداً بعد خط أساس المحاولة وباسم دور مختلف عن السابق، لا مجرد وجود `2/2` قديمة في الواجهة.

وصلت أحداث `task_update` حقيقية، ودور `run-6` انتقل من `working` ومهام0 إلى إنشاء مهمتين، ثم إنجاز1، ثم إنجاز2، ثم `done`. الإطار الجديد النهائي عند `1788916595697` يحمل `total=2, completed=2, pending=0, in_progress=0, blocked=0`. عرض DOM مشروع `workspace` وعنوان «تحديث الدور التالي» أثناء تقدمه، ثم عدّادي `2/2` ومرحلة «انتهى». لا يُنسب هذا إلى دورَي Codex السابقين؛ كلا تقريريهما يحمل مهام0 ولا يحوي `task_update`.

هذا يثبت استمرار تحديث المهام الحقيقي لدور لاحق في المشروع نفسه. لم تُزرع في التجربة الحية رسالة SDK قديمة متأخرة؛ إثبات ذلك التداخل يبقى للحارس الآلي الخاص بـOBS-148.

## إعادة التحميل والعدّادات

الدليل `step-1788916651158-reload.json` يسجل `sameDevice=true` و`restoredWithoutPairing=true`. ارتفع سقف حجز عدّاد الإرسال من `8` إلى `16` بعد إعادة تحميل PWA واستعادة الجلسة؛ لم يُعد الاقتران. هذا سقف الحجز المحفوظ، وليس عدد الرسائل المرسلة. التجربة إعادة تحميل للصفحة، ولا تثبت التعافي من انهيار العملية أو انقطاع الطاقة.

## العيب المرئي المكتشف وإصلاحه في المصدر

PASS المهام أعلاه محصور في مسار الحالة وعدّاداتها. كشف التقرير نفسه أن `statusText` بقي «أكّد الحاسوب انتهاء الدور الجاري.» من دور `run-4`، حتى بعد بدء وإنهاء `run-6` وظهور مهامه2/2. أثبت سجل ما قبل reload بقاء الرسالة، وبعد reload صارت «متصل — في انتظار طلب…». لذلك لم تُعتمد سلامة الرسالة بالاستنتاج من نجاح المهام.

أعاد حارس `pwa-dom-live-test` الحالة عبر `pollLoop` الإنتاجية: إقرار A، ثم state أحدث للدور نفسه، ثم state لدور جديد قبل بطاقة إذنه. سقط الحارس قبل الإصلاح وفق إيصال القائد؛ نص حالة الفشل نفسه محفوظ في عضّة `disabled` أدناه. الإصلاح في `pwa/app.js`: حساب `runChanged` قبل تحديث `currentRun` داخل فرع الحالة المقبولة، وإعادة نص الاتصال عند تغير الدور فقط. نبضات الدور نفسه تحفظ الإقرار، وتبقى حراسة `pendingStop` والنتائج القديمة. رُفع `pwa/sw.js` إلى `satr-pwa-v16` كي تصل القشرة المحدثة عند النشر/البناء اللاحق.

## عرف العضّة — 1: تعطيل مسح رسالة الدور السابق

الأوامر المنفذة:

```powershell
node dist/mobile-live-20260909/bite.cjs plant disabled
npm run test:pwa-dom
node dist/mobile-live-20260909/bite.cjs restore
```

الزرع: استبدال `if (runChanged)` بـ`if (false && runChanged)` في سطر `setStatus` المحدد. إيصال العدّ:

```text
BEFORE original=1 mutant=0
AFTER original=0 mutant=1
```

سقط الحارس؛ النص الآتي من `bite-disabled.log` المقروء بـUTF-16LE، مع التفاف السطر كما حُفظ:

```text
node.exe : pwa-dom-live-test: FAIL
At line:1 char:1
+ & "C:\Program Files\nodejs/node.exe" "C:\Program Files\nodejs/node_mo ...
+ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: (pwa-dom-live-test: FAIL:String) [], RemoteException
    + FullyQualifiedErrorId : NativeCommandError
 
  - OBS-147: الدور الجديد يزيل تأكيد إيقاف الدور السابق — توقّعنا "متصل — في انتظار طلب…" ووجدنا "أكّد الحاسوب انتهاء 
الدور الجاري."
```

أمر الاستعادة أعلاه يعيد Buffer المحفوظ من `app-fixed.bytes`، لا نسخة Git. إيصال القائد:

```text
RESTORED exact_bytes=true sha256=3d6e5d75a9a7aba55f35d1d1687934dccc0e3d88f410147e3178ce4b8ca31bb8
```

## عرف العضّة — 2: مسح التأكيد عند كل نبضة

الأوامر المنفذة:

```powershell
node dist/mobile-live-20260909/bite.cjs plant unconditional
npm run test:pwa-dom
node dist/mobile-live-20260909/bite.cjs restore
```

الزرع: استبدال `if (runChanged)` بـ`if (true)` في السطر نفسه. إيصال العدّ:

```text
BEFORE original=1 mutant=0
AFTER original=0 mutant=1
```

سقط الحارس المقابل الذي يشترط حفظ تأكيد الدور نفسه. النص الحرفي من `bite-unconditional.log` المقروء بـUTF-16LE:

```text
node.exe : pwa-dom-live-test: FAIL
At line:1 char:1
+ & "C:\Program Files\nodejs/node.exe" "C:\Program Files\nodejs/node_mo ...
+ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: (pwa-dom-live-test: FAIL:String) [], RemoteException
    + FullyQualifiedErrorId : NativeCommandError
 
  - OBS-147: لقطة أحدث للدور نفسه تحفظ تأكيد إيقافه — توقّعنا "أكّد الحاسوب انتهاء الدور الجاري." ووجدنا "متصل — في 
انتظار طلب…"
```

الاستعادة البايتية بالأمر أعلاه أعادت SHA256 نفسه. وأعيد قياس المصدر والنسخة المحفوظة أثناء تحرير هذه الوثيقة؛ تطابقا على `3d6e5d75a9a7aba55f35d1d1687934dccc0e3d88f410147e3178ce4b8ca31bb8`. لم تُستعمل `reset` أو `stash` أو استعادة من Git، فبقيت التعديلات السابقة محفوظة.

## حالة التحقق بعد الاستعادة

بحسب إيصال تشغيل القائد بعد الاستعادة، نجحت `npm run test:pwa-dom`: فحوص DOM **72**، والقرائية **6/6**، وSW **7/7**. لم تُرفع مهل الاختبار. هذه نتيجة المصدر بعد الإصلاح، منفصلة عن التجربة الحية السابقة على حزمة v15. اكتمل طقم الهاتف لاحقاً بنجاح9/9؛ النتيجة النهائية أدناه.

قياس القرائية الحي المحفوظ قبل بناء v16 في `step-1788916970501-visual.json`: العرض الفعلي390 و839، `direction/contrast/overflow/font=0` و`unseen=0` و`truncated=false`؛ النواة قاست عنصراً نصياً دلالياً واحداً في هذه الشاشة، فلا تعني تغطية كل النصوص والأزرار. لقطتاه `visual-1788916970391-390.png` و`visual-1788916970486-839.png`. الحارس الأوسع للشاشات الست موثق في [PWA-READABILITY-EVIDENCE.md](PWA-READABILITY-EVIDENCE.md).

## النتيجة النهائية بعد البناء والتجربة

نجح `npm run test:mobile` بعد الاستعادة **9/9** برمز0، ويشمل تكامل293 ونقل54 ومحركات7 وDOM72 وكاش7/7 وقرائية6/6. لم تُرفع المهل.

بُنيت `dist/final-batch-preview-v16/win-unpacked/Satr.exe` بالأمر `npm run dist:dir -- --config.directories.output=dist/final-batch-preview-v16 --publish never`؛ انتهت term_38 برمز0. طابقت175/175 من ملفات التشغيل المصدر. بصمة app.asar: `548c11e3149d6453ed7ad03d92396458e255a2843b2d49fecb294822ff0b4039`. الدليلان `dist/final-batch-v16-package-evidence.json` و`dist/final-batch-v16-lan-evidence.json`. الإصدار بقي2.16.21 بلا التزام أو نشر.

أُعيد السماح وكتابة `approved-v16.txt` ثم الإيقاف المطابق حياً على البناء الجديد: term_41 خرج0 و`scenario-allow.json` يحفظ النتيجة. تقرير السماح السابق علىv15 محفوظ في `scenario-allow-v15.json`.

ثم نجح `node dist/mobile-live-20260909/final-next.cjs` في term_42 برمز0. الملف `final-v16-next-run.json` يسجل عند2026-09-09T01:28:43.702Z: قبل الدور الجديد ظهر تأكيد الإيقاف؛ ثم وصل إطارworking بمعرّف مختلف في المشروع نفسه، وصار النص «متصل — في انتظار طلب…» وبقي صحيحاً حتى انتهى الدور. لا حقن لحالة أو إقرار في هذا الفحص.

قياس البناء النهائي في `step-1788917435236-visual.json` عند390 و839 أعطى العدادات الأربعة0 وunseen=0 وtruncated=false. قاست النواة عنصراً دلالياً واحداً؛ لا تعني تغطية كل الأزرار والنصوص. اللقطتان `visual-1788917435059-390.png` و`visual-1788917435203-839.png`.

آخر طقم كامل103/103 ذو الإعادة الواحدة يخصv15 قبل إصلاح رسالة الدور؛ لم يُعد الكامل بعده. تحققv16 هو طقم الجوال والعضّتان والبناء المطابق والتجربة الحية اللاحقة. لا يُمحى فشل102/103 السابق أو تعثر489ms.

## التنظيف وحدود التسليم

أُبطلت أجهزة التجربة وحدها وعاد العدد10 إلى العدد الأصلي10. أُزيل PIN وجلسةIndexedDB الخاصان بالتجربة، وأُغلق الوسيط ومجلد الملفات الاصطناعية. بقيت بيئة التوصيلات `project-a` و`project-b` و`profile` محفوظة. عادت الواجهة إلى `project-a` و`codex` و`gpt-5.6-luna` دون دور جارٍ؛ `final-state.json` هو الإيصال. البناء النهائي يعمل وحده في term_39.

اكتملت التجربة البرمجية داخل معاينة Satr بطلب المستخدم. لا يثبت ذلك هاتفاً فعلياً أو قبول شهادةLAN أو relay أو بيانات خلوية أو Push. لا تُطلب إعادة هذه الدورة يدوياً ضمن المتبقي؛ تظل حدود الجهاز والشبكة الفعليين معلنة.
