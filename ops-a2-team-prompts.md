# برومبتات الدفعة أ‑2 «كلام بشري» — توزيع ثلاثي (تُطلق بعد اعتماد محمد ختم أ‑1)

المرجع الحاكم: `docs/OPS-ROOM-POLISH-SCOPE.md` — الدفعة أ‑2. نمط العمل: worktrees معزولة
بملكية ملفات صارمة (نمط satr-2-kimi/satr-2-opus)، دفعة واحدة لكل منفذ، لا commit إلا
من القائد بعد المراجعة.

## خطوة القائد قبل الإطلاق (تفك تبعية الملف المشترك)

ينشئ القائد `src/ui/lib/lifecycle-labels.js` **كاملة** قبل إطلاق المنفذين ويلتزمها:
- `LIFECYCLE_LABELS`: خريطة عربية واحدة لكل حالات lifecycle الظاهرة
  (`running/completed/approve/reject/changes_required/stopped/stopping/starting/queued/
  preparing/pending_confirmation/passed/failed/timed_out/interrupted/cleanup_failed/
  conflict/capturing/paused/active`).
- `countLabel(n, {one, two, plural})`: تصريف العدد العربي (ملف واحد/ملفان/N ملفات) —
  البند 7.
- `truncateWords(text, maxPoints)`: قص على حدود الكلمات بنقاط Unicode مع «…» — البند 6.
الملف بعدها **قراءة فقط للمنفذين الثلاثة** (يستهلكونه ولا يعدلونه؛ أي نقص يُرفع للقائد).

## قواعد مشتركة (تُلصق في كل برومبت)

- اعمل حصراً داخل worktree المخصص لك. الملفات المملوكة محددة أدناه — ممنوع غيرها.
- لا اعتماديات، لا style مضمّن، الألوان tokens فقط، نصوص عربية والمعرّفات/الأرقام LTR.
- لكل بند: إصلاح + توسعة اختبار قائم (لا سكربتات جديدة). لا تشغّل test:full (للقائد).
- التسليم: TASLIM-A2.md في جذر الـworktree (ملخص لكل بند + خواتم الاختبارات حرفياً).
  لا commit.

---

## §1 — كيمي (worktree ‏satr-2-kimi): الدردشة

**الملكية**: `src/ui/components/chat.js` + `scripts/chat-rtl-test.js` وfixture‑ه.
**البنود**:
- **11+21 (جانب الدردشة)**: بطاقات `showOpsEvent` وكل نص lifecycle يظهر في الدردشة
  يستهلك `LIFECYCLE_LABELS` من `src/ui/lib/lifecycle-labels.js` — لا حالة إنجليزية خام
  (`completed`/`approve`…) تصل المستخدم.
- **10**: بطاقة `showOpsEvent` تعرض النص مرتين (العنوان/الملخص المكرر) — أزل الازدواج
  مع إبقاء عقد البطاقة المطوية.
**الاختبار**: توسعة اختبار chat الحي بحالتي بطاقة ops (نص معرّب، لا تكرار).

## §2 — كودكس (worktree ‏satr-2-codex): نصوص غرفة العمليات

**الملكية**: `src/ui/components/ops-room.js` + `scripts/opsroom-ui-test.js`.
**البنود** (كلها في ops-room.js):
- **11+21**: استبدال كل عرض حالة خام بخريطة `LIFECYCLE_LABELS` المشتركة (تبقى
  `TEAM_STATES` التفصيلية حيث هي أدق، لكن لا يصل المستخدم توكن إنجليزي خام).
- **27**: مراجعة أوقفها المستخدم لا تُعرض «الحكم: changes_required» — بل «أوقفها
  المستخدم قبل اكتمال الأحكام».
- **19**: بصمة الأثر بادئة 12 محرفاً في الحوارات وصفوف الأحداث؛ الكاملة خلف «التفاصيل».
- **7**: كل صيغ العدد عبر `countLabel` («سيُدمج ملفان…»، «N ملفات»).
- **30 (جانب العرض)**: صف التاريخ يعرض `task_excerpt` و`run_kind` والوقت المقروء متى
  وفّرها الفهرس (عمل أوبس)، بسقوط رشيق للمعرّف وحده حين تغيب.
- **5+12**: إسقاط صفوف metadata الفارغة («غير محدد») وتوحيد تسمية الفاعل عربياً.
- **6**: قص نصوص المهام عبر `truncateWords`.
- **8**: استبدال الجمل التنفيذية («لا يُبثّ patch الكامل إلى الواجهة») بإرشاد مستخدم.
- **13**: رسالة بوابة الدمج تُبنى ديناميكياً من الحالة (ما تبقى فقط، لا قائمة ثابتة).
- **4**: `timeLabel` ميلادي بأرقام لاتينية LTR (قاعدة المشروع — استبدل ar-SA).
- **16**: عزل المسارات/`code` ‏LTR في رسائل الحالة (نفس علاج panelSheet).
- **3**: إرشاد واحد لا اثنان، ويختفي عند اكتمال شروطه.
- **17**: افتراضي قسم «السجل» بعد إعادة التشغيل «التاريخ» (أو أول عرض فيه محتوى).
- **23 (جانب العرض)**: صفوف الفحوص تعرض `exit_label` متى وفّره أوبس، وإلا الكود الخام
  LTR بلا ⚠️ حين يكون الإيقاف مقصوداً.
**الاختبار**: توسعة test:opsroom-ui (فحوص ساكنة + دوال نقية حيث أمكن).

## §3 — أوبس (worktree ‏satr-2-opus): الجانب الخلفي

**الملكية**: `electron/opsroomindex.js` + مواضع محددة في `electron/main.js` (كتلة
upsert الفهرس + devServerInfo) + `electron/integration.js` + `electron/verify.js`
(سطر exit_label فقط) + `src/ui/components/preview-panel.js` (شارة الخادم) +
`scripts/opscontinuity-test.js` و`scripts/integration-test.js`.
**البنود**:
- **30 (جانب الفهرس)**: مدخل `opsroomindex` يكسب حقلين اختياريين additive منقّيين:
  `task_excerpt` (≤80 نقطة Unicode بعد إزالة التحكم/Bidi، من مهمة العامل الأول) و
  `run_kind` (`team|loop`). يعبّئهما `updateOpsRoomIndex` في main.js. schema يبقى v1
  (حقول اختيارية لا تكسر القراءة القديمة)، والتنقية fail-closed في sanitizeEntry.
- **23**: خريطة أكواد الخروج المعروفة (وحدة نقية صغيرة أو ثابت في verify.js):
  `-1073741510` (0xC000013A = Ctrl+C/إيقاف مقصود من سطر)، `-1073741819` (0xC0000005
  ‏Access Violation)، `1` عام… تُضاف `exit_label` عربية منقّاة إلى صفوف checks في نتيجة
  التحقق (additive في الحدث والتخزين المقصوص — لا كسر schema).
- **20**: `satr:devServerInfo` يميز مهمة معاينة تكاملية مؤقتة (worktree preview) عن
  غياب الخادم: يعيد علماً `integration_preview:true` حين تكون مهمة المعاينة التكاملية
  حية لهذا cwd، وpreview-panel تعرض «معاينة تكاملية مؤقتة تعمل» بدل «لا خادم مسجّل».
**قيود**: كل توسعة additive بقوائم سماح؛ لا مسار مطلق ولا patch في أي حدث؛ التنقية في
main.js. **الاختبار**: توسعة opscontinuity (الحقلان الجديدان وتنقيتهما) وintegration
(exit_label) وفحص ساكن لعقد devServerInfo.

---

## بوابة الختم (القائد بعد التسليمات الثلاثة)

مراجعة diff كل تسليم + دمج بالتسلسل (كودكس ⇒ أوبس ⇒ كيمي) ⇒ توسعة مشاهد ui:audit
(10/11 يلتقطان النصوص الجديدة تلقائياً + لقطة تاريخ بشرية) ⇒ `npm run test:opsroom-all`
ثم `npm run test:full` في طرفية سطر المرئية (نمط SUITE_EXIT وقراءة الخاتمة ذاتياً) ⇒
تحديث CLAUDE.md ووثيقة النطاق ⇒ التزام ⇒ اعتماد محمد قبل الدفعة ب.
