# Session refresh — محوّل Gemini CLI (طبقة المحوّلات)

العمل في `D:\sater\satr-2` (مشروع «سطر» — تطبيق Electron عربي لأدوات CLI الذكية).
هدف هذه الجلسة: **المرحلة 5 المؤجلة — طبقة `adapters/` ومحوّل Gemini CLI**: إثبات
رؤية «البيت العربي لكل أدوات CLI» بمحرك ثانٍ بعد Claude، بنفس الواجهة العربية.

## أين نقف

- v2.4.0 صدر ودُفع (كود + Release + مثبّت): طرفيات متعددة بتبويبات، أداة
  `run_in_terminal` (النموذج يشغّل في طرفية مرئية عبر MCP داخلي)، وكلاء فرعيون
  ببطاقات حية، مزامنة أوامر CLI في قائمة «/»، جهد التفكير + مجلدات إضافية.
- المحركان الحاليان في `main.js`: **sdk** (agent.js — الافتراضي الغني) و**cli**
  (`runCli` — احتياطي `claude -p stream-json` عبر stdin). قائمة «المحرك» في الواجهة
  تمرّر `engine` في payload.
- الواجهة تفهم أحداث `satr:event` بأنواع: `system/assistant/user/result/stream_text/…`
  (العقد كاملاً في CLAUDE.md) — **تطبيع خرج Gemini لهذه الأنواع نفسها يعني صفر تغيير
  في الواجهة**، وهذا هو النهج الموصى به.
- منهجية المشروع المثبتة: **تحقق حيّ قبل البناء** (أوقفت /rewind ونجّحت MCP)، مرحلة
  واحدة في الجلسة، قبول المالك بعد كل مرحلة، التجربة بـ `npm start` فقط (المثبّت فحص
  نهائي)، العربية أولاً، وتحديث CLAUDE.md/PLAN.md مع كل عقد جديد.

## الخطوات التالية

1. **بحث حيّ أولاً (لا بناء)**: وثائق Gemini CLI الرسمية **اليوم** — الوضع غير
   التفاعلي (`gemini -p`؟)، هل له خرج JSON بثّي أو ما يعادله، آلية المصادقة، أوضاع
   الموافقة على الأدوات (yolo/approval؟)، دعم الجلسات والاستئناف والصور. ثم فحص محلي:
   هل gemini مثبّت عند المالك؟ (`gemini --version`). الخلاصة: جدول قدرات مقابل عقد
   أحداث «سطر»، وقرار Go/No-Go بحدود موثّقة (نمط تقرير /rewind).
2. **تصميم واجهة المحوّل**: `adapters/` بواجهة `{start(payload, cwd, emit), stop()}`
   حيث emit يبثّ أحداث satr:event المطبَّعة. قرار مفتوح: لفّ مسار **cli** الحالي فقط
   في `adapters/claude-cli.js` (نقل runCli كما هو) وإبقاء محرك SDK خاصاً — أنصح بهذا
   بدل لفّ SDK (أدواته الخاصة: canUseTool/hooks/mcp لا تعمّم بسهولة).
3. **`adapters/gemini.js`**: تشغيل غير تفاعلي، تطبيع الخرج (نص تدريجي ⇐ stream_text
   إن أمكن، نهائي ⇐ assistant+result بكلفة/مدة إن توفرت)، معالجة الأخطاء بالعربية.
4. **الواجهة**: خيار «Gemini CLI» في قائمة المحرك (نفس select القائم) + تعطيل ما لا
   يدعمه المحرك بلطف (صور/مهارات/أدوات — كما يُفعل مع cli حالياً: تنبيه وإسقاط).
5. **قبول المالك**: يبدّل المحرك من القائمة ويحاور Gemini بالعربية بنفس الواجهة،
   والعربية تُعرض سليمة. ثم تحديث PLAN.md (المرحلة 5 ✅) وCLAUDE.md (عقد adapters).

## الملفات التي تُفتح (اقرأ ولا تعد الاشتقاق)

- `docs/PLAN.md` — المرحلة 5 الأصلية (مواصفة المحوّلات) + خارطة ما بعد 8 + قرارات
  القبول المقفولة (أسلوب npm start، التجميدات)
- `electron/main.js` — توجيه المحركين الحالي + `runCli` (النمط الذي سيُنقل لمحوّل) +
  التنقية (SAFE_*) + عقد satr:send
- `src/index.html` — قائمة المحرك (select#engine) + معالج onEvent (الأنواع التي يجب
  أن يطبّع Gemini إليها) + سلوك «الصور مدعومة في SDK فقط» (النمط نفسه لقيود Gemini)
- `electron/agent.js` — للمقارنة فقط: ما الذي يبقى خاصاً بمحرك SDK ولا يُعمَّم
- `electron/preload.js` — الجسر (لا يُتوقع تغييره: نفس satr:send/satr:event)

## قرارات مفتوحة (تُحسم مع المالك قبل البناء)

- هل يُلفّ محرك SDK في adapter أم يبقى خاصاً؟ (توصيتي: يبقى — لفّ cli فقط)
- إن لم يكن لدى Gemini CLI خرج بثّي مهيكل: هل يكفي عرض «نص نهائي دفعة واحدة» في
  الإصدار الأول؟ (توصيتي: نعم، بحدّ موثّق)
- المصادقة: إن تطلبت تسجيل دخول تفاعلياً — نمط «بوابة أول التشغيل» القائم يُوسَّع؟
