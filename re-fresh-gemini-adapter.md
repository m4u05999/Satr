# Session refresh — محوّل Gemini CLI (طبقة المحوّلات)

العمل في `D:\sater\satr-2` (مشروع «سطر» — تطبيق Electron عربي لأدوات CLI الذكية).
هدف هذه الجلسة: **المرحلة 5 المؤجلة — طبقة `adapters/` ومحوّل Gemini CLI**: إثبات
رؤية «البيت العربي لكل أدوات CLI» بمحرك ثانٍ بعد Claude، بنفس الواجهة العربية.

## أين نقف (آخر إصدار v2.4.1، كله مدفوع على GitHub)

- **المرحلة 8**: طرفية عربية مدمجة (xterm.js + node-pty/ConPTY) بعرضَي BiDi‏/شبكي.
- **المرحلة 14**: تكافؤ Claude Code — مزامنة أوامر «/»، وكلاء فرعيون، جهد التفكير،
  مجلدات إضافية. (‏/rewind محجوب بقيد SDK — موثّق.)
- **المرحلة 15**: تعدد الطرفيات بتبويبات (كائن مستقل لكل تبويب).
- **المرحلة 16**: أداة `run_in_terminal` — النموذج يشغّل في طرفية مرئية عبر خادم MCP
  داخلي (`createSdkMcpServer`)، تمر بـ canUseTool مثل Bash.
- **المرحلة 17**: التحديث التلقائي (electron-updater + إصدارات GitHub، بلا توقيع، v2.4.1
  آخر تثبيت يدوي). `electron/updater.js`.
- **المحركان الحاليان في `main.js`**: **sdk** (agent.js — الافتراضي الغني) و**cli**
  (`runCli` — احتياطي `claude -p stream-json` عبر stdin). قائمة «المحرك» (select#engine)
  في الواجهة تمرّر `engine` في payload `satr:send`.
- **مفتاح التصميم**: الواجهة تفهم أحداث `satr:event` بأنواع
  `system/assistant/user/result/stream_text/permission_request/…` (العقد كاملاً في
  CLAUDE.md). **تطبيع خرج Gemini لهذه الأنواع نفسها = صفر تغيير في الواجهة** — النهج الموصى.

## منهجية المشروع المثبتة (طُبّقت طوال الجلسات وأثمرت)

- **تحقق حيّ قبل البناء**: أوقف /rewind (نقاط الحفظ لا تعمل مع الإدخال البثّي) ونجّح MCP
  ودمج الطرفية. لكل قدرة SDK/CLI: مِحكّ صغير يثبتها قبل كتابة واجهة فوقها.
- مرحلة فرعية واحدة في الدور، توقف لقبول المالك بعد كلٍّ، تجربة بـ `npm start` فقط
  (المثبّت فحص نهائي)، العربية أولاً، تحديث CLAUDE.md/PLAN.md مع كل عقد جديد.
- **حذار محارف التحكّم في index.html**: بايت NUL كسر هاش CSP بصمت (درس موثّق) — أي
  محرف تحكّم في المصدر يمرّ باختبار إقلاع.

## الخطوات التالية

1. **بحث حيّ أولاً (لا بناء)**: وثائق Gemini CLI الرسمية **اليوم** — الوضع غير التفاعلي
   (`gemini -p`؟)، هل له خرج JSON بثّي أو ما يعادله، آلية المصادقة، أوضاع الموافقة على
   الأدوات، الجلسات/الاستئناف/الصور. ثم فحص محلي: `gemini --version` (مثبّت عند المالك؟).
   الخلاصة: جدول قدرات مقابل عقد أحداث «سطر»، وقرار Go/No-Go بحدود موثّقة (نمط تقرير /rewind).
2. **تصميم واجهة المحوّل**: `adapters/` بواجهة `{start(payload, cwd, emit), stop()}` حيث
   emit يبثّ أحداث satr:event المطبَّعة. توصية: لفّ مسار **cli** الحالي في
   `adapters/claude-cli.js` (نقل runCli كما هو) وإبقاء محرك SDK خاصاً (أدواته:
   canUseTool/hooks/mcp لا تعمّم بسهولة).
3. **`adapters/gemini.js`**: تشغيل غير تفاعلي، تطبيع الخرج (تدريجي ⇐ stream_text إن أمكن،
   نهائي ⇐ assistant+result)، معالجة الأخطاء بالعربية.
4. **الواجهة**: خيار «Gemini CLI» في select#engine + تعطيل ما لا يدعمه المحرك بلطف
   (صور/مهارات/أدوات — نمط «الصور مدعومة في SDK فقط» القائم: تنبيه وإسقاط).
5. **قبول المالك**: يبدّل المحرك ويحاور Gemini بالعربية بنفس الواجهة، والعربية سليمة.
   ثم PLAN.md (المرحلة 5 ✅) وCLAUDE.md (عقد adapters).

## الملفات التي تُفتح (اقرأ ولا تعد الاشتقاق)

- `docs/PLAN.md` — المرحلة 5 الأصلية (مواصفة المحوّلات) + قرارات القبول المقفولة
- `electron/main.js` — توجيه المحركين + `runCli` (النمط الذي سيُنقل لمحوّل) + التنقية
  (SAFE_*) + عقد `satr:send`
- `src/index.html` — select#engine + معالج onEvent (الأنواع التي يطبّع Gemini إليها) +
  سلوك «الصور مدعومة في SDK فقط» (نمط قيود Gemini)
- `electron/agent.js` — للمقارنة: ما يبقى خاصاً بمحرك SDK ولا يُعمَّم
- `electron/preload.js` — الجسر (لا يُتوقع تغييره: نفس satr:send/satr:event)

## قرارات مفتوحة (تُحسم مع المالك قبل البناء)

- هل يُلفّ محرك SDK في adapter أم يبقى خاصاً؟ (توصية: يبقى — لفّ cli فقط)
- إن لم يكن لـ Gemini CLI خرج بثّي مهيكل: هل يكفي «نص نهائي دفعة واحدة» في الإصدار الأول؟
  (توصية: نعم، بحدّ موثّق)
- المصادقة: إن تطلبت تسجيل دخول تفاعلياً — يُوسَّع نمط «بوابة أول التشغيل» القائم؟
