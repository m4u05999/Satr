/**
 * سطر — مرساة اللغة العربية (‏OBS-001، الدفعة 4 — بقرار مالك 2026-08-15).
 *
 * وحدة نقية بلا اعتماديات (نمط `mobilestate.js`): المصدر الواحد لنصّ عقد اللغة
 * وللمرساة الذيلية، يستهلكها `envbrief` والمحرّكات — نسخ النص في كل سطح يعيد
 * نمط «قارئين يتباعدان بصمت».
 *
 * **التشخيص الذي بُنيت عليه (مقيس لا مفترض — دفعتا 2 و3)**: الانهيار ليس انجرافاً
 * ولا تلويثاً — الجلسات المنهارة الثماني كلها جلسات عمل وكيلية نمطها: ردود نهائية
 * عربية و**سرد عمل بين استدعاءات الأدوات إنجليزي من أول سطر**. التعليمة القديمة
 * («في الشرح والردود») تُفهَم على الإجابات ويُعامَل السرد كأنه داخلي. فالعلاج:
 *   1. **توسيع نص العقد** ليسمّي سرد العمل وسطور الحالة صراحةً.
 *   2. **مرساة ذيلية** (~25 رمزاً) تُلحق بعد رسالة المستخدم — الحداثة تغلب
 *      الموضع صفر في السياقات الطويلة، والإلحاق في الذيل لا يمسّ البادئة
 *      المخبَّأة فكلفته على الكاش صفر (حجة كلود في العصف).
 *   3. **صيغة أقوى** للدور الأول من الجلسة ولأول دور بعد الضغط — القياس الرجعي
 *      أثبت أن مصير الجلسة يتحدد عند أول ردّ مساعد.
 *
 * **خارج نطاق هذه الوحدة عمداً** (الحدّ الحرفي المعتمد — docs/LANGUAGE-CONTRACT.md):
 * التفكير المعروض (commentary) وبرومبتات المنفّذين خارج الإلزام حتى تحسمهما A/B؛
 * لا شيء هنا يلزمهما. وطلب المستخدم الصريح للغة أخرى يجُبّ العقد كله.
 */

'use strict';

/**
 * سطر العقد داخل موجز البيئة — يستهلكه `envbrief.build` لكل المحرّكات.
 * ⚠️ عبارة «تواصل بالعربية» مثبتة حرفياً: يفحصها `language-probe` و`envbrief-test`.
 */
const CONTRACT_LINE = 'تواصل بالعربية افتراضياً في كل نثر موجّه للمستخدم — الإجابات'
  + ' والشرح **وسرد العمل بين استدعاءات الأدوات وسطور الحالة القصيرة** — وأبقِ الكود'
  + ' والمسارات والأوامر والمصطلحات التقنية بالإنجليزية LTR؛ واتبع لغة أخرى إن طلبها'
  + ' المستخدم صراحةً.';

// المرساة داخل وسم كي تبقى قابلة للتمييز في السجلات والتصدير (وقابلة للحذف لاحقاً)
const ANCHOR_OPEN = '<satr_lang>';
const ANCHOR_CLOSE = '</satr_lang>';

const ANCHOR_NORMAL = 'تذكير: سردُ عملك وشرحُك بالعربية — بما فيه سطور الحالة بين'
  + ' الأدوات؛ الكود والمسارات بالإنجليزية.';

// الصيغة الأقوى: القياس الرجعي أثبت أن أول ردّ مساعد يحدد مصير الجلسة كلها
const ANCHOR_STRONG = 'مهم — لغة هذه الجلسة: كل نثرك بالعربية من أول سطر، بما فيه'
  + ' سرد العمل بين استدعاءات الأدوات وسطور الحالة القصيرة والخطط؛ يبقى الكود'
  + ' والمسارات والأوامر والمصطلحات التقنية بالإنجليزية LTR. طلب المستخدم الصريح'
  + ' للغة أخرى يجُبّ هذا التذكير.';

/**
 * المرساة الذيلية — تُلحق **آخر** محتوى الدور بعد رسالة المستخدم.
 * @param {{strong?: boolean}} [options] — القوية للدور الأول ولأول دور بعد الضغط.
 */
function anchor(options) {
  const strong = Boolean(options && options.strong);
  return ANCHOR_OPEN + ' ' + (strong ? ANCHOR_STRONG : ANCHOR_NORMAL) + ' ' + ANCHOR_CLOSE;
}

module.exports = { CONTRACT_LINE, anchor, ANCHOR_OPEN, ANCHOR_CLOSE };
