/**
 * fixture — مسبار سليم يخرج فوراً. يثبت أن السلسلة **تكمل** بعد مسبار معلَّق بدل أن
 * تُحجب — وهو نصف العقد الآخر في `OBS-062` (المعلَّق حجب أربعة بعده).
 *
 * اسمه ينتهي بـ`-fixture.js` لا بـ`-probe.js` كي لا يظهر في `--list` كمسبار حقيقي.
 */
'use strict';

console.log('probe-runner-ok: مسبار سليم — خرج فوراً');
process.exit(0);
