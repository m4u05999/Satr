/**
 * fixture — يحاكي عطل `OBS-062` حرفياً: يطبع تقريره كاملاً ثم **لا يخرج**.
 *
 * السبب هو نفسه المرصود في `kimi-capability-probe.js`: سلسلة `setTimeout` تعيد جدولة
 * نفسها بلا شرط توقّف، فتبقى حلقة الأحداث دائرة أبداً بعد انتهاء الشغل المرئي.
 * وجودُه يجعل حارس `probe-runner` يعضّ فعلاً بدل أن نفترض أنه يعضّ.
 */
'use strict';

console.log('hanging-probe: التقرير اكتمل — وهذا كل ما يراه القارئ');
const spin = () => setTimeout(spin, 50); // بلا شرط توقّف — عمداً
spin();
