/**
 * لا منطق قياس هنا عمداً: السكربت المقيس هو `READABILITY_FN` المستخرج من
 * `electron/preview.js` نفسه وقت التشغيل — فلا تُقارَن نسخة بنسخة (درس
 * `test:langmetric`: حارس «مجمَّد» يستورد فرعه من الإنتاج يقارن الشيء بنفسه).
 * هذا الملف يعلن جاهزية التخطيط فقط.
 */
'use strict';

window.__readabilityReady = true;
