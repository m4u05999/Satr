# تبعيات
node_modules/

# مخرجات البناء (المثبّت يُرفَع يدوياً لصفحة الإصدارات، لا يُلتزَم)
dist/

# سجلّات
*.log

# إعدادات Claude Code المحلية (شخصية — تحوي مسارات الجهاز وقوائم سماح خاصّة، لا تُنشَر)
.claude/settings.local.json

# ملفات وتجارب اختبار مؤقّتة
scripts/_*.js
_bidi_test/
_killtest/
*.tmp.js

# ملاحظات handoff مؤقتة (re-fresh) — شخصية، لا تُلتزم
/re-fresh-*.md

# ملفات نظام التشغيل
.DS_Store
Thumbs.db
desktop.ini

# ملف شخصي لا يُنشَر
بسم_الله.txt

# مخرجات TestSprite المولّدة محلياً (حالات/تقارير/روابط تشغيل خارجية)
/testsprite_tests/

# checkout خاص اختياري للتطوير المحلي؛ كود Enterprise لا يدخل مستودع Community
/enterprise/

# مخرجات فيديو البرومو المولّدة محلياً — تُعاد صناعتها من مصادر promo/ ولا تدخل Git
/promo/**/*.mp4
/promo/**/*.mp3
/promo/**/*.wav
/promo/frames/
/promo/.frames/
/dist-installer-*/

# وثيقة الاستراتيجية الداخلية — محلية لا تُنشر (قرار 2026-08-06)
/docs/MONETIZATION.md
