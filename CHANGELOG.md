# سجل التغييرات

يوثّق هذا الملف الجديد في كل إصدار من «سطر». يقرأ منه سير النشر قسمَ الإصدار الجاري
ويضعه في صفحة الإصدار على GitHub، وزرّ **«ما الجديد؟»** في إشعار التحديث يفتحها.

الصيغة: قسم لكل إصدار بعنوان `## <رقم> — <وصف قصير>`.

## 2.16.16 — GPT-6 Astra يظهر باشتراكك، وقارئ ويب أرخص، وحزمة متجر مايكروسوفت

### نماذج كان «سطر» يخفيها عنك

مُرشِّحٌ في «سطر» كُتب حين كانت `gpt-5.6` أحدث عائلة، فصار **يُسقط كل جيل بعدها**: كان
Codex يعلن سبعة نماذج ويعرض «سطر» أربعة. الآن يظهر **GPT-6 Astra** — وهو النموذج الافتراضي
لحسابك عند Codex — ومعه `gpt-5.4-mini` و`gpt-5.3-codex-spark`. المرجع صار إعلان Codex نفسه
فلا يتقادم مع كل إصدار جديد.

وأُضيف **GPT-6 Astra** أيضاً لمحرك «OpenAI — مفتاح API»، ومعه **GPT-5.6 Cyber** موسوماً
«يتطلب موافقة Daybreak» لأنه لا يوجد لحساب غير مفحوص — فلا يُقرأ حجبُ المزوّد عطلاً هنا.

### قارئ مقالات رخيص بالرموز

أداة `read_article` تعطي الوكيل **متن المقال وحده** بصيغة Markdown بدل لقطة شاشة. القياس على
صفحة MDN حقيقية: المقال كاملاً **4,749 بايت** نصّاً، مقابل **198,808** بايت للقطة واحدة لا
تُري إلا خُمس الصفحة. والنصّ العربي يخرج بترتيبه المنطقي لأنه من DOM لا من صورة.

### حزمة Microsoft Store

بناء **MSIX** بهوية ناشر حقيقية، والمُحدِّث معطَّل داخلها (المتجر يحدّث)، مع شارة «نسخة
المتجر» في ⚙. مُجرَّبة داخل الحاوية: الطرفية وأدوار المحرّكات والمعاينة والمفاتيح كلها تعمل.

### أسرع وأرخص في كل دور

- **نتائج الأدوات القديمة تُمسح قبل التلخيص**: حجم سجل الطلب `60,952 ⇒ 5,810` بايت
  (‏**−90.5%**) بلا كلفة استدلال، مع إبقاء آخر جولة كاملة وحقيقة القرص سليمة.
- **بادئة البرومبت ثُبّتت** فلم يعد «سطر» يُبطل تخزين المزوّد المؤقت بنفسه كل دور.
- ترقية محرك Claude Agent SDK إلى `0.3.261`.

### أعطال كنت ستلاحظها

- قسما **حساب Claude وCodex في ⚙** كانا مجمَّدين لا يتحدّثان عند الفتح.
- **التشكيل العربي المُلصق** في الطرفية كانت حركاته تسقط قبل أن تبلغ الصدفة.
- **المعاينة** لا تعود بعد التسليم البشري.
- **عدد الملفات المُستعادة** بعد «↩ استرجع» كان يأتي من معاينة جافة لا من القرص، فقد يكذب.
- عند تجاوز حدود الطبقة المجانية (‏`429`) صارت الرسالة **تسمّي الحدّ** بالعربية وتنتظر
  انتظاراً مسقوفاً بدل تمرير خطأ المزوّد الخام.
- شارة **«يعمل في الخلفية»** على بطاقة الوكيل كانت مبنية ولا تصل.
- تفريع جلسة Kimi، وجاهزية المحرّك عند التبديل، وسعة تثبيت الجلسات.

### أمان

- تنبيه عند وجود **خطّافات Claude مزروعة** في المشروع.
- **بصمة تكوين خوادم MCP** وتنبيه عند تغيّرها.
- قنوات Enterprise تمرّ من حارس ثقة IPC نفسه الذي تمرّ منه قنوات النواة.
- تحصين `edit_file` بالكتل الذرية وبوابة «اقرأ قبل التعديل».

### المهارات

استيراد مهارات المجتمع، وقارئ فهرس marketplace، ومدقّق مواصفة Agent Skills عند الفهرسة.

## 2.16.15 — «سطر» يفتح بمفتاح مجاني بلا اشتراك، ويكشف لك ما كان يخفيه

### البوابة تفتح بمفتاح API مجاني

لم يعد «سطر» يشترط اشتراك Claude Code أو Codex أو Kimi ليعمل: مفتاح مجاني من
**NVIDIA NIM** أو **Groq** (حساب فقط، بلا بطاقة) يفتح التطبيق مباشرةً. وحقل إدخال المفتاح
صار **داخل البوابة نفسها** مع شرح لمن لا يعرف ما المفتاح وأين يُنشأ. والمفتاح الذي لا
يستطيع جهازك فكّه (منسوخ من جهاز آخر) لا يفتح البوابة بعد الآن ليتركك مع أدوار فاشلة.

### جهد التفكير كما يعلنه النموذج فعلاً

كان المنتقي يعرض كل المستويات لكل نموذج، والمحرّك يخفّض ما لا يدعمه **صامتاً**. صار
يعرض المستويات المدعومة للنموذج المختار فقط، ويخبرك إن سقط اختيارك المحفوظ إلى الافتراضي.

### تنبيهات كانت غائبة

- أول تعديل ملف في مجلد **ليس مستودع git** أو مستودع بلا أي commit ⇒ تنبيه واحد غير حاجب:
  التراجع متاح داخل الجلسة فقط ويضيع بإغلاق «سطر».
- أمر مهمة خلفية بصياغة cmd/POSIX (‏`cd /d`، ‏`&&`، ‏`2>/dev/null`) كان يفشل صامتاً وتبدو
  المهمة حيّة. صار يُرفض قبل الإقلاع مع اقتراح نظيره في PowerShell.
- نسخة التطوير تحمل شارة «نسخة تطوير» تعلن أن تفضيلاتها منفصلة عن النسخة المثبتة.

### الجلسات

- «🌿 فرّع من هنا» صار يعمل لجلسات **Kimi Code** أيضاً (تفريع من النهاية).
- جلسات الأدوات (عوامل غرفة العمليات والمراجعون) تُوسَم وقت إنشائها فتُخفى من لوحة
  الجلسات حتى حين تجري داخل مجلد مشروعك الحقيقي.

### المعاينة والأداء

- لقطات الشاشة التي يستلمها النموذج تُصغَّر على العرض وحده وتُرمَّز بالصيغة الأصغر فعلاً
  (PNG أو JPEG بالقياس) — أخفّ في الرموز وبلا انهيار لعرض الصفحات الطويلة.
- فحص جاهزية Codex لم يعد يكلّف ثانية ونصفاً عند كل «أعد الفحص».

### صفحة الهبوط

روابط الصفحة ترسو تحت الرأس الثابت على الجوال بدل أن يبتلعه، واللقطات المرجعية حُدّثت
لتطابق الواجهة الحالية، وأُضيف تسجيل اهتمام بنسخة ماك/لينكس يقرّر إن كانت تُبنى.

## 2.16.14 — «سطر» يعمل بمفتاح مجاني، وأسماء تفهمها من أول نظرة

### منصتان مجانيتان بمفتاح API — بلا اشتراك مدفوع

انضمت **NVIDIA NIM** و**Groq** إلى قائمة المحرّكات: مفتاح مجاني بحساب فقط (بلا بطاقة
ائتمان) يعطيك وكيلاً كامل الأدوات — يقرأ ملفاتك ويعدّلها ويشغّل أوامرك بإذنك — على
نماذج مثل Nemotron 3 Super وDeepSeek V4 وGPT-OSS 120B، ومعها **ALLaM** العربي السعودي
للدردشة. المفتاح يُحفظ من ⚙ «مفاتيح المزوّدين» ويعمل فوراً.

> كل نموذج في القائمة **مثبت حياً** بجولة أدوات حقيقية قبل إدراجه — وقد كشف الفحص أن
> نماذج كنا نعرفها من التوثيق ماتت فعلاً، فلم تدخل.

### أسماء محرّكات بشرية

«SDK — بث وأذونات حية» صارت **«Claude — اشتراك Claude Code»**، و«Kimi Code — أصلي (ACP)»
صارت **«Kimi Code — اشتراك»** مقابل **«Kimi K3 — مفتاح API»**، وكل مزوّد بمفتاح يقول
ذلك في اسمه. المبدأ: العلامة + طريقة الدخول، بلا مصطلحات مطوّرين.

### نماذج Claude بأسمائها الرسمية

كان المنتقي يعرض «Fable» بلا رقم و«Opus (1M context)» بلا رقم، فبدا **Fable 5.1**
غائباً وهو موجود. صارت الأسماء مطابقة لتسمية أنثروبيك حرفياً: Fable 5.1 وOpus 5
وSonnet 5 وHaiku 4.5 وOpus 4.8.

### زرّ «الجلسات» في الشريط العلوي

لوحة الجلسات كانت تُفتح بأمر `/جلسات` فقط. صار لها زرّ ظاهر بجوار «جلسة جديدة».

## 2.16.13 — قائمة الجلسات تعرض أرشيفك كاملاً

### كنتَ تبحث في نافذة لا في أرشيفك

اللوحة كانت تعرض **آخر 100 جلسة فقط**، والبحث يرشّح المعروض. فمن يبحث عن مشروع
قديم لا يجده — لا لأنه أخطأ الكلمة، بل لأن الجلسة **لم تكن في مجموعة البحث أصلاً**.
على جهاز مستُخدم حقيقي: 156 جلسة على القرص، 100 معروضة، وأقدم ما يُرى قبل 17 يوماً.

صارت اللوحة تقرأ الأرشيف كاملاً. والسقف القديم كان يحمي من كلفة غير موجودة — قِسناها:
مسح الأرشيف كله يستغرق **62 مللي ثانية**.

### ومجموعات بدل قائمة متشابكة

كانت 22 مشروعاً مختلطة في قائمة واحدة مرتّبة بالوقت. صارت مجموعات قابلة للطيّ:
مشروعك الحالي أولاً بلون الهوية ومفروداً، والمثبّتة فوقه، والبقية مطوية حتى تفتحها.

وجلسات الأدوات (عوامل غرفة العمليات والمراجعون) تُخفى افتراضياً بمربع تُطفئه متى شئت،
مع عدّاد يقول «‏N من M» فلا يكون الإخفاء صامتاً.

### واستئناف جلسة لم يعد يبدّل مجلد عملك صامتاً

حين تفتح جلسة من مشروع آخر لتقرأها، كان مجلد عملك يتبدّل بلا إشعار **ويبقى متبدّلاً
بعد إعادة التشغيل** — فتُولد جلساتك التالية في المشروع الخطأ. صار «سطر» يخبرك
بالمجلدين ويعطيك زرّ «↩ أعِد مجلدي».

> والسبب كان أدقّ مما يبدو: أداة الطرفية تتنقّل بين مجلدات المشروع أثناء العمل، وكنّا
> نقرأ **آخر** مجلد وقفت عنده بدل المجلد الذي بدأت فيه الجلسة. فحصنا 156 جلسة: لا ملف
> تضرّر، والأثر كان في الأرشيف وحده.

### وزرّ «حدّث المحرك» كان يفشل صامتاً

على أي جهاز بإعداد ويندوز الافتراضي، كان الأمر يُحجب لأن ويندوز يمنع تشغيل سكربتات
npm. أُصلح في تسعة مواضع — ويعمل الآن بلا لمس أي إعداد أمان على جهازك.

### ومهارة جديدة: تلخيص فيديو YouTube

أعطِ الوكيل رابط فيديو فيستخرج نصّه ويلخّصه بالعربية بطوابع زمنية تعود بها إلى
الموضع. تحتاج تثبيت `yt-dlp` مرة واحدة، ويرشدك إليه عند أول استعمال.

## 2.16.12 — مهامّ الخلفية لا تعلق بعد اليوم

### أمرٌ طويل أو متعدد الأسطر كان يوقف المهمة صامتة

حين تطلب من الوكيل تشغيل خادم أو مهمة طويلة، كان الأمر يُكتب حرفاً حرفاً في سطر
الطرفية. وأيّ أمر من عدة أسطر تلتصق جُمله فتصير جملة ناقصة، فتقف القشرة تنتظر
بقيتها إلى الأبد — والمهمة تبدو **حيّة وهي لا تعمل**.

صار الأمر يُسلَّم إلى الصدفة عند تشغيلها مباشرةً، فلا يمرّ بسطر الطرفية إطلاقاً:
لا حدّ لطوله، ولا التصاق لأسطره. وإن كان الأمر ناقصاً فعلاً فإنه **يفشل فوراً
برسالة واضحة** بدل أن يعلق بلا تفسير.

> وللدقة: ظننّا أولاً أن السبب طول الأمر، فقِسناه — أمرٌ بطول 7986 محرفاً مرّ
> سليماً. السبب الحقيقي كان حذف أسطر الأمر، وهو ما أُصلح.

### سجلّ الطرفية لا يقطع الكلام في منتصفه

عند قراءة ذيل سجلّ طويل كان القصّ يشطر الحرف العربي فيظهر تالفاً، ويبتر أول سطر
بلا علامة فيبدو كاملاً وهو ليس كذلك. صار القصّ يحترم حدود المحارف، **ويعلن**
كم بايتاً حُذف بدل تمرير كسرٍ يُقرأ سلامةً.

### قياس قرائية الصفحات العربية

أداة جديدة يستعملها الوكيل على أي صفحة يبنيها لك: تقيس رسوّ اتجاه كل فقرة
بالبكسل، والتباين، والتجاوز الأفقي، وتحميل الخطوط — وتغطي العربية والفارسية
والأردية والبشتو والكردية والسندية والأويغورية. ومعها مهارة تحمل خبرة «سطر»
المقيسة في هذا الباب إلى مشاريعك أنت.

### وصيانة داخلية

أُعيد التحقق من عقود محرّكات Claude وCodex بعد ترقيتها فصمدت، وصار لكل مسبار
تشخيصي مهلة قاطعة فلا يعلق أحدها بلا نهاية.

## 2.16.11 — حارس العربية يخرج من الصمت

كانت أقدم شكوى مفتوحة أن «سطر» يردّ بالإنجليزية أحياناً داخل واجهة عربية. عالجناها
على مراحل منذ منتصف أغسطس، وهذا الإصدار يختمها.

### زرّ «أعد الصياغة بالعربية»

حين يخرج ردٌّ عن العربية فعلاً، يظهر أسفله زرّ واحد. **تضغطه أنت** — لا يعيد التطبيق
توليد شيء من تلقائه ولا يترجم آلياً. والطلب المرسَل يحفظ القاعدة نفسها: الكود
والمسارات والأوامر والمصطلحات التقنية تبقى إنجليزية.

**ولن يزعجك**: قِسنا 921 رسالة حقيقية على مدى 12 يوماً قبل تشغيله، فتبيّن أن الزرّ
كان سيظهر في **1% منها تقريباً**. والحالات غير القاطعة تُركت خارج العرض عمداً —
حارسٌ يُنذر بالخطأ يُطفَأ ثم لا يحرس شيئاً.

- **إن طلبتَ أنت لغةً أخرى** («أجبني بالإنجليزية») فلا يظهر الزرّ إطلاقاً.
- **جدولٌ تقنيٌّ داخل ردّ عربي ليس انزلاقاً**: كان جدول فيه `share | same | same`
  يُحسب خروجاً عن العربية، رغم أن هذه أسماء حقول يجب أن تبقى إنجليزية.

### محرك Kimi صار داخل المنظومة

كان Claude وCodex يُذكَّران بالعربية في كل دور، وKimi وحده منسيّاً — فمحادثاته
الطويلة عرضة للانزلاق الذي عولج في نظيريه. صار الآن مثلهما تماماً.

### وتمييز العربية من الفارسية

المقياس كان يعدّ الحروف فقط، والفارسية تُكتب بحروف عربية — فكان نصٌّ فارسي خالص
يُسجَّل «ملتزماً بالعربية 100%». صار يعرف الفرق. لا أثر لهذا في واجهتك، لكنه يمنع
تلويث القياس الذي تُبنى عليه دقّة الحارس.

### تحت السطح

بوابة الاختبارات كانت **تتجمّد صامتة** حين يعلق اختبار — لا نجاح ولا فشل. صارت
تنطق بالفشل خلال دقائق وتُكمل بقيّة المجموعات.

## 2.16.10 — جرّبها بنفسك قبل أن تعتمدها

إصدار محتواه **مهارة واحدة** — لا تغيير في سلوك التطبيق.

### الوكيل يسلّمك خطوات تجربة، لا وصفاً لما فعل

- حين ينهي الوكيل عملاً يمسّ **ما تراه أنت**، صار يملك بروتوكولاً يتّبعه: يبني نسخة،
  ويجهّز بيئة تجربة يعرف نتيجتها سلفاً، ثم يسلّمك خطوات مرقّمة لكل خطوة **«ما يجب أن
  تراه» و«ما يعني الفشل»** مكتوبين **قبل** أن تجرّب. فتحكم بمعيار لا بانطباع.
- **أنت من ينقر، لا هو**: ممنوع أن يحاكي الخطوات بأدواته ثم يعلن نجاحها — تلك تصير
  اختباراً آلياً ثانياً، والقيمة كلها في أن يجرّبها إنسان.
- **لا يدّعي ما لم تُظهره لقطتك**، وإن خالف الدليلُ ما تذكره فلا يجادلك ولا يصدّقك:
  يقرأ الكود ثم يصمّم **خطوة واحدة حاسمة** تفصل الاحتمالين.
- ويعرف **متى لا يفعل ذلك**: تغيير داخلي بلا سطح تراه، أو تعديل من سطرين، أو ما
  يمسكه اختبار آلي — فلا يضيّع وقتك في طقس.

**لماذا أُضيفت**: طُبّقت أول مرة على إصدار 2.16.9 نفسه، فكشفت عطلاً كان يُسقط التطبيق
عند كل مراجعة بمحرك Codex — بينما كانت 75 مجموعة اختبار خضراء بالكامل.

## 2.16.9 — صقل ما تراه كل يوم

خمس ملاحظات صغيرة من لقطات شاشة أرسلها المستخدم — كلها في الطريق نفسه: حوار يسألك
شيئاً بينما المعاينة مفتوحة.

### الحوار صار يجيب عن نفسه

- **الزر المعطَّل يبدو معطَّلاً**: كان «إرسال الإجابة» ذهبياً بارزاً قبل أن تختار
  شيئاً، فتضغطه ولا يحدث شيء وتظنّ التطبيق عالقاً. صار رمادياً محايداً ومعه تلميح
  «اختر إجابة أولاً» — تقرأ الحالة بلا نقر تجريبي.
- **ترى ما تجيب عنه**: الحوار كان يغطّي حرفياً الفقرة أو الجدول الذي بُني عليه
  السؤال، فتجيب من الذاكرة أو تلغي. صار يعرض مقتطفاً من آخر ما كتبه النموذج داخله.
- **المعاينة لم تعد تنطفئ صامتة**: كانت تصير مستطيلاً أسود أثناء الحوار (تنحٍّ مقصود
  كي لا تحجب السؤال)، فتقرأها عطلاً. صارت تقول «مخفية مؤقتاً أثناء الحوار — تعود فور
  ردّك».

### إصلاح: «راجع تغييراتي» كان يُسقط التطبيق

- كشفَته تجربة يدوية على هذه النسخة نفسها: عند انتهاء مراجعة يقوم بها Codex، كان
  التطبيق ينهار بحوار خطأ أحمر — فلا تصل بطاقة المراجعة أصلاً. عطلٌ قائم منذ 2.16.8.
- السبب أن خطأ الأنبوب في هذه الحالة **غير متزامن**، فلا يمسكه الحارس الموضوع له،
  ويصعد استثناءً يوقف العملية. صار مُعالَجاً من ثلاث جهات.
- ومعه: طلبٌ داخلي كان قد يعلّق بلا نهاية لو ماتت عملية المحرك تحته — وجدها **المراجع
  الأعمى نفسه** حين راجع هذا الإصلاح، فأُغلقت.

### الوكيل يرى صفحتك أوضح

- **سجل console بلا ضجيج**: رسائل غلاف المتصفح وأدواته لم تعد تُخلط بأخطاء مشروعك،
  ويُذكر عددها كي لا يختفي شيء بصمت. ما يظهر للوكيل صار من صفحتك أنت.
- **قراءة تصمد أمام صفحة عدائية**: صفحة تُخرّب دوالّ المتصفح كانت قادرة على إعماء
  الوكيل عن محتواها — أو على إخفاء أن زرّاً يُرسل نموذجاً إلى موقع آخر، فيمرّ الفعل
  بلا سؤالك. صارت قراءات المعاينة كلها — بما فيها ما تُبنى عليه أسئلة الإذن — تجري
  في عالم معزول لا تصل إليه الصفحة.

## 2.16.8 — «راجع تغييراتي الآن»

### رأي مستقل على تغييراتك، بأمر واحد

- **أمر `/راجع` جديد**: يأخذ تغييراتك غير الملتزمة، ويرسلها إلى **محرّك آخر** يقرأها
  في مجلد فارغ لا يرى مشروعك ولا محادثتك، ثم يعرض المخاطر في المحادثة نفسها.
- **لماذا يهم**: الوكيل الذي كتب الكود لا يراجعه بعينين نظيفتين مهما طلبتَ منه — رآه
  وهو يُكتب. وإن كنت تعمل وحدك فلا زميل يراجع لك. هذه أقرب شيء لرأي ثانٍ حقيقي.
- **قراءة فقط**: لا يلمس فهرس git ولا يعدّل ملفاً ولا يدمج شيئاً. الملفات الجديدة
  تُقرأ بلا إضافتها للفهرس.
- **لا يراجعك محرّكك نفسه**: إن لم يتوفّر محرّك آخر يقول ذلك صراحةً بدل إعطائك رأياً
  ليس مستقلاً. وإن بدا في تغييراتك ما يشبه مفتاحاً أو سراً، تتوقف المراجعة قبل أن
  يغادر شيء جهازك.
- بلا فتح لوحة ولا انتقال إلى سطح آخر — النتيجة بطاقة في المحادثة.

## 2.16.7 — رأي ثالث في العصف، وطرق حقيقية بدل تمثيلها

### العصف صار ثلاثياً حين تملك ثلاثة

- **Kimi Code ينضم رأياً ثالثاً** في «🧠 عصف» بغرفة العمليات، بجانب Claude وCodex —
  كلٌّ في مجلد فارغ خاص به، بلا أدوات ولا وصول للمشروع ولا تخاطب بينهم.
- **اختياري لا إلزامي**: إن لم يكن Kimi مثبَّتاً أو مسجَّل الدخول فالعصف يعمل برأيين
  كما كان تماماً، بلا رسالة خطأ ولا تعطيل. فحص الجاهزية على القرص فقط فلا يبطئ البدء.
- رأي العصف لا يحجز مقعداً من مقعدَي جلسات Kimi الحيّة ولا يطرد جلستك الخاملة.

### الوكيل يعرض الطرق بدل أن يصف عجزه

- طلبتَ منه «عصفاً بآراء متعددة» فأجابك بما **لا** يستطيع، بينما الطرق الثلاث متاحة
  كلها. صار يعرضها: مهارة `satr-diverge`، وعصف غرفة العمليات، وتشغيل محرّك آخر في
  الطرفية المرئية بأعلامه الصحيحة.
- والدليل داخل التطبيق (`satr-guide`) يشرحها بلغتك مع تنبيه صريح: الوكلاء الفرعيون
  هم المحرّك نفسه بسياقات معزولة — **تنوّع أسلوب لا تنوّع نماذج**.

## 2.16.6 — مهامك الخلفية لم تعد تختفي بصمت

مصدر هذه الدفعة تقرير قائد فريق يعمل في مشروع آخر داخل «سطر»، رتّب عيوب الأدوات
بالوقت المهدور فعلاً. تبيّن بفحص الكود أن أثقل عيبين **عطلٌ واحد**، وأن علاجه كان في
المستودع أصلاً ولا يُستدعى حيث يحتاجه.

### انتظر المهمة بدل أن تستقصيها

- **أداة جديدة `wait_for_background_task`**: تحجب حتى تخرج المهمة الخلفية ثم تعيد رمز
  خروجها وذيل سجلها **لحظة انتهائها**. كان الوكيل ينام ثم يسأل «هل انتهت؟» في حلقة —
  رُصدت أكثر من 25 دورة في جلسة واحدة. عند المهلة تعود «ما زالت تعمل» بلا خطأ فيمدّد
  الانتظار بنداء واحد.
- **نهاية «الموت الصامت»**: مهمة تختفي من القائمة صار سبب خروجها محفوظاً — يظهر في
  `list_background_tasks` وفي `get_background_output` بمعرّفها. ومهمة تنتهي بينما لا
  دور جارٍ يعرف عنها الوكيل في أول رسالة تالية.
- **لا دور تلقائي**: «سطر» لا يبدأ محادثة من تلقاء نفسه ولا ينفق بلا حضورك، ولا
  يقاطع دوراً جارياً.

### سجل المهمة صار مقروءاً

- `get_background_output` كان يعيد رموز ANSI ومحارف تموضع المؤشر خاماً فيدفن سطر
  الخطأ المفيد. صار يمرّ بالتنقية نفسها التي يمرّ بها إشعار انتهاء المهمة: ANSI ومحارف
  التحكم تُزال، والأسرار تُحجب — مع سقف أوسع يتّسع لسجل خادم طويل.

### المعاينة تقول لماذا ضيّقت العرض

- طلب عرض سطح مكتب بينما وضع محاكاة الأجهزة مفعّل كان يُضيَّق **بصمت**: تطلب `1280`
  فتحصل على `390` بلا تفسير، فيتعذّر الحكم على التخطيط. صارت الأداة تقول صراحةً إن وضع
  «موبايل» أو «لوحي» مفعّل — أو أن اللوحة نفسها ضيقة — وتذكر كيف يُلغى.

### صدق رمز الخروج والترميز

- **`exit 0` رغم فشل الأمر**: أمر PowerShell يفشل بلا رمز خروج أصلي كان يُعلَن ناجحاً.
  صار الفشل يظهر. وحين تتناقض إشارتا الصدفة (فشل معلن مع رمز خروج 0) يُعرض تنبيه ولا
  يُكذَّب رمز الخروج. ⚠️ قد يبدأ أمرُ تحقّقٍ كان يمرّ صامتاً بالفشل — وهو الصواب.
- **العربية من Python** لم تعد تتكسّر في الطرفية: يُفعَّل وضع UTF-8 تلقائياً.
- **قراءة ملف قيد الكتابة**: ملف عُدّل قبل لحظات يُسبَق بتنبيه أنه قد يكون نسخة ناقصة.

### وثيقة أعلام المحرّكات

- ملف جديد يوثّق أعلام تشغيل Codex وKimi وClaude من سطر الأوامر، مفصولاً فيه **المثبت
  بالتجربة** عن **المُبلَّغ غير المتحقَّق منه**. سببه أن قائد الفريق الخارجي ظنّ أن
  `codex exec` لا ينفّذ شيئاً فبنى ست سكربتات بديلة بلغت إحداها 176 ألف حرف — والسبب
  علم مفقود لا عطل منصة.

## 2.16.5 — «ما الجديد» متاح متى شئت

- **زر «ما الجديد؟» في ⚙** بجانب رقم الإصدار. حتى الآن كان الزر لا يظهر إلا في بطاقة
  التحديث — ومن هو على أحدث نسخة لا يرى تلك البطاقة أصلاً، فكان يتعذّر عليه قراءة
  ملاحظات إصداره أو حتى تجربة الميزة حتى يصدر إصدار تالٍ.
- يفتح الحوار نفسه داخل «سطر» بملاحظات نسختك الحالية.

## 2.16.4 — «ما الجديد» يُقرأ داخل «سطر»

- **ملاحظات الإصدار تُعرض داخل التطبيق** بدل فتح متصفح خارجي. الأخير كان يخالف قاعدة
  المشروع المعلنة («افتح الويب داخل سطر لا في متصفح خارجي»)، وكان يصطدم بتحقق
  GitHub‏ 2FA لمن هو مسجّل دخول — اعتراضٌ لا علاقة له بالمحتوى، فالمستودع عام
  وملاحظاته تُقرأ بلا حساب.
- النص يُجلب في العملية الرئيسية (فلا توسيع لـCSP ولا كوكيز)، ويُعرض بـtextContent
  لأنه محتوى خارجي غير موثوق. و«افتح في المتصفح» يبقى خياراً ثانوياً صريحاً.
- Escape أو ✕ أو النقر خارج البطاقة يغلق الحوار.

## 2.16.3 — محرّكاتك لا تتقادم في صمت

### إصدارات المحرّكات

- **قسم جديد في ⚙** يعرض Claude Code وCodex وKimi Code: المثبَّت عندك، وأحدث إصدار
  معلن، وكم أنت متأخر — وزر «⬆ حدّث الآن» يشغّل الأمر الصحيح لكل محرك في طرفية مرئية.
- **كان «سطر» لا ينبّه أصلاً**: العتبة محفورة في الكود عند `2.1.197`، فبقي `2.1.220`
  يُعدّ حديثاً وهو متأخر 21 إصداراً — ولم يكن هناك فحص لـCodex ولا Kimi إطلاقاً.
  صار الكشف حياً بمقارنة مع أحدث إصدار معلن، بكاش يومي فلا يستعلم مع كل إقلاع.
- **بلا تحديث تلقائي — عمداً.** عقود المحرّكات هنا مثبتة بمسابير حية، وإصدار جديد قد
  يكسر عقداً فيتعطّل التطبيق بلا سبب ظاهر؛ وأسوأ منه أن يقع التحديث أثناء دور جارٍ.
  فالتحديث بضغطة صريحة، ومرفوض أثناء دور، ويجري أمام عينك في تبويب 🛠.
- **كل محرك وقناته**: Claude وCodex عبر npm (لا المثبّت الأصلي — يكسر اكتشاف المسار في
  «سطر»)، وKimi على ويندوز عبر سكربت التثبيت لأن `kimi upgrade` يرفض الترقية صراحةً
  على هذه المنصّة ويوجّه إليه.

### إصلاح

- زر «ما الجديد؟» كان يختفي لحظة الضغط على «نزّل الآن» — أي أن نافذة القراءة تنغلق حين
  تحتاجها. صار يبقى ظاهراً طوال دورة التحديث، ويحرسه فحص حيّ.

## 2.16.2 — تنبيهات تصل في وقتها، وتحديثٌ تعرف ما فيه

### إشعارات تصل حين يتوقّف العمل

- **إشعار عند طلب الإذن أو السؤال.** كان الوكيل يتوقّف منتظراً قرارك بلا أي تنبيه —
  أصمت حالة في «سطر» رغم أنها أكثرها إلحاحاً. صار يصلك إشعار نظام عند بطاقة الأذونات،
  وعند سؤال الاختيار، وعند طلب إدخال موصّل.
- **النقر على الإشعار يفتح نافذته فعلاً.** كان لا يفعل شيئاً، لأن رفع نافذة Electron
  من الواجهة لا يعمل على ويندوز حين تكون نافذة أخرى في المقدمة. صار الرفع يقع في
  العملية الرئيسية — ومن يشغّل عدة نسخ لعدة مشاريع يصل إلى النسخة الصحيحة، لأن الإشعار
  صدر منها.
- **لا تتكدّس الإشعارات.** إشعار النوع نفسه يستبدل سابقه بدل أن يصطفّ في مركز الإشعارات.
- **انتهى فيض توستات الاختبارات.** كان توست تقدّم TestSprite يُعرض مع كل تحديث — وهو
  يُبثّ عند كل تقدّم **وكل 30 ثانية** حتى بلا جديد. صار لا يظهر إلا حين يتغيّر الرقم فعلاً.

### تحديث تعرف ما فيه

- زرّ **«ما الجديد؟»** في إشعار التحديث يفتح صفحة الإصدار قبل أن تقرّر التنزيل. والرابط
  يُبنى في العملية الرئيسية من إعداد النشر — لا عنوان يعبر من الواجهة.
- صفحة كل إصدار صارت تحمل هذا السجل، لا اسم الملف وبصمته فقط.

### بوابة أول التشغيل تفتح لأي محرك

- كانت البوابة تشترط Claude Code، فيُحجب من يملك **Codex** أو **Kimi Code** جاهزاً بلا
  سبب تقني. صارت تفتح لأي محرك جاهز، وتُرشد عند الحجب إلى الثلاثة بأوامرها، وتميّز
  «مثبّت غير مسجّل» من «غير مثبّت».
- منتقي المحرك يُصحَّح تلقائياً: من يملك Codex وحده لا يبقى منتقيه على SDK فيفشل أول
  طلب صامتاً.
- فحوص الإقلاع صارت متوازية، فكلفة البوابة أبطأ فحصٍ واحد لا مجموعها.

### منهج القرار المتباعد (satr-diverge)

- **إيصال العزل**: لم يعد يُقبل ادّعاء «شغّلت فروعاً معزولة» بلا دليل قابل للتحقق —
  كشف تقييم داخلي أن الفروع لم تكن تُطلق أصلاً بينما التقرير يعلن عكس ذلك.
- **خطوة صفر**: لا تبدأ المراحل قبل قراءة المنهج من ملفه.
- **ميزانية تدقيق معلنة**، وإسناد كل واقعة إلى مصدرها، ومواجهة مجهّلة الهوية.
- حزمة نقل مولَّدة تأخذ المنهج وأداة قياسه إلى أي مشروع.
