# AGENTS.md — دليل Codex لمشروع «سطر» (Satr)

> هذا الملف لوكلاء Codex. النسخة الأشمل (بالتاريخ والدروس المفصّلة) في `docs/internals/` —
> الفهرس `docs/internals/README.md`؛ ارجع إليه عند الحاجة لتفاصيل ميزة بعينها. هنا **الجوهر الملزِم**
> كي لا ينحرف التطوير.
> **الكود بالإنجليزية، والتعليقات والنصوص بالعربية، والقواعد أدناه غير قابلة للتفاوض.**

## ما هذا المشروع
تطبيق سطح مكتب **Electron** يحلّ عرض العربية (RTL + التشكيل) عند استخدام أدوات الذكاء
الاصطناعي في سطر الأوامر (Claude Code أولاً، ثم Codex وغيرهما). يشغّل الأدوات في الخلفية
ويعرض المحادثة في واجهة HTML عربية سليمة. الواجهة **صفر اعتماديات وقت تشغيل**.

## القواعد الإلزامية (اقرأها قبل أي تعديل)
1. **الأمان أولاً**: لا تعطّل `contextIsolation` أو `sandbox`، ولا تفعّل `nodeIntegration`.
   كل قدرة جديدة تُكشف عبر `electron/preload.js` بدالة محدّدة فقط — **لا تكشف `ipcRenderer`
   كاملاً أبداً**.
2. **التحقق من المدخلات في `main.js`**: أي قيمة تدخل وسائط `spawn`/مساراً يجب أن تمرّ على
   regex صارم (انظر `SAFE_SESSION`, `SAFE_MODEL`, `SAFE_ENGINE`, `SAFE_EDIT_ID`). البرومبت
   آمن لأنه عبر stdin. مسارات الملفات تُتحقَّق داخل جذر المشروع حصراً (لا `..` ولا مطلق).
3. **العربية أولاً**: كل نص واجهة بالعربية. الأكواد/المسارات/الأرقام التقنية
   `direction: ltr` دائماً. و**اتجاه النص المختلط بحسب الوعاء لا بقاعدة واحدة**
   (‏`OBS-061`): فقرة/عنصر قائمة/عنوان/خلية جدول/فقاعة ⇒ **حسم إحصائي صريح** عبر
   `textDir()` من `src/ui/lib/text-dir.js` ثم `dir` على العنصر — لأن
   `unicode-bidi: plaintext` و`dir="auto"` يحسمان من أول حرف قوي، فالفقرة العربية
   البادئة برمز لاتيني (`SHA-256 …`) ترسو LTR كاملة. أما الحاوية والنص القصير بلا رمز
   بادئ فـ`dir="auto"` مقبول، والكتلة متعددة الأسطر تأخذ اتجاهاً واحداً للكتلة.
   ⚠️ `getComputedStyle(el).direction` **لا يكشف** العطل (يعيد الموروث)؛ الدليل موضع
   أول محرف بالبكسل — `scripts/arabic-rtl-probe.js` وأداة `browser_readability`.
4. **الكود إنجليزي، التعليقات عربية**: أسماء المتغيّرات/الدوال إنجليزية، والشرح عربي.
5. **أقل اعتماديات ممكنة**: لا تُضف حزمة npm إلا لضرورة قصوى مبرّرة وموثّقة في `CLAUDE.md`.
   الواجهة صفر اعتماديات (xterm والخط **مُضمّنان vendored** في `src/vendor/`، ليسا اعتمادية
   تشغيل). العملية الرئيسية: `@anthropic-ai/claude-agent-sdk` و`node-pty` (أصلية، مبرّرة) فقط.
6. **لا تكسر العقد بين الطبقات**: أي تغيير في صيغة أحداث IPC يتطلّب تحديث الطرفين معاً
   (`main.js` ⇄ `preload.js` ⇄ `src/ui/`) وتحديث التوثيق.
7. **اختبر على ويندوز ذهنياً**: مسارات `\`، أوامر `.cmd`، ترميز UTF-8. هذه البيئة الأساسية.
8. **CSP صارم — لا `unsafe-inline`**: ممنوع `<style>`/`<script>` مضمّن أو سمات `style=`/`onclick=`
   في `index.html`. استعمل `styles/base.css` و`ui/app.js` و`addEventListener` وCSSOM. أنماط
   المكوّنات عبر `adoptedStyleSheets` حصراً (`<style>` داخل Shadow **محجوب بـ CSP**).
   `scripts/update-csp.js` يعمل تلقائياً قبل start/dist ويهشّ أي كتلة مضمّنة عائدة.

## المعمارية (خريطة مختصرة)
- `electron/main.js` — العملية الرئيسية: النافذة، توجيه المحرّكات، معالجات IPC، **كل التنقية**.
- `electron/preload.js` — الجسر الآمن الوحيد (`window.satr`).
- `electron/agent.js` — محرك Claude Agent SDK (بثّ + `canUseTool` + خطّافا Pre/PostToolUse للفرق).
- `electron/codex.js` — **محرك Codex الأصيل** (فوق `codex app-server` JSON-RPC). خاص مثل sdk.
- `electron/adapters/` — محوّلات المزوّدين «العمياء» (cli/gemini/deepseek/…): عقد
  `start(input,cwd,emit)→{stop()}`. سجلّ قابل للحقن.
- `electron/sessions.js` / `codexsessions.js` / `chats.js` — قراءة الجلسات (Claude/Codex/المحوّلات).
- `electron/files.js` `search.js` `gitdiff.js` `gitactions.js` `diff.js` `inject.js` `tools.js`
  `term.js` `preview.js` `keys.js` `features.js` `bgprocs.js` — انظر `docs/internals/` لتفاصيل كلٍّ.
- `src/index.html` — هيكل الواجهة (وسوم المكوّنات). `src/styles/base.css` — Design Tokens + أنماط.
- `src/ui/app.js` — قشرة الإقلاع والتوجيه + حالة التطبيق + مجرى `satr:event`.
- `src/ui/components/` — مكوّنات Web Component (بادئة `satr-`). `src/ui/lib/` — وحدات ES مشتركة.

## تدفّق البيانات وعقد الأحداث
الواجهة تستدعي `window.satr.send({prompt, cwd, sessionId, model, permissionMode, engine, …})`.
حسب `engine`: `sdk`→`agent.js`، `codex`→`codex.js` (كلاهما خاص، يسكن `currentRun` بـ
`resolvePermission`+`stop`)، وغيرهما عبر `adapters.get(engine)` (`currentCliRun`). الأحداث تصل
الواجهة عبر `satr:event` بأنواع موحّدة: `system`(init: session_id) · `assistant`(content[]:
text/tool_use) · `user`(tool_result) · `stream_text` · `result`(التكلفة/المدة/session_id) ·
`permission_request`→`window.satr.permission(id,allow,always)` · `file_edit`(بطاقة فرق)→
`window.satr.undoEdit(id)` · `proc_done`/`stderr`/`spawn_error`. **أي محرك جديد يطبّع خرجه
لهذه الأنواع نفسها فلا تتغيّر الواجهة.** محرك kimi-code يضيف: `system/available_commands`
(أوامر ACP المائلة المعلنة — تُغطى بأسماء عربية ثابتة `/ضغط /سياق /حالة /مهام /مساعدة`)
وتسميات عربية لأدوات Kimi الداخلية عبر `KIMI_TOOL_LABELS` في `electron/kimi.js`
(العنوان الأولي لـ tool_call فقط؛ عناوين التحديثات الحرة تبقى كما هي).

## محرك Codex (إن عملت عليه)
`electron/codex.js` يتكلّم JSON-RPC مع `codex app-server` (stdio، أسطر JSON بـ `\n`). نقاط حرجة
مثبّتة بالتجربة (لا تكسرها):
- طلب الإذن `ServerRequest` **قد يكون `id === 0`** ⇒ استعمل `id != null` لا فحص truthy.
- مفردات قرار v2: **`accept | acceptForSession | decline | cancel`** (لا مفردات v1).
- `approvalPolicy` الصالحة (0.144): `untrusted | on-request | granular | never`. **`never` لا
  يعني «اقبل تلقائياً»** بل «لا تصعّد» فيحجب الكتابة بصندوق read-only. acceptEdits = on-request
  مع قبول apply_patch تلقائياً في المعالج.
- الأحداث بقناتين متوازيتين (`codex/event/*` v1 و`item/*`/`turn/*` v2) — **ابنِ على v2**.
- لا تحزم ثنائي Codex — `resolveCodexBin` يحلّ المثبّت عالمياً. النماذج المعروضة **حديثة فقط**
  (gpt-5.6-sol/terra/luna, gpt-5.5) — لا نماذج قديمة (قرار المالك).

## البناء والتشغيل
```
npm install        # مرة واحدة
npm start          # تشغيل للتطوير (يلتقط تعديلات المصدر عند إعادة التشغيل)
npm run dist       # مثبّت ويندوز NSIS في dist/
npm run dist:dir   # مجلد بلا مثبّت (أسرع للتجربة)
```
- **لا تحزم ثنائي claude** (نوجّه SDK للمثبّت عالمياً عبر `pathToClaudeCodeExecutable`).
- **`npmRebuild: false`** — node-pty يشحن prebuilds تعمل تحت Electron 33 كما هي (لا Visual Studio).
- بعد أي تعديل: شغّل `npm start`، تحقّق يدوياً من السلوك، ثم قدّم ملخّصاً. **لا تلتزم (commit)
  إلا إن طلب المالك.** التزم عند حدود نظيفة (مهمة واحدة/التزام واحد).

## الاختبار الحي في نسخة ثانية

مهارة المشروع `.agents/skills/satr-live-test/SKILL.md` تنظّم اختبار مسار الإنتاج حياً
وتسجيل الدليل عند طلبه؛ ليست مهارة `builtin` ولا تلزم لكل تعديل داخلي.
`scripts/live-test.js prepare` ينشئ تجربة؛ ثم `launch <id> --debug` عبر
`run_in_background` بعد `list_background_tasks`، و`status <id>` يعيد هويتها.
`scripts/lib/live-test-client.js` يتيح `connectMain(repoRoot, id)` ثم `evaluate/close`
للنافذة الرئيسية المحددة؛ أدوات المعاينة لا تتحول تلقائياً إلى تلك النافذة.
المشغّل يحمّل `main.js` وواجهة الإنتاج من المصدر: النتيجة `source` وليست `packaged`.
العزل للمنزل و`profile` والبيئة والتنزيلات؛ ليس صندوق نظام تشغيل، والمصدر والاعتماديات
مشتركة. لا ينقل مصادقة المحركات؛ إعداد محرك حقيقي يحتاج تفويضاً وميزانية منفصلين.
`--record-consent` يستهلك لتسجيل واحد بعد إذن صريح قائم؛ لا يُسأل المالك مجدداً عن إذن منحه.
تسجيل `app-window` صامت بأبعاده الأصلية، يستمر بين الأدوار حتى إيقافه، وأصله في
`dist/live-tests/<id>/home/Downloads`. التصيير التسويقي لاحق بطلب المالك واعتماد خطه الزمني.
حفظ المقطع ليس `PASS` للسيناريو؛ بصمة الإقلاع تخص خمسة ملفات فقط، لا كل المصدر.
احفظ خط الأساس ونظّف موارد التجربة وحدها؛ لا `git clean` ولا استعادة تعديلات المالك.
للتجربة الحية قيمة مستقلة، ويظل القبول البشري المطلوب محصوراً في الفجوة التي لم تثبت.

## القبول البشري لدفعات الواجهة (قرار مالك 2026-08-25)
دفعة تمسّ **ما يراه المستخدم** لا تُعتمد بالطقم القطعي وحده: ابنِ نسخة (`dist:dir`)،
وجهّز بيئة تجربة تعرف نتيجتها سلفاً، وسلّم المالك خطوات مرقّمة لكل خطوة معيار
**«الصواب / الفشل» مكتوب قبل التنفيذ** — ثم انتظر لقطاته. المهارة المضمّنة
`satr-accept` تحمل البروتوكول كاملاً ومثالاً حياً؛ حمّلها بـ`load_skill` عند إنهاء
دفعة كهذه.
- **هو ينقر لا أنت**: قيادة الوكيل اختبار حي آلي ذو قيمة، ولا تُسمّى قبولاً بشرياً.
  لا تكرر على المالك ما ثبت آلياً؛ سلّمه الفجوة البشرية المتبقية بمعيارها.
- **لا تدّعِ ما لم تُظهره اللقطة**؛ وإن خالف الدليلُ ذاكرةَ المالك فلا تجادل ولا تصدّق:
  اقرأ الكود ثم صمّم **خطوة واحدة حاسمة**.
- **متى لا تُستعمل**: تغيير بلا سطح مرئي (‏`looprunner`/`merger`/تنقية `main.js`) أو دفعة
  من سطرين — الطقم أصدق وأرخص. وما يمكن لحارس آلي مسكه، حوّله إلى حارس ولا تُنفق وقت بشر.
- **ترس أوسط — «فحص مصغّر»**: إن غطّى الحارس معظم الادعاء وبقيت **فجوة بصرية واحدة**
  تُعرض في نسخة عاملة **بلا بناء**، فاطلب نقرة واحدة بمعيار مكتوب سلفاً بدل البروتوكول
  كاملاً — وبدل إغلاق الفجوة بالاستنتاج. الشروط الثلاثة في المهارة، و«هو ينقر» لا يُخفَّف.
- **نظّف بعدها**: احفظ الأدلة المطلوبة، ثم أزل موارد التجربة وحدها بعد مقارنتها بخط الأساس؛
  حافظ على ملفات المالك وتعديلاته السابقة، وانتبه أن بناءً خارج `.gitignore` يلوّث الشجرة.

المبرّر مقيس: أول تطبيق كامل له (‏2.16.9) كشف عطلاً كان يُسقط التطبيق في إصدار منشور،
بينما الطقم أخضر 75/75.

## أعراف العمل
- عدّل ملفاً قائماً بأسلوبه المحيط (كثافة التعليقات، التسمية، الاصطلاح) — لا تُدخل نمطاً غريباً.
- لا تُدخل لوناً صلباً جديداً في CSS — كل لون عبر متغيّر Token (شرط عمل الوضعين فاتح/داكن).
- عند الشكّ في تفصيل ميزة، اقرأ ملفها في `docs/internals/` (الفهرس في `README.md` هناك) قبل التعديل —
  لا تُعِد اشتقاق ما هو موثّق. والدفعة الجديدة تُوثَّق في ملفها هناك لا في `CLAUDE.md` (نواة ≤ 200 سطر).

## توجيه النماذج — الغالي يقرّر ويراجع، الرخيص ينفّذ، الطقم يحكم

السياسة كاملةً في `docs/MODEL-ROUTING.md` (قرار مالك 2026-09-10). الجوهر الملزِم:

- **الافتراضي T2**: ‏Claude Code على `opusplan` (تخطيط بـOpus، تنفيذ بـSonnet) وجهد `medium`؛ Codex على
  `gpt-5.6-luna`/`sol` بجهد `low`/`medium`. النموذج الغالي (‏Fable/Opus `xhigh`/Astra) **بسبب مكتوب** في
  المهمة: قرار مفاضلة، عقد (‏IPC/langanchor)، أمن، ConPTY/BiDi/مصيّر، مراجعة عدائية قبل الدمج، أو فشلان متتاليان.
- **الشطيرة**: الغالي يكتب المواصفة (ملفات، معايير قبول، اختبار قضم، ما لا يُلمس) ← الرخيص ينفّذ تحت
  الحارس ← الغالي يراجع الفرق وحده ← الطقم يحكم. لا محرّكات ثلاثة على مهمة روتينية.
- **الطقم من داخل الوكيل صامتاً**: `npm run test:full:evidence -- --quiet` (السجل الكامل في
  `dist/test-runs/`، والكونسول رؤوس وخاتمة وذيل الساقط فقط). خطّاف `.claude/settings.json` يعيد كتابة
  `npm run test:full` إليه تلقائياً؛ لا تعطّله.
- **النواة لا تنتفخ**: `CLAUDE.md` ≤ 200 سطر يحرسه `test:claude-md-size`؛ التفاصيل والدفعات الجديدة في
  `docs/internals/` وقواعد `.claude/rules/` تحيل إليها عند لمس مساراتها.
- **الرصد قبل الرأي**: `/usage` و`/context` أول كل يوم؛ الحكم «هل يدوم شريط Fable/Astra الأسبوع كله على
  القرارات والمراجعات؟».

## البحث الدلالي (zg — أداة خارجية اختيارية)

`zg` (‏`@zvec/zvec-grep`) أداة CLI خارجية قد تكون مثبّتة على جهاز المالك — **ليست
اعتمادية للمشروع ولا تدخل `package.json` ولا تُحزم**. إن وجدتَ للمجلد فهرساً
(مجلد `.zvec-grep\` داخله) فلأسئلة «أين الكلام عن المعنى الفلاني» على وثائق كبيرة
استعمل `zg query "…"` من جذر ذلك المجلد بدل تكرار Grep التخميني؛ وللبحث الحرفي
(معرّف/رسالة خطأ) أبقِ Grep أو `zg query --rg`. قواعد مقيسة (تجربة 2026-09-10 على
`docs/`): النموذج الملزم للعربية `local/potion-multilingual-128m` (الافتراضي ضعيف
دلالياً لها)؛ صِغ السؤال بمفردات قريبة من الوثيقة (الصياغة الطبيعية البعيدة فوّتت
الهدفين في القياس)؛ اقرأ الخرج بحدود (`--limit`)؛ ونتيجة بعلامة `stale` لا تُوثَق
حتى يُعاد `zg index`. غياب الأداة أو الفهرس يعني السقوط إلى Grep بلا خطأ. دمجها
أداةً أصيلة (MCP) **مجمَّد بقرار مالك** بانتظار قياس أسبوع استخدام — `OBS-152`.

## رادار سطر

`docs/radar/` سجلّ ما يتغيّر في العالم ويمسّ سطر: أعداد تقترح ولا تنفّذ.
الحالة الآلية في `docs/radar/state.json` — الإصدارات المثبّتة مقابل الأحدث،
الخيوط المفتوحة، رغبات الجبهة الهجومية F11، قائمة المراقبة، **والمقبرة**:
ما رُفض بسبب مكتوب. القواعد:

- قبل اقتراح تبعية أو تقنية جديدة حمّل مهارة `satr-radar` وراجع المقبرة؛
  ما فيها لا يُقترح ثانيةً — الجواب «رُفضت في العدد N بسبب …» لا اقتراح.
- بنود الرادار اقتراحات؛ القرار في `docs/OBSERVATIONS.md` بيد المالك.
- `npm run test:radar-graveyard` يعضّ إن دخلت حزمة محظورة إلى `package.json`؛
  حذف الاسم من `npm_blocklist` يسبق إضافة الحزمة، بالتزام يشرح لماذا عاد القرار.
- مولّد الأعداد يعيش خارج المستودع (بايثون) — لا تبحث عنه في `scripts/`.
- **العمود الذي لا يراه الرادار مسؤوليتنا**: هو يقرأ `package.json` والسجلّات، ولا
  يستطيع تشغيل `codex --version` على جهاز المالك. فبعد **كل ترقية محرّك** شغّل
  `npm run radar:baseline -- --write` — يكتب `baseline.engines_on_owner_machine`،
  وما نكتبه نحن يغلب ما يرصده هو. و`test:radar-graveyard` يحرس الحقل بنيوياً
  ويحرس أن `baseline.satr` يطابق `package.json` (تقادم إلى إصدارين قبل أن يُكتشف).

## عرف العضّة — كيف يُثبت المنفّذ أن حارسه يعضّ

لكل حارس/اختبار جديد، اتّبع الخطوات الأربع بنفس الترتيب:

1. **ازرع العطل** — عدِّل الكود ليدخل الحالة الخاطئة (وليس لتُحاكيها كلامياً).
2. **تحقّق أن الزرع طُبِّق فعلاً** — شغِّل أمر الزرع وعدِّد المطابقات من الطرفين:
   الطرف الأصلي يختفي والمتحوِّر يظهر.
3. **أثبت أن الحارس سقط** — شغِّل الاختبار/الحارس على النسخة المزروعة وانسخ رسالة
   الفشل حرفياً.
4. **أعِد** الكود — استرجع الزرع بعد التأكد من السقوط.

قاعدة الإخراج الحرفي: تقرير كل عضّة **يلزم** أن يحتوي على:

- أمر الزرع كما نُفِّذ.
- العدّ قبل/بعد بأرقام من الطرفين (الأصل + المتحوِّر).
- نصّ رسالة فشل الحارس كما طُبعت.
- أمر الاستعادة.

**استدلال بلا إخراج = عضّة غير مثبتة تُعاد.**

قاعدة العدّ: إذا كان النمط يحوي محارف هروب (مثل الشرطة المائلة الخلفية `\`)،
لا تعتمد `grep -c` داخل `$( )` في bash؛ استعمل `node` أو `python`:

```bash
python3 -c "import re; s=open('file').read(); print('قبل:', len(re.findall(r'^- \*\*الوسم\*\*: \`ui\`', s, re.M)))"
```

قالب التقرير (5 أسطر):

```text
زرع: sed -i 's/SAFE_MODEL=.*/SAFE_MODEL=unsafe/' electron/main.js
قبل/بعد: 3 → 0 (الأصل) · 0 → 3 (المتحوّر)
فشل الحارس: AssertionError: expected 'unsafe' to match /^[a-z0-9-]+$/
استعادة: git checkout electron/main.js
```
